# --- Application ---
APP_ENV=development
LOG_LEVEL=DEBUG

# --- Database ---
POSTGRES_USER=projecthub
POSTGRES_PASSWORD=changeme
POSTGRES_DB=projecthub
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- Admin bootstrap (used only on first run) ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=400427aa96a360a368b3780f5c8e6676a8ce7c3502769251
ADMIN_DISPLAY_NAME=Admin

# --- GitHub integration ---
GITHUB_PAT=ghp_xxxxx
# Webhook secrets are per-board, generated and stored in DB

# --- MCP / Server ---
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=5174

# CORS allowlist (comma-separated). Tailscale or local IPs.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://192.168.1.104:5173,http://192.168.1.104:5174,http://100.121.78.35:5173,http://100.121.78.35:5174

# --- Security ---
SECRET_KEY=generate-a-random-string-here
TOKEN_HASH_ROUNDS=12

# --- SonarQube integration (PH-191) — added during activation 2026-06-06 ---
SONARQUBE_ENABLED=true
SONARQUBE_URL=http://sonarqube:9000
SONARQUBE_TOKEN=squ_049b0c6e9ca007fa412b8b298a9534a1ae2b6b54
SONARQUBE_POLLING_INTERVAL_SECONDS=300
SONARQUBE_PROJECT_KEY_MAP={"PH":"project-hub"}
SONARQUBE_PORT=9000
SONAR_SCAN_URL=http://localhost:9000
SONARQUBE_DB_USER=sonarqube
SONARQUBE_DB_PASSWORD=sonarqube
SONARQUBE_DB_NAME=sonarqube

# PH-257: host .NET toolchain for C#/Unity scans (dotnet-sdk + dotnet-sonarscanner)
SONAR_DOTNET_ENABLED=true
