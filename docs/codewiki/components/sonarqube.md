---
type: component
files: [backend/app/services/sonarqube.py, backend/app/services/serializers.py, backend/app/schemas.py, backend/app/api/boards.py, backend/app/api/scans.py, backend/app/db/models/core.py, frontend/src/components/sonarqube/SonarSetupSection.tsx, frontend/src/components/sonar/SonarDashboard.tsx, frontend/src/components/sonar/SonarRepoHealthCards.tsx, frontend/src/components/sonar/MetricCard.tsx, frontend/src/components/sonar/metricMeta.ts, frontend/src/components/SonarHealthPanel.tsx, frontend/src/components/SonarIssueDrawer.tsx, frontend/src/pages/BoardDetail.tsx, frontend/src/api/client.ts, frontend/src/types/api.ts, scripts/sonar-scan-board.sh, scripts/sonar-scan-watcher.sh]
last_touched_ticket: PH-327
related: [[components/backend]], [[components/frontend]]
status: active
---

# SonarQube Integration (board health)

> Self-hosted SonarQube Community ingestion: a poll cron caches each board's quality
> snapshot, an issues proxy lists live issues, and one-click setup/sync/status
> endpoints link a board to its project — all best-effort, never-500, secret-free.

## Current behavior

`backend/app/services/sonarqube.py` is the single integration surface for a
self-hosted **SonarQube Community Build**. Everything is gated by the
`settings.sonarqube_enabled` kill switch (default `False` → the stack boots with no
sonar dependency) and bounded by a `_TIMEOUT=10s` httpx timeout. Error isolation is
layered: the httpx clients return `None` / `status="unreachable"` on ANY error
(down / 401 / malformed JSON / project not yet scanned), and every consumer degrades
to a status flag rather than raising.

**Project key resolution** (`resolve_project_key`) is the shared contract: it returns
`Board.sonarqube_project_key` first, then a key from the `sonarqube_project_key_map`
JSON setting, else `None` (board skipped — not an error). All consumers (poll cron,
issues proxy, scanner, setup/sync) agree on this one key.

**Poll cron** (PH-193, `sonarqube_poll_cron` → `poll_board`): the FastAPI lifespan
creates the task only when `sonarqube_enabled and interval > 0`. Each tick resolves
the key, fetches the quality-gate status + measures, upserts the single
`SonarQubeMetric` row per board (`uq_sonarqube_metric_board`), and publishes a
`sonarqube_synced` event on `board:{id}` for the frontend to live-patch.

**Issues proxy** (PH-203, `fetch_issues` + `GET .../sonarqube/issues`): proxies
`/api/issues/search`, strips the `<projectKey>:` component prefix to a relative file
path, and returns a graceful-200 `SonarIssuesResponse` (`status` ∈ ok / unreachable /
not_configured / no_project_key). `dashboard_url` is HOST-facing
(`sonarqube_scan_url` + `/project/issues?id=key`), never the compose-internal
`sonarqube_url`, never the token.

**Setup / sync / status** (PH-223, three thin service fns over three routes on
`/api/boards/{board_id}`, all graceful-200):
- `setup_board_project(session, board, project_key?)` + `POST .../sonarqube/setup`
  (admin) — persists `Board.sonarqube_project_key` to the supplied key or the derived
  default (`derive_default_project_key`). **Default-key precedence (PH-229):**
  (1) **PH literal FIRST** → `project-hub` (must match `sonar-project.properties`; never
  basename-derived even though the basename happens to coincide); (2) a NON-PH board WITH a
  resolvable `repos_path` → the **path basename** (`/Users/.../kims` → `kims`,
  `.../GameX` → `GameX`) — the natural scanner project identity, the "uses the board path"
  requirement; (3) fallback → `board.key.lower()` for a null path, an empty basename, or a
  `RepoPathError` (path outside `HOST_HOME` / `..`). `_path_basename_key` validates via
  `to_container_path` then takes the HOST basename and never raises (so a bad path degrades,
  no 500). **Idempotent** — only writes when the value changes. Provisioning = **scan-time
  auto-create**: persisting the key is enough (the post-merge `sonar-scanner` auto-creates
  the Community project on first analysis); NO admin-API `projects/create` call (no admin
  token provisioned — out of scope); the scanner working dir itself stays out of this Python
  module. The key is persisted even when sonar is disabled (config allowed offline).
- `setup_repo_project(session, board, repo, project_key?)` + `POST
  .../repositories/{selector}/sonarqube/setup` (admin, PH-254) — the PER-REPO successor
  to board-level setup: persists ONE `Repository.sonarqube_project_key` so the per-repo
  Quality dashboard can configure secondary repos (the primary inherits the board key;
  a sibling defaults to `<primaryKey>-<slug>` via `derive_repo_project_key`). Same
  persist-only / scan-time-auto-create / never-500 / secret-free contract as the
  board-level fn, **idempotent** (write-on-change). `{selector}` = repo slug-or-id
  (`resolve_repository`); the route lives on the **boards router** (not the repositories
  router) so it reuses `require_board_admin` verbatim. Returns the just-configured
  repo's `RepoHealth` (the cached metric row when present, else an honest unscanned
  card carrying the freshly-stored key) so the FE re-renders ONE card in place.
  **Decision (a) persist-only** (no live Sonar poll on the write path); **decision (b)
  intra-board duplicate key → 409, cross-board reuse allowed** (see Known gotchas).
  Blank/whitespace key → 422; unknown repo/board → 404; non-admin → 403; sonar disabled
  → still 200 + persists.
- `request_repo_scan(session, board, repo, requested_by?)` + `POST
  .../repositories/{selector}/sonarqube/scan` (admin, PH-255) — the PER-REPO successor
  to board-level scan: enqueues ONE `SonarScanJob` for the targeted repo (idempotent per
  `(board_id, repo_id)` via `_find_or_create_queued_job` — a 2nd scan while a queued job
  waits returns the SAME `job_id`, no stacking). The single-repo slice of
  `request_board_scan`: composes `_build_repo_plan` → `_plan_scan_status` → (on `queued`
  ONLY) `_find_or_create_queued_job`; NO new resolution logic. A non-scannable repo
  (csharp-`needs_dotnet_setup` / genuinely-`unsupported` / no-key-`unconfigured` /
  no-path-`error` / `disabled`) returns 200 with the honest `scan_status` + `job_id=null`
  and NO job (**decision A** — never a 409/422). Returns `SonarRepoScanResponse {repo_slug, project_key, language,
  scan_status, job_id, message}`; the FE (PH-256) polls/awaits `job_id`. The host watcher
  drains the job unchanged.
- `sync_repo_now(session, board, repo)` + `POST .../repositories/{selector}/sonarqube/sync`
  (admin, PH-255) — the PER-REPO successor to board-level sync: re-poll (NOT re-scan) the
  repo's EXISTING analysis for its derived key (`derive_repo_project_key` → `poll_repo`,
  10s-bounded), upsert the single `(board_id, repo_id)` `SonarQubeMetric` row, and return
  the fresh `RepoHealth` so the FE re-renders ONE card in place. A NEVER-scanned repo
  (`poll_repo` fetch-None) returns 200 with the honest `unscanned_repo_health` card (null
  gate / null metrics / null `fetched_at` / derived key / `dashboard_url=null`) — **decision
  B**, NOT a 404. Disabled → 200 with NO live attempt (current cached or unscanned card).
  Both endpoints live on the **boards router** (reuse `require_board_admin` + `resolve_repository`
  verbatim, mirroring PH-254 setup); unknown repo/board → 404; non-admin → 403; secret-free.
- `sync_board_now(session, board)` + `POST .../sonarqube/sync` (admin) — an on-demand
  **re-poll** (reuses `poll_board` → reads the *existing* analysis, fast, 10s-bounded),
  upserts the metric cache, returns the fresh status. It does NOT trigger a scanner run
  (scans stay post-merge in `sonar-scan.sh`). When disabled it makes NO live attempt.
- `build_setup_status(session, board, reachable?)` + `GET .../sonarqube/status`
  (member) — assembles the `SonarSetupStatus` from settings + the cached
  `SonarQubeMetric` with **no network call** (a read must never hang). **PH-235 —
  HONEST status classification.** It no longer derives `reachable` from metric
  presence (that overload made a configured-but-never-scanned board render a FALSE
  "unreachable"). Instead it computes `has_analysis = metric is not None` and an
  explicit `status` discriminator ∈ `disabled | unconfigured | no_analysis | ok |
  unreachable`: `not enabled → disabled`; `not configured → unconfigured`;
  `reachable is False` (ONLY a real failed live `sync`) `→ unreachable`; the pure
  read path (`reachable is None`) or a succeeded sync `→ ok if has_analysis else
  no_analysis`. Absence of a metric on the read path becomes **`no_analysis`, never
  a false `unreachable`** — `reachable=False` is reachable ONLY via `sync` passing a
  genuinely failed live attempt (a real outage is never masked). `reachable` is kept
  for backward compat (best-effort = `has_analysis` on the read path) but the UI
  keys its messaging off `status`. `_setup_status_message` is driven off `status`:
  `no_analysis` → "linked to <key> — no analysis yet (run a scan)" (NOT unreachable).

**Per-board scan — "Scan now" (PH-236, C2). `scan` ≠ `sync`.** `sync_board_now`
re-polls a board's EXISTING analysis; `request_board_scan` plans a NEW analysis run.
The backend runs INSIDE a container and CANNOT `docker compose run`, so the scan
endpoint NEVER launches the scanner — it is a cheap, NON-blocking, never-500 PLANNER:
- `detect_board_language(container_source)` — infers the primary language from the
  board's code tree at the translated `/repos/<rel>` path (the scanner sees the SAME
  tree). A **Unity layout** (`Assets/` + (`ProjectSettings/` | `Packages/`)) →
  `csharp` (a marker shortcut that wins before extension counting, since Unity
  `.csproj`/`.sln` are generated, not in VCS); otherwise a bounded `os.walk`
  (skipping `build`/`.gradle`/`node_modules`/`Library`/`Temp`/… and capped at
  `_DETECT_MAX_FILES=4000`) tallies recognized source extensions and returns the most
  common language. Missing/empty/unreadable → `None`. NEVER raises.
- `_language_supported(language)` — "is there ANY scan path" gate (NOT "can the
  container CLI do it"). KNOWN-unsupported → False (the `_CE_UNSUPPORTED_LANGUAGES`
  frozenset, **EMPTY since PH-257**); everything else (incl. `csharp` and unknown `None`)
  → True. **PH-257**: `csharp` moved to `_CE_DOTNET_LANGUAGES = {"csharp"}` — it IS
  analyzable, but ONLY via the host SonarScanner-for-.NET pipeline (the container CLI is a
  stub for C#: empirical probe indexed `.cs` but reported 0 ncloc / 0 issues). `supported`
  returning True does NOT mean it queues — `_plan_scan_status` applies the
  `_CE_DOTNET_LANGUAGES` + `SONAR_DOTNET_ENABLED` gate (→ `needs_dotnet_setup` until the
  host prerequisite is declared ready).
- `build_scan_plan(board) -> SonarScanPlan` — the single shared resolution (project_key
  + container source + language + supported + `sonar.exclusions`). Pure (settings +
  filesystem only, no network), never-500. DRY between `scan` and `scan-plan`. **PH-244 —
  `_SCAN_EXCLUSIONS` broadened** to also cover vendored/native/generated trees
  (`**/cpp/**,**/.cxx/**,**/*.so,**/*.a,**/*.o,**/external/**,**/bazel-*/**,**/third_party/**,**/vendor/**,**/androidndk/**,**/ndk/**,**/toolchains/**`)
  — a board's 6.1 GB vendored LiteRT/NDK/bazel tree was being indexed (108k files) and
  aborting the scan; none of the added globs match legitimate app source
  (`src/main/{kotlin,java}`, `app/`, `frontend/src`, `backend/app`). **`**/*.java` is
  language-conditional, NOT in the base constant:** `build_scan_plan` appends it to the
  emitted `exclusions` ONLY when `language != "java"` (Fix B1), so a non-java board (GXA
  kotlin) drops incidental vendored `.java` while a java-primary board keeps `.java`
  analyzable. Value-only broadening of the EXISTING frozen `exclusions` field — NO shape
  change (PH-236 frozen `{project_key, container_source, host_source, language, supported,
  reason, exclusions}` preserved).
- `_resolve_container_source(board)` — the scan SOURCE resolver. **PH-242: PREFERS the
  board's primary linked `Repository.local_path` over the coarse `board.repos_path`.**
  `repos_path` is often the PARENT dir of the real code root (GXA: repos_path=`…/GameX`
  but code lives in `/repos/…/GameX/GameXCore`), so scanning `repos_path` binds Sonar to
  the wrong tree (0 LoC + junk dirs). Resolution: (1) primary repo with a truthy
  `local_path` → use it; AS-IS when it already starts with `settings.repos_root`
  (`/repos`, the documented column invariant — `local_path` is ALREADY container-form, do
  NOT re-translate), else (legacy host-form row) `to_container_path` with a `repos_path`
  fallback on `RepoPathError`; (2) no usable primary → existing `repos_path` →
  `to_container_path` (PH/KIM/FN/BENCH/GXI byte-for-byte unchanged). `host_source`
  (informational only) is set from `to_host_path(local_path)` best-effort when the repo
  branch wins. `project_key` resolution is INDEPENDENT (untouched) — a source change can't
  rename a project. Reading the primary repo goes through `_loaded_primary_repository`,
  which returns the primary ONLY when `Board.repositories` is already eager-loaded (the
  SQLAlchemy `inspect(board).unloaded` check) — an unloaded collection degrades to the
  fallback instead of an async lazy-load, keeping never-500. Both production callers
  (`get_board` → scan / scan-plan) eager-load `repositories`, so the primary branch is
  live there.
- `request_board_scan(session, board) -> SonarScanResult` + `POST .../sonarqube/scan`
  (admin) — returns a `scan_status` enum in order (`_plan_scan_status`): no key →
  `unconfigured`; disabled → `disabled`; no/invalid `repos_path` (RepoPathError) →
  `error`; genuinely unsupported language → `unsupported`; **csharp (.NET) +
  `SONAR_DOTNET_ENABLED` OFF → `needs_dotnet_setup`** (PH-257 honest skip, NO job); else →
  `queued` (intent recorded; the message names the host command). NEVER blocks, NEVER 500. v1 transport
  is the scan-plan HTTP read (no shared-volume file coupling — sidesteps container↔host
  uid issues); `session` is accepted for future manifest persistence.
- `GET .../sonarqube/scan-plan` (admin) → the **FROZEN** JSON the host runner + frontend
  C3 (PH-237) consume: `{ project_key, container_source, host_source, language,
  supported, reason, exclusions }`. The seam that keeps the host script dumb — the
  backend owns ALL key/path/language resolution in one place.
- **Host runner** `scripts/sonar-scan-board.sh <board-key> [repo-slug]` (sibling of
  `sonar-scan.sh`, which is UNCHANGED). **PH-248 — per-repo, two-mode.** It now curls the
  LIST endpoint `GET .../sonarqube/scan-plans` (a JSON ARRAY of per-repo plan elements,
  PH-246) and ARRAY-parses it with the SAME jq→python3 fallback discipline (`_plan_count`
  / `_plan_field <i> <key>` / `_plan_index_by_slug <slug>`, each independent + `// empty`-
  lenient). The scanner invocation is factored into one helper `run_scanner <project_key>
  <container_source> <exclusions>` carrying the PH-242/243/244 `-D` props VERBATIM, applied
  PER repo. **Mode 1 — TARGETED (watcher path, normal flow):** a non-empty 2nd arg
  `repo-slug` → pick the ONE plan element whose `repo_slug` matches and scan EXACTLY that
  repo (one claimed `SonarScanJob` = one repo = one scanner run, so GXA's 3 jobs scan 3
  distinct projects, NOT 9; a slug not in the list → log + `skipped`). **Mode 2 —
  ITERATE-ALL (manual / post-merge / legacy no-slug):** iterate EVERY plan element, scan
  each `supported=true` repo, SKIP `supported=false` repos with an honest log line, and
  NEVER abort the loop on one repo's failure (`set +e` around each `run_scanner`, RC
  captured + logged + loop continues); it emits ONE AGGREGATE `SONAR_SCAN_RESULT` marker
  (`failed` if ≥1 scanner exited non-zero, else `ok` if ≥1 scanned, else `skipped`). A
  single-repo board is a 1-element list → exactly one `run_scanner` call with `-D` args
  byte-identical to the pre-PH-248 single-`/scan-plan` invocation. BOTH modes ALWAYS exit
  0 and preserve every guard (SONARQUBE_ENABLED kill switch, reachability probe, token from
  `.env` only). NO `timeout` dependency (macOS-portable; curl's native `-m` flags only).
  Each per-board `-D` invocation runs
  `docker compose --profile scan run --rm sonar-scanner`
  (`-Dsonar.projectBaseDir=/repos/<path> -Dsonar.projectKey=<key>
  -Dsonar.projectName=<key> -Dsonar.sources=/repos/<path> -Dsonar.tests=
  -Dsonar.exclusions=… -Dsonar.java.binaries=. -Dsonar.scm.disabled=true`); the
  **static `-Dsonar.java.binaries=.` (PH-244)** is a universal belt-and-suspenders guard
  so SonarQube's JavaSensor never HARD-ABORTS the whole scan on a stray `.java`
  (`org.sonar.java.AnalysisException` demands `sonar.java.binaries` be set — `.` = the
  board root, always exists; with no compiled classes it reports no bytecode rules rather
  than aborting). Board-scan-ONLY — the sibling `sonar-scan.sh` (PH self-scan) does NOT
  carry it. The **`projectBaseDir` pin
  (PH-243)** anchors the scanner to the board's own source root so it does NOT load
  project-hub's `/usr/src/sonar-project.properties` (the compose `working_dir`), and the
  explicit empty `sonar.tests=` guards against PH's `backend/tests,tests` ever bleeding
  into a board scan; if `supported=false` it logs
  the reason + exits 0 (no scan). **PH-257 — C# (Unity) branch:** in `scan_one`, a plan
  element with `language=="csharp"` is dispatched to `run_dotnet_scanner <project_key>
  <host_source> <exclusions> <slug>` (NOT the container `run_scanner` — the container CLI
  is a stub for C#). The dispatch is `if run_dotnet_scanner …; then _dnrc=0; else _dnrc=$?; fi`
  — an `if`-guard, NOT a bare call, so an honest rc 2 can't abort `scan_one` under `set -e`
  (the qa_failed marker-swallow bug; a `set +e` wrapper is insufficient because the callee
  re-enables `set -e` internally). It uses the element's **`host_source`** (not
  `container_source` — `.csproj` + `dotnet build` read the HOST filesystem), guards `dotnet`
  + `dotnet-sonarscanner` + a real host dir (any missing → honest `skipped`, return code 2,
  NEVER a fake `ok`), and if no `.csproj` exists generates them via
  `_dotnet_generate_unity_solution` — which injects a temporary `Assets/Editor/JarwisSolutionSync.cs`
  that drives the CONCRETE `SdkStyleProjectGeneration().GenerateAndWriteSolutionAndProjects()`
  (Unity 6 default; LegacyStyle / Rider / legacy SyncVS fallbacks, all via reflection) because
  `SyncVS.SyncSolution` + `DefaultExternalCodeEditor.SyncAll()` are no-ops/`.sln`-only on
  Unity 6. Editor version is read from `ProjectSettings/ProjectVersion.txt` →
  `/Applications/Unity/Hub/Editor/<ver>/…/Unity` (newest-installed fallback). **Success is
  verified by `.csproj` FILESYSTEM existence** (NOT rc, NOT `.sln`); the injected script + its
  baked-in `<Compile Include>` line are cleaned up (else `dotnet build` CS2001), and the
  project is left byte-identical. `_mark_main_projects` then forces
  `<SonarQubeTestProject>false</SonarQubeTestProject>` on every non-test csproj (Unity refs
  `nunit` from all of them → SonarScanner else imports all C# as TEST → ncloc=0). Then
  `dotnet-sonarscanner begin /k:<key> /d:sonar.host.url=$SONAR_SCAN_URL /d:sonar.token=…
  /d:sonar.exclusions=<unity-defaults+plan>` → `dotnet build` (the `.sln` if present, else
  every `.csproj`) → `end` — using the **HOST-reachable** `$SONAR_SCAN_URL` (the host scanner
  can't resolve the compose-internal `$SONARQUBE_URL` hostname). Marker mapping: rc 0 → `ok`,
  1 → `failed`, 2 → `skipped`. Secret-free (token only as a `/d:` arg, never echoed; no shell
  tracing). Live-verified on FN: `ncloc≈14.9k cs`, real violations (no longer "0 ncloc"). Keeps the PH-194/PH-208 contract: ALWAYS exit 0,
  token from `.env` only (never committed/echoed). On first analysis SonarQube
  **auto-creates** the project (e.g. `GameX`), then the poll cron / `sync` ingests the
  fresh measures. The `sonar-scanner` compose service gained
  `${PROJECTS_ROOT:-${HOME}}:/repos:ro` (mirroring the PH-228 backend mount) so it sees
  board code at the same `/repos/<rel>` path; `/usr/src` (project-hub self-scan) is
  kept. `SonarScanResult` / `SonarScanPlanResponse` are SECRET-FREE (no token, no
  compose-internal `sonarqube_url`).

`SonarSetupStatus` (PH-235 additive) is SECRET-FREE:
`{ status, has_analysis, enabled, reachable, configured, project_key,
last_metric_fetched_at, quality_gate_status, dashboard_url, message }` — never the
token, never the compose-internal URL; `dashboard_url` = `sonarqube_scan_url` +
`/dashboard?id=key`. `status` + `has_analysis` are additive (PH-235); every prior
field is unchanged (backward-compatible). `BoardResponse` also carries
`sonarqube_project_key` (PH-235) so the board-header panel can tell "no key" from
"key set, no analysis yet" without a separate status call.

**Frontend consumer** (PH-226 / C6, `frontend/src/components/sonarqube/SonarSetupSection.tsx`,
rendered as the `sonarqube` tab in `BoardSettings.tsx`): a single member-level status
query keyed `['board', boardKey, 'sonar-setup']` (DEDICATED key — not `['board', boardKey]`
— so it never collides with BoardDetail's `BoardResponse` cache) drives a status panel
(quality-gate pill, `project_key`, relative `last_metric_fetched_at`, enabled/reachable/
configured chips, an "Open dashboard" anchor `target=_blank rel='noopener noreferrer'`,
omitted when `dashboard_url` is null) plus the UX-state banners. **PH-235 — banners +
chips key off `status`, not the boolean trio:** the yellow `sonar-unreachable-banner`
shows ONLY for `status==="unreachable"` (a genuine outage); `status==="no_analysis"`
(configured but never scanned) shows a NEUTRAL `sonar-no-analysis-banner` ("No analysis
yet — run Sync (or a scan)") and an honest "No analysis" chip in place of the false
"Reachable off" chip (the Reachable chip renders only when `status` is `ok`/`unreachable`).
`enabled=false → "not enabled on this server" + buttons disabled; configured=false → Setup
is the glowing primary`. The board-detail `SonarHealthPanel` now takes a `projectKey` prop
(threaded from `BoardResponse.sonarqube_project_key` via `BoardDetail.tsx`): on a null-health
board with a key set it reads "Linked to <key> — no analysis yet · run a scan", and only a
null key keeps the original "Connect a project key to see quality metrics" copy. Two ADMIN
mutations back the buttons:
**Setup** (one-click, empty body → backend derives `project-hub` for PH; idempotent) and
**Sync now** (re-poll). Sync's `onSuccess` invalidates THREE families so a board-detail tab's
`SonarHealthPanel` tile refreshes without a reload — the status query, `['board', boardKey]`
(BoardResponse.health), and the `['board', boardKey, 'sonar-issues', ...]` live-counts family
(mirroring BoardDetail's `sonarqube_synced` WS handler). The dev/frontend_dev token lacks board
admin → setup/sync return 403; the buttons are hidden for `!isAdmin` AND both mutations'
`onError` catch `ApiRequestError.status===403` → an inline "Admin role required" message (no
crash). The SonarHealthPanel (board-detail header) is the SINGLE other Sonar surface and is left
untouched — there is NO second sync button there (settings owns the controls, per tight scope).

**"Scan now" consumer** (PH-237 / C3, same `SonarSetupSection.tsx`): a THIRD admin action next
to Sync, consuming the PH-236 scan endpoints via `api.sonarqube.scan` (POST → `SonarScanResult`)
+ `api.sonarqube.getScanPlan` (GET → `SonarScanPlan`). A **lazy, admin-only** scan-plan query
keyed `['board', boardKey, 'sonar-scan-plan']` (`enabled: boardKey && tabActive && isAdmin`,
`staleTime 60s`, `retry:false`) resolves the detected `language` + CE `supported`/`reason`
BEFORE the click — it MUST stay `isAdmin`-gated because scan-plan is admin-only (403 for
non-admins → it would 403-spam). It feeds the status-panel "Language: <lang>" row + a
"Scannable" / "Not scannable in Community Edition" chip (with the honest `reason` below when
unsupported). The Scan button (Radar icon, NOT RefreshCw — visually DISTINCT from Sync's cheap
re-poll, with helper copy "runs a new analysis on the host … slower … in the background") is
DISABLED up-front when `plan.supported === false` (annotated with `reason`), or `!enabled` /
`!configured` / `busy`. On click, the scan mutation maps the SIX-value `scan_status`
(`queued|running|unsupported|disabled|unconfigured|error`) to HONEST inline copy:
`queued`/`running` → success-tone "Scan queued — analysis runs in the background; metrics appear
after it completes. Re-sync to refresh." (deliberately NOT the generic "status refreshed" line —
that would LIE about instant metrics); `unsupported` → the backend `message`/reason;
`unconfigured` → "Run Setup …"; `disabled` → "not enabled"; `error` → the message. The generic
Setup/Sync success region is EXCLUDED for scan (its honest async copy comes from
`scanResultFeedback`). Scan `onSuccess` invalidates the SAME three Sync families PLUS the
`['board', boardKey, 'sonar-scan-plan']` key (re-resolve language/support) — the metric is NOT
present immediately (honest async, not a bug; there is no progress endpoint, so NO polling/
progress-bar is invented). The scan mutation `onError` folds into the shared `mutationError`
(`setup ?? sync ?? scan`) so a hard 403 still surfaces "Admin role required". The
`SonarScanResult`/`SonarScanPlan` TS types mirror the backend schemas VERBATIM with the wire
field name `scan_status` (snake) 1:1 (a `status` rename would silently read `undefined`).

**Auto-scan execution loop** (PH-239): "Scan now" now actually scans with NO operator
hand-running a script. `request_board_scan` PERSISTS a `SonarScanJob(state=queued)` (table
`sonar_scan_jobs`: `board_id`, `project_key` snapshot, `state` queued→running→done|failed,
`requested_by`, `requested_at`/`started_at`/`finished_at`, `detail`; idempotent per board).
A host daemon `scripts/sonar-scan-watcher.sh` long-polls `GET /api/scans/pending`
(`backend/app/api/scans.py`), `POST .../claim`s each job (queued→running, 409-guarded against
a double run), runs `scripts/sonar-scan-board.sh <key> [repo-slug]` (which emits a
`SONAR_SCAN_RESULT=ok|failed|skipped` marker so its always-exit-0 contract doesn't hide the
real outcome), then `POST .../complete {success, detail}`. **PH-248 — the watcher now
forwards `repo_slug`:** `_parse_pending` emits a THIRD TSV column `repo_slug`
(`jq: \(.repo_slug // "")`; python3 `j.get("repo_slug") or ""`) and the job loop passes it
as the runner's Mode-1 (targeted) 2nd arg, so each claimed per-repo job scans EXACTLY its
own repo. A legacy board-level job (`repo_slug` null/empty) calls the runner with NO 2nd arg
→ Mode 2 iterate-all (= pre-PH-248 behavior, back-compat). The marker → `/complete` mapping
(ok/failed/skipped) is UNCHANGED. On `success=true` the backend
IMMEDIATELY calls `poll_board` to ingest the fresh analysis (metrics in seconds; the 300s poll
cron is the backstop for SonarQube's async indexing); on failure it records `detail` and does
NOT ingest. The first successful scan auto-provisions the Community project (no admin
`projects/create`), so `localhost:9000/dashboard?id=<key>` resolves afterwards. The watcher is
the ONE host piece — if it is down, jobs sit honestly `queued` (never fake-done). Runbook:
`docs/sonar-watcher.md` (launchd/systemd/nohup; cron is a documented degraded fallback).

**Per-repo SonarQube — projectKey + scan plan + health are now PER-REPO, not per-board
(PH-246).** A board may own N linked `Repository` rows; each gets its OWN Sonar
`project_key` (the scan source of truth), so a multi-repo board (GXA: gamexcore + gamexsdk
+ gamexandroiddemoapp) maps to N distinct Sonar projects instead of one. The model + scan +
job + health layers all became repo-keyed while every SINGLE-repo board stays byte-compatible:
- **Key derivation** `derive_repo_project_key(board, repo)`: (1) an explicit
  `repo.sonarqube_project_key` wins (idempotent override); (2) the **primary** repo INHERITS
  the board's existing key (`board.sonarqube_project_key or derive_default_project_key`) — so
  KIM/PH/GXA-primary keep their EXACT current Sonar project (no rename, no orphaned dashboard;
  PH still short-circuits to `project-hub`); (3) a **non-primary** repo →
  `<base>-<slug>` (e.g. `GameX-gamexsdk`), `repo.slug` (the stable `uq_repository_board_slug`
  identity) Sonar-key-sanitized via `_slugify_key_segment`.
- **N scan plans** `build_scan_plans(board) -> list[SonarScanPlan]` iterates
  `board.repositories` (primary-first), resolving each repo's `project_key` +
  `container_source` (from THAT repo's `local_path` via the extracted
  `_resolve_repo_container_source`) + language/supported/exclusions (the shared
  `_language_plan_fields`, PH-244 lang-aware `**/*.java` rule preserved). A repo with no
  usable `local_path` → an UNSCANNABLE element (`supported=False`), KEPT not dropped (the
  runner skips it, the others still scan — never-500). `SonarScanPlan` gained ADDITIVE
  `repo_id` + `repo_slug` (the 7 PH-236 frozen fields untouched). **`build_scan_plan(board)`
  is KEPT as a thin wrapper** returning `build_scan_plans(board)[0]` (the primary plan / the
  board-level fallback when no repos), so all single-plan callers + the kept single-object
  `/scan-plan` endpoint are byte-compatible. A board with no linked repos → exactly ONE
  board-level plan (`_board_level_plan`) = today's output.
- **Endpoints**: the EXISTING single-object `GET .../sonarqube/scan-plan` is PRESERVED
  (primary repo — the deployed `sonar-scan-board.sh` object-parses it; back-compat) AND a NEW
  `GET .../sonarqube/scan-plans` returns `list[SonarScanPlanResponse]` (PH-248 switches the
  host script to iterate this). NO existing consumer breaks.
- **Per-repo jobs**: `request_board_scan` enqueues one `SonarScanJob` PER SCANNABLE repo
  (`SonarScanJob` gained `repo_id` + `repo_slug`); idempotency moved from per-board to
  per-`(board_id, repo_id, queued)` (`_find_or_create_queued_job`) so re-clicking Scan while
  GXA's 3 jobs wait re-uses them (no stacking 6). `scan_status` becomes a per-board AGGREGATE
  (`queued` if ≥1 repo queued; else the uniform status; else `error` for a mixed set); the
  additive `SonarScanResult.repos` / `SonarScanResponse.repos` carries per-repo outcomes.
  `PendingScanItem` gained `repo_slug` (the watcher knows which repo a job targets).
- **Per-repo health**: `SonarQubeMetric` gained `repo_id` (FK, nullable) and its unique
  constraint relaxed `uq_sonarqube_metric_board (board_id)` → `uq_sonarqube_metric_board_repo
  (board_id, repo_id)` — 1 board : N metric rows, upsert-latest per repo. `poll_repo(board,
  repo, project_key)` upserts `(board_id, repo_id)`; `poll_board` loops repos (or falls back
  to a board-level poll for a repo-less board); `complete_scan_job` ingests the job's SPECIFIC
  repo. `BoardResponse.health` is KEPT = the PRIMARY repo's metric (back-compat — single-repo
  boards + existing FE unchanged) via the new `Board.primary_sonarqube_metric` accessor; a NEW
  additive `BoardResponse.repo_health: list[RepoHealth]` (serializer `repo_health_list`) carries
  the per-repo breakdown (`repo_id, repo_slug, repo_name, is_primary, project_key, the 7 metrics,
  dashboard_url`). **PH-252 — `repo_health_list` enumerates the board's LINKED repositories
  (`board.repositories`) LEFT-JOINed (in-Python) to each repo's latest metric**, NOT the metric
  rows: a linked-but-unscanned repo (no metric row) now emits an honest `_unscanned_repo_health`
  card (null gate + null 7 metrics + null `fetched_at`, but real identity + derived non-null
  `project_key` + `dashboard_url=None`), ordered primary-first then siblings by slug (mirrors
  `build_scan_plans`). A legacy repo-less board (no `Repository` rows, only a `repo_id IS NULL`
  aggregate metric) keeps today's freshest-first aggregate row via the `not board.repositories`
  fallback. The ONLY shape change: `RepoHealth.fetched_at` widened required→optional
  (`datetime|None` / TS `string|null`); `BoardHealth.fetched_at` + `project_key` stay non-null.
  PH-249 reads `repo_health`; old consumers reading only `health` are
  unaffected. **PH-251 — `RepoHealth.is_primary: bool` (non-Optional)** was added so the FE
  per-repo `primary` badge is sourced from THIS un-gated surface; `repo_health()` reads
  `metric.repository.is_primary` (non-null Boolean, already eager-loaded), and a legacy
  board-level aggregate row (`repo_id IS NULL` / `metric.repository is None`) → `False`. (The `Board.sonarqube_metric` 1:1 relationship was renamed to
  `sonarqube_metrics` 1:N — update any `selectinload(Board.sonarqube_metric)`.) **PH-256 — the FE per-repo cards (`SonarRepoHealthCards`) are now INTERACTIVE:** each card with a non-null `repo_slug` carries an admin-gated Setup/Scan/Sync action row wired to the PH-254/255 per-repo endpoints (`api.sonarqube.{setupRepo,scanRepo,syncRepo}`), every mutation invalidating `["board", boardKey]` so the same `repo_health` re-renders in place. See Design decisions/Known gotchas for the gate + `SonarRepoScanResult` type.
- **`Board.sonarqube_project_key` is KEPT** (legacy fallback + the PH self-scan identity that
  MUST equal `sonar-project.properties` `sonar.projectKey=project-hub`) — NOT dropped (a later
  cleanup ticket). It is no longer the per-repo scan source of truth.
- **Migration** `b1468dc15870` (ADDITIVE-ONLY, backfilled, SQLite-safe via
  `batch_alter_table`): adds the nullable cols + FKs; backfills the primary repo's
  `sonarqube_project_key` from the board column (no rename), and metric/job `repo_id` from the
  board's primary repo; relaxes the metric unique constraint. Safe on the live KIM SQLite
  snapshot (snapshot-first per kims-production-constraint); existing single-repo boards keep
  their exact project_key + health.

**In-app quality dashboard** (PH-241 / epic PH-238 child C, `frontend/src/components/sonar/`):
the SonarQube READ surface now has a native, branded home in the board's MAIN area — a third
**"Quality"** tab (`#quality`, refresh-safe) in `BoardDetail.tsx`'s tablist alongside Kanban /
Branch Graph, rendering `<SonarDashboard>` in a `role="tabpanel"` (`#panel-quality`). It is
PURELY PRESENTATIONAL + prop-driven (`{ health, boardKey, projectKey }`, mirroring
`SonarHealthPanel`) — NO new endpoint, NO new fetch: it reuses `BoardResponse.health` (the 7
metrics) + `sonarqube_project_key` (empty-state copy) + `useSonarLiveCounts` (live BUG/VULN/
CODE_SMELL totals reconciled `?? health.*`, same as the strip so counts never diverge), so the
existing `sonarqube_synced` WS invalidation in BoardDetail refreshes the grid + counts live with
ZERO extra wiring. Layout: a **quality-gate hero** (Passed/Failed/Warning/Unknown reusing the
strip's GATE_MAP tones; on ERROR a "See failing conditions in SonarQube" deep-link — BoardHealth
carries only the aggregate gate, not per-condition detail) + a **6-card responsive grid**
(`grid-cols-1 sm:2 lg:3`). Each card (`MetricCard.tsx`) shows the metric value (count→integer,
percent→`NN.N%`, **null→em-dash "—" NOT a fabricated 0**, Risk R1), a canonical static SonarQube
description + a good-direction hint, sourced from `metricMeta.ts`'s `METRIC_META`/`GATE_META`
(pure data, no JSX, unit-testable). Issue-backed cards (bugs/vulns/smells, `issueType != null`)
are real focusable `<button>`s (`aria-haspopup="dialog"`, `aria-label="View N bugs"`) that mount
the UNCHANGED `SonarIssueDrawer` on click — DISABLED when count is 0/null so they never open an
empty drawer; coverage/duplications/ncloc/gate are non-interactive `<div>`s. The dashboard lifts
`openType`/`triggerRefs`/`handleClose` so focus restores to the clicked card on drawer close
(a11y parity with the strip). A host-facing "Open in SonarQube" link uses the `dashboard_url`
from the existing issues query. **Honest empty states (Risk R3):** `health == null` + key set →
"Linked to &lt;key&gt; — no analysis yet · run a scan" (KIM's live state); no key → "Connect a
project key" — NEVER blank or a fake-zero grid. `SonarHealthPanel` is KEPT as the glanceable
strip and gains ONE optional `onOpenDashboard` prop → a "View details →" deep-link that calls
`switchTab("quality")` (existing callers unaffected; no other strip behavior changed).

## Design decisions (recent)

- board-scope authorization gate reaches the SonarQube read endpoints on this router [PH-327] — the broken-access-control hotfix (boards + tickets read gate across the REST+MCP shared service seam) also touches `api/boards.py`, which this page maps. The two SonarQube endpoints on this router that were authenticated-only — `GET /api/boards/{id}/sonarqube/issues` and `GET /api/boards/{id}/sonarqube/status` — now call `require_board_member(actor, board)` right after `get_board` (so a non-member can no longer enumerate a board's code-issue detail or its sonar setup state). Order is load-bearing and matches the admin-gated setup/sync/scan siblings: unknown board → 404 FIRST (`get_board` → NotFound), resolved-but-non-member → 403 SECOND. The graceful-200 contract for sonar DEGRADATION (disabled / no key / unreachable → 200 + status flag) is unchanged — the 403 is an AUTHZ decision that precedes the sonar-status assembly, NOT a sonar error. The admin-gated write endpoints (setup/sync/scan/scan-plan/scan-plans via `require_board_admin`) are UNCHANGED. The other PH-327 changes to `api/boards.py` (membership-scoped `GET /api/boards` list, `GET /api/boards/{id}` member gate, and the `PATCH /api/boards/{id}` write-IDOR fix → `require_board_admin`) are non-sonar and documented on their own surfaces; this bullet records the sonar-facing slice so a reader of the sonar page is not surprised the file changed. NO SonarQube service/schema/scanner behavior changed.
- boards.py additionally hosts the PH-322 members-enrichment surface — ORTHOGONAL to SonarQube [PH-322] — `api/boards.py` is a MULTI-CONCERN router (the sonar setup/sync/scan/status endpoints documented on this page PLUS the board membership endpoints), so it is `.codemap`-mapped here. PH-322 touched ONLY `api_list_members`: `GET /api/boards/{id}/members` now carries additive nullable `owner` (resolved via `services.owners.resolve_owner_slug`) + `local_path` (that owner's checkout path on the board) per row, filled from ONE batched `SELECT * FROM project_paths WHERE board_id=?` map (no N+1). NO SonarQube code/behavior changed. The feature itself (per-owner project-path registry: `Actor.owner_slug`, `ProjectPath` model, MCP `get/set/list_my_project_path`, REST `/api/profile[/project-paths]`) lives in `api/profile.py` + `services/{owners,project_paths}.py` + `core/permissions.require_board_member` — all `.codemap`-less. This bullet records that a non-sonar change touched a file this page maps, so a future reader of the sonar page is not surprised. **Revision (F2):** `api_list_members` is now gated by `require_board_member` (was authenticated-only) — because the enrichment exposes each member's `owner` + filesystem `local_path`, an ungated read let any authenticated / cross-owner token harvest other owners' checkout paths; the gate restores symmetry with the member-gated sibling `list_project_paths`. Order is `get_board` (404) → `require_board_member` (403) [PH-322].
- C# host-runner qa_failed fix: if-guard the C# dispatch + drive `SdkStyleProjectGeneration` + force MAIN classification → real live ncloc>0 [PH-257] — QA's Mode-B verify passed Phase 1 (gate/plan/pytest/live-API) but FAILED Phase 2 live with TWO bugs the original PH-257 code missed; this turn fixes both and proves ncloc>0 end-to-end on FN. **(1) `set -e` marker-swallow (PRIMARY):** `scan_one`'s C# branch called `run_dotnet_scanner` as a BARE command under `set -e`. On an honest precondition miss it returns 2, which aborted `scan_one` BEFORE `_dnrc=$?` — so NO `SONAR_SCAN_RESULT` marker was emitted and the script died rc 2, breaking the "ALWAYS exit 0 + ALWAYS emit marker" contract (watcher logged `result=unknown`). A plain `set +e` wrapper is INSUFFICIENT here because `run_dotnet_scanner` re-enables `set -e` internally (its own pipeline guard) before returning — so the fix is the `if run_dotnet_scanner …; then _dnrc=0; else _dnrc=$?; fi` form (immune to `set -e` regardless of the callee's set-state, like `run_scanner`'s guard). **(2) Unity 6 generation no-op + nunit test-misclassification (SECONDARY, the real ncloc>0 blocker):** the original `_dotnet_generate_unity_solution` used `-executeMethod UnityEditor.SyncVS.SyncSolution`, a NO-OP in Unity 6 (writes nothing). Rewritten to inject a temporary `Assets/Editor/JarwisSolutionSync.cs` that drives the CONCRETE `Microsoft.Unity.VisualStudio.Editor.SdkStyleProjectGeneration().GenerateAndWriteSolutionAndProjects()` via reflection (the abstract base ctor NREs; `DefaultExternalCodeEditor.SyncAll()` writes only a `.sln`, no `.csproj`). Success is checked by `.csproj` existence (not rc, not `.sln` — the `.sln` write is order-dependent so the runner builds the `.csproj` set directly when absent). The injected `.cs` + its baked-in `<Compile Include>` line in the generated csproj are stripped on cleanup (else `dotnet build` CS2001). Finally `_mark_main_projects` forces `<SonarQubeTestProject>false</SonarQubeTestProject>` on every non-test csproj because Unity references `nunit` from all of them → SonarScanner else classifies ALL C# as TEST → MAIN ncloc=0. **Live self-verified on host (FN/fruit-ninja2, Unity 6000.3.9f1 + dotnet 10.0.300 + dotnet-sonarscanner 11.2.1):** `SONAR_SCAN_RESULT=ok`, exit 0, 5 `.csproj` generated → built → `ncloc=14917` (cs=14610;xml=307), 502 violations, 74 files in SonarQube (the "indexed but 0 ncloc" probe failure is GONE). Honest-skip still works (dotnet-absent sandbox → `skipped` + exit 0, regression-locked by shell test T8; the if-guard + csproj/MAIN/strip mechanisms are statically asserted by T9). The user's Unity project is left byte-identical (injected script + meta cleaned; manifest/Packages NEVER touched). NEVER edited the QA failing-test contract. Verified: host shell suite 0 failures (T1-T9), in-container `pytest -k sonar` 199 pass / 3 host-only skip (0 regression). Out of scope (operator, NOT code): the `.csproj` are regenerated each cold run; a project with no code-editor package installed honest-skips.
- real C# (Unity) analysis via the HOST .NET pipeline, gated by `SONAR_DOTNET_ENABLED` [PH-257] — C# repos (FN/GXL) were gate-blocked as `unsupported` because the container `sonar-scanner-cli` is a STUB for C# (empirical probe: indexes `.cs` but reports 0 ncloc / 0 issues). SonarQube CE *does* ship the C# plugin, but analysis requires **SonarScanner for .NET** (`begin → MSBuild/dotnet build → end`), which needs a .NET SDK the backend container can't host. **Decision — analyze C# on the HOST, not in a container** (the watcher already runs host-side; a Docker .NET+Unity-DLL-mount path was rejected as brittle against Unity version paths). **4 sub-decisions:** (1) **gate flip without a fake-queued** — `csharp` removed from `_CE_UNSUPPORTED_LANGUAGES` (now EMPTY) into the NEW `_CE_DOTNET_LANGUAGES = {"csharp"}`; `_language_supported("csharp") → True` (a scan PATH exists), but `_plan_scan_status` adds a step: `language in _CE_DOTNET_LANGUAGES and not SONAR_DOTNET_ENABLED → needs_dotnet_setup` (a NEW honest `scan_status`, NO job enqueued — `request_board_scan`/`request_repo_scan` only enqueue on `queued`, so fake-done is impossible). Flag ON → `queued`. (2) **opt-in flag `SONAR_DOTNET_ENABLED`** (config.py, default False, alias-honored) is the operator's "host prerequisite ready" signal — the container backend can't see the host's `dotnet`, so the flag stands in for it; the host runner *independently* re-guards. (3) **host runner C# branch** — `scan_one` dispatches `language=="csharp"` to NEW `run_dotnet_scanner` (uses the plan's **`host_source`**, not `container_source` — `.sln` + `dotnet build` read the host fs): guards `dotnet` + `dotnet-sonarscanner` + a host dir (missing → honest `skipped`, rc 2, NEVER fake `ok`), generates a missing Unity `.sln` via `_dotnet_generate_unity_solution` (batch-mode `UnityEditor.SyncVS.SyncSolution`, editor version from `ProjectVersion.txt`, fail → `skipped`), then `begin /d:sonar.host.url=$SONAR_SCAN_URL /d:sonar.token=… → dotnet build → end` (HOST-reachable url, NOT the compose-internal `$SONARQUBE_URL` hostname). rc 0/1/2 → ok/failed/skipped. (4) **non-dotnet languages + PH self-scan UNTOUCHED** — kotlin/python/java keep `run_scanner` (container CLI) verbatim; `sonar-scan.sh` (PH self-scan) carries no `.NET` path (asserted). SECRET-FREE: token only as a `/d:` arg, never echoed, no shell tracing. **Phased:** Phase 1 (backend gate + plan + tests) is toolchain-agnostic + merge-able now (csharp honestly returns `needs_dotnet_setup`); Phase 2 (host runner + FE tooltip) needs the host prerequisite (`brew install --cask dotnet-sdk` + `dotnet tool install --global dotnet-sonarscanner` + `SONAR_DOTNET_ENABLED=true` + watcher restart) before QA can verify live ncloc>0 — those are operator steps, NOT code. **FE:** `needs_dotnet_setup` tooltip case in `SonarSetupSection.tsx` + `SonarRepoHealthCards.tsx` (honest reason verbatim, never a fake "queued"); `SonarScanStatus` union += `needs_dotnet_setup`. Verified: 71 sonar pytest green (csharp flag-off→needs_dotnet_setup+0-jobs / flag-on→queued+1-job for both board + repo scan; parametrized regression proving kotlin/python/java/unknown are flag-INDIFFERENT; host-only shell-grep tests for the C# branch skip in-container), ruff clean, mypy `--strict` 0 new errors on touched files, `tsc --noEmit` clean. Out of scope: coverage import (`sonar.cs.opencover`), Unity license automation, Docker .NET path (rejected).
- per-repo card Setup/Scan/Sync ACTION BUTTONS wire the read-only cards to the PH-254/255 endpoints [PH-256] — epic PH-253 child C3, FINAL (the FE consumer of the backend per-repo endpoints). `SonarRepoHealthCards.tsx` was PURELY presentational (`{ repoHealth }`); PH-256 makes it INTERACTIVE-but-still-prop-driven-for-DATA: the parent (`BoardDetail`) keeps owning the `["board", boardKey]` query, the cards gain admin-gated mutations whose `onSuccess` invalidates that EXACT key so the grid re-renders with fresh `repo_health`. **Five layers:** (1) **API client** — 3 new fns under `api.sonarqube` in `client.ts`: `setupRepo(boardKey, selector, body?)→RepoHealth`, `scanRepo(boardKey, selector)→SonarRepoScanResult`, `syncRepo(boardKey, selector)→RepoHealth`; `selector = repo.repo_slug` (backend accepts slug-or-id). (2) **Type** — NEW `SonarRepoScanResult {repo_slug, project_key, language, scan_status, job_id, message}` in `types/api.ts`, DELIBERATELY distinct from the board-level `SonarScanResult` (carries `repo_slug` + a nullable `job_id`, NO `container_source` — reusing `SonarScanResult` would read `undefined` on `job_id`, Risk R3); REUSES the existing `SonarScanStatus` union + `RepoHealth`/`SonarSetupRequest`. (3) **Admin gate** — `BoardDetail` computes `const isAdmin = useBoardRole(boardKey) === "admin"` (the SAME hook `SonarSetupSection` uses) and threads `isAdmin`/`boardKey` into `<SonarRepoHealthCards>`; non-admin → the new `RepoCardActions` sub-component is NOT rendered → no per-repo mutation/query fires (AC-6, defense-in-depth on top of the backend's 403). (4) **Card actions** — new `RepoCardActions` inside `RepoHealthCard`, BELOW the metric grid, rendered ONLY when `isAdmin && repo.repo_slug != null` (the null-identity aggregate row has no selector → suppressed; primary + secondaries + UNSCANNED cards all get the row, the primary user need): 3 `useMutation` keyed off `repo.repo_slug`; Setup = an inline disclosure form pre-filled `value={repo.project_key}` (the derived default the backend surfaces on every card); Scan = fire-and-forget + honest queued copy keyed off `job_id`/`scan_status` (job_id non-null → "queued, runs in background, Sync to refresh"; job_id null → honest non-queued reason, NO fake queued); Sync = re-poll → card leaves "No analysis yet" once metrics exist. Per-card inline error region (`role=alert`, 403→"Admin role required", 422/409/other verbatim) + success/queued `role=status` — NO toast dep, never a silent fail, never a crash (AC-5). (5) **Invalidation** — every `onSuccess` → `invalidateQueries({ queryKey: ["board", boardKey], refetchType: "active" })`, the SINGLE key driving `repo_health` (NO `sonar-issues`/`sonar-setup` — the per-repo cards consume neither, unlike the board panel). **Files:** `SonarRepoHealthCards.tsx` (+`RepoCardActions`, +`repoScanFeedback`/`errorMessage` helpers, +`isAdmin`/`boardKey` props), `BoardDetail.tsx` (+`useBoardRole`/`isAdmin`, thread props at the `#quality` card mount), `api/client.ts` (+3 fns), `types/api.ts` (+`SonarRepoScanResult`). UI-verified via Playwright `tests/e2e/ph-256-per-repo-card-actions.spec.ts` (6/6 green, against route-fulfilled GXA-shaped `repo_health` + per-repo endpoints): TC-1 admin sees the action row on all 3 cards incl. the unscanned one; TC-2 Setup pre-fills `repo.project_key` + submit success re-renders the card via invalidation; TC-3 scan honest queued (job_id set) vs honest non-queued (job_id null, unsupported); TC-4 Sync leaves "No analysis yet"; TC-5 422 → inline `role=alert`, no crash; TC-6 non-admin → no action row + no per-repo call fires. tsc clean, eslint clean, console/network clean across all specs. Out of scope: backend endpoints (PH-254/255, merged), bulk "scan all secondaries" (board-level Scan-now covers it), scan progress polling (Sync + `sonarqube_synced` WS suffices), non-admin self-service.
- per-repo SonarQube SCAN + SYNC action endpoints [PH-255] — epic PH-253 child C2 (the two ACTIONS on a single targeted repo, after C1/PH-254 stored the per-repo key). Both are admin-gated, never-500, secret-free, and are the SINGLE-repo slices of the board-level service code — **NO new resolution/key/path logic**. **SCAN `POST /api/boards/{board_id}/repositories/{selector}/sonarqube/scan` → `SonarRepoScanResponse`** via NEW `request_repo_scan(session, board, repo, requested_by?)`: composes `_build_repo_plan` → `_plan_scan_status` → (on `queued` ONLY) `_find_or_create_queued_job` (all PH-246), enqueuing exactly ONE `SonarScanJob` for that `repo_id`, idempotent per `(board_id, repo_id, queued)` (a 2nd scan while a job waits returns the SAME `job_id`, no stacking). **SYNC `POST .../repositories/{selector}/sonarqube/sync` → `RepoHealth`** via NEW `sync_repo_now(session, board, repo)`: disabled-shortcircuit | `derive_repo_project_key` → `poll_repo` (10s-bounded re-poll of the EXISTING analysis, upserts the single `(board_id, repo_id)` metric row + publishes the per-repo `sonarqube_synced` event) → metric-or-`unscanned_repo_health`. **Decision A — non-scannable repo → honest 200, NO 409/422:** a C#-`unsupported` / no-key-`unconfigured` / no-path-`error` / `disabled` repo returns `scan_status` ∈ {unsupported,unconfigured,error,disabled} with `job_id=null` and NO job — consistent with the board-level `request_board_scan`; the FE renders it as a UX state, not a client error. **Decision B — unscanned sync → honest `unscanned_repo_health`, NO 404:** `poll_repo` returns False (no row written, no raise) when Sonar has no analysis for the derived key (fetch-None / never-scanned / unreachable), so the endpoint falls through to the null-metric card (derived non-null `project_key`, `dashboard_url=null`); reachable+has-analysis upserts the row and returns live `repo_health(metric)` — sync is the bridge OUT of "No analysis yet". **Schema:** NEW `SonarRepoScanResponse {repo_slug, project_key, language, scan_status, job_id, message}` (NOT a reuse of the aggregate board `SonarScanResponse` — one repo, no `repos[]`); SYNC reuses `RepoHealth`. **Service dataclass** `SonarRepoScanResult` carries `job_id` (UUID, None unless queued). **Cycle guard:** `sync_repo_now`'s `repo_health`/`unscanned_repo_health` import is FUNCTION-LOCAL (serializers imports `derive_repo_project_key`/`_dashboard_url` from sonarqube at module load → a top-level reverse import would be circular); the `RepoHealth` return annotation is a `TYPE_CHECKING`-only import (safe under `from __future__ import annotations`). `sync_repo_now` re-queries the repo's metric with `selectinload(SonarQubeMetric.repository)` after the poll so `repo_health` never lazy-loads `metric.repository` (MissingGreenlet). Both routes live on the BOARDS router (reuse `require_board_admin` + `resolve_repository` verbatim, like PH-254). NO migration (`SonarScanJob`/`SonarQubeMetric` pre-exist). Verified: 19 NEW tests in `test_sonarqube_repo_scan_sync.py` (service: scan queued-1-job / idempotent-same-job / unsupported-C#-0-jobs / disabled-0-jobs; sync with-analysis-upsert / never-scanned-unscanned-card / disabled-no-live-attempt; endpoint: scan 200-queued-job_id + id-selector + unsupported-200-null + disabled-200 + 404 repo/board + 403; sync with-analysis-live + never-scanned-200-not-404 + disabled-200-no-attempt + 404 repo + 403; SECRET-FREE) — 19/19 green; full sonar/serializer/repo suite 231 green (0 regression), ruff clean on touched files, mypy `--strict` 0 new errors on touched files (the pre-existing `events/bus.py:277` aclose error is unrelated). Out of scope: frontend wiring (PH-256), watcher/`complete_scan_job` ingest, board-level endpoints, new job lifecycle states.
- per-repo SonarQube setup endpoint stores/overrides one repo's project key [PH-254] — epic PH-253 child C1 (foundation). After PH-250..252 the per-repo Quality dashboard LISTS every linked repo, but secondary repos had NO way to be configured — `Repository.sonarqube_project_key` existed (core.py) yet no endpoint populated it. **NEW `POST /api/boards/{board_id}/repositories/{selector}/sonarqube/setup`** on the BOARDS router (`api/boards.py`) — deliberately NOT the repositories router: the boards router's `require_board_admin` dep takes a `board_id` path param (KEY-or-UUID, PH-233) and is reused VERBATIM (DRY, identical admin gate to board-level setup); adding it to the repositories-router prefix `/api/boards/{board_key}` would force a second admin-dep variant. `{selector}` (slug-or-id) resolves via `resolve_repository` (404 on no match — the established `/repositories/{selector}` convention, NOT a new repo_id-only path). **NEW thin service `setup_repo_project(session, board, repo, project_key?)`** mirrors `setup_board_project` EXACTLY (persist-only, scan-time auto-create, never-500, secret-free, idempotent write-on-change): effective key = supplied (stripped) override OR `derive_repo_project_key(board, repo)` (primary inherits the board key, sibling → `<base>-<slug>`). **Decision (a) persist-only, NO validation poll** — a live Sonar probe on the write path would reintroduce the never-500/never-hang risk the whole module avoids, and the project legitimately does not exist until the first scan (Sync/PH-255 confirms later). **Decision (b) intra-board conflict → 409, cross-board allowed** — the `(board_id, repo_id)` metric model means two repos of ONE board pointing at one Sonar project = two cards backed by one key (ambiguous/collision-prone), so a key already held by ANOTHER repo of THIS board is rejected (new `Conflict` 409 exception in `core/exceptions.py`, naming the conflicting repo via a whitelisted `conflicting_repo` payload field); the check spans both a sibling's STORED key AND its DERIVED key (an unset sibling still defends its `<base>-<slug>` / inherited-primary slot). Cross-board reuse is NOT checked (boards are independent Sonar surfaces; no cheap authoritative cross-board check without a network call, violating decision a). **Schema:** new `RepoSonarSetupRequest {project_key?}` (dedicated class, not a `SonarSetupRequest` reuse — per-repo docstring + future divergence); response REUSES `RepoHealth`. **Serializer:** promoted `_unscanned_repo_health` → public `unscanned_repo_health` (now a 2nd caller — the route). **Validation:** blank/whitespace key → `ValueError` → 422 (mirrors the existing `api/boards.py` RepoPathError→422 mapping); unknown repo/board → 404; non-admin → 403; sonar disabled → still 200 + persists. **Note:** overriding a PRIMARY repo's key can desync it from `Board.sonarqube_project_key` — allowed (operator override always wins per `derive_repo_project_key` precedence), NO auto-sync of the board column in scope. NO migration (column pre-exists since PH-246). Verified: 19 NEW tests in `test_sonarqube_repo_setup.py` (service: derive-default/explicit-override/idempotent/blank-ValueError/intra-board-conflict incl. derived-key depth/self-reconfigure-not-conflict/cross-board-allowed; endpoint: 200 derived-sibling-key + custom override + id-selector + idempotent + 422 blank + 409 conflict naming the repo + 404 unknown repo/board + 403 non-admin + 200 disabled-graceful + SECRET-FREE) — 19/19 green; full sonar/serializer suite 171 green (0 regression from the serializer rename), ruff clean on touched files, mypy `--strict` adds 0 errors on touched files. Out of scope: scan/sync endpoints (PH-255), frontend (PH-256), live cross-board probe, board-column auto-sync.
- `repo_health_list` enumerates ALL LINKED repos (not metric rows); unscanned → honest null card; `dashboard_url` null until first scan [PH-252] — bug follow-up to PH-249 (epic PH-245); distinct from the PH-251 *which-repo-is-primary* badge bug — THIS fixes *which repos appear at all*. **Root cause:** `repo_health_list(board)` built `BoardResponse.repo_health` by iterating `board.sonarqube_metrics` (the METRIC rows), so any LINKED repo with no metric row was silently dropped — GXA has 3 linked repos (gamexcore primary + gamexsdk + gamexandroiddemoapp) but only 1 metric (the 2 siblings have queued-but-undrained `SonarScanJob`s, Sonar off by default), so `#quality` rendered ONE card. A CODE bug on EVERY multi-repo board, not GXA-specific. **Fix (additive, NO migration):** enumerate `board.repositories` LEFT-JOINed in-Python to each repo's latest metric (`metric_by_repo = {m.repo_id: m for m in sonarqube_metrics if repo_id is not None}` — the `uq_sonarqube_metric_board_repo` constraint guarantees ≤1 each), ordered `sorted(key=(not is_primary, slug))` (primary first then slug — mirrors `build_scan_plans` sonarqube.py:1376); a repo present in the map → `repo_health(metric)` (scanned, real URL), else → new helper `_unscanned_repo_health(board, repo)` (real `repo_id`/`repo_slug`/`repo_name`/`is_primary` + derived non-null `project_key` via `derive_repo_project_key`, but null `quality_gate_status` + null 7 metrics + null `fetched_at`). A legacy repo-less board (no `Repository` rows, only a `repo_id IS NULL` aggregate metric) keeps today's freshest-first aggregate row via a `not board.repositories` fallback branch (byte-identical). **DECISION — `dashboard_url=None` for an unscanned repo (null until first scan):** a derived URL deep-links to a Sonar project that does NOT yet exist (auto-created on first analysis) → a 404/empty page; the FE already omits the "Open in SonarQube" anchor when `dashboard_url == null`, and `quality_gate_status == null` stays the single canonical "never scanned" discriminator — `dashboard_url` follows it. **Only shape change:** `RepoHealth.fetched_at` widened required→optional (`datetime|None` backend / `string|null` TS); `BoardHealth.fetched_at` + `RepoHealth.project_key` stay non-null. **Frontend (lockstep strict-null tsc — NO behavior change; the cards already render the unscanned state + count `repoHealth.length`):** `types/api.ts` `RepoHealth.fetched_at: string|null`; `metricMeta.relativeSynced(iso: string|null)` with a leading `if (iso == null) return ""`; `SonarRepoHealthCards` footer guard narrowed to `scanned && repo.fetched_at ?`. **Single-repo boards (PH/KIM/FN) UNAFFECTED** — 1 repo WITH a metric → the `else` never fires, `repo_health(metric)` runs identically (byte-identical 1-entry output), `BoardResponse.health` (separate `primary_sonarqube_metric` path) untouched. **Async-safe** — both `board.repositories` + `board.sonarqube_metrics(.repository)` are already eager-loaded on the board-detail path (services/boards.py), no new query/lazy-load. Verified: `test_sonarqube_multirepo.py` breakdown test extended (3rd never-polled sibling, len 2→3, unscanned-entry null gate/bugs/fetched_at + derived project_key + `dashboard_url is None`, primary-first ordering) + 3 NEW (enumerate-unscanned → 2 entries incl. null sibling, single-repo-unchanged → exactly 1 scanned entry + `health` byte-identity, repoless-aggregate-fallback → 1 aggregate row with `repo_slug None`) — 20/20 green; ruff clean on touched files, mypy `--strict` adds 0 errors (pre-existing `events/bus.py` error unrelated), `tsc --noEmit` CLEAN under strict-null; PH-249 e2e all 5 green with the line-123 unscanned fixture flipped `fetched_at: NOW → null` (TC-2 honesty path now exercises the null wire shape). Out of scope: draining the queued `SonarScanJob`s / enabling Sonar (ops — that is WHY the siblings lack metrics; the fix renders them honestly regardless).
- per-repo `primary` badge moved off the member-gated `/repositories` onto `repo_health[].is_primary` [PH-251] — standalone follow-up to PH-249 (references epic PH-245). PH-249 badged the primary card by comparing `repo.repo_slug === primarySlug`, and `primarySlug` was derived in `BoardDetail` from `reposQuery = GET /api/boards/{key}/repositories` — which is **member-gated** (`repositories.py:_require_board_member` → 403 for a non-member authorized viewer, observed `jarwis-frontend` on GXA). On 403 (`retry:false`) `reposQuery.data` is undefined → `primarySlug=undefined` → EVERY card's `isPrimary=false` → the `primary` badge SILENTLY never rendered (the exact "non-member shows cards WITHOUT a badge" gotcha flagged in the PH-249 bullet below). **Fix — option (a), expose primary identity on the un-gated surface:** the cards already render from `BoardResponse.repo_health[]` (NOT member-gated), so the "which repo is primary" signal must live there too. **Backend (additive, NO migration):** `RepoHealth` schema gained `is_primary: bool` (non-Optional, `schemas.py`); `serializers.repo_health()` populates it `repo.is_primary if repo is not None else False` from the ALREADY eager-loaded `metric.repository.is_primary` (non-null Boolean `core.py:379` — a pure read of loaded data, async-safe, never-500); a legacy `repo_id IS NULL` aggregate row → `False`. `is_primary` already exists on `Repository` so NOTHING is added to any table (`repo_health` is computed, not persisted). **Frontend (consume):** `types/api.ts` `RepoHealth` += `is_primary: boolean`; `SonarRepoHealthCards` dropped its `primarySlug` prop and badges off `isPrimary={repo.is_primary}`; `BoardDetail.tsx` stopped threading `primarySlug` into the cards. **`reposQuery`/`primarySlug` are KEPT** — they still drive the `#graph` multi-repo branch switcher (`RepoSwitcher`/`selectedRepo`/`?repo=`), which still 403-degrades to single-repo as before; ONLY the quality-tab badge stopped depending on the gated fetch. Net: the badge is now membership-INDEPENDENT (renders for any authorized board viewer). **Secondary ops note (documented, NOT the code fix):** adding `jarwis-frontend` to GXA membership would make the existing `/repositories` call succeed, but it is a per-actor band-aid that does NOT fix the general non-member case — the code fix is the remedy. **Single-repo boards (KIM/PH) UNAFFECTED** (additive field; the lone repo's `is_primary=true` row badges exactly as today, a board-level aggregate row → `false` = no badge, same as today; no `BoardResponse` shape break). Verified: serializer unit test asserts `is_primary` True(primary)/False(sibling)/False(null-repo aggregate) + the existing multi-repo breakdown test extended (gamexcore True, gamexsdk False); 24 sonar/multirepo pytest green, ruff clean on touched files, mypy `--strict` adds 0 errors (the pre-existing `events/bus.py` error is unrelated), `tsc --noEmit` CLEAN; the PH-249 e2e (`ph-249-…spec.ts`) TC-1 now fulfills `/repositories` with a **403** and STILL asserts the gamexcore `primary` badge renders (regression-locks the fix) — all 5 e2e green. Out of scope: changing the `/repositories` member-gating (intentional); `health`/`SonarDashboard`/`SonarHealthPanel`; per-repo scan/key/job logic (PH-246/248 untouched).
- per-repo SonarQube health cards consume `BoardResponse.repo_health` (one card per linked repo, primary-badged) [PH-249] — epic PH-245 child D, the FE consumer of PH-246's `repo_health` breakdown. The single-repo health UI (`SonarHealthPanel` strip + `SonarDashboard` `#quality` tab) renders only `BoardResponse.health` (the PRIMARY repo's metric), so a multi-repo board's sibling repos were invisible. **NEW presentational `frontend/src/components/sonar/SonarRepoHealthCards.tsx`** (under `sonar/` → covered by the `components/sonar/*.tsx` codemap glob → this page) — PURELY prop-driven `{ repoHealth: RepoHealth[]; primarySlug: string | null }`, NO fetch/query/WS: BoardDetail owns the `["board", boardKey]` query + the `sonarqube_synced` invalidation, so a completed scan refreshes `repo_health` → the cards re-render for free. Renders a `grid-cols-1 sm:2 lg:3` of one `RepoHealthCard` per entry: header = `repo_name` + a cyan `primary` badge when `repo_slug === primarySlug`, a quality-gate pill (shared `GATE_MAP`/`resolveGate` from `metricMeta`), 6 metric tiles (bugs/vulns/smells/coverage/duplications/ncloc — null→em-dash, never a fake 0), the per-repo host-facing `dashboard_url` deep link, and relative `fetched_at`. **Honest states (Risk R3):** a never-scanned linked repo (`quality_gate_status == null`) renders the card frame with a "No analysis yet" pill + a "Linked to `<project_key>` — run a scan" note + em-dash tiles (NOT a crash, NOT a fake all-zero); `repo_health == []` renders nothing (BoardDetail only mounts it when `repo_health.length > 0` — single-repo boards see ONLY the existing `SonarDashboard`, zero regression, AC4). A null-identity AGGREGATE row (backend emits one for a single-repo board with no per-repo `Repository`, e.g. KIM) titles off `repo_name ?? "Board total"` and never crashes on null. **Wired in `BoardDetail.tsx` `#quality`** ABOVE the unchanged `SonarDashboard`; `primarySlug` reuses the already-computed `repositories.find(is_primary)?.slug`. **DRY (Risk R4):** the `GATE_MAP`/`GATE_UNKNOWN`/`resolveGate` + `formatPercent`/`relativeSynced` helpers, previously duplicated inline in `SonarHealthPanel` + `SonarDashboard`, were extracted to the shared `sonar/metricMeta.ts` and all three sonar surfaces now import them (no 3rd copy). **`RepoHealth` interface + `BoardResponse.repo_health: RepoHealth[]`** added to `types/api.ts` (mirrors backend `schemas.py RepoHealth` verbatim — repo_id/slug/name nullable for the aggregate row, the 7 BoardHealth metrics, `dashboard_url`). **Browser-verified (Claude Preview, live)**: GXA `#quality` → "PER-REPOSITORY QUALITY · 1 repo" + a `GameXCore` card (gate Failed, smells 9, ncloc 1174, dashboard link `?id=GameX`) from the real 1-entry `repo_health` (gamexsdk/gamexandroiddemoapp not yet scanned → honest single card, not faked); PH `#quality` → the `project-hub` card carries the `primary` badge (proves the slug===primarySlug match) with gate Failed / coverage 49.3% / ncloc 25058; the always-on strip + `SonarDashboard` render unchanged below on both boards (no regression); 0 console errors. `tsc --noEmit` clean. NOTE: the `primary` badge needs `GET .../repositories` (board-member-gated) to resolve `primarySlug`; a non-member viewer (or a failed repos fetch) shows the cards WITHOUT a badge — graceful, the cards still render from `repo_health`. Out of scope: backend `repo_health` shape (PH-246), host runner (PH-248).
- host runner scans EVERY linked repo via the `/scan-plans` LIST; Mode 1 targeted vs Mode 2 iterate-all [PH-248] — child B of epic PH-245. The whole pipeline was per-repo END-TO-END after PH-246 EXCEPT the one host script that invokes `sonar-scanner`: the watcher claimed GXA's 3 jobs and ran `sonar-scan-board.sh GXA` 3 times, but the runner fetched the SINGLE-object `/scan-plan` (primary only) and scanned ONCE → all 3 jobs re-scanned `GameX`; `GameX-gamexsdk` + `GameX-gamexandroiddemoapp` were never analyzed. The single point of change: make the runner repo-aware. **Chosen seam — TARGETED, with iterate-all as the fallback.** The job lifecycle is already per-repo (`request_board_scan` enqueues one `SonarScanJob` per repo, `complete_scan_job` ingests `job.repo_id`); the ONLY missing link is telling the runner WHICH repo a claimed job is for, and `repo_slug` is ALREADY in `/pending`, so the watcher just forwards it. **Two modes in `sonar-scan-board.sh <board-key> [repo-slug]`:** Mode 1 (non-empty slug, the watcher's normal path) fetches the LIST, `_plan_index_by_slug` filters to one element, scans EXACTLY that repo under its own `project_key`/`container_source`/`exclusions` → 1 job = 1 repo = 1 scanner run (3 distinct projects, NOT 9 — Risk R1); Mode 2 (no slug — manual / post-merge / a not-yet-updated watcher) iterates EVERY element, scans each `supported=true`, skips `supported=false` with an honest log, NEVER aborts on one failure (`set +e`, capture RC, log, continue), and emits ONE AGGREGATE marker. **Both modes parse the SAME `/scan-plans` JSON ARRAY** with the preserved jq→python3 fallback (array-aware `_plan_count`/`_plan_field`/`_plan_index_by_slug`, each `// empty`-lenient so an additive future field never breaks the parse), reuse one extracted `run_scanner()` helper carrying the PH-242/243/244 `-D` props VERBATIM per-repo, and ALWAYS exit 0. **A single-repo board is a 1-element list → exactly one `run_scanner` call byte-identical to today** (AC + test T3). **Watcher** `_parse_pending` emits a 3rd TSV column `repo_slug` and forwards it; an empty slug → Mode 2 (legacy back-compat, R3). NO `timeout` (macOS lacks GNU coreutils; curl `-m` is portable, R4). **Back-compat (R2):** the single-object `/scan-plan` stays as the safety net; runner + watcher ship in the SAME commit. **NO backend change** — per-repo jobs / `/scan-plans` list / `PendingScanItem.repo_slug` / per-`repo_id` ingest ALL shipped in PH-246 (confirmed in-code; the runner never tells the backend which repo it scanned — `job.repo_id` set at enqueue already knows). `scripts/sonar-scan.sh` + `sonar-project.properties` byte-for-byte UNCHANGED. Token from `.env` only, never echoed (no new secret surface, R7). Verified: `bash -n` clean on both scripts; a hermetic parse-level test `tests/scripts/test_sonar_scan_board.sh` (stubs curl+docker+sonar-scanner, no network) — T1 Mode 2 on a 3-element fixture → 2 supported scans under their own projectKeys + the csharp element skipped; T2 Mode 1 slug → exactly 1; T3 1-element list → byte-identical 8-flag `-D` set; T4 all-unsupported → 0 scans + `skipped`; T5 never-abort (one scanner fails → the other still scans, aggregate `failed`, exit 0); T6 python3-fallback (jq hidden) agrees with jq on count + slug-index; T7 unknown slug → 0 scans + `skipped` — all 7 green, all exit 0. Live GXA 3-repo re-scan (3 RepoHealth rows, vendored tree excluded, no PH components) is the Coordinator post-merge op.
- per-repo Sonar project keys + multi-repo scan plan + per-repo health [PH-246] — SonarQube moved from per-BOARD to per-REPO (foundation child of epic PH-245; PH-248 host runner + PH-249 FE consume it). The lever: each linked `Repository` carries its OWN `sonarqube_project_key`, while every single-repo board stays byte-compatible by inheriting the board's existing key/metric onto the PRIMARY repo. **Key derivation `derive_repo_project_key`:** explicit override wins → primary INHERITS the board key (no rename — KIM/PH/GXA-primary unchanged; PH short-circuits to `project-hub`) → siblings `<primaryKey>-<slug>` (slug, not name, is the stable per-board identity). **`build_scan_plans(board) -> list[SonarScanPlan]`** iterates repos (N plans; refactored `_resolve_repo_container_source` + shared `_language_plan_fields` so per-repo resolution is identical to today's, just repo-keyed); **`build_scan_plan` KEPT as a thin primary-repo wrapper** (`build_scan_plans(board)[0]`) so all single-plan callers + the kept single-object `/scan-plan` endpoint are byte-compatible; the 7 frozen PH-236 fields are untouched, `repo_id`/`repo_slug` are ADDITIVE. **Endpoints:** KEPT single-object `GET .../scan-plan` (primary, the deployed host script object-parses it) + ADDED `GET .../scan-plans` (list, PH-248 iterates) — no deployed consumer breaks. **Per-repo jobs:** one `SonarScanJob` (gained `repo_id`+`repo_slug`) per scannable repo, idempotent per `(board_id, repo_id)` (was per-board, would stack N×); `scan_status` aggregate; `PendingScanItem` gained `repo_slug`. **Per-repo health:** `SonarQubeMetric` gained `repo_id` + relaxed unique `(board_id, repo_id)`; `poll_repo` loops; `BoardResponse.health` KEPT = primary metric (back-compat via `Board.primary_sonarqube_metric`), NEW additive `BoardResponse.repo_health: list[RepoHealth]` breakdown. **`Board.sonarqube_project_key` KEPT** (legacy fallback + PH self-scan identity), NOT dropped. **Migration `b1468dc15870` ADDITIVE-ONLY + backfill + SQLite-safe `batch_alter_table`** (FKs + the unique-constraint relax MUST go through batch — SQLite has no ALTER-ADD/DROP-CONSTRAINT): nullable cols, backfill primary key from the board column + metric/job repo_id from the primary repo, relax the metric unique constraint. Verified: upgrade/downgrade/re-upgrade clean on Postgres AND a real SQLite engine (KIM-snapshot path); the 7 boards keep their exact legacy keys; 16 new tests in `test_sonarqube_multirepo.py` (3-plan GXA + distinct keys + per-repo source, single-repo == wrapper, unscannable sibling kept, per-repo job enqueue + idempotency, pending repo_slug, per-repo metric rows, health=primary + repo_health breakdown, `/scan-plans` list + kept single `/scan-plan`, migration backfill + relaxed constraint on real sqlite). Full sonar+repo+board suite 232 green, ruff clean, mypy --strict adds 0 errors on the touched files. Out of scope (flagged for PH-248/249): host-runner list iteration, FE per-repo cards, dropping `Board.sonarqube_project_key`.
- broadened exclusions for vendored/native trees + java-binaries guard [PH-244] — 3rd and FINAL layer of "board scans don't work" (PH-242 fixed the source path, PH-243 isolated config). After PH-243 the GXA scan correctly based at `GameXCore` (language=kotlin) but still ABORTED with `org.sonar.java.AnalysisException`: `GameXCore/src` is 6.1 GB and bundles a vendored native tree (`src/main/cpp/LiteRT/bazel-LiteRT/external/androidndk/toolchains/.../python3/include`) that the scanner indexed (108,578 files across 16 languages), and that tree's stray `.java` made the JavaSensor refuse to run without `sonar.java.binaries` → whole scan failed. **Three coordinated fixes, all keeping the PH self-scan UNCHANGED + the PH-236 frozen SonarScanPlan shape intact (value-only `exclusions` change, NO new field):** (A) broaden `_SCAN_EXCLUSIONS` with `**/cpp/**,**/.cxx/**,**/*.so,**/*.a,**/*.o,**/external/**,**/bazel-*/**,**/third_party/**,**/vendor/**,**/androidndk/**,**/ndk/**,**/toolchains/**` — each targets a path SEGMENT that is native/vendored/generated by convention (or a binary extension), NONE matches `src/main/{kotlin,java}` / `app/` / `frontend/src` / `backend/app`, so PH/KIM/FN are unaffected; (B1, backend, general) `build_scan_plan` appends `**/*.java` to `exclusions` when `language != "java"` so a non-java board drops incidental `.java` → JavaSensor has nothing → no abort, while a java-primary board keeps `.java` analyzable; (B2, script, universal) a static `-Dsonar.java.binaries=.` in `sonar-scan-board.sh` satisfies the JavaSensor's "binaries property set" precondition so it NEVER aborts even on a java board or a java-outside-vendored edge B1 misses (`.` = board root, no bytecode → source-only java, not abort). **Why both B1+B2:** B1 keeps the analysis clean (data-driven, source of truth in the backend); B2 is the absolute belt-and-suspenders guarantee covering the edge B1 doesn't. **HARD RULE held:** `scripts/sonar-scan.sh` + `sonar-project.properties` byte-for-byte UNCHANGED (a test asserts `sonar-scan.sh` carries NO `sonar.java.binaries`; PH has no `.java` so neither fix changes its result). **Out of scope:** a board that LEGITIMATELY needs real `sonar.java.binaries` (compiled `target/classes`) for full bytecode java rules — `.` lets it scan without aborting but yields source-only java analysis; proper per-board java-binaries discovery is a future ticket. Verified: 5 new tests (`_SCAN_EXCLUSIONS` covers the new globs + base-set has NO `**/*.java`; kotlin/None board plan excludes `**/*.java`, java board does NOT; frozen 7-key shape; shell-grep `sonar-scan-board.sh` has `-Dsonar.java.binaries=.` AND `sonar-scan.sh` does NOT) + full sonar suite 129 green (2 shell tests skip in-container, verified on host), ruff clean, mypy adds 0 errors on `sonarqube.py`. Live GXA re-scan (no AnalysisException, GameXCore Kotlin ncloc>0, indexed count far below 108,578, no PH components) is the Coordinator post-merge op.
- board scans isolated via `-Dsonar.projectBaseDir=$CONTAINER_SOURCE`; Kotlin/Gradle detection marker [PH-243] — a SECOND root cause behind "every project bound to a nonsense code section" (PH-242 fixed only the source PATH). The `sonar-scanner` compose service runs with `working_dir=/usr/src` + `.:/usr/src:ro`, so `/usr/src` carries project-hub's OWN `sonar-project.properties`. sonar-scanner-cli reads `sonar-project.properties` from `projectBaseDir`, which DEFAULTS to the CWD (`/usr/src`); `sonar-scan-board.sh` never set it, so EVERY board scan inherited PH's `sonar.tests=backend/tests,tests` (+ coverage + python.version). The `-Dsonar.sources` override masks `sonar.sources` but NOT `sonar.tests` → GameX (projectKey=GameX) Code view showed exactly PH's `backend/tests` + `tests/e2e`, 0 ncloc for GameXCore's 133 real `.kt`. **Fix (script-side, no SonarScanPlan shape change — PH-236 frozen contract preserved):** pin `-Dsonar.projectBaseDir="$CONTAINER_SOURCE"` (the board's own root, which has NO sonar-project.properties) → clean isolation at the source, not a `-D` whack-a-mole; relative paths resolve inside the board tree. Belt-and-suspenders `-Dsonar.tests=` (explicit empty) so a future baseDir regression can never silently re-bleed PH test dirs. `projectBaseDir == container_source` is a pure script-side derivation of the already-emitted field — `build_scan_plan`/`SonarScanPlan` UNCHANGED. **SECONDARY:** `detect_board_language` gained a Gradle/Kotlin marker shortcut (sibling to the Unity shortcut, BEFORE the extension tally): a root `build.gradle.kts`/`settings.gradle.kts` → `kotlin`. An Android/Kotlin project's deep `src/main/kotlin` tree (or stray `.py` tooling) could out-tally / exhaust the bounded `_DETECT_MAX_FILES=4000` walk and misclassify as `python`; since `language` drives the `supported` gate, a real Kotlin board (GameXCore) must classify `kotlin`/supported. Conservative marker-file check won't misfire on a real python repo. **HARD RULE held:** `scripts/sonar-scan.sh` (PH self-scan) + `sonar-project.properties` are byte-for-byte UNCHANGED — the PH self-scan keeps CWD `/usr/src` and loads its props on purpose; only `sonar-scan-board.sh` changed. Verified: new detect tests (Android-Kotlin marker + stray .py → kotlin, settings.gradle.kts-alone → kotlin, plain python repo stays python) + shell-level grep assertions (board-scan carries baseDir/empty-tests; sonar-scan.sh has neither) + full sonar suite green, ruff clean, mypy adds 0 errors on `sonarqube.py`. Live GXA re-scan (ncloc>0, no PH components) is the Coordinator post-merge op.
- scan source prefers `primary_repository.local_path` over `board.repos_path` [PH-242] — Sonar scans bound to the WRONG codebase because `_resolve_container_source` used ONLY the coarse `board.repos_path` (often the PARENT dir) and never consulted the board's linked `Repository.local_path` (the precise, git-synced code root). Concrete break: board GXA had repos_path=`/Users/.../GameX` (parent) but the code lives in `/repos/AndroidStudioProjects/GameX/GameXCore`, so the `GameX` Sonar project showed 0 LoC bound to junk dirs (backend/tests, tests/e2e). Boards that worked did so only because their repos_path COINCIDED with the code root (PH, KIM). Fix scoped to `_resolve_container_source` only: prefer the primary repo's `local_path` (used AS-IS — the column invariant guarantees container-form `/repos/...`, re-translating via `to_container_path` would raise since it expects a HOST path), defensive `to_container_path` only for a legacy host-form row, `repos_path` fallback otherwise (PH/KIM/FN/BENCH/GXI unchanged byte-for-byte). `project_key` resolution kept DECOUPLED/untouched (persisted GXA=`GameX` retained — a source change can't rename a project). `host_source` (informational) set from `to_host_path(local_path)` best-effort when the repo branch wins (cosmetic; `container_source` is load-bearing). **Async-safety guard `_loaded_primary_repository`:** reading `primary_repository` iterates `board.repositories`, which would lazy-load (`MissingGreenlet`) in async if not eager-loaded — both prod callers `selectinload` it, but the guard uses `inspect(board).unloaded` to degrade an unloaded collection to the fallback instead of crashing (never-500 hard contract; also fixed a real `test_sonarqube_scan_jobs.py` breakage where the test helper builds a bare-loaded board). GameX cleanup is a post-merge re-scan (overwrites the stale 0-LoC project in place — no explicit deletion). Verified: 7 new regression tests in `test_sonarqube_scan.py` (GXA→GameXCore subdir, no-repo fallback, PH/KIM root unchanged, no-primary fallback, legacy host-form normalize, unnormalizable fallback, unloaded-collection no-crash) + full sonar suite 121 green, ruff clean, mypy adds 0 errors.
- in-app SonarQube quality dashboard = a board-detail "Quality" tab, NOT a route / not Settings [PH-241] — the user said sonar "ana kisimda gorunmuyor" (only the header strip + the Settings config tab existed). PH-241 adds a native cyan-on-black dashboard as a THIRD `BoardDetail` tab (`#quality`), the most consistent placement with the app's existing in-board tab idiom — rejected: a dedicated `/boards/:key/quality` route (loses the shared board header + WS connection BoardDetail owns), header-strip expansion (the user wanted a real main-area surface, not a bigger strip), and the Settings tab (owns CONFIG, not the read dashboard). **Zero new data layer (Risk R6):** `SonarDashboard` is prop-driven from the SAME `["board", boardKey]` cache + `useSonarLiveCounts` the strip already uses, so the existing `sonarqube_synced` WS handler gives live refresh for free — no new endpoint, no new TanStack key family. New files `components/sonar/{SonarDashboard,MetricCard}.tsx` + `metricMeta.ts` (METRIC_META: key/label/canonical-description/unit/goodDirection/issueType/icon; GATE_META exported separately to dodge `noUncheckedIndexedAccess` on `METRIC_META[0]`). **`.codemap` gotcha (Risk R7):** the new dir is `components/sonar/` — the existing `components/sonarqube/*` glob does NOT cover it, so a new `frontend/src/components/sonar/*.tsx → components/sonarqube.md` line was added to keep the dashboard sync-gated. Reused UNCHANGED: `SonarIssueDrawer` (drill-down, lists `component:line`), `useSonarIssues`, `api/client`, `types/api`. `SonarHealthPanel` kept (not folded) + an optional `onOpenDashboard` deep-link. **LIVE browser-verified (preview, dark+light):** KIM (no analysis) → honest "Linked to kims — no analysis yet" empty state, no crash; PH (has metrics) → gate hero "Failed" + 6 cards (Bugs 0, Vulns 0, Code Smells 11, Coverage 47.0%, Duplications 1.2%, ncloc 23386) with descriptions; clicking Code Smells opened the drawer with 11 rows (first `frontend/src/pages/BoardSettings.tsx:357`) + dashboard_url; close restored focus to the card; no console errors.

- auto-scan execution via a host watcher daemon + `SonarScanJob` lifecycle [PH-239] — "Scan now" used to ONLY enqueue intent (it told the operator to hand-run `scripts/sonar-scan-board.sh kims`) so nothing ever scanned automatically and KIM's `kims` project was never auto-provisioned (404). PH-239 closes the loop: `request_board_scan` now PERSISTS a `SonarScanJob(state=queued)` row (new `sonar_scan_jobs` table; idempotent per board — re-clicking while one is queued REUSES that row, no duplicate scanner run, R5) instead of only returning intent. A NEW non-board-scoped router `backend/app/api/scans.py` exposes the watcher seam: `GET /api/scans/pending` (the queue, secret-free `[{job_id, board_key, project_key}]`), `POST /api/scans/{id}/claim` (queued→running, atomic — a second claim → 409, the double-run guard R2), `POST /api/scans/{id}/complete {success, detail}` (running→done|failed; **on success the backend immediately calls `poll_board` to ingest the metrics** so they appear within seconds, not after the 300s poll cron — the cron stays the backstop for SonarQube's async-indexing race R3; on failure it records `detail` and does NOT ingest). A NEW host daemon `scripts/sonar-scan-watcher.sh` long-polls `pending`, claims each job, runs the UNCHANGED `sonar-scan-board.sh <key>`, and POSTs `complete`. **ADR — mechanism (b) watcher chosen, NOT (a) docker-socket-into-backend** (root-equivalent host control = privilege-escalation surface, rejected on the same security posture as PH-236) NOR (c) cron (coarse 1-min granularity, no clean `running` signal — documented as a degraded fallback only in `docs/sonar-watcher.md`). **Honest RC channel (R4):** `sonar-scan-board.sh` ALWAYS exits 0 (deploy contract), so it now emits a final `SONAR_SCAN_RESULT=ok|failed|skipped` marker line the watcher parses (its exit code can't carry the scanner's real outcome). Auto-provisioning is unchanged (Community auto-creates the project on the first successful scan — no admin `projects/create`). The `scan-plan` JSON shape stays FROZEN. The honest non-scannable outcomes (unsupported/unconfigured/disabled) enqueue NO job (PH-236/235 contract preserved). Migration `e3a479aa5c01` is strictly additive (new table + 2 indexes; no existing table rewritten — safe on the live Kims DB). Runbook: `docs/sonar-watcher.md` (launchd/systemd/nohup). Verified via mocked-scanner pytest (`tests/test_sonarqube_scan_jobs.py`): queue-persist, idempotent re-enqueue, pending/claim/complete contract, 409 double-claim guard, immediate-ingest-on-success / no-ingest-on-failure. **Review hardening:** the 404 `"scan job not found"` literal is hoisted to `_JOB_NOT_FOUND` in `scans.py` (S1192, no behavior change); `claim_scan_job` reads the job via `_get_job(..., for_update=True)` (`SELECT ... FOR UPDATE`) so the read-check-write claim is row-locked — two racing watchers can't both observe `queued` and both claim (TOCTOU closed). The conflict path raises before any write so no lock lingers; `complete_scan_job` is unchanged (no lock).
- frontend "Scan now" is scan-plan-gated + honest-async, DISTINCT from Sync [PH-237] — `SonarSetupSection` gained a third admin action wired to PH-236's `POST .../sonarqube/scan` (`api.sonarqube.scan`) + `GET .../sonarqube/scan-plan` (`api.sonarqube.getScanPlan`). Key calls: (1) the scan-plan query is **lazy AND `isAdmin`-gated** (`enabled: boardKey && tabActive && isAdmin`) — scan-plan is admin-only, so firing it for a non-admin would 403-spam (Risk R1); a non-admin sees NO scan-plan call AND no Scan button (the Actions card is already `{isAdmin && …}`). (2) Support is surfaced UP FRONT: the resolved `language` + a "Scannable"/"Not scannable in Community Edition" chip render in the status panel, and `plan.supported === false` DISABLES the Scan button with the honest `reason` annotation — the user is not surprised on click. (3) The success copy is HONEST about async: `queued`/`running` → "Scan queued — analysis runs in the background; metrics appear after it completes. Re-sync to refresh." (NOT the generic "status refreshed" — that implies instant metrics, the whole point of the epic; Risk R3). All SIX `scan_status` values map to a clear inline message (Risk R5). (4) Radar icon + helper copy make Scan visually + behaviorally DISTINCT from Sync's cheap re-poll (Risk R4). (5) NO progress polling / live-scan indicator invented — the backend is fire-and-forget (no job-id endpoint); `queued` + honest copy + a four-family invalidation (Sync's three + the scan-plan key) is the whole contract (Risk R6). The `scan_status` TS field matches the snake-case wire 1:1 (a `status` rename → silent `undefined`; Risk R2). LIVE browser-verified (Playwright, `tests/e2e/ph-237-sonarqube-scan-now.spec.ts`, against real GXA/FN data): GXA (Kotlin, `supported=true`) → Scannable chip + Scan enabled → click → honest queued copy + scan-plan re-fetch; FN (C#/Unity, `supported=false`) → "Not scannable in Community Edition" + Scan disabled with reason, no POST on force-click; non-admin → no Scan button + scan-plan never fires.
- per-board scan = manifest/plan enqueue + HOST runner; docker.sock REJECTED [PH-236] — the backend container can't `docker compose run`, so `POST .../sonarqube/scan` (`request_board_scan`) only ENQUEUES (`scan_status=queued`) + the actual analysis runs HOST-side via `scripts/sonar-scan-board.sh`, which curls `GET .../sonarqube/scan-plan` (the FROZEN `{project_key, container_source, host_source, language, supported, reason, exclusions}` shape PH-237/the script depend on) and runs `sonar-scanner` with per-board `-D` props against `/repos/<path>`. Option 2 (mount `/var/run/docker.sock` into the backend so it spawns the scanner) was REJECTED — root-equivalent blast radius on a backend that already RO-mounts all of `$HOME` (PH-228) is not worth a convenience. The plan transport is an HTTP read (not a shared-volume file) to sidestep container-written/host-read uid issues (architect R2). The `sonar-scanner` compose service gained `${PROJECTS_ROOT:-${HOME}}:/repos:ro` (mirrors PH-228) so it sees board code; `/usr/src` (project-hub self-scan via `sonar-scan.sh` + `sonar-project.properties`) is UNCHANGED. LIVE-verified: `GXA` (Kotlin) → queued → host scanner auto-created the `GameX` project on the server; `FN` (Unity/C#) → unsupported.
- C# unsupported in CE → `scan_status=unsupported`, no silent fail / no fake queued [PH-236] — SonarQube Community Build cannot analyze C#/.NET (no SonarC# sensor), so `detect_board_language` flags a Unity layout (`Assets/` + `ProjectSettings/`|`Packages/`) OR `.cs`-dominant tree as `csharp` and `_language_supported` gates it to `unsupported` UP FRONT with an honest message — NOT a silent failure and NOT a fake `queued`. The host script likewise sees `supported=false` and exits 0 without scanning. Unknown/unrecognized languages stay optimistic-supported (let the scanner's sensors decide); only KNOWN-unsupported languages are gated. `scan` is DISTINCT from `sync` (sync re-polls existing analysis; scan plans a new run) — separate routes + behavior.
- honest status: no-analysis ≠ unreachable; added `status` enum + `has_analysis` [PH-235] — `build_setup_status` overloaded ONE boolean (`reachable`) to mean both "the server responded on a live attempt" AND "a cached metric exists", so on the pure-READ path `reachable_flag = bool(enabled and configured and metric is not None)` made a configured-but-never-scanned board (no metric row, e.g. GXA→GameX) render as `reachable=false` → the frontend's yellow "SonarQube server is unreachable" banner + a "Reachable off" chip + the board header's "Connect a project key" — all FALSE: the server is up, the board just has no analysis yet. The fix SEPARATES the two concepts: `has_analysis = metric is not None` (the truthful "we have data" signal) and an explicit `status` discriminator (`disabled|unconfigured|no_analysis|ok|unreachable`) that the UI keys ALL messaging off. The read path NEVER emits `reachable=false`/`unreachable` from metric-absence — absence becomes `no_analysis`; `reachable=false`/`unreachable` is reachable ONLY via the `sync` path passing a genuinely failed live attempt (so a REAL outage is still surfaced — locked by `test_build_setup_status_real_failed_sync_is_unreachable`). `reachable` is KEPT (additive/backward-compatible: best-effort = `has_analysis` on the read path) but superseded by `status`. `BoardResponse` gains `sonarqube_project_key` (additive nullable) so the board-header `SonarHealthPanel` distinguishes "no key" from "key set, no analysis yet" without a status call. **Scope = status/messaging classification ONLY** — NOT a new probe (the no-probe gotcha below stays valid); actual scanning is C2/PH-236. Browser-verified: GXA shows the honest "no analysis yet" (no false unreachable, header not "connect a key"); PH (has metrics) unchanged (`status=ok`, full panel).
- `board.repos_path` is now editable via PATCH `/api/boards/{id}` — and sonar key derivation reads its basename [PH-230] — C3 (epic PH-227 FINAL) extends `api_update_board` (`api/boards.py`) to accept `repos_path` (via `BoardUpdate.repos_path`) and `update_board(repos_path=...)`. **Validation reuses the detect/sonar contract**: a non-empty path is run through `repo_paths.to_container_path` inside the handler → `HTTPException(422)` on relative / `..` / outside-HOST_HOME (`RepoPathError` is a `ValueError` subclass authored for exactly this mapping); an empty string clears the path to NULL (board with no path = detection + basename-key disabled, falls back to `board.key.lower()` per PH-229's `_path_basename_key`). **No new auth gate**: the PATCH keeps `current_actor` (Name/Description stay pm-editable) — admin-gating is done in the BoardSettings UI field only, NOT widened to `require_board_admin` (would change Name/Description editability too). Consequence for THIS component: editing a board's path from the UI now changes the sonar default project key for non-PH boards (basename of the new path) on the NEXT setup/sync — PH still short-circuits to the `project-hub` literal first (unchanged). No migration (column exists since PH-228); `setup_board_project`/`build_setup_status`/`sync_board_now` signatures unchanged.
- Default project key is now path-basename-aware, PH literal kept FIRST [PH-229] — C2 (epic PH-227) makes `derive_default_project_key` "use the board path": a non-PH board with a `repos_path` derives its default key from the path basename (`kims`, `GameX`) instead of the bare `board.key.lower()`, because the basename IS the natural scanner project identity and now that PH-228 gives every board a real path, the key should reflect where the code lives. **The PH-literal branch is deliberately FIRST and never basename-derived** — even though `basename(/repos/Documents/project-hub)` coincidentally equals `project-hub`, depending on that coincidence is fragile: the key MUST equal `sonar-project.properties` `sonar.projectKey` or the post-merge scanner WRITE and the poller READ diverge (dashboard goes empty). So PH short-circuits before the basename path. **Total / never-raises:** `_path_basename_key` validates the path through `to_container_path` (consistency with detect's guard) and falls back to `board.key.lower()` on a null path, an empty basename, or a `RepoPathError` — so `setup_board_project` keeps PH-223's never-500 contract for a bad path. Scope held tight: only the DEFAULT-KEY derivation changed; `setup_board_project` signature, idempotent write-on-change, the scan-time-auto-create provisioning model, `build_setup_status`/`sync_board_now`, the secret-free `SonarSetupStatus`, and the dashboard-URL builder are ALL unchanged. The scanner invocation stays post-merge in `sonar-scan.sh` (NOT added here). No migration (logic-only; consumes PH-228's `repos_path`).
- settings-tab Setup/Sync UI; no second sync surface on the health panel [PH-226] — the
  one-click Setup + Sync buttons + status panel live in the BoardSettings `sonarqube` tab
  (mirrors the repository-tab admin-gating precedent), NOT on the board-detail
  `SonarHealthPanel` header (kept purely presentational). Status query key
  `['board', boardKey, 'sonar-setup']` is isolated from BoardResponse; Sync invalidates
  board.health + the sonar-issues family so the header tile still refreshes after a sync from
  settings. 403 (non-admin write) → buttons hidden + inline "Admin role required" (no crash).
- setup/sync/status endpoints + scan-time auto-create [PH-223] — "setup" = persist the
  project key only (auto-create covers one-click; no admin token, model (b) out of scope).
- sync = re-poll, NOT re-scan [PH-223] — reuse `poll_board` to read existing analysis
  within the 10s bound; a synchronous scanner run would block/timeout, so scans stay
  post-merge.
- GET status makes no live probe [PH-223] — reachability is derived from cached-metric
  freshness so a read never hangs on a down server; only `sync` makes a live attempt.
- issues proxy + host-facing dashboard deep links [PH-203] — graceful-200 status flags;
  `sonarqube_scan_url` only, never the internal URL or token.
- poll-cron + single upsert metric row per board [PH-193] — lifespan-gated task, layered
  error isolation, `sonarqube_synced` WS event for live board-health.

## Known gotchas

- C# (Unity 6) project generation: `SyncVS.SyncSolution` is a NO-OP and `DefaultExternalCodeEditor.SyncAll()` writes a `.sln` but NO `.csproj` — drive `SdkStyleProjectGeneration` directly [PH-257 qa_failed fix] — the QA-discovered trap: on Unity 6000.x the legacy `UnityEditor.SyncVS.SyncSolution` exits rc 0 but generates nothing (project generation moved into the code-editor packages). In a headless/CI project with no external editor selected, the ACTIVE `IExternalCodeEditor` is `UnityEditor.DefaultExternalCodeEditor`, whose `SyncAll()` writes only a `.sln` (no `.csproj`) — and a `.sln` referencing `.csproj` files that don't exist makes `dotnet build` fail `MSB3202 "project file not found"`. Even `new ProjectGeneration().GenerateAndWriteSolutionAndProjects()` via reflection NREs (the abstract base ctor doesn't wire the providers). The WORKING path is the CONCRETE subclass `Microsoft.Unity.VisualStudio.Editor.SdkStyleProjectGeneration` (Unity 6 default; `LegacyStyleProjectGeneration` for older setups) whose parameterless ctor wires `AssemblyNameProvider`/`FileIOProvider`/`GUIDProvider` — its `GenerateAndWriteSolutionAndProjects()` writes the real `.csproj` set. The runner injects a temporary `Assets/Editor/JarwisSolutionSync.cs` that drives this via reflection (compile-safe across packages), then cleans it up. **Success is checked by `.csproj` FILESYSTEM existence, NOT Unity's rc and NOT the `.sln`** (the `.sln` write is order-dependent in batch mode and often absent — build the `.csproj` set directly when there's no `.sln`). Do NOT revert to `-executeMethod UnityEditor.SyncVS.SyncSolution` or trust rc 0.
- the injected `JarwisSolutionSync.cs` gets baked into the generated `Assembly-CSharp-Editor.csproj` — strip its `<Compile Include>` line after deleting it or `dotnet build` fails CS2001 [PH-257] — the injected editor script is part of the Editor assembly when the `.csproj` is generated, so the generated csproj carries a `<Compile Include="...JarwisSolutionSync.cs" />`. The runner deletes the `.cs` after generation, which would leave a dangling reference → `dotnet build` errors `CS2001 source file not found`. `_cleanup_injected` therefore also `sed`-strips every `JarwisSolutionSync.cs` line from the generated `.csproj`. These `.csproj` are generated/ephemeral (gitignored Unity artifacts), so stripping them is NOT mutating the user's committed project.
- Unity references `nunit` from EVERY generated `.csproj` → SonarScanner flags ALL C# as TEST → MAIN ncloc=0; force `<SonarQubeTestProject>false</SonarQubeTestProject>` [PH-257] — the second QA-blocking trap behind "indexed but 0 ncloc". Unity bundles `com.unity.ext.nunit` and the generated `Assembly-CSharp.csproj` (the real game code) references `nunit.framework`, so SonarScanner for .NET's test-project heuristic classifies it (and every other csproj) as a TEST project → it imports only test-code results and reports `ncloc=0` for MAIN ("Your project contains only TEST-code ... and no MAIN-code" CE warning). The fix (`_mark_main_projects`) injects `<SonarQubeTestProject>false</SonarQubeTestProject>` into the first `<PropertyGroup>` of each csproj whose filename does NOT contain "test" (genuine `*.Tests.*` keep auto-detection). With the fix FN reports `ncloc≈14.9k cs` + real violations. Use `awk` (write-temp-then-mv), NOT BSD `sed`'s `0,/re/s//`: that GNU address form SILENTLY no-ops on macOS BSD sed (a real false-negative that shipped a no-op marker once before the awk rewrite).
- the C# (.NET) host runner uses `host_source`, NOT `container_source` — and re-guards the toolchain even when the backend said `queued` [PH-257] — every OTHER language scans the container `/repos/<rel>` (`container_source`) via the dockerized `sonar-scanner-cli`, but C# runs `dotnet build <sln>` on the HOST, so `run_dotnet_scanner` reads the plan's **`host_source`** field (the only place it is load-bearing — elsewhere it is "informational only"). A null/absent `host_source` → honest `skipped`. ALSO: `SONAR_DOTNET_ENABLED=true` only declares the operator's *intent* — the backend container can't verify the host `dotnet`/`dotnet-sonarscanner` actually exist, so the runner re-checks them and honest-`skipped`s (rc 2, NEVER fake `ok`) if the flag is on but a tool is missing. The marker stays `ok|failed|skipped` (no new value); only the backend `scan_status` enum gained `needs_dotnet_setup` (for the UI tooltip). The host scanner must use `$SONAR_SCAN_URL` (host-reachable), NOT `$SONARQUBE_URL` (the compose-internal `sonarqube:9000` hostname won't resolve on the host).
- the per-repo SCAN result type is `SonarRepoScanResult`, NOT the board `SonarScanResult` — keyed off `job_id` [PH-256] — the per-repo scan endpoint returns a DIFFERENT shape than the board-level one: `{repo_slug, project_key, language, scan_status, job_id, message}` (has `job_id`, has NO `container_source`). The FE `repoScanFeedback` keys the honest queued-vs-non-scannable split off `result.job_id != null` FIRST (a non-null job_id ⇒ scannable ⇒ "queued" copy; null ⇒ non-scannable ⇒ the honest `scan_status` reason, NEVER a fake queued), then falls back to the `scan_status` switch. Do NOT reuse the board `SonarScanResult` type for the per-repo call — its `job_id` would read `undefined` (always falsy) and an unsupported repo would render as queued. The two types intentionally co-exist in `types/api.ts`.
- the per-repo card actions are gated by `useBoardRole(boardKey) === "admin"` AND `repo.repo_slug != null` [PH-256] — `RepoCardActions` mounts ONLY when `isAdmin` (BoardDetail computes it, threads it in) AND the card has a non-null `repo_slug` (the selector). A non-admin → the sub-component is not rendered AT ALL, so NO per-repo mutation/query fires (not just hidden buttons — the backend 403-guard is defense-in-depth, not the only gate; AC-6). The null-identity AGGREGATE row (repo_slug == null) has no selector → actions suppressed there (it is board-level, covered by `SonarSetupSection` in BoardSettings). Do NOT render the actions on the aggregate row or for non-admins, and do NOT drop the `repo.repo_slug != null` guard — `selector = repo.repo_slug as string` is narrowed on that invariant.
- every per-repo mutation invalidates ONLY `["board", boardKey]` (refetchType active) [PH-256] — that single key drives `BoardResponse.repo_health` → the touched card re-renders in place. Do NOT add the `sonar-issues`/`sonar-setup` invalidations the board-level `SonarSetupSection` uses — the per-repo cards read NEITHER; the extra invalidations would be dead refetches. The Setup form's pre-fill is `value={repo.project_key}` (the derived default the backend already surfaces on every card, scanned or not), kept in local `useState` so it edits independently; on success the form closes and the card refetches.
- per-repo SYNC of a never-scanned repo is 200 + unscanned card, NEVER 404 [PH-255] — `sync_repo_now` / `POST .../repositories/{selector}/sonarqube/sync` returns an honest `unscanned_repo_health` `RepoHealth` (null gate, null 7 metrics, null `fetched_at`, derived non-null `project_key`, `dashboard_url=null`) when SonarQube has no analysis for the repo's key — `poll_repo` returns False (no row written, no raise) for the fetch-None / unreachable / never-scanned case and the endpoint falls through to the unscanned card (decision B). "No analysis yet" is an honest steady state the FE already renders, NOT a "nothing to sync" 404. Do NOT "fix" this by raising 404 when no metric row exists — that breaks the PH-252 contract and the per-repo card. The ONLY 404s are unknown board/repo (via `get_board`/`resolve_repository`). Likewise per-repo SCAN of a non-scannable repo is 200 + honest `scan_status` + `job_id=null` (decision A), never a 409/422.
- `sync_repo_now`'s serializer import is FUNCTION-LOCAL to break a module cycle; re-query the metric with `selectinload(repository)` [PH-255] — `services/serializers.py` imports `derive_repo_project_key`/`_dashboard_url` from `services/sonarqube.py` at module load, so a top-level `from app.services.serializers import repo_health, unscanned_repo_health` in sonarqube.py is a CIRCULAR import (ImportError at startup). `sync_repo_now` imports them INSIDE the function; the `RepoHealth` return annotation is a `TYPE_CHECKING`-only import (never evaluated at runtime thanks to `from __future__ import annotations`). Second trap: after `poll_repo` upserts a fresh row, the route's in-memory `board.sonarqube_metrics` snapshot is STALE and a freshly-loaded metric's `.repository` is NOT eager-loaded → `repo_health(metric)` would lazy-load it (`MissingGreenlet` in async). `sync_repo_now` therefore RE-QUERIES the `(board_id, repo_id)` metric with `.options(selectinload(SonarQubeMetric.repository))` before serializing. Do NOT serialize off the route's stale `board.sonarqube_metrics` collection, and do NOT move the serializer import to module top.
- per-repo setup conflict check is INTRA-board only and spans DERIVED keys, not just stored [PH-254] — `setup_repo_project` rejects (409 `Conflict`) only when ANOTHER repo of the SAME board already holds the effective key, comparing `derive_repo_project_key(board, sibling)` for EACH sibling (so an unset sibling still defends its `<base>-<slug>` / inherited-primary slot, not just an explicitly-stored key). Two traps: (1) do NOT widen it to a cross-board check — boards are independent Sonar surfaces and there is no cheap authoritative cross-board check without a network call, which would violate the never-probe persist-only contract (decision a); cross-board reuse is INTENTIONALLY allowed. (2) do NOT drop the derived-key comparison down to stored-keys-only — an operator could then pin `GameX-gamexsdk` onto a different repo while gamexsdk (no explicit key) silently derives the same key, producing the exact two-cards-one-project collision the 409 exists to prevent. The same-repo self-compare is skipped (`sibling.id == repo.id`) so re-pointing a repo at its own current key is idempotent, never a self-conflict. The board repo set is small (no N+1 concern).
- the per-repo setup route lives on the BOARDS router, reusing `require_board_admin` [PH-254] — `POST /api/boards/{board_id}/repositories/{selector}/sonarqube/setup` is registered on `api/boards.py` (prefix `/api/boards`), NOT on the repositories router (prefix `/api/boards/{board_key}`), specifically so it can reuse `require_board_admin` (whose path param is literally `board_id`, KEY-or-UUID) verbatim. boards.router is included BEFORE repositories.router and the path `/repositories/{selector}/sonarqube/setup` is distinct from any repositories-router route, so there is no shadowing — but do NOT "move it next to the other repo routes": that would need a second admin-dep variant and split the admin gate. The `{board_id}` vs `{board_key}` param-name difference is cosmetic (matching is structural).
- blank-key 422 is mapped in the ROUTE, not the service [PH-254] — `setup_repo_project` raises a plain `ValueError("project_key must not be blank")` on a whitespace-only explicit key; the route catches it and re-raises `HTTPException(422)` (mirroring the existing `api_update_board` RepoPathError→422 pattern). The `Conflict` 409 path is DIFFERENT — it is a `ProjectHubError` subclass auto-handled by `register_exception_handlers` (no per-route try/except), and its `conflicting_repo` attr is surfaced because it was added to `_error_payload`'s whitelist. Do not move the blank-key check into a Pydantic `min_length` — that does NOT trim, so `"   "` would pass validation and persist a blank key.
- `repo_health_list` enumerates `board.repositories`, NOT `board.sonarqube_metrics` [PH-252] — the source of `BoardResponse.repo_health` is the LINKED repos (one card per repo), LEFT-JOINed in-Python to each repo's latest metric. Do NOT "simplify" it back to iterating the metric rows: that silently DROPS any linked-but-unscanned repo (the original PH-252 bug — GXA's 2 metric-less siblings vanished, `#quality` showed 1 card). The unscanned branch (`_unscanned_repo_health`) MUST keep `dashboard_url=None` and null `quality_gate_status`/7-metrics/`fetched_at` — `dashboard_url` is null-UNTIL-FIRST-SCAN (a derived URL 404s on a not-yet-created Sonar project; the FE omits the link on null) and `quality_gate_status == null` is the single canonical "never scanned" discriminator the FE keys on. `project_key` stays non-null (`derive_repo_project_key` is total). The `not board.repositories` fallback (freshest-first aggregate) is what keeps a legacy repo-less board working — do not remove it.
- `RepoHealth.fetched_at` is nullable (`datetime|None` / `string|null`); narrow before `new Date()`/`relativeSynced` [PH-252] — widened required→optional so an unscanned repo can carry `fetched_at=None`. The FE `SonarRepoHealthCards` footer guards `scanned && repo.fetched_at ?` and `metricMeta.relativeSynced` early-returns `""` on null. If the TS field is re-narrowed to non-null (or the guards dropped), `tsc --noEmit` under strict-null breaks AND a null `fetched_at` would `new Date(null)` → "Invalid Date". `BoardHealth.fetched_at` is DIFFERENT — it stays non-null (a `BoardHealth` only exists for a scanned primary metric); do not widen it too.
- the per-repo `primary` badge is driven by `repo_health[].is_primary`, NOT the member-gated `/repositories` [PH-251] — `SonarRepoHealthCards` badges off `repo.is_primary` (from the un-gated `BoardResponse.repo_health[]`). Do NOT "simplify" it back to comparing `repo.repo_slug === primarySlug` where `primarySlug` comes from `GET /api/boards/{key}/repositories`: that endpoint is `_require_board_member`-gated and 403s for a non-member authorized viewer, so `primarySlug` becomes `undefined` and the badge SILENTLY disappears (the original PH-249 bug). `is_primary` is REQUIRED (non-Optional) on both the backend `RepoHealth` schema and the `types/api.ts` mirror — if the TS field is dropped, `repo.is_primary` reads `undefined` → falsy → the same silent badge loss returns. `reposQuery`/`primarySlug` are still computed in `BoardDetail` but ONLY for the `#graph` branch switcher — do not re-thread them into the quality-tab cards.
- `serializers.repo_health()` reads `metric.repository.is_primary` — requires the repository eager-loaded [PH-251] — `is_primary=repo.is_primary if repo is not None else False`. `Repository.is_primary` is a non-null Boolean (`core.py:379`) and `metric.repository` is eager-loaded on the board-detail path (`selectinload(Board.sonarqube_metrics).selectinload(SonarQubeMetric.repository)`), so this is a pure read of loaded data (async-safe, never-500). A legacy board-level aggregate metric (`repo_id IS NULL` → `metric.repository is None`) degrades to `False` — mirroring how `repo_slug`/`repo_name` degrade to `None`. Do NOT add a lazy-load / extra query for `is_primary`; the relationship is already loaded.
- `sonar-scan-board.sh` ARRAY-parses `/scan-plans`, NOT the object `/scan-plan` [PH-248] — PH-248 switched the runner from the single-object `GET .../sonarqube/scan-plan` to the LIST `GET .../sonarqube/scan-plans` (a JSON ARRAY). The old `_json_get key` (object) was replaced by array helpers `_plan_count` / `_plan_field <index> <key>` / `_plan_index_by_slug <slug>`, each keeping the jq→python3→empty two-tier fallback and `// empty` leniency. Do NOT revert it to the object endpoint to "simplify" — that scans ONLY the primary repo (the pre-PH-248 bug where GXA's siblings were never analyzed). The single-object endpoint is KEPT in the backend purely as the deployed-old-runner back-compat net; the current runner does not touch it.
- The watcher MUST forward `repo_slug` or the runner falls to Mode 2 iterate-all [PH-248] — `sonar-scan-board.sh` selects its mode from the optional 2nd arg: a non-empty `repo-slug` → Mode 1 (scan EXACTLY that one repo, the watcher's normal 1-job=1-repo path); empty/absent → Mode 2 (iterate ALL supported repos). If `_parse_pending`'s 3rd TSV column is dropped (or the loop stops passing it), each claimed per-repo job silently runs Mode 2 and N×-scans the whole board (GXA's 3 jobs → 9 scanner runs + 3 ingests racing the same metric rows — Risk R1). An EMPTY slug is treated identically to absent (a legacy board-level job has `repo_slug=null` → Mode 2 is the intended back-compat). Keep the column AND the forward together.
- Mode 2 NEVER aborts on one repo's scanner failure, and the script ALWAYS exits 0 in BOTH modes [PH-248] — each `run_scanner` call is wrapped `set +e` / capture RC / `set -e`, and Mode 2's loop logs the RC + continues so the remaining repos still scan; the aggregate `SONAR_SCAN_RESULT` is `failed` if ≥1 scanner exited non-zero (so a human sees it), `ok` if ≥1 scanned and none failed, else `skipped`. Do NOT add `set -e` around the loop body or an early `return`/`exit` on a non-zero RC — it would abort the remaining repos and break the best-effort never-block contract (the PH-194/208 hard rule). And do NOT introduce `timeout` to bound a scanner: macOS has no GNU `timeout` (only the in-container `sonar-scan.sh` may use it); curl's native `-m` is the only portable time bound here.
- `build_scan_plan` is now the PRIMARY-repo wrapper; `build_scan_plans` is the multi-repo source of truth [PH-246] — `build_scan_plan(board)` no longer computes a plan directly; it returns `build_scan_plans(board)[0]` (the primary repo's plan, or the board-level fallback for a repo-less board). To scan a multi-repo board you MUST call `build_scan_plans` (N elements) — calling only `build_scan_plan` silently scans ONLY the primary repo (the sibling repos are invisible). The new `/scan-plans` list endpoint exposes all N; the kept single-object `/scan-plan` is back-compat for the deployed host script and returns ONLY the primary.
- `Board.sonarqube_project_key` is the LEGACY fallback + PH self-scan identity, NOT the per-repo scan source of truth [PH-246] — per-repo scanning reads `Repository.sonarqube_project_key` (via `derive_repo_project_key`). The board column is KEPT only as (a) the value the primary repo INHERITS at migration/derivation time (so KIM/PH/GXA-primary keep their exact project — no rename) and (b) the PH self-scan identity that MUST equal `sonar-project.properties` `sonar.projectKey=project-hub`. Do NOT re-route per-repo scanning back through the board column, and do NOT drop it (a later cleanup ticket owns that) — dropping it would orphan the legacy fallback + break the PH self-scan key agreement.
- `SonarQubeMetric` is 1:N per `(board_id, repo_id)` — never `scalar_one_or_none()` on `board_id` alone [PH-246] — the unique constraint relaxed from `(board_id)` to `(board_id, repo_id)`, so a board may now carry N metric rows. Code that did `select(SonarQubeMetric).where(board_id==...).scalar_one_or_none()` will raise `MultipleResultsFound` on a multi-repo board. `build_setup_status` was changed to `.order_by(fetched_at.desc()).first()` (freshest representative); `BoardResponse.health` uses `Board.primary_sonarqube_metric` (matches the primary repo's row, falls back to a legacy `repo_id IS NULL` row, else the first). A legacy board-level metric (repo_id NULL — e.g. KIM, no linked repo) is still valid.
- The migration's FK + unique-constraint changes MUST go through `batch_alter_table` (SQLite) [PH-246] — `b1468dc15870` adds the `repo_id` FKs AND relaxes the metric unique constraint. SQLite has NO `ALTER TABLE ... ADD/DROP CONSTRAINT`; a bare `op.create_foreign_key`/`op.drop_constraint` raises `NotImplementedError` on the live KIM SQLite snapshot. Both the FK creation and the unique-constraint relax/restore are wrapped in `op.batch_alter_table` (copy-and-move table rebuild on SQLite; plain in-place DDL on Postgres). The backfill of `repo_id` runs BEFORE the relaxed `(board_id, repo_id)` constraint is created so it's already unique (R3). Verified driving `upgrade()`/`downgrade()` on a real `sqlite://` engine (`test_migration_backfill_and_relaxed_constraint_on_sqlite`). Do NOT "simplify" any of these to a bare `op.*` call — it breaks the SQLite path.
- `**/*.java` is NOT in `_SCAN_EXCLUSIONS` — it is appended PER-PLAN only for non-java boards [PH-244] — the base constant deliberately omits `**/*.java` because a java-PRIMARY board must keep its `.java` analyzable. `build_scan_plan` computes `exclusions = _SCAN_EXCLUSIONS` then, when `language != "java"`, appends `,**/*.java`. Do NOT "simplify" by moving `**/*.java` into the base constant — that would silently make every java board source-blind (zero java analysis). The test `test_scan_exclusions_cover_vendored_native_globs` asserts the base set has NO `**/*.java`, and `test_build_scan_plan_java_board_keeps_java_analyzable` asserts a java board's plan does NOT exclude it.
- `sonar-scan-board.sh` carries `-Dsonar.java.binaries=.` but `sonar-scan.sh` must NEVER [PH-244] — the static java-binaries guard is BOARD-SCAN-ONLY (it stops SonarQube's JavaSensor from aborting on incidental vendored `.java` in an arbitrary board tree). The PH self-scan (`sonar-scan.sh`) has no `.java` and must stay byte-for-byte unchanged; `test_ph_self_scan_script_unchanged_no_basedir` now also asserts `sonar.java.binaries` is absent from it. Adding the flag to the wrong script would silently alter the PH self-scan's java handling. On a board that LEGITIMATELY needs real binaries, `.` yields source-only java (no bytecode rules) — accepted limitation, NOT a place to hardcode `target/classes` (per-board java-binaries discovery is a future ticket).
- the broadened vendored globs target path SEGMENTS, not whole subtrees of app code [PH-244] — `**/cpp/**` matches an Android-NDK `src/main/cpp` segment, never `src/main/kotlin|java`; `**/toolchains/**`/`**/androidndk/**`/`**/ndk/**` match NDK toolchain trees; `**/external/**`/`**/bazel-*/**` match bazel vendored externals; `**/vendor/**`/`**/third_party/**` match conventional vendored deps; `*.so`/`*.a`/`*.o` are binaries. The accepted edge (Risk R2): a board with a LEGIT app dir literally named `toolchains`/`vendor` would be over-excluded — extremely rare for a CE-analyzable language, and the 6.1 GB NDK win dominates. Do NOT widen these to bare directory names (e.g. `external` without `**/.../**`) — keep the glob form so they only match path segments.
- A board scan MUST pin `-Dsonar.projectBaseDir=$CONTAINER_SOURCE` or it bleeds PH's `sonar-project.properties` [PH-243] — the `sonar-scanner` compose service has `working_dir=/usr/src` with project-hub bind-mounted there (`.:/usr/src:ro`), and `/usr/src` carries PH's OWN `sonar-project.properties`. sonar-scanner-cli loads `sonar-project.properties` from `projectBaseDir`, which defaults to the CWD — so without the pin EVERY board scan inherits PH's config (`sonar.tests=backend/tests,tests`, coverage, python.version) and the board's project shows PH's test dirs at 0 ncloc. Setting baseDir to the board's own root (no sonar-project.properties there) is the isolation. Do NOT "simplify" by removing the baseDir flag and relying only on `-Dsonar.sources` — sources masks `sonar.sources` but NOT `sonar.tests`, which is exactly why the bleed happened.
- `-Dsonar.sources` does NOT mask `sonar.tests` — only baseDir isolation stops the test-dir bleed [PH-243] — the original board-scan passed `-Dsonar.sources=/repos/<path>` and assumed that fully isolated the board. It only overrode `sonar.sources`; the inherited `sonar.tests=backend/tests,tests` (from PH's props) stayed live, so PH's `backend/tests` + `tests/e2e` showed up as TEST components in the board project. The real fix is baseDir (no foreign props loaded at all) + an explicit empty `sonar.tests=`; never add board-scan `-D` neutralizers one property at a time expecting `-Dsonar.sources` to cover the rest.
- `scripts/sonar-scan.sh` (PH self-scan) must NEVER pin `projectBaseDir` [PH-243] — the two scripts are siblings but OPPOSITE: `sonar-scan-board.sh` pins baseDir to escape PH's props; `sonar-scan.sh` (the post-merge PH self-scan) deliberately keeps CWD `/usr/src` so it DOES load `sonar-project.properties` (that file IS the PH self-scan config). Editing the wrong script (adding baseDir to `sonar-scan.sh`) silently breaks the PH self-scan. `test_ph_self_scan_script_unchanged_no_basedir` asserts `sonar-scan.sh` carries no `projectBaseDir`/`-Dsonar.tests=`.
- `detect_board_language` Kotlin/Gradle marker fires BEFORE the extension tally [PH-243] — a root `build.gradle.kts`/`settings.gradle.kts` short-circuits to `kotlin` regardless of the file tally, because a real Android/Kotlin project's deep `src/main/kotlin` tree (or stray `.py` build scripts) can out-tally or exhaust the bounded `_DETECT_MAX_FILES=4000` walk and misclassify as `python` — which would wrongly drive `_language_supported`. The marker is a conservative root-file presence check (won't misfire on a real python repo, which has no `*.gradle.kts`). It is a sibling of the Unity `Assets/`+`ProjectSettings/` shortcut and must stay ordered after Unity but before the tally.
- `Repository.local_path` is ALREADY container-form (`/repos/...`) — do NOT pass it through `to_container_path` [PH-242] — the column invariant (core.py: NOT NULL, "must start with `/repos/`") means the primary repo's `local_path` is the IN-CONTAINER path already. `to_container_path` expects a HOST path under `HOST_HOME` and RAISES `RepoPathError` for a `/repos/...` input, so `_resolve_container_source` uses `local_path` AS-IS when it starts with `settings.repos_root`; `to_container_path` is only the DEFENSIVE branch for a legacy/host-form row, and even then a `RepoPathError` falls back to `repos_path` (never-500). Contrast `board.repos_path`, which IS a HOST path and DOES go through `to_container_path`.
- reading `board.primary_repository` requires `repositories` eager-loaded — guard via `_loaded_primary_repository` [PH-242] — `primary_repository` iterates `self.repositories`; in async an UNLOADED collection triggers a lazy-load → `MissingGreenlet` (crash). Both production scan callers go through `get_board`, which `selectinload(Board.repositories)`, so the primary branch is live there. But any bare-loaded `Board` (e.g. a test helper or a future caller doing `select(Board)` without the option) must NOT crash — `_loaded_primary_repository` checks `inspect(board).unloaded` and returns None (→ `repos_path` fallback) when the collection isn't loaded. Do NOT remove this guard to "simplify"; it is the never-500 backstop and is regression-tested (`test_resolve_source_unloaded_repositories_falls_back_no_crash`).
- `sonar-scan-board.sh` ALWAYS exits 0, so read `SONAR_SCAN_RESULT=`, NOT its exit code [PH-239] — the per-board runner hard-guarantees exit 0 (the PH-194/208 deploy contract: a scan problem must never block). Its exit code therefore tells a caller NOTHING about whether the SCANNER succeeded. The watcher must parse the final `SONAR_SCAN_RESULT=ok|failed|skipped` marker line the script now emits on stdout — keying off `$?` would report every run (even an auth failure or a skipped unsupported board) as success and falsely ingest/complete-success. `skipped` (disabled/unconfigured/unsupported/unreachable — no analysis produced) is completed as `failed` with a detail, NOT success.
- `/api/scans/*` is NOT board-scoped — it uses `current_actor`, not `require_board_admin` [PH-239] — a scan job is addressed by its own id (the watcher has no board context when draining the queue), so the three endpoints live on their own `/api/scans` router gated by an authenticated actor (the watcher sends the same admin bearer the host scripts already use via `SONAR_API_TOKEN`). Do NOT try to reuse `require_board_admin` here (it needs a `board_id` path param that these routes do not have). The 404 (unknown/malformed job id) and 409 (illegal lifecycle transition) are DELIBERATE client-error signals, not never-500 violations.
- The scan-plan query MUST stay `isAdmin`-gated, or non-admins 403-spam [PH-237] — `GET .../sonarqube/scan-plan` is admin-only (`require_board_admin`). The frontend's scan-plan `useQuery` is `enabled: Boolean(boardKey) && enabled && isAdmin` — drop the `isAdmin` clause and every non-admin viewing the SonarQube tab fires a guaranteed-403 request (and a retry storm without `retry:false`). The Scan button itself is already hidden for non-admins (the Actions card is `{isAdmin && …}`), but the QUERY is the easier thing to leave ungated. Keep both gated.
- The `SonarScanResult` TS field is `scan_status` (snake), NOT `status` [PH-237] — the wire key from `SonarScanResponse` is `scan_status`; `request<T>` does NO key remapping, so naming the interface field `status` (like `SonarSetupStatus.status`) makes every `switch` branch read `undefined` and silently fall through to the default. The interface field name must match the JSON 1:1. (`SonarSetupStatus.status` legitimately matches ITS wire field `status` — different endpoint, do not copy the name across.)
- "Scan now" success is async — `queued` does NOT mean metrics are present [PH-237] — the Scan button's success copy deliberately says metrics appear AFTER the background host run ("re-sync to refresh"), never "status refreshed". The scan `onSuccess` invalidates the status/board/issues families + the scan-plan key, but the SonarHealthPanel tile will still show the OLD (or no) analysis until `scripts/sonar-scan-board.sh <key>` runs on the host AND a subsequent poll/sync ingests measures. Do NOT "fix" this by reusing the generic "Done — status refreshed" line — that re-introduces the dishonest instant-metrics implication the epic exists to kill.
- The `sonar-scanner` compose service NEEDS the `${PROJECTS_ROOT:-${HOME}}:/repos:ro` mount or board scans see no code [PH-236] — it must mirror the PH-228 backend mount EXACTLY (same `${PROJECTS_ROOT:-${HOME}}` left side, same `/repos` right side) so a board's `repos_path` translated by `to_container_path` (`/repos/<rel>`) resolves to the SAME tree the backend detected the language from. If the two mounts diverge, the scanner's `-Dsonar.sources=/repos/<path>` points at nothing and the analysis is empty. The `/usr/src` mount (project-hub self-scan) is kept alongside — do NOT replace it.
- `scan` returns `queued` but metrics only appear after the host runner runs AND the next poll/sync — it is async BY DESIGN [PH-236] — `POST .../sonarqube/scan` does NOT analyze (the backend can't `docker compose run`); it records intent. You must run `scripts/sonar-scan-board.sh <key>` (the message tells you the exact command) for the analysis, then the PH-193 poll cron or a `sync` ingests the measures. So a `queued` response with a still-`no_analysis` board for a minute is expected, not a bug.
- `to_container_path` calls its OWN `get_settings` (in `repo_paths`) — tests must patch BOTH [PH-236] — patching only `sonarqube.get_settings` leaves `repo_paths.get_settings` returning the REAL `host_home`, so a tmp_path under `/tmp` raises `RepoPathError` and the scan path degrades to `error` instead of `queued`. `test_sonarqube_scan.py` patches `sonarqube`, `repo_paths`, AND `app.api.boards` get_settings together (see `_patch_all`). Same pitfall applies to any future test driving the path-translation branch.
- `detect_board_language` reads the FILESYSTEM, not just a string [PH-236] — unlike `derive_default_project_key`'s basename (a pure string), language detection `os.walk`s the real tree at `container_source`, so it needs the board code actually mounted at `/repos/<rel>` (the scanner mount). A configured board whose path is valid-but-unmounted detects `None` (generic sources scan) — that is the optimistic path, not an error. It is bounded (`_DETECT_MAX_FILES=4000`, skip-dirs pruned) so a huge Unity asset tree can't stall the cheap `scan` endpoint.
- The PH default key MUST be `project-hub` (not `ph`, not basename-derived) [PH-223 / PH-229] — it has to equal
  `sonar-project.properties` `sonar.projectKey`, or the scanner WRITE and the poller
  READ diverge and the dashboard shows empty. PH-229 added a path-basename default for
  non-PH boards but kept the PH-literal branch FIRST in `derive_default_project_key` so PH
  is NEVER routed through the basename path (even though `basename(.../project-hub)` would
  also yield `project-hub`, the literal must not depend on that coincidence). If you reorder
  that function, PH must short-circuit before any path logic.
- The basename default uses the HOST path, and a typo'd (unmounted) path still yields a key [PH-229] — `_path_basename_key` derives a STRING basename and only validates the path is well-formed + under `HOST_HOME` (via `to_container_path`); it does NOT check the path exists on disk. So a board whose `repos_path` is syntactically valid but not mounted (typo) still produces a basename key — that is intentional (sonar config is a string identity, decoupled from filesystem presence, unlike DETECT which needs the dir to exist). A path that is null, empty, or raises `RepoPathError` (outside `HOST_HOME` / `..`) is the only case that falls back to `board.key.lower()`.
- Setup persists the key even when `sonarqube_enabled=false` [PH-223] — the status then
  reports `enabled=false` so the UI shows "linked, but disabled", not a live link.
- NEVER add a synchronous reachability probe to `GET .../status` [PH-223 / PH-235] — a read
  must not block on a down SonarQube. (The no-probe rule still holds.) **CORRECTED (PH-235):**
  the old "report `reachable` from `last_metric_fetched_at`" guidance was the BUG — deriving
  `reachable` from metric presence made a configured-but-never-scanned board (no metric) report
  a FALSE `unreachable`. Metric presence now drives **`has_analysis`**, NEVER `reachable`. The
  pure-read path emits `status=no_analysis` (not `unreachable`) for a configured board with no
  metric; `reachable=false`/`status=unreachable` comes ONLY from a real failed live `sync`. Do
  NOT re-couple `reachable` to metric freshness — that re-introduces the false-outage signal.
- Secret leak is the HIGH risk [PH-223 / PH-203] — `SonarSetupStatus` / `SonarIssuesResponse`
  and every log line must never carry `sonarqube_token` or the compose-internal
  `sonarqube_url`; only `sonarqube_scan_url`-derived host links.
- A genuinely missing board is a legit 404 [PH-203 / PH-223 / PH-233] — the never-500 rule
  applies only to SonarQube degradation, not to a non-existent board (`get_board` → NotFound).
  For admin-gated setup/sync, `require_board_admin` now resolves the board via `get_board`
  FIRST, so an unknown board (KEY or UUID) → **404** (resolve-before-authz); a resolved board
  with no admin membership → 403. (Pre-PH-233 the gate parsed `uuid.UUID(board_id)` and 403'd
  on unknown ids — corrected.)
- Admin-gated board routes accept board KEY or UUID — the gate MUST resolve via `get_board`,
  never raw `uuid.UUID()` [PH-233]. `require_board_admin` (`api/deps.py`) once parsed the raw
  `{board_id}` path param with `uuid.UUID()` and 403'd on any non-UUID, so EVERY key-based
  admin call (the UI always sends the KEY) was blanket-denied, even for a real admin. It now
  mirrors the correct sibling `repositories.py:_require_board_admin`: `get_board(session,
  board_id)` (key-or-uuid) then the `BoardMembership` admin check. The ordering is
  load-bearing — resolve (404 on miss) BEFORE authz (403 on non-admin), never the reverse.

## Related

- [[components/backend]] — owns the board model, REST router, lifespan task wiring
- [[components/frontend]] — `SonarHealthPanel` + the PH-226 settings buttons consume these
