---
type: component
files:
  - frontend/index.html
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.js
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/api/client.ts
  - frontend/src/stores/auth.ts
  - frontend/src/stores/identityGuard.ts
  - frontend/src/lib/queryClient.ts
  - frontend/src/hooks/useWebSocket.ts
  - frontend/src/hooks/useMe.ts
  - frontend/src/hooks/useEnsureBoardWorkflow.ts
  - frontend/src/pages/Boards.tsx
  - frontend/src/pages/BoardDetail.tsx
  - frontend/src/pages/BoardSettings.tsx
  - frontend/src/pages/TicketDetail.tsx
  - frontend/src/pages/Login.tsx
  - frontend/.env.development.example
  - frontend/src/components/Layout.tsx
  - frontend/src/components/RequireAuth.tsx
  - frontend/src/components/ThemeProvider.tsx
  - frontend/src/components/WorkflowEditor.tsx
  - frontend/src/components/PermissionMatrix.tsx
  - frontend/src/lib/a11y.ts
  - frontend/src/components/MembersTab.tsx
  - frontend/src/components/NotificationBell.tsx
  - frontend/src/components/MermaidBlock.tsx
  - frontend/src/components/MarkdownRenderer.tsx
  - frontend/src/components/diff/DiffViewer.tsx
  - frontend/src/components/diff/FileDiffView.tsx
  - frontend/src/components/diff/HunkView.tsx
  - frontend/src/components/git/BranchGraph.tsx
  - frontend/src/components/git/branchGraphLayout.ts
  - frontend/src/components/git/TicketCommits.tsx
  - frontend/src/components/git/index.ts
  - frontend/src/components/repository/RepositoryStatusPanel.tsx
  - frontend/src/components/repository/RepositoryConfigForm.tsx
  - frontend/src/components/repository/RepositoryOperationsPanel.tsx
  - frontend/src/components/repository/RotateSecretModal.tsx
  - frontend/src/components/repository/DetachConfirmModal.tsx
  - frontend/src/components/repository/RepositoryList.tsx
  - frontend/src/components/repository/AddRepositoryPanel.tsx
  - frontend/src/components/repository/RemoveRepoConfirmModal.tsx
  - frontend/src/lib/time.ts
  - frontend/src/lib/stateColor.ts
  - frontend/src/lib/utils.ts
  - frontend/src/lib/diff/parseDiff.ts
  - frontend/src/types/api.ts
  - frontend/src/types/git.ts
  - frontend/src/components/SonarHealthPanel.tsx
  - frontend/src/components/SonarIssueDrawer.tsx
  - frontend/src/hooks/useSonarIssues.ts
  - frontend/src/hooks/useSonarLiveCounts.ts
  - frontend/src/components/git/RepoSwitcher.tsx
  - frontend/src/components/sonarqube/SonarSetupSection.tsx
  - frontend/src/components/sonar/SonarDashboard.tsx
  - frontend/src/components/sonar/MetricCard.tsx
  - frontend/src/components/sonar/metricMeta.ts
  - frontend/src/components/ConceptTagChip.tsx
  - frontend/src/components/ConceptTagsEditor.tsx
  - frontend/src/pages/Space.tsx
  - frontend/src/components/space/SpaceGraph.tsx
  - frontend/src/components/space/SpaceGraphPanel.tsx
  - frontend/src/components/space/TicketNode.tsx
  - frontend/src/components/space/TagNode.tsx
  - frontend/src/components/space/BoardNode.tsx
  - frontend/src/components/space/LabeledEdge.tsx
  - frontend/src/components/search/GlobalSearch.tsx
  - frontend/src/components/search/SearchResultsPanel.tsx
  - frontend/src/hooks/useDebouncedValue.ts
  - frontend/src/stores/searchFilter.ts
  - frontend/eslint.config.js
last_touched_ticket: PH-278
status: active
---

# Frontend

> React 18 + Vite + Tailwind + shadcn/ui-style component layer that consumes the [[components/backend]] REST API plus an MCP-over-HTTP control surface and renders the live ProjectHub kanban + ticket workspace, with real-time updates via a self-managed WebSocket hook.

## Current behavior

The frontend is a single-page React 18 app bundled with Vite 5 (`vite.config.ts`), strict TypeScript (`tsconfig.json`, `noUncheckedIndexedAccess`, `strict: true`), and Tailwind 3 (`tailwind.config.js`) for styling. It boots from `src/main.tsx`, which mounts `<App />` inside a top-level `QueryClientProvider` (TanStack Query 5, `retry: 1`, `refetchOnWindowFocus: false`). `src/App.tsx` wraps everything in `ThemeProvider` and `BrowserRouter` and declares the route table: a public `/login` and an `Outlet`-style protected shell (`RequireAuth → Layout`) that hosts `/` (Boards index), `/boards/:boardKey` (BoardDetail kanban), `/boards/:boardKey/settings` (BoardSettings + WorkflowEditor + MembersTab + PermissionMatrix), and `/boards/:boardKey/tickets/:ticketKey` (TicketDetail). The `Layout` shell (`src/components/Layout.tsx`) renders the persistent header (logo, Boards nav, `NotificationBell`, `ThemeToggle`, logout button) plus the `Outlet` for nested routes.

Backend communication is centralized in `src/api/client.ts`, which exposes a flat `api` object (a hand-rolled `request<T>` helper plus typed methods for boards, tickets, transitions, claims, comments, history, notifications, board membership, and actor listing). Two transports are used in parallel against the same backend: REST under `/api/*` for everything user-facing (boards, tickets, comments, notifications, auth/me, membership) and an MCP-over-HTTP envelope (`mcpCall<T>` → `POST /mcp/call/{tool}`, unwraps the `{ tool, result }` envelope) for workflow operations (`list_workflows`, `create_workflow`, `update_workflow`, `add_transition`, `set_field_gates`, `activate_workflow`, `ensure_board_workflow`, `delete_workflow`, `delete_state`). Both surfaces share auth via `getStoredToken()` from the Zustand auth store (`src/stores/auth.ts`); a bearer token is read from `localStorage["projecthub.token"]` and attached to every request. On `401` both helpers call `useAuth.getState().logout()`, clearing the token and triggering `RequireAuth` to redirect to `/login`. Errors are normalized into an `ApiRequestError` class carrying the HTTP status plus the parsed `{ error, message, detail }` body so call sites can surface backend domain errors (`invalid_transition`, `field_gate_not_met`, `permission_denied`) verbatim. Vite proxies `/api` and `/mcp` to `http://backend:8000` in dev (`vite.config.ts`), and the `@/*` import alias maps to `src/*`.

State management is intentionally lean. TanStack Query owns server cache; mutations invalidate keyed queries to refetch board/ticket state after writes. Zustand only holds the auth token (`useAuth`) — there is no global UI store. Real-time updates flow through `src/hooks/useWebSocket.ts`, a 400-line custom hook that opens `ws(s)://<host>/ws/boards/:boardId?token=<bearer>` and pumps `EventEnvelope` messages from the backend Redis bus into React state. It implements: ping-pong heartbeat (default 30 s) with latency-derived `ConnectionQuality` (`excellent | good | poor | disconnected`), exponential-backoff reconnect with jitter (base 2 s, ×1.5, cap 30 s, max 10 attempts), structured `error` / `system_degradation` message routing, a connection-timeout watchdog (default 15 s), and stable callback refs so consumers can hot-swap `onMessage` without forcing reconnects. Pages call `useWebSocket({ boardId, token, onMessage })` and `queryClient.invalidateQueries(...)` inside their `onMessage` handler to keep the kanban + ticket views live. Theming (since F1/PH-170) is a **CSS-variable token layer** ("Cyan on Black" design system): `src/index.css` carries a dark `:root` (default) + `html.light` override copied verbatim from `.claude/skills/ProjectHub Design System/colors_and_type.css`, and `tailwind.config.js theme.extend` maps every semantic utility to `var(--token)` (e.g. `surface: "var(--bg-surface)"`) rather than baked hexes. `darkMode` is `["selector", "html:not(.light)"]` — **dark is the default**, `ThemeProvider` toggles a `.light` class (NOT `.dark`), with localStorage persistence + `prefers-color-scheme: light` seeding. Surfaces consume token utilities (`bg-surface`, `text-text-primary`, `border-hairline`, `text-accent`, `font-mono`); the `html.light` override repaints the same utility so the ~735 historical `dark:` variants get dropped (not rewritten) as F2–F8 migrate. Inter is self-hosted under `frontend/public/fonts/` (12 weights+italics via `@font-face`); JetBrains Mono via Google Fonts `@import` is semantic (SHAs, ticket keys, branches). State-color resolution lives in `src/lib/stateColor.ts`, which prefers a per-workflow-state `color` hex (rendered at 10 %/30 % alpha for background/border — theme-agnostic, unchanged) and falls back to a `STATE_CATEGORIES`-keyed token-utility class. Ticket markdown rendering uses `react-markdown` + `remark-gfm` (`MarkdownRenderer.tsx`) with `MermaidBlock.tsx` upgrading fenced ```mermaid blocks into diagrams via the `mermaid` lib. The workflow editor (`components/WorkflowEditor.tsx`, ≈ 27 KB) is built on `@xyflow/react` (formerly React Flow) for visual state/transition editing, with sortable dnd via `@dnd-kit/core` + `@dnd-kit/sortable`. TypeScript domain types live in `src/types/api.ts` (`Priority`, `TicketType`, `AgentPhase`, `ActorSummary`, `WorkflowState`, `FieldGates`, etc.) and are kept in lockstep with the backend via `npm run generate:types` (openapi-typescript script, per `frontend/README.md`). All dev/build/test commands run inside the `frontend` Docker container per the project's container-only rule (`rules.md` §1): `docker compose exec frontend npm run typecheck | lint | dev | build`.

**G8 — Git client surface** (`src/types/git.ts`, `src/api/client.ts`, `src/hooks/useWebSocket.ts`): G8 adds the complete TypeScript type layer and API client for the G1–G7 git backend without introducing any UI components (those come in G9–G13). All git-surface types are defined in the new dedicated module `src/types/git.ts` (~230 LOC): literal unions (`GitProvider`, `GitChangeType`, `GitRefreshStatus`), repository types (`RepositorySummary`, `RepositoryResponse`, `RepositoryUpsertPayload`), commit types (`GitCommitSummary`, `GitCommitFile`/`GitCommitFileEntry` alias, `GitCommitDetail`), branch types (`GitBranchEntry`/`GitBranch` alias), list/graph responses (`GitGraphResponse`/`GitGraph`, `GitCommitsListResponse`, `GitBranchesListResponse`), diff types (`FileDiff`, `DiffResponse`/`CommitDiff`, `RangeDiffResponse`/`RangeDiff`), refresh types (`GitRefreshStatus`, `GitRefreshResponse`), ticket-commit types (`TicketCommitEntry`, `TicketCommitsResponse`), and WS payload type (`GitSyncedPayload`). Every interface carries a `@see backend/app/schemas.py:<ClassName>` JSDoc for manual cross-check. `src/api/client.ts` gains an `api.git` nested namespace (9 methods) plus two flat admin helpers (`api.setRepository`, `api.detachRepository`). The `api.git.refresh` method uses a direct `fetch` (no Bearer header) because the endpoint uses shared-secret auth via `X-Git-Refresh-Token` — see Gotchas. `src/hooks/useWebSocket.ts` gains an exported `isGitSyncedMessage(msg)` type-guard that narrows `WebSocketMessage` to `{ type: "git_synced"; payload: GitSyncedPayload }` for G9 invalidation use; `WebSocketMessage` interface is now exported. Existing consumers (`BoardDetail.tsx`, `TicketDetail.tsx`) are unmodified.

**G10 — Branch Graph tab** (`src/components/git/BranchGraph.tsx`, `src/components/git/branchGraphLayout.ts`, `src/components/git/CommitNode.tsx`, `src/components/git/BranchLegend.tsx`, `src/components/git/index.ts`; modified `src/pages/BoardDetail.tsx`): G10 adds a SourceTree-like branch graph to `BoardDetail` as a second tab ("Kanban | Branch Graph"). The `BranchGraph` container fetches `api.git.getGraph(boardKey, { limit: 200 })` + `api.git.getStatus(boardKey)` via TanStack Query and renders a `@xyflow/react` canvas (`ReactFlow` with `Controls + MiniMap + Background` panels) populated by a custom two-pass deterministic lane algorithm (`branchGraphLayout.ts`, pure ~150 LOC, O(N+E), memoized via `useMemo`). Pass 1 seeds lanes from branch heads (default branch → lane 0, others sorted by name); Pass 2 walks commits newest-first and propagates lane ownership to parents (first parent inherits, merge parents take leftmost free lane), and FREES a lane slot whenever the row's commit was the slot's current owner — i.e. the branch terminated/rejoined — so the slot recycles for a later disjoint branch (PH-265; previously gated on root commits only → lanes leaked on real 0-root windows). Each commit renders as a `CommitNode` custom xyflow node (colored dot + short_sha + 1-line summary + ref chips [first 2 inline, +N for rest] + clickable `ticket_key` links → `/boards/:key/tickets/:key`). The `BranchLegend` left rail lists branches sorted (default first) with color dots and a HEAD badge; clicking a branch sets `selectedBranch` state (G11 hook point); clicking a commit fires `onCommitSelect(sha)` (G12 hook point) and shows a "Selected: <sha>" pill. Live sync: `BoardDetail`'s `onMessage` handler now calls `isGitSyncedMessage(msg)` first; on match it calls `queryClient.invalidateQueries({ queryKey: ["git", boardKey, "graph"], refetchType: "active" })` (silent refetch) and highlights new shas (amber pulse) for 3s via `highlightedShas` state passed to `BranchGraph`. Tab state persists in `location.hash` (#graph / #kanban) for shareable URLs. Kanban code is wrapped in `{activeTab === "kanban" && <...>}` — zero regression risk. Empty state: "Henüz commit yok"; No-repo state: "Bu board'a repo bağlı değil. Settings'ten bağlayabilirsiniz." + link + G13 note; Error state: "Graph yüklenirken hata oluştu." + Tekrar dene button. Playwright-verified: 200 nodes rendered, branch legend click (aria-pressed), commit click (Selected indicator), dark mode. Screenshots under `.jarwis/logs/PH-159/qa-screenshots/`. **[SUPERSEDED by PH-167 — see below: the `@xyflow/react` canvas + `CommitNode` + `BranchLegend` were removed/retired and replaced by a SourceTree-style vertical commit list.]**

**G14b / PH-167 — Branch Graph SourceTree rework** (`src/components/git/BranchGraph.tsx` REWRITTEN; `src/components/git/branchGraphLayout.ts` MOD; `src/components/git/CommitNode.tsx` retired-but-kept; `src/components/git/index.ts` MOD; `src/pages/BoardDetail.tsx` MOD; `src/components/Layout.tsx` MOD; `src/App.tsx` MOD): The user rejected the G10 `@xyflow/react` node-card graph as "untraceable" (kullanıcı feedback). PH-167 replaces it with a SourceTree-style **3-pane** layout: `[Branch sidebar] | [vertical commit list + SVG lane gutter] | [diff panel (conditional)]`. `BranchGraph` is now self-contained (no `@xyflow/react` import) and renders commits newest-first as compact rows (`ROW_H=36px`, not cards): each row = an inline SVG **gutter cell** (per-row `<svg aria-hidden>` drawing pass-through lane lines, the own-lane line above/below the dot, merge curves to parent lanes, and a `<circle>` dot — merge commits get a larger dot; selected = open/white-filled dot) + a clickable `<button role="listitem">` showing short_sha (mono) · summary (truncate) · ref badges (branch/tag, HEAD/default emphasized) · ticket_key links · author · relative time. The lane math is still `branchGraphLayout.ts assignLanes()` (two-pass, REUSED unchanged — only rendering moved from xyflow nodes/edges to SVG rows); `laneColor(lane)` gives one color per lane (cyclic). `MAX_LANES=10` caps gutter width. Per-row "active lanes" (lanes that span across a row, i.e. have commits both above and below) are computed from lane first/last row extents so pass-through lines only draw where a lane is actually continuous. The **branch sidebar** (`<aside aria-label="Branch list">`) lists "All" (default selected) + branches (default first, color dot, HEAD badge); clicking a branch sets internal `selectedBranch` state and fetches `api.git.listCommits(boardKey, { branch, limit: 80 })` (queryKey `["git", boardKey, "commits", branch, 80]`) → the list filters to that branch's first-parent history; "All" shows the full `getGraph` result. Clicking a commit row toggles `selectedCommitSha`; when set, a third pane `<aside aria-label="Commit diff panel">` mounts `<DiffViewer fetch={{ kind: "commit", boardKey, sha }}>` (REUSED G9 component) showing per-file/per-line diffs — this directly fixes the "git diffler görünmüyor" complaint. Live sync is unchanged in mechanism (`BoardDetail` `isGitSyncedMessage` → invalidate `["git", boardKey, "graph"]` + `highlightedShas` amber pulse 3s) but because the list is newest-first the new commit now appears at the **TOP** of the list. The dev `/dev/diff-demo` nav link is removed from `Layout.tsx` and its route removed from `App.tsx` (AC4); the `DiffDemo.tsx` page file + `DiffViewer` component are preserved (no route → no access). `BoardDetail` no longer passes `onCommitSelect`/`onBranchSelect` (branch + commit selection are managed internally by `BranchGraph`; those props remain optional in `BranchGraphProps` for external reuse). Browser-verified (Playwright `ph-167-branch-graph-rework.spec.ts`, 10 TCs passed + Preview screenshots): vertical list 150 rows newest-first, 150 lane gutter dots, NO `.react-flow`, branch filter (all=150 → main=80), commit→diff panel opens with real file/line hunks (`11 files changed +1209 -16` on a substantive commit), demo link absent, live commit appears at top, dark+light parity, 0 console errors. Screenshots under `.jarwis/logs/PH-167/screenshots/`. **[Layout SUPERSEDED by PH-188 — the flex+gapped-rounded-card 3-pane became a full-bleed CSS-grid 3-pane and the floating glass detail card (PH-179) was removed; lane geometry/data fetching/WS/branch-filter unchanged. See PH-188 below.]**

**PH-188 — Branch Graph refit to ui_kit `branchgraph.jsx` (CSS-grid 3-pane, floating card → inline diff panel, compact mode)** (`src/components/git/BranchGraph.tsx` REWRITTEN; `src/components/git/branchGraphLayout.ts` MOD `ROW_H 40→44`; `src/components/diff/DiffViewer.tsx` MOD `+showSummary` prop; `src/components/diff/FileDiffView.tsx` MOD `rounded-md→rounded-[10px]`). The current Branch Graph matches the authoritative ui_kit `branchgraph.jsx` + `kit.css .bg-*`/`.diff-*` (screenshots `04-branch-graph[-commit-selected].png`). **Outer layout is now a CSS `grid`, NOT flex**: the wrapper `<div className="grid border-t border-hairline">` uses `gridTemplateColumns: selectedCommit ? "200px minmax(0,1fr) 340px" : "200px minmax(0,1fr)"` (the `.no-diff` collapse), `gap:0`, `height: calc(100vh - 56px - 132px)`, `minHeight:480` — edge-to-edge with hairline dividers ONLY (grid `border-top`, sidebar `border-r`, diff panel `border-l`, list-head `border-b`); NO inter-pane gaps and NO per-pane rounded `bg-surface` cards (the prior flex `gap-3` + `rounded-lg border bg-surface` per pane is gone). **Pane 1 — `BranchSidebar`** (200px grid track, `border-r border-hairline bg-surface px-2.5 py-3.5`): "Branches" eyebrow + "All" pseudo-row + branch rows (lane-color dot + mono name + HEAD chip), `bg-accent-soft` selected; a bottom `mt-auto border-t pt-3.5` toolbar holds a **real** "Ticketed only" filter (`role="switch"`, kit `.checkbox.on` look `bg-accent-strong border-accent` + Check glyph). **Pane 2 — commit list** (`bg-base`): a 38px `.bg-list-head` (column labels SHA/Message/Author/Time) over the scrollable `role="list"` rows; rows are 44px `<button role="listitem">` with `.bg-sha` (70px, mono, **always `text-accent`**, 12px) + `.bg-msg` (flex, 13px `text-text-primary`, ref chips: HEAD/default → `bg-accent-soft` cyan, others → `bg-raised text-state-backlog`) + ticket key-chip + author (110px) + time (64px right); hover `bg-raised`, selected `bg-accent-soft`, fresh-WS `animate-glowin`. **Compact mode**: `compact = Boolean(selectedCommit)` hides the Author + Time columns in BOTH the rows and the list-head (matching kit `.bg-row.compact`/`.bg-list-head.compact`). **Pane 3 — `CommitDiffPanel`** (340px grid track, `border-l border-hairline bg-surface`) REPLACES PH-179's floating glass card: selecting a commit opens this panel DIRECTLY (single `selectedCommitSha` state — the PH-179 `diffOpen` two-step + `FloatingDetailCard` are DELETED). Header `.diff-head` = mono 12-char sha + close X + `.diff-title` (14px/600 summary) + `.diff-stat` ("**N** files changed +adds −dels" from `getCommit`, skeleton while loading, hidden on error); body = `<DiffViewer fetch={{kind:"commit",boardKey,sha}} showSummary={false}>` (the new prop suppresses DiffViewer's own duplicate "N files changed" header so the panel's `.diff-stat` owns it). The continuous-bezier `LaneOverlay`/`computeLanePaths` (PH-179), `assignLanes`/`laneColor`, the 3 `useQuery` git fetches, `highlightedShas` WS live-highlight, branch filtering, lane-assignment, theme-aware `var(--lane-*)`, and a11y (`role=list/listitem`, `aria-pressed`, focus rings, Esc-to-close) are PRESERVED; only `ROW_H` (40→44) and the gutter constants (`LANE_PX 14→15`, `GUTTER_PAD 8→12`) changed for kit pixel-parity (dots re-verified centered at `rowIdx*44+22`). Side-by-side verified vs the ui_kit `index.html` Branch Graph (Claude Preview, live PH board real data + the kit served on :8899): grid cols/gap/borders, 200px sidebar, 340px diff panel border-left, 44px rows, 70px accent sha, 12px gap all match; commit-select → 3-pane + compact + `.diff-stat` "2 files changed +83 −42" + exactly ONE "files changed" (no DiffViewer dup); "Ticketed only" 150→144 rows with dot-count==row-count (no orphaned lanes); both dark+light; build+typecheck green, 0 console errors.

**PH-190 — Branch Graph lane geometry ported from ui_kit `Gutter` (contiguous spans, lane-0-anchored single-row fork/merge)** (`src/components/git/branchGraphLayout.ts` MOD — body of `computeLanePaths` REWRITTEN; `src/components/git/branchGraphLayout.test.ts` NEW; `tsconfig.json` MOD `+exclude` test files). The lane gutter now reproduces the ui_kit `branchgraph.jsx` `Gutter` geometry EXACTLY on REAL git history. The PH-179 emitter drew ONE vertical run per lane from the lane's GLOBAL first→last appearance plus multi-row sweeping beziers between arbitrary parent lanes; because the REAL PH history reuses lane numbers (lane N recycled by many short feature branches that merge back), the global run bridged the IDLE rows between two disjoint branches sharing a lane number → the wiggly/tangled lanes the user reported. This bug only manifests on real data (the seeded mock has no lane reuse), so PH-179/PH-188 passed on mock yet the live app still looked wrong. The rewrite computes, per lane, its **contiguous active spans** (maximal runs of consecutive active rows) from a single forward pass over the displayed commits + the frozen `laneOfSha` map: a lane is active at a row if the row's commit is on that lane, OR the lane is a first-parent pass-through (a commit above is on the lane and its first parent — same lane — is displayed at/below the row, so every interior row is filled). Spans are then collapsed and emitted PER ROW, anchored to the main lane (lane 0, `x0 = laneCx(0)`): straight verticals within a span (own-lane opacity 1.0, other-lane pass-through 0.55, matching the kit); a single-row **branch-out** curve at `spanFirst` from main-lane TOP → branch lane MID (kit `M lane0x 0 C lane0x mid*0.7, x mid*0.5, x mid` → `M x0 top C x0 (top+rowH*0.35), x (top+rowH*0.25), x mid`); a single-row **merge-in** curve at `spanLast` (when the terminal commit's first parent is on lane 0) from branch-lane MID → main-lane BOTTOM (kit `M x mid C x mid*1.5, lane0x mid*1.3, lane0x ROW_H` → `M x mid C x (top+rowH*0.75), x0 (top+rowH*0.65), x0 bottom`). NO segment ever spans more than one `rowH`; idle gaps on reused lanes are NEVER bridged; off-list parents (branch-filtered / "Ticketed only") end the span with a straight `top→mid` and NO fabricated merge curve (R3 graceful terminate preserved). `assignLanes`/`laneColor`/`LANE_COLORS`/`computeMaxLane`/the lane-index map are BYTE-IDENTICAL; the `LaneSegment`/`LaneDot`/`LaneGeometry` shapes, the single full-height `LaneOverlay` `<svg>`, dots (one per commit at `(laneCx(lane), y(rowIdx))`, merge=5/regular=4 radius, hollow-when-selected, WS-fresh drop-shadow glow), `BranchGraph.tsx` layout/data/WS/branch-filter/a11y, and the theme-aware `var(--lane-*)` strokes are ALL unchanged. Verification used the **REAL PH history** (319 commits, 73 merges, the actual `main` branch) dumped from the live backend's `graph_payload`, not a hand-crafted mock: running the production `assignLanes` + `computeLanePaths` against it yields max segment y-extent = exactly 44px (one rowH) — no multi-row sweep, no idle-gap bridge — across 51 detected idle gaps on the reused lane, 93 fork/merge curves all anchored to main, 319 dots == 319 rows; the rendered overlay (both dark+light) shows a crisp straight cyan main backbone with single-row fork/merge curves and straight parallel branch columns, matching the ui_kit `Gutter` STYLE (only the branch COUNT differs, as it depends on real history). A colocated unit test (`branchGraphLayout.test.ts`, run via Node's built-in `node:test` + native TS type-strip, NO package.json dependency added: `node --test --experimental-strip-types src/components/git/branchGraphLayout.test.ts`) guards AC4 (reused lane → two disjoint spans, no idle-gap bridge, no >1-rowH segment), AC2 (single-row main-anchored fork/merge curves), AC3 (pass-through dims to 0.55), AC5/AC7 (dot-count == row-count) — 4/4 pass. `tsconfig.json` excludes `*.test.ts(x)` from the app `tsc` (they use `node:` imports + `.ts`-extension imports the app build does not type-check); build + typecheck green.

**PH-198 — Branch Graph: single-commit fork-edge restored + per-branch color** (`src/components/git/branchGraphLayout.ts` MOD — `computeLanePaths` span-emission + dots + a NEW `branchColor()` export; `src/components/git/branchGraphFork.test.ts` NEW from QA). Two PH-190-era geometry/color regressions are fixed, both in the pure layout layer (`BranchGraph.tsx` is unchanged — it already renders `seg.color`/`dot.color` straight from the geometry). **(1) Single-commit branch fork/divergence edge.** For a one-commit branch the lane span is a SINGLE row, so `spanFirst === spanLast`; the old `isFirst` block emitted only the upward branch-out curve (main TOP → branch MID) and `return`ed BEFORE the `isLast` descent block, so the divergence edge (branch MID → main BOTTOM, descending to the base-lane parent the branch forked from) was never drawn — the user's "tek commit içeren branchin ayrıldığı kısım çizilmiyor." Multi-commit branches were unaffected because their base commit is the span's LAST row (`isFirst !== isLast`), so the descent already ran. The fix factors the descent into a shared `emitDescentIfMerges()` helper and calls it in BOTH the single-row `isFirst===isLast` path and the multi-row `isLast` path, so a single-commit branch now emits its merge-in curve too (verified on real PH history: PH-197 `fe4113f4` row dot at y=110, lane col x=64.5, now has BOTH `M 19.5 88 C … 64.5 110` fork-out AND `M 64.5 110 C … 19.5 132` divergence-descent). The PH-190 invariants hold: each curve is still single-row (≤ one `rowH` y-extent), main-anchored to `x0`, and there is still exactly one dot per commit. **(2) Per-branch color (not per-lane-index).** `laneColor(lane)=LANE_COLORS[lane%len]` keyed color by LANE INDEX, so disjoint feature branches that recycle a lane number (the PH-178..186 region) all rendered the SAME color (one green). A new `branchColor(key)` export hashes a stable branch identity (FNV-1a 32-bit over the span's TIP sha — the commit at `span.first`) into `BRANCH_PALETTE = LANE_COLORS.slice(1)` (the non-lane-0 hues; lane-0 cyan stays reserved for the `main` backbone). Each `Span` now carries a `color` field (lane 0 → `laneColor(0)`; side lanes → `branchColor(tipSha)`); all branch-out/merge/in-span-vertical segments + the side-lane DOT use `span.color` instead of `laneColor(lane)`. Two disjoint spans reusing one lane therefore get DISTINCT colors. The color stays a `var(--lane-*)` string (theme-aware, resolved at paint) and is deterministic (pure function of the sha) → stable across re-renders + theme switches. Browser-verified on the live PH board graph tab (real history, 150 commits / 311 paths / 150 dots): the most-reused lane column now carries 7 distinct branch-curve colors (was 1), all 8 palette hues appear in the overlay, and single-commit branches (PH-194/PH-197) show a full fork→divergence loop; 0 console errors. QA's reproduction `branchGraphFork.test.ts` (5 cases: S1 fork-edge, S1b multi-commit contrast, S2 distinct-color, S2-root not-lane-keyed, regression invariants) is 5/5 green and the sibling `branchGraphLayout.test.ts` PH-190 guard stays 4/4 green. `assignLanes`/`laneColor`/`LANE_COLORS`/`computeMaxLane` and the `LaneSegment`/`LaneDot`/`LaneGeometry` shapes are UNCHANGED; `branchColor` is purely additive.

**PH-199 — Branch Graph: open (unmerged) vs merged branch distinction** (`src/components/git/branchGraphLayout.ts` MOD — corrected merge predicate + new `OpenTip` geometry; `src/components/git/BranchGraph.tsx` MOD — open-ring affordance; `src/components/git/branchGraphOpenMerged.test.ts` NEW from QA). An OPEN/unmerged branch (its tip never merged back to main — e.g. PH-178 `f3d25623`, ticket stuck `in_review`) was drawn with the SAME closed fork→merge loop as a genuinely MERGED branch (PH-177 `fd250d38`+`7e88e9a4`, PH-179 `bf456a9b`+`c2708256`), so the two were visually indistinguishable. **Root cause:** PH-198's `emitDescentIfMerges()` gated the merge-return curve on `mergesOntoMain(commit, lane)` which returned `laneOf(firstParent) === 0` — but a branch tip's first parent IS its divergence point on main for EVERY feature branch (merged OR open), so this is a FORK test, not a MERGE test, and open branches got the descent curve too. **Fix part 1 (correctness):** the merge signal is orthogonal and lives in the parent DAG already passed to `computeLanePaths` — a branch is MERGED iff its TIP sha is a parent of some lane-0 (main) commit (a real merge commit references it). We precompute `mergedTips = { side-lane shas referenced as a parent by any lane-0 commit }` and replace the helper with `branchMerges(span)` keyed on the span's TIP (`commits[span.first].sha`) ∈ `mergedTips`. (Keyed on span TIP, not the descent row's first-parent, because the descent is emitted at the span's LAST row but merge status is a property of the TIP. For a single-commit branch first===last so both coincide — keeps PH-198 single-commit merged loops intact.) An open branch now emits NO merge-return curve. **Fix part 2 (visible affordance):** the layout emits a new `openTips: OpenTip[]` array — one entry per open span at its tip (cx/cy/r/color/sha). `BranchGraph.tsx`'s `LaneOverlay` renders each open tip dot HOLLOW (`fill=var(--bg-base)`, like a selected dot) plus a dashed hollow ring cap (`strokeDasharray="2 1.5"`, `r=tip.r+2`, `<title>Open branch (unmerged)</title>`) in the branch hue — reading clearly as "open / not merged" within the Cyan-on-Black system, distinct from a merged branch's closed lane + solid dot. Browser-verified on the live PH board graph (real history, PH-190 gotcha): PH-178 `f3d25623` renders with the dashed open-ring cap and NO return curve, while PH-177/PH-179 keep their closed fork→merge loops + solid dots; exactly 2 open rings (ph-178, ph-199) on the whole graph, 0 console errors. QA's reproduction `branchGraphOpenMerged.test.ts` (AC1 open=no-curve, AC2 merged≠open, AC3 merged keeps curve, AC4 PH-190 invariants) is 4/4 green; siblings `branchGraphFork.test.ts` 5/5 + `branchGraphLayout.test.ts` 4/4 stay green; tsc green. `assignLanes`/`laneColor`/`LANE_COLORS`/`branchColor` and the segment emission are otherwise unchanged; `OpenTip`/`openTips` are purely additive (existing `LaneSegment`/`LaneDot` shapes untouched).

**G11 — Branch detail panel** (`frontend/src/components/git/BranchPanel.tsx` NEW; `frontend/src/pages/BoardDetail.tsx` MOD; `frontend/src/components/git/index.ts` MOD): G11 adds a right-column branch detail panel to the Branch Graph view via state lift in `BoardDetail`. `BoardDetail` gains `const [selectedBranch, setSelectedBranch] = useState<string | null>(null)` and a `useEffect` that resets it to `null` when the active tab is not `"graph"` (AC9 tab-change reset). The graph section JSX wraps `<BranchGraph>` and `<BranchPanel>` side-by-side in `<div className="flex flex-col lg:flex-row gap-3">` (AC10 responsive stacking) — `BranchGraph` in `flex-1 min-w-0` and `BranchPanel` in `w-full lg:w-80 lg:flex-shrink-0`. The `onBranchSelect` callback uses a toggle pattern: `setSelectedBranch(prev => prev === branch ? null : branch)`. `BranchPanel` is a pure `<aside role="complementary" aria-label="Branch detail panel">` container with four interior sections: (1) Header: branch name (font-semibold truncate) + HEAD badge (default branch only) + `<Link>` to ticket page if `branch.ticket_key` + X close button (`aria-label="Close branch panel"`); (2) Ahead/Behind chip: `↑ N · ↓ M` in green/red mono; `null` → `...` with deep-divergence tooltip (AC4); default-branch chip shows `↑ 0 · ↓ 0`; (3) Diff toggle button: `disabled` + tooltip `"Default branch — kendine diff yok"` when `is_default=true` (AC7); on click → shows `<DiffViewer fetch={{ kind: "range", boardKey, base: defaultBranchName, head: selectedBranch }}>` (AC6) + a Geri button that collapses diff back to commit list (AC8); (4) Commit list: `useQuery(['git', boardKey, 'commits', branch, 30])` → `api.git.listCommits(boardKey, {branch, limit:30})`; skeleton (3 pulse rows) while loading; red banner + Tekrar dene on error; `<ul role="list">` with per-item short_sha + summary + author + relative time (AC3); empty: `"Bu branch'te commit yok."`. Empty state (selectedBranch=null): centered icon + `"Sol panelden bir branch seçin"`. `staleTime`: branches 60s, commits 30s. Diff close/open controlled by local `diffOpen` state which resets on branch change (via `prevBranch` comparison). Barrel export `components/git/index.ts` updated with `export { BranchPanel }`. `BranchGraph.tsx` unchanged — the existing `onBranchSelect` callback is wired to `setSelectedBranch` in `BoardDetail`. Browser-verified (Preview tool): layout renders, dark+light mode, empty state placeholder, flex-col mobile stacking, `aria-label` on aside. API verification: `getBranches` and `listCommits` return correct data from the real backend. Screenshots under `.jarwis/logs/PH-160/`.

**C5 — Git settings multi-repo + auto-detect UI** (`frontend/src/pages/BoardSettings.tsx` MOD; `frontend/src/components/repository/RepositoryList.tsx` NEW; `frontend/src/components/repository/AddRepositoryPanel.tsx` NEW; `frontend/src/components/repository/RemoveRepoConfirmModal.tsx` NEW; `frontend/src/components/repository/RepositoryConfigForm.tsx` MOD; `frontend/src/components/repository/RepositoryStatusPanel.tsx` MOD; `frontend/src/api/client.ts` MOD; `frontend/src/types/git.ts` MOD; `frontend/src/lib/time.ts` MOD) [PH-225]: the **single-repo** Repository tab (G13's status panel + config form + ops triad, all assuming `gitStatusQuery.data.repository` is THE one repo) is replaced by a **multi-repo list + add** surface. A single-repo board now renders as a **1-row list (= the primary, badge shown)** — no backend change (PH-221/PH-222 endpoints all merged). **`types/git.ts`** gains `RepositoryCreatePayload` (POST-collection body; extends the singular upsert with optional `name`/`slug`), `DetectedRepo` (1:1 onto `RepositoryCreatePayload`: `local_path`/`name`/`is_git`/`remote_url`/`default_branch`/`provider_guess`/`already_linked`), `DetectedReposResponse`. **`api/client.ts`** gains four `api.git.*` methods beside `listRepositories` (PH-224, REUSED not duplicated): `addRepository(boardKey, payload)` (POST `/repositories`, 201), `removeRepository(boardKey, slug)` (DELETE `/repositories/{slug}`, 204), `setPrimaryRepository(boardKey, slug)` (POST `…/set-primary`), `detectRepositories(boardKey)` (GET `…/detect`). The **slug** is the selector (URL-safe, human-stable). **`BoardSettings`** drives two queries (both `enabled: activeTab==='repository'`): the **list** `useQuery(['repositories', boardKey], …, select: r=>r.repositories, staleTime: 30_000)` — same key as PH-224's BoardDetail switcher (benign shared cache, same endpoint/shape) — and the **detect** `useQuery(['repositories', boardKey, 'detect'], …, enabled: …&& showAdd)` fired **lazily** only when the Add panel is disclosed (5s FS-scan budget — never poll). **`RepositoryList`** renders `<ul role="list">` of `RepositoryRow`s: name + slug `<code>`, provider chip, remote_url (truncate, italic `local-only` when null), default_branch `<code>`, last-sync via the shared `humaniseRelativeTr` (lifted from RepositoryStatusPanel into `lib/time.ts` — DRY, kills the S4144 copy-paste), short SHA, local_path; a **BİRİNCİL** (primary) badge on `is_primary`; and an admin-only action cluster — `Birincil yap` (disabled when already primary), `Kaldır` (opens `RemoveRepoConfirmModal`), and an `İşlemler` disclosure that reveals the board-scoped `RepositoryOperationsPanel` (refresh/rotate-secret/hook). **`AddRepositoryPanel`** (admin-only) has two sub-tabs: **Algılanan** (detect candidates — name/path/remote_url/branch + one-click `Ekle` that maps `DetectedRepo`→`RepositoryCreatePayload`; `already_linked` rows greyed + disabled + `Zaten ekli`; empty → graceful `algılanan git deposu yok` note, NOT an error) and **Elle** (the existing `RepositoryConfigForm` in NEW `mode='add'` → POSTs a new repo via `addRepository` instead of the singular PUT, so a manual add does NOT overwrite the primary). **All mutations REFETCH (not optimistic)** — invalidate `['repositories', boardKey]` + `['repositories', boardKey, 'detect']` + `['git', boardKey, 'status']` — because set-primary/remove trigger server-side primary auto-promotion the client cannot predict. **403 handling**: list + detect are member-auth (the dev/frontend_dev role CAN read), but add/remove/set-primary are admin-auth → non-admin gets 403. Primary defence: write actions are HIDDEN when `!isAdmin` (read-only rows + amber banner). Defensive: every write `onError` catches `ApiRequestError && status===403` → inline `Bu işlem için admin yetkisi gerekli.` (no crash to the error boundary) — guards the PH-224 trap where board-admin membership and the UI `role` disagree. **Browser-verified (Claude Preview, live PH + a temporarily-seeded 2nd repo `demo-web`)**: single-repo back-compat (1 row, primary badge, non-admin banner, actions hidden); detect lists `/repos/project-hub` `already_linked`→greyed/disabled AND `/repos/demo-web` unlinked→addable; one-click Add `demo-web` → 2 rows, candidate flips to linked; Set-primary `demo-web` → badge swaps + list re-orders primary-first; Remove (confirm modal) → back to 1 row + detect re-shows the candidate as unlinked; non-admin write → server 403 (`required: board.admin`) with actions hidden; defensive 403 (forced via fetch-override) → inline message renders, list stays mounted, NO error boundary; 0 console errors; seeded repo + temp admin grant fully removed (PH ends single-repo). Screenshots under `.jarwis/logs/PH-225/qa-screenshots/`. The G13 status/config/ops panels remain as REUSED building blocks (OperationsPanel folds into each row's İşlemler disclosure; StatusPanel retired from the tab but still imports the shared time helper); the singular `setRepository`/`detachRepository` client methods are UNTOUCHED (back-compat). **No regression to BranchGraph** (PH-224's `?repo=` switcher reads the same list; add/remove just changes the option count).

**G13 — Board Settings Repository tab** (`frontend/src/pages/BoardSettings.tsx` MOD; `frontend/src/components/repository/RepositoryStatusPanel.tsx` NEW; `frontend/src/components/repository/RepositoryConfigForm.tsx` NEW; `frontend/src/components/repository/RepositoryOperationsPanel.tsx` NEW; `frontend/src/components/repository/RotateSecretModal.tsx` NEW; `frontend/src/components/repository/DetachConfirmModal.tsx` NEW; `frontend/src/api/client.ts` MOD; `frontend/src/types/git.ts` MOD): G13 adds the Repository management tab to `BoardSettings`. **(NOTE — superseded by C5/PH-225: the status/config/ops triad is no longer the tab's primary structure; see the C5 paragraph above. The G13 components survive as reused building blocks.)** `TabValue` union extended to `"general" | "workflow" | "members" | "repository"`. Tab button renders with `<GitBranch>` icon after Members (AC-F1). The tab panel mounts three cards: `RepositoryStatusPanel` (status read-only; `<dl>` key-value grid: provider, remote_url, default_branch, local_path, last_synced_at humanised via `Intl.RelativeTimeFormat("tr")`, last_synced_sha 7-char; AC-F2), `RepositoryConfigForm` (admin-only; provider select + remote_url [github/gitlab only] + default_branch + local_path with /repos/ client-side prefix check + server-side 422 inline errors; AC-F3/AC-F4), `RepositoryOperationsPanel` (admin-only, connected-only; three action buttons + hook snippet block; AC-F5/AC-F6/AC-F7/AC-F9/AC-F11). Non-admin renders a read-only amber banner + only the status card (AC-F8). `api.git.refresh` modified from `(boardKey, refreshToken)` to `(boardKey, opts?)` where `opts: { refreshToken?: string; useBearer?: boolean }` — G13 hybrid auth: when `useBearer=true` (or opts omitted with no refreshToken), sets `Authorization: Bearer` instead of `X-Git-Refresh-Token`; shared-secret path preserved for hook callers (AC-F5 backend B1 integration). `api.rotateRefreshSecret(boardKey)` added as flat admin helper (POST → `RotateRefreshSecretResponse`). `RotateRefreshSecretResponse` interface added to `types/git.ts`. `RotateSecretModal`: two-step confirm (warning → API call → one-shot display of secret + hook_install_command with copy buttons; plaintext cleared from React state on close; AC-F6). `DetachConfirmModal`: board-key confirmation input (case-sensitive; DELETE → 204 → status invalidate; AC-F7). Hook snippet in `RepositoryOperationsPanel` always visible when connected (AC-F9). gitStatusQuery uses `queryKey: ["git", boardKey, "status"]`, `enabled: activeTab === "repository"`, `staleTime: 30_000`. Browser-verified: status panel connected, Şimdi Yenile 202 Bearer path, rotate secret 200 one-shot modal, dark mode parity. Screenshots under `.jarwis/logs/PH-162/qa-screenshots/`.

**G12 — Ticket commits + live diff** (`frontend/src/components/git/TicketCommits.tsx` NEW; `frontend/src/pages/TicketDetail.tsx` MOD; `frontend/src/components/diff/DiffViewer.tsx` MOD; `frontend/src/components/diff/FileDiffView.tsx` MOD; `frontend/src/components/git/index.ts` MOD; `frontend/src/types/api.ts` MOD): G12 enriches the TicketDetail ActivitySection "Git" tab with a rich commit view and makes the branch chip actionable. `TicketCommits` is a new component (`~370 LOC`) that fetches `api.git.getTicketCommits(ticketKey)` (queryKey `['ticket-commits', ticketKey]`, `staleTime: 30_000`), renders each commit as a collapsible row with `short_sha` (mono), `summary` (1-line truncate), author, relative time (tooltip = absolute), `+additions / -deletions` (green/red mono), and `files_changed` count (newest-first). Expanding a row fetches `api.git.getCommit(boardKey, sha)` (queryKey `['git','commit',boardKey,sha]`, `staleTime: 60_000`) and lists changed files with `change_type` badge (A/M/D/R/C), path (rename: `old → new`), and +/- counts; binary files show a "binary" chip and are not clickable. Clicking a file opens an inline `<DiffViewer fetch={{ kind:'commit', boardKey, sha, path }}>` (per-file path filter, `G9 FetchCommit.path?` extension) with a commit sha banner above the diff content for "which commit?" context (AC4). `ActivitySection` gains a `boardKey` prop, renders `<TicketCommits ticketKey boardKey />` at the top of the Git section (above `GitEventBadge` history timeline), and updates the Git tab count badge to `commits.length` (only rich commit rows counted; history badges listed separately as "Git olayları"). The `useWebSocket.onMessage` handler gains one line: `qc.invalidateQueries({ queryKey: ["ticket-commits", ticketKey] })` — fired for ALL `LIVE_EVENTS` (not just git events), TanStack no-op on cold cache. Sidebar Row "Branch" chip upgraded from `<span>` to `<button>` (a11y: `type="button"`, `aria-label`, `title`): click opens a `[role="dialog"]` modal showing `<DiffViewer fetch={{ kind:'range', boardKey, base: default_branch, head: branch_name }}>` with title `"${base}...${head}"`; disabled when `branch_name == null` (hidden), `!default_branch` (tooltip "Repo bağlı değil"), or `branch_name === default_branch` (tooltip "Default branch — kendine diff yok", consistent with G11 BranchPanel). `BoardResponse` type in `frontend/src/types/api.ts` gains the missing `repository: RepositorySummary | null` field (matching backend `schemas.py:274`; previously missing caused TS error). G9 minor fix: `FileDiffView.tsx:127` truncated body condition `file.truncated && file.patch === null` → `file.truncated`; sliced non-null patch is also marked as truncated and not rendered (partial hunks are misleading). Browser-verified: Git tab count badge, commit row expand → file list → DiffViewer with sha banner, branch chip → range diff modal, dark mode, 0 console errors. Screenshots under `.jarwis/logs/PH-161/qa-screenshots/`.

**G9 — DiffViewer component** (`src/lib/diff/parseDiff.ts`, `src/components/diff/*.tsx`, `src/pages/DiffDemo.tsx`): G9 introduces the complete diff rendering stack. `src/lib/diff/parseDiff.ts` is a custom unified diff parser (no external library; ~150 LOC; sideeffect-free pure function `parseUnifiedDiff(patch: string): Hunk[]`). It handles all real-world edge cases: hunk-without-comma (`@@ -1 +1 @@` → default 1 line), `\ No newline at end of file` meta lines, rename-only and mode-change-only patches (produce `[]`), and empty patch strings. The component tree has three layers: `HunkView.tsx` (internal, one hunk = `<table>` with 3 columns: old line no | new line no | content; row bg is `bg-green-50 dark:bg-green-950/30` for add, `bg-red-50 dark:bg-red-950/30` for del; hunks with >50 lines are collapsed by default with an expand button); `FileDiffView.tsx` (presentational, takes a `FileDiff` prop; handles binary marker `"Binary file — preview unavailable"`, truncated marker `"Diff truncated (file >1 MiB)"`, rename paths shown as `old → new`, change_type badge A/M/D/R/C with matching Tailwind colors, per-file collapse toggle); `DiffViewer.tsx` (container, 2-mode: `files={FileDiff[]}` data-prop or `fetch={kind:'commit'|'range', ...}` TanStack Query mode with loading/error states, summary header with file count and total +N/-M, empty state "No file changes"). A barrel export `src/components/diff/index.ts` re-exports all three. The dev route `/dev/diff-demo` (`src/pages/DiffDemo.tsx`) is registered outside `RequireAuth` (public) so QA/Reviewer can verify without login; it contains 8 hardcoded `FileDiff` samples covering every AC edge case plus a live commit-fetch form. Verified browser screenshot (light + dark) saved under `.jarwis/logs/PH-158/qa-screenshots/`.

**F6 / PH-175 — Branch graph & diff restyle to Cyan-on-Black** (`src/components/git/branchGraphLayout.ts` MOD; `src/components/git/BranchGraph.tsx`, `src/components/git/TicketCommits.tsx`, `src/components/diff/DiffViewer.tsx`, `src/components/diff/FileDiffView.tsx`, `src/components/diff/HunkView.tsx` MOD; `src/index.css` MOD — additive `@layer utilities`): visual-only migration of the SourceTree 3-pane git view + the diff viewers to the F1 (PH-170) token contract, in both dark (default) and light. **Zero logic change**: `assignLanes` two-pass algorithm, `LANE_W=16`/`ROW_H=36`/`MAX_LANES`/`LANE_PX`, the 3 `useQuery` git fetches, the `highlightedShas` WS live-commit highlight, `parseDiff`, and selection/expand state are byte-identical. The load-bearing change is in `branchGraphLayout.ts`: `LANE_COLORS` is now an array of **`var(--lane-*)` reference strings** (order: cyan, emerald, amber, rose, violet, sky, teal, indigo → lane 0 = cyan = accent = default branch) instead of hardcoded hex; `laneColor(lane): string` signature is unchanged. SVG `stroke`/`fill` and inline `style` resolve the `var()` at paint time, so the same string repaints when `html.light` redefines `--lane-*` — **lanes recolor on theme switch with no JS theme branch** (browser-verified: the same `<circle>` DOM node's computed stroke goes `#FB7185` dark → `#E11D48` light from the class flip alone). Selected commit row = `bg-accent-soft` + a left cyan hairline (`border-l-2 border-hairline-cyan`); the dot becomes a hollow `fill="var(--bg-base)"` ring on the canvas. New-commit rows (`highlightedShas`) play a one-shot `.animate-glowin` (`@keyframes ph-glowin: accent-soft → transparent`, 2.4s, added to `index.css @layer utilities`) + the dot gets `filter: drop-shadow(0 0 4px var(--accent))`; the global `@media (prefers-reduced-motion: reduce)` block neutralizes the animation for free (no per-component guard). Diff viewers: add lines `bg-success-soft`/`text-success`, del `bg-danger-soft`/`text-danger`, context `text-text-secondary`, hunk header `bg-inset`/`text-text-muted` with `@@` in `text-info`, change-type badges A=success / M=info / D=danger / R=accent / C-default=raised; all SHAs/branch names/paths/line numbers/diff content stay `font-mono`. Focus rings on commit rows, branch buttons, file rows, collapse/close/retry → `focus-visible:ring-accent`. Browser-verified both themes via Claude Preview (a temporary query-cache-seeded harness mounting the real components with mock git data — no backend token needed; harness deleted before commit); typecheck + build green; full-surface grep for `slate-`/`indigo-`/`dark:`/`*-NNN` color utilities = zero.

**F8 / PH-177 — Board Settings restyle to Cyan-on-Black** (`pages/BoardSettings.tsx`, `components/WorkflowList.tsx`, `WorkflowStateList.tsx`, `WorkflowEditor.tsx`, `NodePropertyPanel.tsx`, `EdgePropertyPanel.tsx`, `PermissionMatrix.tsx`, `MembersTab.tsx`, `MembershipRow.tsx`, `AddMemberModal.tsx`, `repository/{RepositoryStatusPanel,RepositoryConfigForm,RepositoryOperationsPanel,RotateSecretModal,DetachConfirmModal}.tsx` MOD; `src/index.css` MOD — additive scoped `.react-flow` block): the final + largest surface (15 live files, 4 tabs + the visual workflow editor) migrated to the F1 (PH-170) token contract in both themes. **Visual-only**: every `useQuery`/mutation, workflow CRUD/graph-data/layout/drag-connect-delete handlers, `nodeTypes`/`edgeTypes`, dnd reorder, repository ops, and all `data-testid`/`aria-*` are byte-identical. Shell tab strip → F7 accent-underline idiom (active `text-accent` + absolutely-positioned `<span bg-accent shadow-glow-cyan-sm>`, `role=tablist/tab`+`aria-selected` kept). All 7 glass modals (Add-State, WorkflowList delete/activate, StateList delete, AddMember, RotateSecret, DetachConfirm) use the inline `color-mix(in srgb, var(--bg-surface) 94%, transparent)` + `blur(12px)` + `hairline-cyan` + `shadow-glass` recipe with a `var(--bg-overlay)` scrim; the two property panels (Node/Edge) are solid right drawers (`bg-surface`+`border-hairline`) for legibility. **React Flow theming (the load-bearing piece)**: instead of per-component `dark:` twins, a single scoped `.react-flow { --xy-*-default: var(--token); }` block in `index.css` retargets xyflow's own CSS vars (background dots→`--hairline`, edge stroke→`--hairline-strong`, selected edge/connection→`--accent`, node bg→`--bg-surface`, selected node box-shadow→`--glow-cyan`, handles→`--accent`/`--bg-base` border, Controls/MiniMap→token surfaces) — vars cascade so `html.light` repaints the graph for free (theme-aware, no JS theme branch). The custom `WorkflowStateNode` inline styles went token (`background: var(--bg-surface)`, border `state.color || var(--hairline)`, handle border `2px solid var(--bg-base)`, indicator-dot border `var(--bg-base)`, settings btn `bg-raised`); the node's own `shadow-sm` class was DROPPED so xyflow's `--xy-node-boxshadow-selected` cyan glow wins on selection; state name is now `.mono`. `WorkflowTransitionEdge`'s field-gate label → `bg-raised`+`border-hairline`, Lock `text-warning`. PermissionMatrix sticky header + sticky first column keep OPAQUE token bg (`bg-raised`/`bg-surface`, not translucent `-soft`), column headers uppercase token eyebrows, transition ids `.mono`, checkbox cells `accent-[var(--accent)]` (cyan when checked), Any-role badge `bg-info-soft text-info`. MembershipRow role display uses a static `text-role-*` map (Recipe C, JIT-safe); agent kind tag → `bg-accent-soft text-accent`, human → `bg-success-soft text-success`; agent `display_name` → `.mono`. Repository status → semantic pills (connected `success-soft`, disconnected `danger-soft`, loading `warning-soft`); secret/URL/branch/SHA chips `.mono` on `bg-inset`. Destructive confirm buttons use inline `background: var(--danger)` + `text-text-on-accent` (red reserved for destructive per design). The only inline hexes left are the data-driven new-state default `#8b5cf6` (a user-editable `state.color` value, the AC-allowed exception) — all rendered colors resolve through F1 tokens. `BoardSettingsDialog.tsx` confirmed dead (no imports anywhere) → left untouched, deletion ticket recommended. Both themes browser-verified (Claude Preview, query-cache-seeded harness — stale env token 403s the live route, so the real BoardSettings page was mounted via `setQueryData` mock board/workflow/members/git data; harness not committed): all 4 tabs render on token surfaces, the React Flow editor's nodes/edges/background/Controls recolor on theme toggle (dark node `#0C1119`/edge `#3A4859` → light `#FFFFFF`/`#CBD5E1`), permission matrix sticky cells stay opaque, glass modals + role chips correct; typecheck+build green; 15-file grep `slate-/indigo-/blue-/emerald-/amber-/sky-/red-/green-NNN/dark:` = zero.

**PH-204 — SonarHealthPanel tiles drill into a lazy issue drawer** (`src/components/SonarHealthPanel.tsx` MOD; `src/components/SonarIssueDrawer.tsx` NEW; `src/hooks/useSonarIssues.ts` NEW; `src/api/client.ts` MOD `+api.getSonarIssues`; `src/types/api.ts` MOD `+SonarIssue*`; `src/pages/BoardDetail.tsx` MOD `+boardKey` prop): the three issue-backed SonarHealthPanel metric tiles (Bugs→`BUG`, Vulns→`VULNERABILITY`, Smells→`CODE_SMELL`) become real focusable `<button>`s (`ClickableMetricTile`, `aria-haspopup="dialog"`, `aria-label="View N bugs"`, `disabled` when count is 0/null so an empty drawer never opens) that open `SonarIssueDrawer`; **Coverage and Duplications stay plain `<div>` `MetricTile`s** (aggregate percentages, no issue list backs them — not clickable, no hover/focus affordance). `SonarHealthPanel` gains a `boardKey` prop (threaded from `BoardDetail`, which already has it via `useParams`) plus `openType` state and a per-type `triggerRefs` map; on drawer close, focus is restored to the triggering tile (a11y). `SonarIssueDrawer` is a right-side overlay that REUSES the hand-rolled `DetachConfirmModal` shell verbatim (`fixed inset-0 z-50` `var(--bg-overlay)` scrim, inner `.card` with `--hairline-cyan` + `--shadow-glass` + `blur(12px)`, `role="dialog" aria-modal="true" aria-labelledby`, ESC `keydown` listener, backdrop-click-to-close with inner `stopPropagation`, X-close `<button aria-label>`) — NO new modal lib (there is no shadcn `ui/` dir). Its header = type label + live count (`Bugs · 35`), the lucide type icon (`Bug`/`ShieldAlert`/`Sparkles`), and a **"View in SonarQube"** link rendered ONLY when `dashboard_url != null` (host-facing, used verbatim, `target="_blank" rel="noopener noreferrer"`). Body has 4 mutually-exclusive states: **loading** (`Loader2` spin), **error** (`status !== 'ok'` OR query throw → human reason: unreachable/not_configured/no_project_key/error message), **empty** (`status==='ok' && total===0`), **list** (rows = mono `component:line` · severity badge · message · rule). A Prev/Next pagination footer shows ONLY when `total > page_size` (drives the `page` query param). Data comes from `useSonarIssues(boardKey, type, page)` — a TanStack Query hook with `enabled: Boolean(boardKey) && type != null` so the panel pays ZERO network cost until a tile is clicked (LAZY; drawer also mounts only when `openType != null`), `retry: false` (the PH-203 endpoint is **graceful-200**: a non-`ok` status is a successful HTTP response carrying an error reason, NOT a thrown error), key `['board', boardKey, 'sonar-issues', type, page]`. `api.getSonarIssues(boardKey, {type, severity?, page?, page_size?})` builds the querystring and uses the shared `request<T>` helper (auth + `ApiRequestError` for free). Severity badges map to semantic tokens ONLY (BLOCKER/CRITICAL→`text-danger bg-danger-soft`, MAJOR→`text-warning bg-warning-soft`, MINOR→`text-text-muted bg-raised`, INFO→`text-info bg-info-soft`, unknown→muted fallback) — no baked hexes, theme-aware. New TS types mirror PH-203 verbatim: `SonarIssueType`, `SonarIssueSeverity`, `SonarIssuesStatus`, `SonarIssue` (`severity`/`status` widened with `| string` so out-of-set backend values stay assignable), `SonarIssuesResponse`. Browser-verified on the LIVE PH board (real SonarQube data: 35 bugs / 269 smells / 0 vulns) via Claude Preview: BUGS tile → drawer "Bugs · 35", 35 rows with `backend/app/main.py:86` · MAJOR/MINOR badges · message · `python:S7497` rule, "View in SonarQube" → `http://localhost:9000/project/issues?id=project-hub`; SMELLS tile → "Code Smells · 269" with a "Page 1 of 6" footer, Next → `page=2` refetch (first row changes); network proves NO `/sonarqube/issues` call until first tile click then `type=BUG&page=1` / `type=CODE_SMELL&page=1` / `&page=2`; VULNS tile `disabled` (count 0); Coverage/Duplications are `<div>` (not buttons); ESC closes + focus returns to the `sonar-tile-BUG` button; `role=dialog`/`aria-modal=true`. tsc + eslint green.

**PH-218 — tile counts reconciled to the LIVE drawer source so they can never contradict it** (`src/hooks/useSonarLiveCounts.ts` NEW; `src/components/SonarHealthPanel.tsx` MOD; `src/pages/BoardDetail.tsx` MOD): the three issue-tile counts (Bugs/Vulns/Smells) previously read the poller-cached `health.bugs`/`health.vulnerabilities`/`health.code_smells` (5-min `SonarQubeMetric` snapshot), while the PH-204 drawer reads the live `getSonarIssues(...).total` — so between a scan and the next poll the tile could show a non-zero count while the drawer list was empty. The counts now derive from the SAME live `total` the drawer uses, via a new `useSonarLiveCounts(boardKey)` hook (`useQueries` over `BUG`/`VULNERABILITY`/`CODE_SMELL`, each `api.getSonarIssues(boardKey, { type, page_size: 1 })` — `ps=1` returns the true `total` cheaply, ONE payload row; read `total` ONLY, never `issues.length`). Each count query is keyed `['board', boardKey, 'sonar-issues', 'count', type]` — same `sonar-issues` family as the drawer (so the `sonarqube_synced` invalidation hits it) but a dedicated `'count'` segment so a `ps=1` count fetch never collides with the drawer's `page`-keyed list cache. `enabled: Boolean(boardKey)`, `staleTime: 30_000`, `retry: false` (graceful-200, same as `useSonarIssues`). **Graceful fallback (AC-2):** each tile reconciles as `liveCounts.counts[type] ?? health.<field>` — the live total when resolved, FALLING BACK to the poller-cached `health.*` count while the live fetch is loading OR errored (SonarQube down → panel still renders cached numbers, never blank); the `health == null` "No SonarQube scan yet" early return stays FIRST. Gate / Coverage / Duplications / ncloc / `fetched_at` are UNCHANGED — still sourced from `board.health` (poller-cached measures, no per-issue list backs them). **Live update (AC-3):** the `sonarqube_synced` branch in `BoardDetail.tsx` (after invalidating `['board', boardKey]`) now ALSO predicate-invalidates the `['board', boardKey, 'sonar-issues', …]` family (`refetchType: 'active'`), so the live counts AND the open drawer refetch on the same scan event as `board.health` — scoped to the sonar family only, unrelated board children untouched. Frontend-only (no backend change — `getSonarIssues` + `SonarIssuesResponse.total` already provide everything). Browser-verified on the live PH board: BUGS/VULNS/SMELLS tile count == the drawer header total == list length for each type; tsc + build exit 0, eslint clean.

**PH-276 — Concept-tag chips + editor on ticket card and detail** (`src/components/ConceptTagChip.tsx` NEW; `src/components/ConceptTagsEditor.tsx` NEW; `src/types/api.ts` MOD; `src/api/client.ts` MOD; `src/lib/utils.ts` MOD `+hashString`; `src/components/TicketCard.tsx` MOD; `src/pages/TicketDetail.tsx` MOD): renders the epic-PH-271 cross-board concept tags on a ticket, kept VISUALLY and structurally SEPARATE from `labels`. `types/api.ts` adds `ConceptTag` (mirrors backend `ConceptTagSummary` — EXACTLY `{id, slug, name, color: string|null}`, NO `description`), the richer `ConceptTagDetail`/`ConceptTagListResponse` (for the picker, mirrors `ConceptTagResponse`), and a NEW `concept_tags: ConceptTag[]` field on `TicketResponse` (distinct from `labels: string[]`; NOT added to create/update payloads — attach/detach is its own endpoint). `api/client.ts` adds three methods: `listConceptTags()` (`GET /concept-tags`, gate `tag.read`), `attachConceptTag(ticketKey, tagId)` (`POST /tickets/{key}/concept-tags/{tagId}`), `detachConceptTag(ticketKey, tagId)` (`DELETE …`) — both attach/detach pass the ticket KEY (backend resolves key-or-UUID) and return the FULL updated `TicketResponse` (idempotent 200). **`ConceptTagChip`** is hand-written Tailwind (NO shadcn) that is distinct from a `.label-chip` on THREE axes: square `rounded-[5px]` (vs the pill), a leading lucide `Tag` icon (labels have none), and a data-driven tinted left-border + `color-mix` soft background. Color = `tag.color` applied via inline `style` (a RUNTIME data value, not a hardcoded className hex) when set, else a deterministic `var(--lane-*)` fallback from `laneColor(hashString(tag.slug) % 8)` — theme-reactive, stable per tag. `hashString` (FNV-1a 32-bit) is a new `lib/utils.ts` export mirroring the `branchGraphLayout` helper. **`ConceptTagsEditor`** mirrors the `FieldEditor` card chrome (`card` → `field-head` + `field-body`) but the body is a structured picker (native `<select>` of available tags = all minus attached) + removable current-tag chips; `canEdit` gates the picker (set to `role !== null` in TicketDetail). Mutations use **invalidate-on-success, NOT optimistic** (architect §D): `onSuccess` writes the authoritative response via `qc.setQueryData(["ticket", ticketKey], updated)` + invalidates `["tickets", boardKey]` so the board card row refreshes; the picker list key `["concept-tags"]` is independent and not invalidated. Render sites: **TicketCard** gets a SEPARATE read-only concept-tag row (`slice(0,3)` + `+N` overflow, below the labels row); **TicketDetail** gets a read-only sidebar `Concept Tags` `side-row` adjacent to Labels PLUS the `<ConceptTagsEditor>` card in the main field column. `ticket.labels` is never touched by any concept-tag mutation (independent arrays both sides). Browser-verified via host-side Vite preview against a ticket seeded with 2 concept tags (one authored `#7dd3fc`, one null→lane-indigo fallback): on both the board card and the detail sidebar the chips render distinct from the grey label pills (computed `border-radius:5px`, `lucide-tag` icon, `borderLeftColor: rgb(125,211,252)`/`rgb(129,140,248)`, `color-mix` bg) — screenshot proof; the un-propagated-caps 403 on `GET /concept-tags` and on a detach click surfaces the graceful inline "Bu işlem için yetkiniz yok" message with NO crash and NO phantom chip (the chip stays because removal waits for a 200, validating the non-optimistic choice); the detach fired the exact `DELETE /api/tickets/PH-275/concept-tags/<tagId>` URL with a Bearer header. typecheck + eslint green.

## Design decisions (recent)

- Cross-board unified SEARCH UI: a global combobox in the header + grouped result listbox + a Zustand filter store wired into the PH-277 SpaceGraph highlight seam [PH-278] — epic PH-271 child 7/7, the **FINAL** child; after this merges epic PH-271 (concept graph) is COMPLETE. The app had NO search box anywhere; this adds one (user feedback req #5: "tag ile arama yapılabilmeli"). **Client (`api/client.ts`, codemap-mapped → this page):** `searchAll(q, tags?)→SearchResponse` added — `GET /api/search?q=&tags=` (PH-275); `tags` joins to a CSV; gated by the GLOBAL `tag.read` cap so it MAY 403 in dev (same un-propagated-caps gap as `getConceptGraph`/`listConceptTags`) → the panel renders an error branch, never throws. The caller gates the query on a non-empty trimmed term (matches the backend blank-`q` short-circuit). **Types (`types/api.ts`, NOT codemap-mapped):** `TicketSearchHit {id,key,title,board,board_id,state}` + `SearchResponse {tickets, concept_tags}` mirror backend `schemas.py` VERBATIM; `concept_tags` REUSES the existing `ConceptTag` alias (the compact `ConceptTagSummary`) — no new tag interface. `board` is the board KEY (not id), mirroring `GraphNode.board`, so a hit navigates via the KEY-based route. **Debounce (`hooks/useDebouncedValue.ts`, NEW):** generic `useDebouncedValue<T>(value, delayMs=250)` (`setTimeout`+cleanup) — none existed; the search query fires on the SETTLED term not every keystroke. **Query:** `useQuery(['search', trimmed], …, { enabled: trimmed.length>0, placeholderData: keepPreviousData, staleTime: 30s })` — `keepPreviousData` (TanStack Query v5) is BOTH the no-flicker-between-keystrokes UX and the stale-response race guard (React Query orders/dedups by key). **Combobox (`components/search/GlobalSearch.tsx` + `SearchResultsPanel.tsx`, NEW):** the input is `role=combobox` (`aria-expanded`/`aria-controls`/`aria-activedescendant`/`aria-autocomplete=list`); the panel is `role=listbox` with TWO `role=group`s (Tickets / Concept Tags) that are VISUALLY SEPARATED by a hairline divider, NEVER mixed (AC1 + the backend contract is already grouped). Ticket rows show key(mono)+title+board-KEY badge+state; tag rows render the REUSED `ConceptTagChip`. Keyboard: ↑/↓ move a highlight across the FLATTENED option list (tickets then tags), Enter activates, ESC closes+clears+blurs, blur-close is delayed 120ms and rows use `onMouseDown`+`preventDefault` so a click registers before the blur. States: empty term ⇒ panel closed (query disabled); `isFetching` ⇒ `aria-live` "Yükleniyor…"; zero results ⇒ "Sonuç bulunamadı"; error ⇒ `role=alert` (403 → "Arama yetkisi yok"). Hand-written Tailwind + CSS-var tokens, NO shadcn (AC5). Mounted FIRST in the `Layout.tsx` header nav (`hidden sm:block`, ~56–72 width tiers). **Tag filter → SpaceGraph (`stores/searchFilter.ts` NEW + `pages/Space.tsx` + `SpaceGraphPanel.tsx`):** a tiny Zustand store `{selectedTags, addTag, removeTag, clear}` is the SHARED channel because the search box lives in `Layout` (above the router outlet) while the chips+graph live in `SpacePage` (a different subtree) — lifting state would force a context provider around the whole shell. A tag hit (or graph tag-click) `addTag`s + routes to `/space`, where chips render as removable `ConceptTagChip`s + a "Temizle" clear button. The store drives the PH-277 SpaceGraph seam via `selectedTagId = "tag:"+lastTag.id` (the seam is SINGLE-id) + `onSelectTag` (parses `"tag:<uuid>"` back, syncs the store both directions); `SpaceGraphPanel` gained OPTIONAL `selectedTagId?`/`onSelectTag?` pass-through props it forwards VERBATIM to `SpaceGraph` (pure conduit — SpaceGraph internals UNCHANGED). **Decision — HIGHLIGHT not server hard-filter:** the seam already dims non-neighbours client-side (no refetch, positions untouched, instant, keeps the full cross-board map visible); swapping `getConceptGraph({tag})` per chip would re-run the 300-tick d3-force layout + lose context. The `tag` param remains available as a documented future "focus mode" (PH-279 territory) but is NOT built. Multi-tag graph narrowing is also PH-279 — v1 highlights ONE tag (the most-recent chip) while extra chips stay visible. **UI-verified=preview-stub (Chrome connector down → Claude Preview on an isolated host Vite, frontend_dev token to pass the auth gate):** live `GET /api/search` returns **403** (un-propagated `tag.read`, EXPECTED — `update_board_roles` NOT run), so verification stubbed `/api/search` + `/api/graph` with a 2-board fixture (PH-274/PH-278 on PH + KIM-12 on KIM; graph/search tags). Screenshot+DOM evidence: typing "graph" rendered the grouped Tickets(PH+KIM)/Concept-Tags panel (cross-board, AC4); ↑↓ moved `aria-activedescendant` + `aria-selected`; Enter on PH-274 navigated to `/boards/PH/tickets/PH-274` AND a mousedown-click on the KIM-12 hit navigated to `/boards/KIM/tickets/KIM-12` (cross-board KEY route, AC2); clicking the "graph" tag landed on `/space` with a "graph" chip whose highlight EMPHASIZED graph's neighbours (PH-274/PH-278 opacity 1) + DIMMED the non-neighbours (search tag + KIM-12 to 0.18, the search→KIM edge to 0.12) — AC3; ESC closed+cleared the panel; a `q=zzz` empty stub showed "Sonuç bulunamadı". `tsc --noEmit` clean, eslint clean on touched, `npm run build` succeeds (confirms zustand import + bundling). Out of scope: live-API verification (403 gate, `update_board_roles` NOT run), advanced ranking/autocomplete/full-text snippets (identity-only hits), multi-tag server-narrowed subgraph (PH-279). The `.codemap` sync gate fires (`api/client.ts` → this page).
- `/space` Obsidian-style cross-board concept graph: d3-force HEADLESS one-shot layout feeding ReactFlow; bipartite ticket+tag nodes; tag-click is a frontend-only highlight with a PH-278 controlled-prop seam [PH-277] — epic PH-271 child 6/7, the FE consumer of PH-274's `GET /api/graph`. **Client (`api/client.ts`, codemap-mapped → this page):** `getConceptGraph(params?: {board?, tag?})→GraphResponse` added (NAMED `getConceptGraph` NOT `getGraph` — `api.git.getGraph` already exists for the board-scoped git DAG; collision avoided). Gated by the GLOBAL `tag.read` cap (`services/graph.py`), so — like `listConceptTags` — it MAY 403 in dev until the live board roles JSON is refreshed; the `/space` page renders its error branch rather than crashing. `board`/`tag` query params are server-side filters wired NOW (PH-277 sends neither = full cross-board graph) so PH-278's filter UI needs no client change. **Types (`types/api.ts`, NOT codemap-mapped):** `GraphNode`/`GraphEdge`/`GraphResponse` mirror backend `schemas.py` VERBATIM (the stable topology contract) — type-prefixed id `"ticket:<uuid>"|"tag:<uuid>"` + a redundant `type` discriminator so we style/filter without string-parsing the id; topology-only (NO coords — the FE runs the layout); ticket-only fields (board/board_id/key/state/title) null on tag nodes, tag-only (slug/color) null on ticket nodes. **Layout (`components/space/SpaceGraph.tsx`):** `computeLayout` builds a `forceSimulation` (link+manyBody+center+collide), `.stop()`s it, then drains 300 ticks SYNCHRONOUSLY in a `for` loop (NO requestAnimationFrame physics loop) → an `id→{x,y}` map fed into `useNodesState`; `fitView` on next rAF. After mount positions live in ReactFlow state, so node drag still works and there is zero live physics cost. Mirrors WorkflowEditor's ReactFlow setup (Controls + MiniMap + Background, themed via the existing `.react-flow` `--xy-*` CSS-var tokens — NO shadcn, hand-written Tailwind). `SpaceGraph` calls `useReactFlow()` so `Space.tsx` wraps it in a `ReactFlowProvider`. **Nodes (`TicketNode`/`TagNode`):** custom ReactFlow node types, visually distinct (ticket = square card with state badge, board-hued via `laneColor(hashString(board)%8)`; tag = rounded pill with a lucide `Tag` glyph + `color-mix` fill from `tag.color` or a deterministic `var(--lane-*)` fallback — the same ConceptTagChip color language). Hidden `connectable={false}` Handles anchor edges; `data.dimmed`/`data.emphasized` are pure-visual highlight flags. **Interactions:** ticket click → `navigate('/boards/'+data.board+'/tickets/'+data.key)` (no uuid parse, AC3); tag click → toggle a frontend-only highlight that dims every non-neighbour (opacity 0.18) + emphasizes the tag's 1-hop neighbour tickets ACROSS ALL boards (AC4), pane click clears it. **PH-278 SEAM:** `SpaceGraph` exposes OPTIONAL `selectedTagId?`/`onSelectTag?` controlled props — omitted ⇒ internal `useState` (PH-277 default), supplied ⇒ controlled (PH-278 flips to a parent-driven filter UI WITHOUT touching internals). **Route/nav:** `/space` route in `App.tsx` (mirrors the existing protected-route pattern under `RequireAuth`+`Layout`) + a "Space" `NavLink` in `Layout.tsx` (same `bg-accent-soft text-accent` active style as Boards). **UI-verified=preview-stub (Chrome connector was down → Claude Preview on an isolated host Vite):** live `GET /api/graph` returns 403 (auth + un-propagated `tag.read`), so verification used a throwaway harness route + a 2-board fixture (3 ticket + 2 tag nodes, has_tag/tag_link/epic edges) mounting the REAL `SpaceGraph`. DOM probe confirmed 3 ticket + 2 tag nodes (distinct types) + 5 edges + Controls/MiniMap/Background; tag-click highlight emphasized graph-viz's neighbours (api/PH-277/KIM-15) and dimmed PH-150 to 0.18 (screenshot evidence); ticket-click fired `navigate` to `/boards/PH/tickets/PH-277` (intercepted by `RequireAuth`→`/login` with `state.from.pathname=/boards/PH/tickets/PH-277`, the definitive proof the route was computed from `board`+`key`). The harness + a `.claude/launch.json` were REVERTED before commit (tree = only PH-277 paths). `tsc --noEmit` clean, eslint clean on touched, `npm run build` succeeds (confirms d3-force resolves + bundles). Out of scope: live-API verification (403 gate — needs `update_board_roles`, explicitly NOT run), the `board`/`tag` filter UI (PH-278), graph search/zoom-to-node. The `.codemap` sync gate fires (`api/client.ts` → this page).
- `/space` EXPANSION: per-board "Space" tab + collapsed board-node + expandable per-type edge labels + a LIVE node-spacing slider + board/tag clustering [PH-277] — the 3-leg expansion (architect design + 3 user refinements) on top of the base global `/space`, consuming PH-279 graph-v2. **Board-scope wiring (LOAD-BEARING):** `getConceptGraph` (`api/client.ts`, codemap-mapped) now also accepts/emits `scope?: 'global'|'board'`. The per-board tab MUST send `{scope:'board', board:<key>}` — sending `board` ALONE is the OLD PH-274 board-FILTER (drop-dangling), NOT the PH-279 collapse; `scope=board` WITHOUT `board` → 422. Global callers pass nothing ⇒ `scope` omitted ⇒ backend default `global` ⇒ byte-identical to the base. **Types (`types/api.ts`):** widened ADDITIVELY to mirror backend `schemas.py` — `GraphNode.type` += `'board'` (collapsed neighbour: `id="board:<KEY>"`, `board`/`board_id` set, `key/state/title/slug/color` null); `GraphEdge.type` += `'reference'` (ticket→ticket key-mention) + `'board'` (collapsed cross-board); `GraphEdge.context: string|null` (a human label the backend populates for EVERY edge — the FE READS it, never computes it; `relation` stays tag_link-only). **Board "Space" TAB (`pages/BoardDetail.tsx`):** a 4th tab on the existing tab strip (extends `BoardTab` union + `initialTab`/`TAB_HASH`/tablist button + a `#space` `role=tabpanel`) — NO new route, hash-routed `#space`. Its `useQuery(['concept-graph','board',boardKey], …{scope:'board',board:boardKey})` is LAZY (`enabled: activeTab==='space'`) so opening a board never fires `/api/graph` (perf + avoids 403 noise). **`SpaceGraphPanel.tsx` (NEW):** the shared loading/error/empty/`<ReactFlowProvider>` ladder rendered ONCE — BOTH `pages/Space.tsx` (global) and the BoardDetail `#space` panel render it (the 664-line BoardDetail doesn't grow a 2nd copy of the chrome). **`BoardNode.tsx` (NEW):** custom `type:'board'` ReactFlow node, VISUALLY distinct from ticket (square) and tag (pill) — a heavier DOUBLE-DASHED rounded card with a lucide `Layers` glyph + board name + mono KEY chip, hued by `colorForBoard(data.board)` (same `laneColor` as it'd read globally) with a stronger `color-mix` fill. Click → `navigate('/boards/'+data.board)` (wired in `SpaceGraph.onNodeClick`, board branch BEFORE the ticket branch). **`LabeledEdge.tsx` (NEW, registered as the single `edgeTypes={labeled}` for ALL edges):** `EdgeLabelRenderer`+`BaseEdge`+`getBezierPath`; per-type stroke/dash/marker comes from `SpaceGraph` `EDGE_META` (has_tag=hairline solid, tag_link=accent solid animated+arrow, epic=secondary thick arrow, reference=muted `4 3` dashed arrow, board=accent-strong `2 4` dotted arrow). **USER REFINEMENT C — expandable edge label:** the chip is COMPACT by default (`tag`/`rel`/`epic`/`ref`/`board`) and EXPANDS to the full context on HOVER (transient) or CLICK (sticky-pinned, accent ring + glow; collapse on 2nd click / blur). Expanded text is a rich string naming relation + endpoint (`references PH-274`, `cross-board → KIM`, `relation: depends-on`, `epic parent`, `tagged: <name>`). The WRAPPER is `pointerEvents:'none'` and ONLY the tiny chip `<button>` is `pointerEvents:'all'` (+ `nodrag nopan`) so it NEVER blocks node clicks / pane drag. A `<Panel top-left>` legend lists the present edge types (per-scope) + node shapes. **USER REFINEMENT A — live node spacing:** a `<Panel top-right>` `<input type=range>` (0.5–2.5×) drives `spacing` state → `computeLayout(graph, spacing)` re-runs in a `useMemo([graph,spacing])` + `fitView` re-frames; `spacing` scales the d3-force link distance / charge / cluster-ring radius / collide radius. Persisted per-scope in `localStorage` (`ph-space-spacing:<scope>`). **USER REFINEMENT B — clean clustering:** `computeLayout` no longer uses `forceCenter`; instead each cluster (tickets by `board:<KEY>`, all tags in one `__tags__` lane, each board-node its own lane) gets a centroid on a RING and nodes are pulled to it via `forceX`/`forceY` (strength 0.18) + `forceCollide` to prevent overlap → visually GROUPED blobs, not a hairball. **PH-278 seam (`selectedTagId`/`onSelectTag`) UNCHANGED** — `scope`/`boardKey` props are additive; the `isControlled` logic + highlight derivation are byte-identical; board nodes participate in `neighbourIds` for free (a board node is never a tag, so tag-highlight never selects one). **UI-verified=preview-stub (Chrome connectors down → Claude Preview on host Vite, throwaway `__SpaceVerify` harness route REVERTED before commit):** a 2-pane fixture mounting the REAL `SpaceGraph` for BOTH global (6 ticket+3 tag nodes; has_tag/tag_link/epic/reference edges) and board scope (board's tickets/tags + a `type:'board'` "Kitchen/KIM" node + a `cross-board` edge). DOM+screenshot evidence: all edge labels render per-type + a per-scope legend; clicking `ref`/`rel`/`epic` chips EXPANDED them to `references PH-274` / `relation: depends-on` / `epic parent` (`aria-expanded` flips); the spacing slider at 1.00× vs 2.50× moved nodes ~2.5× apart live (transform deltas captured + screenshot); layout is clustered (KIM-15 sits apart from the PH blob); the board node renders distinctly and its click navigated to `/boards/KIM` (→`/login` via RequireAuth = the route fired); ticket click → `/boards/PH/tickets/PH-274`; tag click stayed on-page and dimmed the non-neighbour (frontend-only highlight intact). Zero console errors. `tsc --noEmit` clean, eslint clean on touched, `npm run build` succeeds. Out of scope: live-API verification (same 403 gate, `update_board_roles` NOT run), the filter UI (PH-278). The `.codemap` sync gate fires (`api/client.ts` → this page).
- ConceptTagChip is data-driven-color hand-written Tailwind, distinct from `.label-chip` on 3 axes; concept-tag mutations are invalidate-on-success not optimistic [PH-276] — the chip differs from a label pill via square `rounded-[5px]` + a lucide `Tag` icon + a tinted left-border/`color-mix` background derived from `tag.color` (inline `style`, a runtime data value — NOT a hardcoded className hex) or a deterministic `var(--lane-*)` fallback (`laneColor(hashString(slug)%8)`) when `color` is null; NO shadcn, no baked hex. Cache: attach/detach return the authoritative full `TicketResponse`, so `onSuccess` does `setQueryData(["ticket", key], updated)` + invalidate `["tickets", boardKey]` — chosen over optimistic precisely because the dev-time un-propagated-`tag.*`-caps 403 would otherwise show a phantom chip that then vanishes; non-optimistic shows the chip ONLY after server confirmation. `concept_tags` is a SEPARATE `TicketResponse` field from `labels` (mirrors backend `ConceptTagSummary` = 4 fields, color nullable) — the embedded type is NOT widened with `description` (the picker uses the richer `ConceptTagDetail`).
- BranchGraph merge-classification is now backend-flag-driven, lane-0 inference is a no-flag LEGACY fallback [PH-268] — the frontend `mergedTips` set in `computeLanePaths` (`branchGraphLayout.ts`) previously inferred merge-ness PURELY from the windowed parent DAG by scanning only LANE-0 commits and collecting their side-lane parents (PH-199). On real data a genuine merge commit can land on a NON-zero lane (a merge nested inside another merge region — "merge-of-merge"), so its 2nd-parent reference to the merged side tip was never scanned → that tip was absent from `mergedTips` → drawn with a FALSE dashed open/unmerged ring even though it is an ancestor of `main` (live repro: `54804385` whose merge commit `43005309` sits on lane 2, at the full-cache window; production GRAPH_LIMIT=150 had zero false rings post-PH-266, so this only surfaced on expanded windows). **Fix**: the backend now serves an authoritative per-commit `merged_into_default: boolean` (sha reachable from the default head computed over the FULL cache, backend half commit 17e6c23). `frontend/src/types/git.ts` `GitCommitSummary` gains an OPTIONAL `merged_into_default?: boolean` (optional for back-compat with cached/un-upgraded payloads). `computeLanePaths` computes `const hasMergedFlag = commits.some(c => c.merged_into_default !== undefined)`: when ANY displayed commit carries the flag it is TRUSTED for ALL commits (`mergedTips` = shas whose flag is true — keyed on the tip's OWN sha, which is exactly the span TIP `branchMerges` checks); when NO commit defines it, the original PH-199 lane-0 parent scan runs UNCHANGED as the legacy fallback. `branchMerges(span)`, `emitOpenTipIfUnmerged`, `emitTerminus`, `computeLanePaths` geometry, the PH-265 recycle guard, the PH-267 seed guard, and `MAX_LANES` are ALL unchanged — only the `mergedTips` derivation. The FE no longer INFERS merge-ness from an incomplete window (which also misclassified git-stash 2nd-parent edges, e.g. `8c8f8f31`); a genuinely abandoned leaf (`7700a2f9`, `merged_into_default=false`) correctly STAYS open. Tests extend `branchGraphOpenMerged.test.ts` (+4): flag path — a merge-of-merge tip flagged true draws NO open ring (the bug), a `false`-flagged tip stays open (no over-close), the flag overrides the legacy lane-0 inference where they disagree; legacy path — a no-flag payload reproduces the lane-0 inference exactly. All four branch-graph suites green on HOST Node 26 (24/24 = prior 20 + 4 new; the frontend Docker container is Node 20 → `ERR_UNKNOWN_FILE_EXTENSION` on `.ts` node:test). UI-verified=node-test. `docker compose exec frontend npm run typecheck` clean.
- BranchGraph ghost-lane fix: `seedBranchHeadLanes` (`branchGraphLayout.ts`, pass 1) now SKIPS seeding a lane for any non-default branch head whose `head_sha` is NOT in the displayed commit window [PH-267] — diagnosis finding #5, deferred from the 2026-06-11 branch-graph investigation. Pass 1 seeded a lane for EVERY non-default head; a branch whose tip scrolled past the 150-commit window is never visited in pass 2 (no commit row owns its sha), so its seeded lane was never freed → a permanent empty "ghost" lane. Dormant on current PH data (all 4 heads in-window) but with ≥9 lexically-early stale branches whose heads are out-of-window it re-triggers the MAX_LANES clamp pile PH-265 fixed: each ghost consumes a `nextLane++` slot BEFORE any real commit is placed in pass 2, displacing live commits onto/past the cap column. `gutterW` is `maxLanes`-derived (not `maxLane`-derived) so the harm is interior ghost columns + clamp displacement, not width. **Fix**: `assignLanes` derives `const commitShas = new Set(commits.map(c => c.sha))` and threads it into `seedBranchHeadLanes(st, branches, commitShas)`; the seed loop does `if (!commitShas.has(branch.head_sha)) continue;` before allocating. The DEFAULT branch head is still seeded UNCONDITIONALLY (lane 0 is the backbone anchor even if its head were somehow off-window). `assignLanes`'s public signature stays `(commits, branches)` — the set is derived internally, so the sole production caller `BranchGraph.tsx:609-610` needs NO change (verified compiling unchanged). Scope was `seedBranchHeadLanes` + the threading + the test ONLY — `MAX_LANES`, `computeLanePaths` geometry, the PH-265 recycle guard, and all backend untouched. Tests extend `branchGraphAssignLanes.test.ts` with group (c): (c1) an out-of-window head is absent from the returned map + `computeMaxLane === 0`; (c2) 12 lexically-early ghost heads + 1 in-window feature → ghosts absent, feature on a small lane, `computeMaxLane ≤ 4`, zero dots on `laneCx(9)`; (c3) an in-window non-default head STILL gets its lane (no over-skip regression). All 3 verified to FAIL pre-fix and PASS post-fix; full suite 20/20 (the prior 17 + 3 new). Runs HOST Node 26 only (`node --test --experimental-strip-types`; the frontend Docker container is Node 20 → ERR_UNKNOWN_FILE_EXTENSION). UI-verified=node-test. tsc clean.
- BranchGraph lane recycling fix: `assignLanes` (`branchGraphLayout.ts`) now frees a lane slot whenever `laneActive[myLane] === commit.sha`, dropping the prior `commit.parents.length === 0 &&` (root-only) guard [PH-265] — real time-windowed git payloads contain ~0 root commits, so the recycle branch was dead code: every merged feature branch leaked a fresh monotonic lane via `nextLane++` (live PH board: 150 commits / 64 merges / 0 roots → maxLane=66, 73 commits clamped onto the MAX_LANES=10 cap column — an unreadable diagonal pile). Dropping the root guard is safe by construction: `propagateToParents` reassigns `laneActive[myLane]` to an unplaced FIRST parent when one exists (first-parent inheritance), so the equality only holds when the branch genuinely terminated/rejoined → the slot is freed for `findFreeLane` to recycle. Result on real data: maxLane 66→4 (~2 visible lanes), 0 commits clamped, default-branch head stays lane 0, time-overlapping branches keep distinct lanes, time-disjoint branches reuse one. Scope was the single-line guard only — `MAX_LANES`, `computeLanePaths` geometry, and all backend untouched. NEW direct guard `branchGraphAssignLanes.test.ts` (none of the 13 prior tests drove `assignLanes` — they hand-built `laneOfSha` and only exercised geometry, so the leak shipped unguarded): a synthetic 0-root window of 12 `--no-ff` merged branches asserts `computeMaxLane ≤ 4` + zero dots on `laneCx(9)`, plus concurrency invariants (overlap→distinct, disjoint→reuse, default head→lane 0). Both fixtures were verified to FAIL pre-fix (maxLane=12, dots on cap; disjoint not reused) and PASS post-fix. Runs HOST Node 26 only (`node --test --experimental-strip-types`; the frontend Docker container is Node 20 → ERR_UNKNOWN_FILE_EXTENSION). UI-verified=node-test (17/17 incl. the 4 new). tsc clean.
- `api.sonarqube.{setupRepo,scanRepo,syncRepo}` + admin-gated per-repo card action buttons [PH-256] — epic PH-253 child C3 FINAL, the FE consumer of PH-254 (setup) + PH-255 (scan/sync). **Client (`api/client.ts`, codemap-mapped → this page):** 3 fns added to the existing nested `api.sonarqube` namespace, the per-repo siblings of the board-level `setup`/`sync`/`scan`: `setupRepo(boardKey, selector, body?)→RepoHealth`, `scanRepo(boardKey, selector)→SonarRepoScanResult`, `syncRepo(boardKey, selector)→RepoHealth`, all via the shared `request<T>` helper (auth + `ApiRequestError` 403 normalisation). `selector = repo.repo_slug` (backend accepts slug-or-id). **Type (`types/api.ts`, NOT codemap-mapped):** NEW `SonarRepoScanResult {repo_slug, project_key, language, scan_status, job_id, message}` mirrors backend `SonarRepoScanResponse` — DISTINCT from the board `SonarScanResult` (carries `repo_slug` + nullable `job_id`, NO `container_source`; reusing the board type would read `undefined` on `job_id`). Reuses the existing `SonarScanStatus` union + `RepoHealth`/`SonarSetupRequest`. **Component (`SonarRepoHealthCards.tsx`, mapped → `components/sonarqube.md`):** was purely presentational; now each card with a non-null `repo_slug` mounts an admin-only `RepoCardActions` (Setup/Scan/Sync) below the metric grid. Gate = `BoardDetail` computes `const isAdmin = useBoardRole(boardKey) === "admin"` (the SAME hook `SonarSetupSection` uses) and threads `isAdmin`/`boardKey` into the cards; non-admin → the row is not rendered → no per-repo mutation fires. 3 `useMutation` keyed off `repo.repo_slug`; Setup = inline disclosure form pre-filled `value={repo.project_key}`; Scan = honest queued copy keyed off `job_id` (non-null → queued; null → honest non-queued reason, no fake queued); Sync = re-poll. Every `onSuccess` invalidates ONLY `["board", boardKey]` (refetchType active) — the single key driving `repo_health`; per-card inline error (`role=alert`, 403→"Admin role required", 422/409/other verbatim) — no toast dep, never silent, never a crash. **UI-verified (Playwright `tests/e2e/ph-256-per-repo-card-actions.spec.ts`, 6/6 green, route-fulfilled GXA `repo_health` + per-repo endpoints):** admin action row on all 3 cards (incl. unscanned); Setup pre-fill + invalidation re-render; scan queued vs non-scannable; Sync leaves "No analysis yet"; 422 inline error no crash; non-admin → no row + no per-repo call. tsc clean, eslint clean, console/network clean. The `.codemap` sync gate fires (`api/client.ts` + `components/sonar/*.tsx`) → this page + `components/sonarqube.md` + `log.md` updated same branch. Out of scope: backend (PH-254/255), bulk scan-all, scan progress polling, non-admin self-service.
- Repo-labeled ticket commits + per-repo commit drill-down threads `repo_slug` into `getCommit`/`DiffViewer` [PH-249] — epic PH-245 child D (the FE consumer of PH-247's commit tagging). `TicketCommitEntry` (`types/git.ts`) gained 3 REQUIRED fields `repo_id`/`repo_slug`/`repo_name` (mirror backend `schemas.py TicketCommitEntry` — a real commit always has a NOT-NULL repo, unlike `RepoHealth`'s nullable aggregate row), and the `api/__smoke__/git.types.ts` `_ticketCommitEntry` literal was updated in LOCKSTEP (the PH-247-flagged tsc build-blocker: adding required fields without fixing the fixture breaks `tsc`). `TicketCommits.tsx` renders a quiet `repo_slug` chip per commit row (`bg-raised text-text-muted`, `max-w-[8rem] truncate`, `title=repo_name` — visually uniform/harmless on a 1-repo board, no row-wrap on narrow widths) between short_sha and summary; `CommitFiles` gained a `repo?: string` prop threaded from `commit.repo_slug` into BOTH `api.git.getCommit(boardKey, sha, {repo})` AND the inner `<DiffViewer fetch={{kind:'commit', …, repo}}>` (its query key also carries `repo ?? "primary"` so two repos sharing a short sha don't collide in cache). This is the 404-fix: PH-247 aggregates a ticket's commits across ALL of a board's repos, so a NON-PRIMARY repo's commit drill-down would otherwise resolve against the board primary and 404 — both `getCommit` + `getCommitDiff` already accepted `repo` since PH-224, so this is pure threading (NO `client.ts` change). `getTicketCommits(ticketKey)` itself takes NO repo param (PH-247 aggregates board-wide). **Browser-verified (Claude Preview, live)**: PH ticket PH-247 Git tab → each commit row shows a `project-hub` repo badge; expanding a commit fires `GET /api/boards/PH/git/commits/{sha}?repo=project-hub → 200` (the threaded `repo` param on the wire, the definitive 404-fix proof), file list + diff render with no "Dosyalar yüklenemedi" banner. `tsc --noEmit` clean. Out of scope: a per-repo commit FILTER chip (deferred — the badge satisfies the AC), per-repo ticket branch identity (PH-247 deferred it). The `.codemap` sync gate fires (`types/git.ts` + `components/git/*.tsx` → this page).
- in-app SonarQube quality dashboard as a third `BoardDetail` "Quality" tab [PH-241] — epic PH-238 child C. New `frontend/src/components/sonar/` dir (`SonarDashboard.tsx` + `MetricCard.tsx` + `metricMeta.ts`) renders a native cyan-on-black dashboard in the board's MAIN area — a third `role="tab"` (`#tab-quality`, `#quality` hash, refresh-safe) added to BoardDetail's existing tablist alongside Kanban / Branch Graph, with a matching `role="tabpanel"` (`#panel-quality`). **Prop-driven, zero new data layer**: `SonarDashboard({ health, boardKey, projectKey })` reuses the EXISTING `["board", boardKey]` cache (`BoardResponse.health` + `sonarqube_project_key`) + `useSonarLiveCounts` (live BUG/VULN/CODE_SMELL totals reconciled `?? health.*`, same as `SonarHealthPanel` so counts never diverge), so BoardDetail's existing `sonarqube_synced` WS invalidation refreshes the grid + counts live — NO new endpoint, NO new TanStack key family. Layout = a quality-gate hero (Passed/Failed/Warning/Unknown reusing the strip's GATE_MAP tones; ERROR → a "See failing conditions in SonarQube" deep-link, since `BoardHealth` carries only the aggregate gate) + a `grid-cols-1 sm:2 lg:3` card grid. `metricMeta.ts` (`METRIC_META` + a separately-exported `GATE_META` to dodge `noUncheckedIndexedAccess` on `[0]`) is pure data (no JSX): key/label/canonical-SonarQube-description/unit/goodDirection/issueType/icon. `MetricCard` formats count→integer, percent→`NN.N%`, **null→em-dash "—" (NOT a fabricated 0)**; issue-backed cards (bugs/vulns/smells) are real focusable `<button>`s (`aria-haspopup="dialog"`, `aria-label="View N bugs"`) that mount the UNCHANGED `SonarIssueDrawer` on click (disabled at 0/null so no empty drawer), the rest are non-interactive `<div>`s. The dashboard lifts `openType`/`triggerRefs`/`handleClose` so focus restores to the clicked card on close (a11y parity). `SonarHealthPanel` KEPT (not folded) + an OPTIONAL `onOpenDashboard` prop → a "View details →" deep-link calling `switchTab("quality")` (existing callers unaffected). Honest empty states: null health + key → "Linked to <key> — no analysis yet · run a scan", no key → "Connect a project key", never blank/fake-zero. **`.codemap`**: the new dir is `components/sonar/` (the `components/sonarqube/*` glob does NOT cover it) → a new `frontend/src/components/sonar/*.tsx → components/sonarqube.md` mapping line added so it stays sync-gated. **Browser-verified (Claude Preview, dark+light)**: KIM (no analysis) → honest empty state, no crash; PH (metrics) → gate hero "Failed" + 6 cards (Bugs 0 / Vulns 0 / Code Smells 11 / Coverage 47.0% / Duplications 1.2% / ncloc 23386) with descriptions; Code Smells click → drawer with 11 `component:line` rows (first `BoardSettings.tsx:357`) + dashboard_url; close → focus back on the card; light theme tokens resolve (white card / red danger tint); 0 console errors. `tsc --noEmit` clean. Out of scope: backend metrics shape (PH-240), per-condition gate breakdown, in-app source viewer.
- `api.sonarqube.{scan,getScanPlan}` + a "Scan now" button (scan-plan-gated, honest-async) [PH-237] — epic PH-234 FINAL child, the frontend consumer of PH-236's two endpoints. **Client**: `api.sonarqube.scan(boardKey)` (POST → `SonarScanResult`) + `api.sonarqube.getScanPlan(boardKey)` (GET → `SonarScanPlan`) added to the existing nested `api.sonarqube` namespace, both via the shared `request<T>` helper (auth + `ApiRequestError` 403 normalisation — non-admin → `ApiRequestError(403)`). **Types** (`SonarScanStatus` six-value union + `SonarScanResult` + `SonarScanPlan` in `types/api.ts`, NOT codemap-mapped) mirror `schemas.py:452/479` VERBATIM — the wire field is `scan_status` (snake), so the TS field is named `scan_status` 1:1 (a `status` rename → silent `undefined`; `LooseString`-widened so an unknown seventh value never collapses the union, S6571). **`SonarSetupSection`**: a THIRD admin action next to Sync. A lazy, **`isAdmin`-gated** scan-plan query (`['board', boardKey, 'sonar-scan-plan']`, `enabled: boardKey && tabActive && isAdmin`, `staleTime 60s`, `retry:false`) — gated because scan-plan is admin-only (403 → would spam non-admins; the Scan button is ALSO hidden for them since the Actions card is `{isAdmin && …}`). The plan feeds a status-panel "Language: <lang>" row + a "Scannable"/"Not scannable in Community Edition" chip + the honest `reason`, so support is informative BEFORE the click; `plan.supported === false` DISABLES the Scan button (annotated with `reason`), as do `!enabled`/`!configured`/`busy`. The scan mutation maps the SIX `scan_status` values to HONEST inline copy: `queued`/`running` → "Scan queued — analysis runs in the background; metrics appear after it completes. Re-sync to refresh." (NOT the generic "status refreshed" — that would lie about instant metrics, the point of the epic); `unsupported`/`error` → the backend message; `unconfigured` → "Run Setup …"; `disabled` → "not enabled". Visual distinction from Sync = a `Radar` icon (NOT `RefreshCw`) + helper copy ("runs a new analysis on the host … slower … in the background"). Scan `onSuccess` invalidates the SAME three Sync families PLUS the scan-plan key — the metric is NOT present immediately (honest async; NO progress-polling invented — backend is fire-and-forget). Scan errors fold into the shared `mutationError` (`setup ?? sync ?? scan`) so a hard 403 still surfaces "Admin role required". **Browser-verified (Playwright `tests/e2e/ph-237-sonarqube-scan-now.spec.ts`, LIVE GXA/FN data, 5/5 green + screenshots)**: GXA (Kotlin, supported) → Scannable chip + Scan enabled → click → honest queued copy + scan-plan re-fetch, no fake "status refreshed"; FN (C#/Unity, unsupported) → "Not scannable in Community Edition" + Scan DISABLED with reason, no POST on force-click; non-admin → no Scan button AND scan-plan never fires (no 403 spam); error/unconfigured/unsupported scan_status each map honestly. `tsc` clean. The `.codemap` sync gate fires (`api/client.ts` + `components/sonarqube/*.tsx`) → both this page + `components/sonarqube.md` + `log.md` updated same branch. Out of scope: backend scan trigger/scanner (PH-236), scan-progress polling, scheduled auto-scan.
- honest SonarQube status messaging: no-analysis ≠ unreachable [PH-235] — the `SonarSetupSection` (settings tab) + `SonarHealthPanel` (board header) keyed their messaging off the boolean trio, so a configured-but-never-scanned board (no metric, e.g. GXA→GameX) showed a FALSE yellow "server is unreachable" banner + a "Reachable off" chip, and the header read "Connect a project key" even with a key set. Backend PH-235 adds a `status` enum (`disabled|unconfigured|no_analysis|ok|unreachable`) + `has_analysis` to `SonarSetupStatus` and `sonarqube_project_key` to `BoardResponse`; the FE now keys off `status`: the yellow `sonar-unreachable-banner` shows ONLY for `status==="unreachable"` (a genuine outage), `status==="no_analysis"` shows a NEUTRAL `sonar-no-analysis-banner` ("No analysis yet — run Sync") + an honest "No analysis" chip in place of the false Reachable chip (Reachable chip renders only for `ok`/`unreachable`). `SonarHealthPanel` takes a new `projectKey` prop (threaded from `BoardResponse.sonarqube_project_key` via `BoardDetail.tsx`): null-health + key set → "Linked to <key> — no analysis yet · run a scan"; null key keeps "Connect a project key". TS: `SonarSetupState` union (LooseString-widened, mirrors `SonarIssuesStatus`), `status`/`has_analysis` on `SonarSetupStatus`, `sonarqube_project_key` on `BoardResponse`. **Browser-verified (Claude Preview)**: GXA settings → no false unreachable banner, neutral "no analysis" note + chip, message "linked to GameX — no analysis yet"; GXA header → "Linked to GameX — no analysis yet" (not "connect a key"); PH (has metrics) → `status=ok`, Reachable chip, full panel — no regression; console clean. Scope = status/messaging only (scanning is PH-236). tsc clean; 8/8 ph-226 e2e green (incl. new TC-5 no_analysis).
- `me` query token-scoped + `queryClient.clear()` on every identity change — fixes admin gating stuck after an in-app login [PH-232] — security bug: after an in-app token switch to the admin token WITHOUT a hard reload, every `isAdmin`-gated control (SonarQube Setup/Sync, repos_path editor, repo add/remove, members) stayed locked behind "Admin role required" because `useMe()` keyed `["me"]` (token NOT in key, `staleTime: 5min`) and neither `setToken` nor `logout` invalidated it → TanStack served the PRIOR identity's cached `me` → `useBoardRole`→`isAdmin` was stale-false. **Two complementary changes, both shipped**: (a) `useMe()` queryKey → `["me", token]` (token-scoped → a switch yields a NEW cache entry, can NEVER serve the prior identity; `isAdmin`/`useBoardRole` derive from it so they follow automatically). (b) The Zustand auth store calls `queryClient.clear()` on EVERY real identity change — inside `setToken` (guard `prev !== token`) AND `logout` (guard `had`) — the single chokepoint every token change flows through, INCLUDING the two outside-React 401 auto-logouts in `client.ts` (`request<T>` L107 + `mcpCall` L64) that already route through `logout()`, so **`client.ts` is deliberately NOT touched** (and the `.codemap` HARD gate on `client.ts` does not fire). **clear-all over surgical `invalidateQueries(["me"])`**: a token switch is a TRUST BOUNDARY — surgical invalidate would leave board-scoped data (role, repos, sonar status, tickets, members) from the prior identity in cache (cross-identity bleed = the security defect); `clear()` guarantees NO prior-identity bytes persist. **Import-cycle avoidance**: the `QueryClient` singleton was lifted out of `main.tsx` into a dedicated leaf module `lib/queryClient.ts` that BOTH `main.tsx` and the store import (a store→`main.tsx` import would form a cycle since `main.tsx`→`<App/>`→store). **Guard extracted** to a dependency-free `stores/identityGuard.ts` (`shouldClearCacheOnIdentityChange(prev,next) = prev!==next`) so the security-relevant decision is unit-testable under `node --test --experimental-strip-types` (mirrors the `branchGraphLayout.ts` pure-logic pattern) — both `setToken` and `logout` route through it; `set({token})` runs BEFORE `clear()` so the new `["me", token]` key is live when observers re-subscribe (no one-frame fetch under the old key). **Browser-verified (Claude Preview, live PH)**: started as non-admin (jarwis-frontend / `frontend_dev`) → SonarQube tab "Read-only — admin role required" banner + no Setup/Sync, General repos_path read-only; switched to the admin token via the store `setToken` chokepoint WITHOUT a reload → 8 cached entries cleared to 0, banner gone, Setup/Sync + "Re-affirm setup" enabled, repos_path became an editable input, Repository-tab add/remove/set-primary appeared — all on the same React Router page; on an UNCONFIGURED-sonar board (BENCH) the Setup button is enabled + the request fires as admin (a 403 there is a PRE-EXISTING BACKEND `have:[]` membership quirk: admin's `/me` reports `BENCH→admin` but `require_board_admin` returns empty — NOT this ticket's frontend cache scope); logout→login-as-different-identity cleared cache at BOTH boundaries with NO prior-admin entry surviving (`leakedAdminIdentityEntry: false`), and the non-admin lock correctly returned; `/api/auth/me` fired only ~once per identity boundary (no refetch storm) and 0 console errors. 6/6 Node unit tests green (`stores/identityGuard.test.ts`); `tsc --noEmit` clean. No DB/board state changed (PH `project-hub` + BENCH NULL sonar keys unchanged). Out of scope: backend `/api/auth/me` (verified correct), the BENCH `require_board_admin` 403 quirk (separate backend ticket), re-keying every board-scoped query by token (unnecessary — `clear()` covers them).
- Board `repos_path` is VIEW+EDIT-able from BoardSettings → General tab (C3) [PH-230] — epic PH-227 FINAL child; makes the HOST filesystem root (PH-228 backfilled, drives git auto-detect + SonarQube key derivation) editable from the UI, not just DB. **Placed in the GENERAL tab** (next to Name/Description), NOT the repository tab — it is a board-level property (one HOST root per board, upstream of all repos), so it lives where `updateBoardMutation`'s `onBlur` PATCH pattern already is. **`api.updateBoard` payload extended** with `repos_path?: string | null` (`client.ts`) — and `BoardResponse` in `types/api.ts` gained `repos_path: string | null` (was MISSING — backend served it since PH-228 but the FE type omitted it). **Admin field-gating, NOT an auth refactor**: the board PATCH endpoint keeps `current_actor` auth (Name/Description stay pm-editable — deliberately NOT widened to `require_board_admin` to keep blast radius small); the PATH FIELD is gated behind `isAdmin` in the UI (editable `<input>` for admin, read-only `<output>` for others) + a graceful inline 403 if a PATCH is somehow rejected. **A dedicated `updatePathMutation`** (separate from `updateBoardMutation`) isolates the path field's inline error (422 invalid / 403 non-admin) + "saved" banner from Name/Description; both its `onSuccess` AND `updateBoardMutation.onSuccess` now invalidate `['repositories', boardKey, 'detect']` ALONGSIDE `['board', boardKey]` so the Repository-tab detect panel re-scans the new path immediately. **`commitReposPath` helper** clears any prior error at the START of every blur (so re-correcting back to the saved value visibly clears a stale message even on a no-op skip) and only PATCHes on a real change; "saved" tracked via an explicit `pathSaved` state (NOT the mutation's `isSuccess`, which a board-refetch re-render can desync). **Empty path is a valid state** (clears to NULL → detection disabled): the input allows empty, and the Repository tab shows a `repository-no-path-hint` ("No project path set — auto-detection is disabled…") with a button that jumps to the General tab. `AddRepositoryPanel` hardcoded `/repos/` copy → "this board's project path" wording (path is now per-board). **Validation is backend-authoritative**: a non-empty path is run through `repo_paths.to_container_path` at the PATCH boundary → 422 on relative / `..` / outside-HOST_HOME; the UI hint is advisory only. **Browser-verified** (live PH board): General tab renders the real backfilled path; admin edit→Save persists + "saved" banner; invalid (`/etc/passwd`, `/etc/shadow`, relative, `..`) → inline 422 message, no crash; valid edit clears the error + invalidates detect; empty-path hint + goto-General link render when path is null; non-admin sees the read-only field; detect lists the `project-hub` repo; PH path restored to `/Users/huseyinkanat/Documents/project-hub`.
- `api.sonarqube.{getStatus,setup,sync}` namespace + a SonarQube tab in BoardSettings (C6) — settings-tab one-click Setup/Sync + a member-readable status panel [PH-226] — epic PH-220 FINAL child, the frontend consumer of PH-223's three secret-free endpoints (`GET status` member / `POST setup` admin / `POST sync` admin), all returning `SonarSetupStatus` 200 (never-500/never-hang). **`api.sonarqube` is a nested namespace** (mirrors the newer `api.git.*` convention from PH-224/225, NOT three top-level fns) — all three route through the shared `request<T>` helper (auth + `ApiRequestError` normalisation); the legacy top-level `getSonarIssues` stays put (no churn). **Types `SonarSetupStatus`/`SonarSetupRequest`** mirror `schemas.py:394-431` verbatim (`datetime`→ISO `string`). **`SonarSetupSection` (NEW `components/sonarqube/SonarSetupSection.tsx`)** owns ONE status query keyed `['board', boardKey, 'sonar-setup']` — a DEDICATED key (NOT `['board', boardKey]`) so it never collides with BoardDetail's `BoardResponse` query (Risk R3) — plus two admin mutations. **Sync's `onSuccess` invalidates THREE families** so a board-detail tab's `SonarHealthPanel` tile refreshes WITHOUT a reload (Risk R2): the status query, `['board', boardKey]` (BoardResponse.health), and a predicate over `['board', boardKey, 'sonar-issues', ...]` (the `useSonarLiveCounts`/drawer family) — the SAME keys BoardDetail's `sonarqube_synced` WS handler hits (BoardDetail.tsx:263-277). **403 is handled in TWO layers (Risk R1)**: (1) the Setup/Sync buttons RENDER only when `isAdmin` (`useBoardRole==='admin'`); the status panel is always shown (GET status is member-level); (2) defensively, both mutation `onError`s catch `ApiRequestError && status===403` → inline `role=alert` "Admin role required to run this action.", never throwing to the error boundary (board-admin membership and the UI role can disagree — the live frontend_dev token 403s every write here). **UX states** drive off the status flags: `enabled=false` → "not enabled on this server" banner + both buttons disabled; `reachable=false` → unreachable note but Sync STAYS enabled (retry); `configured=false` → Setup is the glowing `btn-primary`, else it demotes to a `btn-ghost` "Re-affirm" and Sync is primary. Setup is one-click (empty body → backend derives `project-hub` for PH); idempotent. Dashboard link is `target=_blank rel='noopener noreferrer'`, omitted when `dashboard_url` is null (host-facing, secret-free — Risk R5). No toast dep added — success is an inline `role=status` line. Design-system-pure (Cyan-on-Black tokens, GATE_MAP mirrors SonarHealthPanel, relative time via inline `Intl.RelativeTimeFormat`). The SonarHealthPanel is left UNTOUCHED — no second sync surface on the board-detail header (settings is the single control surface, per the user's tight scope). **Browser-verified (Claude Preview, live PH board)**: status panel renders `configured=true`, `project_key=project-hub`, QG pill "Failed" (ERROR), enabled/reachable/configured chips, relative last-fetched, dashboard link `http://localhost:9000/dashboard?id=project-hub`; non-admin (frontend_dev) → read-only banner + buttons hidden; temp-promoted to admin → Setup + Sync render, clicking each → real `POST .../sync→403` + `POST .../setup→403` caught into the inline "Admin role required" message with NO crash (no Vite error overlay, no unhandled rejection); membership restored to `frontend_dev` + re-verified after. `tsc` clean. The `.codemap` sync gate fires (`api/client.ts` + new `components/sonarqube/*.tsx` → this page + `components/sonarqube.md`), so both pages + `log.md` are updated in the same branch. Out of scope: backend (C3/PH-223 done), a Sync button on the health panel, server kill-switch toggle UI, full scanner run from UI.
- Git settings multi-repo + auto-detect UI (C5) — one-click add of detected gits + list/set-primary/remove [PH-225] — epic PH-220, the settings counterpart to PH-224's branch-view switcher; consumes the PH-221/PH-222 backend (list/add/remove/set-primary/detect) with NO backend change. **One-click Add maps `DetectedRepo`→`RepositoryCreatePayload` by a trivial field copy** (`provider_guess`→`provider`, `default_branch ?? 'main'`, `local_path`/`name`/`remote_url` verbatim) so the user never types a path — the contract is frozen 1:1 on the backend side (`schemas.py DetectedRepo`). **Refetch, NOT optimistic, on every mutation** — set-primary and remove have server-side primary auto-promotion (removing the primary promotes the oldest remaining repo) the client cannot predict, so the list query is the single source of truth; all three mutations invalidate `['repositories', boardKey]` + `[…, 'detect']` + `['git', boardKey, 'status']` (the last keeps the PH-224 branch-view/switcher coherent). **Slug is the selector** (URL-safe, human-stable) for remove/set-primary — not the id (PH-221 id-precedence gotcha; slug is unambiguous within a board). **Detect fires lazily** (`enabled: …&& showAdd`) — the FS scan has a 5s server budget, so it runs only when the Add panel is disclosed, deduped by `staleTime: 30_000`, never polled. **403 is handled in TWO layers**: (1) primary — write actions HIDDEN when `!isAdmin` (list + detect are member-auth so the read views still render for the dev/frontend_dev role); (2) defensive — every write `onError` catches `ApiRequestError && status===403` → inline `Bu işlem için admin yetkisi gerekli.`, never throwing to the error boundary, because board-admin membership and the UI `role` can disagree (the canonical PH-224 trap; the live frontend_dev token 403s every write here, verified). **Manual-add threads the POST path explicitly** — `RepositoryConfigForm` gained a `mode: 'add' | 'edit-primary'` prop (default `edit-primary` = the legacy singular PUT upsert, byte-identical single-repo edit); `mode='add'` routes to `api.git.addRepository` (POST collection) so a manual add creates a NEW repo instead of overwriting the primary (the architect-flagged regression). **`humaniseRelativeTr` lifted to `lib/time.ts`** (was private to RepositoryStatusPanel) so the new list rows + the old status panel share one impl (DRY — kills the S4144 copy-paste); kept the Turkish `Intl.RelativeTimeFormat` locale (matches the tab copy), distinct from the English `relativeTime` used by branch-view/notifications. **Shared `['repositories', boardKey]` key with PH-224's BoardDetail switcher is intentional** (same endpoint/shape → one benign cache; invalidating from settings also refreshes the switcher — desired). **Browser-verified (Claude Preview, live PH + temp-seeded `demo-web`)**: 1-row back-compat + badge/banner/hidden-actions, detect greyed-already-linked + addable-unlinked, one-click add → 2 rows, set-primary badge swap + primary-first re-order, remove → back to 1 row + candidate re-appears unlinked, non-admin 403 (`required: board.admin`) with hidden actions, forced-403 inline message with no crash; 0 console errors; seeded repo + temp admin grant removed (PH single-repo). tsc + eslint clean. The `.codemap` sync gate fired (`components/repository/*.tsx`, `api/client.ts`, `types/git.ts` → this page), so this page + `log.md` updated same branch. Out of scope: backend (C1+C2 done), SonarQube UI (C6), per-repo independent Sonar projects.
- Branch-view repo switcher (C4) — multi-repo branch screens via a `?repo=` URL-synced selector [PH-224] — epic PH-220, builds on PH-221's `GET /api/boards/{key}/repositories` (1:N list; exactly one `is_primary`) + the optional `repo` query selector on every `/git/*` read. **Types (`types/git.ts`)**: `RepositorySummary` gained the PH-221 identity fields `slug`/`name`/`is_primary` (were missing → tsc-caught the smoke fixture); added `RepositoryListResponse` + a `Repository = RepositoryResponse` ergonomic alias. **API client (`api/client.ts`)**: added `api.git.listRepositories(boardKey)` and threaded an OPTIONAL `repo?: string` into the 7 git reads (`getGraph`/`getBranches`/`listCommits`/`getCommit`/`getCommitDiff`/`getRangeDiff`/`getStatus`) — appended as `repo=<slug>` only when set, so single-repo callers emit a BYTE-IDENTICAL request (no `repo=`, backend → primary; the explicit no-regression guarantee at the client layer). **`RepoSwitcher` (NEW `components/git/RepoSwitcher.tsx`)**: a native `<select>` (keyboard-accessible for free) styled with Branch-view shadcn tokens, in a thin header bar ABOVE the `.bg-wrap` grid; renders `name` (slug fallback) + a `(primary)` option suffix + a `PRIMARY` badge on the selected primary; returns `null` (belt-and-braces) when `repositories.length <= 1` — the PARENT also gates rendering on `>1` so single-repo boards get ZERO extra markup / no layout shift. **`BranchGraph`**: gained an optional `repo?` prop threaded into all git query keys (`['git', boardKey, repo ?? 'primary', kind, …]` — the `repo ?? 'primary'` segment isolates per-repo caches so a switch refetches instead of bleeding repo A's graph into repo B) AND each call's `repo` param; the nested `CommitDiffPanel`'s `getCommit` query + the `<DiffViewer fetch={{kind:'commit', …, repo}}>` were also threaded (omitting repo there would 404 a repo-B sha against the primary). **`DiffViewer`**: `repo?` added to both arms of the `FetchSpec` union (`commit`/`range`) + `useCommitDiff`/`useRangeDiff` query keys (`repo ?? 'primary'` segment) + the underlying api calls. **`BoardDetail`**: owns the repo state — fetches `listRepositories` (cached `['repositories', boardKey]`, graceful fallback to single-repo on error so the tab never blocks), derives the primary slug, reads/writes `?repo=` via `useSearchParams`, renders `RepoSwitcher` only when `>1` repo, passes `repo={selectedRepo}` + `key={selectedRepo}` to `BranchGraph` (the `key` remounts the subtree on switch → auto-resets `selectedBranch`/`selectedCommit`, no stale lane geometry). Selecting the primary deletes the param (clean URL); an unknown `?repo=` slug falls back to primary + cleans the param (shared-stale-URL guard). The WS `git_synced` invalidation moved from a prefix `queryKey` to a `predicate` (`isBoardGitQuery`) because the per-repo segment shifted the `kind` slot — now invalidates every repo's graph/status board-wide. **Browser-verified (Claude Preview, live PH + a seeded 2nd repo)**: AC1 single-repo → no switcher, byte-identical 3-pane + diff-on-click, git calls carry NO `repo=`; AC2 ≥2 repos → switcher visible, defaults to primary; AC3 switch to 2nd repo → sidebar/graph/commits re-render to its DISTINCT branches (`main`+`feature/login-page`) + SHAs, `git/graph?repo=demo-web` on the wire; AC4 `?repo=demo-web` survives reload + shared-URL, unknown slug → primary + param cleaned; AC5 network tab confirms no `repo=` on single-repo. Seeded 2nd repo removed after verification (PH back to single-repo). tsc + eslint clean; 13 branchGraphLayout Node-tests stay green (layout untouched). The `.codemap` sync gate fired (`types/git.ts`, `api/client.ts`, `components/git/*`, `components/diff/*` → this page), so this page + `log.md` updated same branch. Out of scope: add/remove/detect/set-primary repo UI (C5/PH-225), `TicketCommits` repo-scoping (a ticket's commits live in its board's primary).
- Final fixable-TS smell residual cleared behavior-preserving; 2 deprecated-API + 2 redundant-role smells left as documented wontfix [PH-219] — the last cleanly-fixable TS smell remainder after epic PH-210 (blocks dropped the count 279→50). **S1854 ×1 (`git/BranchGraph.tsx:220`)**: removed the dead `const color = laneColor(lane)` store (the long-standing pre-existing unused-var the whole epic flagged to leave alone — finally cleaned). `lane` itself became dead once `color` went, so it + the now-unused `laneOfSha` prop on `CommitRow` (type + destructure + the single call site — `laneColor` is pure, only fed the removed color) were dropped too; the SEPARATE `LaneOverlay laneOfSha` prop is untouched (still load-bearing for the gutter geometry). **S6772 ×2 (ambiguous JSX spacing)**: `BranchGraph.tsx:426` Ticketed-only filter — added explicit `{" "}` between the checkmark `<span>` and the label text; `TicketCommits.tsx:241` repo-not-connected hint — the trailing period after the Settings `<a>` made explicit as `{"."}` (no space, period attaches to the link — same render). **S3358 ×2 (`EdgePropertyPanel.tsx:173,178`)**: the field/role pluralization (`count === 1 ? "field" : "fields"`) nested inside the summary template-literal ternary was lifted to `fieldNoun`/`roleNoun` locals — same strings. **S6853 ×2 (`NodePropertyPanel.tsx:138,153`)**: the Initial/Terminal-state checkbox labels (which wrap the input + a nested `<div>` of text, too deep for the rule's default control-association depth) got explicit `htmlFor`/`id` pairs (`node-state-initial`/`node-state-terminal`) — additive, click-to-toggle already worked. **S6842 ×1 (`PermissionMatrix.tsx:209`)**: dropped `role="grid"` from the LOADING-SKELETON `<table>` (grid implies a keyboard-navigable composite widget; the placeholder has no interaction, and the REAL matrix table at ~310 already carries no role — now consistent). **S6481 ×1 (`ThemeProvider.tsx:58`)**: the context provider `value={{theme,toggleTheme,setTheme}}` was a fresh literal every render (forced all consumers to re-render); `setTheme`/`toggleTheme` wrapped in `useCallback([])` (stable — they only call `setThemeState`) + `value` memoized with `useMemo([theme,...])`. **Browser-verified** (Claude Preview, live app): theme toggle flips `html.light` + persists `localStorage.theme` + the toggle's aria-label updates BOTH directions → memoized value still propagates to consumers, 0 console errors. **S6551 ×3 (object default-stringification)**: a new shared `lib/utils.ts:stringifyUnknown(value)` helper (primitives → `String()`, objects/arrays → `JSON.stringify` with a `String()` fallback) replaces `String(e.old_value)`/`String(e.new_value)` in `TicketDetail.tsx:renderChange` (field-change history) — and a local `childrenToText(children)` in `MarkdownRenderer.tsx` (flatten React children string/number leaves) replaces `String(children)` for mermaid code-block source. **Browser-verified**: TicketDetail "Geçmiş" history timeline renders field-change entries (status/field changed) with NO `[object Object]` anywhere on the page. **LEFT AS-IS (wontfix, documented)**: **S1874 ×2 (`BoardSettings.tsx:72,87`)** — `api.addWorkflowState`/`api.updateWorkflowStates` are deprecated REST endpoints (will 404). The non-deprecated path is the MCP-backed `api.updateWorkflow(workflowId, {states, transitions})`, but that takes the FULL states+transitions array (not an incremental add/reorder) and carries the PH-103 rename/edge-sync semantics — migrating the live admin Board-Settings add-state modal + drag-reorder to it is a FUNCTIONAL change with real regression risk, out of behavior-preserving smell-cleanup scope. Recommend a dedicated functional ticket to migrate these two mutations to `updateWorkflow`. **S6822 ×2 (`TicketCommits.tsx:103,281`)** — `role="list"` on the file/commit `<ul>`s is LOAD-BEARING (the B3-flagged wontfix): Tailwind Preflight (`@tailwind base`, confirmed active, no override) resets `ul { list-style: none }`, which makes Safari+VoiceOver drop the implicit `list` role; the explicit `role="list"` restores it. Removing it regresses a11y. **S1874 ×1 (`WorkflowEditor.tsx`)** WAS fixed: the deprecated `e.returnValue = ""` in the `beforeunload` handler removed — modern browsers show the unsaved-changes prompt from `e.preventDefault()` alone. **S2004 ×3** fixed: `BoardDetail.tsx` WS handler's deep `setLiveTickets` updaters + the `sonar-issues` invalidate `predicate` hoisted to module-scope helpers (`upsertLiveTicket`/`removeLiveTicket`/`replaceLiveTicket`/`isSonarIssuesQuery`, matching the existing `*TicketInCache` factory pattern); `WorkflowEditor.tsx:350` `onSettingsClick` inline arrow (5 levels deep) hoisted to a `useCallback` `makeSettingsClick(nodeId)` factory (effect dep `states` → `makeSettingsClick`, same re-run behavior). **Verification**: `npm run typecheck` + `npm run build` both exit 0; eslint clean on all touched files (the `BranchGraph.tsx:220` pre-existing error the prior epic blocks flagged is now RESOLVED by the S1854 fix). The `.codemap` sync gate fired (`components/git/*.tsx` → this page) for `BranchGraph.tsx` + `TicketCommits.tsx`, so this page + `log.md` are updated in the same branch. Live SonarQube counts stay until the post-merge full scan re-evaluates (issues API is scan-snapshot, not live-AST).
- SonarHealthPanel BUGS/VULNS/SMELLS counts now derive from the live issues endpoint `total` (shared with the drawer, fallback to `board.health` when the live fetch loads/errors) [PH-218] — the poller-cached `health.*` counts could lag up to one 5-min poll interval behind a scan, so the resting panel could show a non-zero count while the PH-204 drawer (which reads live `getSonarIssues(...).total`) listed nothing. New `useSonarLiveCounts(boardKey)` hook (`useQueries` × 3 types, `getSonarIssues(boardKey, { type, page_size: 1 })`, read `total` only) is the single source for the three counts shared with the drawer, so a non-zero tile + empty drawer is now impossible. Keyed in the `['board', boardKey, 'sonar-issues', 'count', type]` family (dedicated `'count'` segment avoids colliding with the drawer's `page`-keyed list cache — Risk R2). Reconciled per-tile as `live ?? health.*` so SonarQube-down falls back to the cached count (never blank — AC-2); gate/coverage/duplications/ncloc/`fetched_at` STAY on `board.health`. `BoardDetail`'s `sonarqube_synced` WS branch additionally predicate-invalidates the `sonar-issues` family so counts refetch live with `board.health` (AC-3). Frontend-only, no backend change. tsc + build green; eslint clean; browser-verified tile==drawer on the live PH board.
- Cognitive-complexity (typescript:S3776) cluster reduced via behavior-preserving helper/hook extraction across 5 dense functions [PH-215] — epic PH-210 block B5 (RISKY, run last), frontend half of a ticket whose python:S3776 half the backend did first on the same branch (commit 4f0c24d). ALL 5 flagged TS functions were REFACTORED (0 wontfix); every change is pure cohesive-sub-block extraction with the same return values / side effects / order, NO observable behavior change. **`lib/diff/parseDiff.ts:parseUnifiedDiff` (cc 29→<15)**: extracted `hunkFromMatch` (regex-match → Hunk shell, comma-less `oldLines/newLines` default-to-1 preserved), `classifyDiffLine` (the `+`/`-`/` `/`\ `/unknown glyph dispatch + cursor `oldNo/newNo` mutation, via a `LineCursor` box), and `parseHunkBody` (the inner consume-until-next-`@@`-or-EOF loop incl. the EOF-trailing-empty-line break). The dropped explicit `if (!line)` outer-loop guard is equivalent — `HUNK_HEADER_RE.exec("")` is null so an empty line still falls to the `if (!match) { i++; continue; }` path. **Differentially verified**: original-vs-refactored output byte-identical across 8 patch shapes (empty, whitespace-only, standard 2-comma hunk, comma-less `@@ -5 +7 @@`, no-newline-meta + multi-hunk, rename-only/no-hunks, trailing-EOF-empty-line, unknown-glyph). **`components/git/branchGraphLayout.ts:assignLanes` (cc 36→<15, the RISKIEST)**: the two-pass deterministic lane-assignment algo was kept byte-equivalent by extracting `claimLaneForSha` (the repeated `findFreeLane → ?? nextLane++ → set → grow(`<=`) → occupy` block used by the commit-self + merge-parent cases), `seedBranchHeadLanes` (pass-1; **critically keeps the pass-1 `while (laneActive.length < lane)` strict-`<` grow, which DIFFERS from `claimLaneForSha`'s `<=` — pass 1 is NOT routed through the helper**), and `propagateToParents` (first-parent-inherits-`myLane` vs merge-parent-claims-free-lane). `nextLane` is boxed in a mutable `LaneState` so helpers can bump it (was a `let` closure var). **Differentially verified**: identical `laneOfSha` maps across 6 graph shapes (linear, fork+merge, lane-reuse with recycled lane numbers, name-sorted multi-branch heads, orphan roots, empty); the 13 existing `branchGraph*.test.ts` Node-test cases (`computeLanePaths`, unchanged) all green; **browser-verified** the Branch Graph draws identical lane geometry (per-branch colors, fork/merge curves, open-tip ring cap) on real PH history + branch-filter re-render. **`hooks/useWebSocket.ts:handleMessage` (cc 17→<15)**: extracted the pure `latencyStatus(latency)` ladder (the `<100`/`<500`/else tiers; S3923 dead `<5000` arm already removed earlier) and the three message-type branches into `handlePong`/`handleErrorMessage`/`handleDegradation` `useCallback`s; `handleMessage` is now a thin `if/else if/else` dispatch. The early-`return`-per-branch control flow is preserved as mutually-exclusive branches; `handlePong` carries the `isConnected` dep so `handleMessage` identity still changes when `isConnected` flips (the `handleMessageRef` re-sync effect is unaffected). **`components/MermaidBlock.tsx:render` (cc 17→<15)**: extracted `mermaidErrorMessage(e)` (the `Error.message`→`string`→generic fallback) and `renderMermaidSvg(renderId, code, theme)` (the `mermaid.initialize` config + `parse` + `render` core, returning the SVG string / throwing as before); the effect's `render()` keeps only the cancellation + container-write/clear flow. **`pages/TicketDetail.tsx:TicketDetailPage` (cc 21→<15)**: hoisted `LIVE_EVENTS` to a module const (was an inline `new Set` rebuilt every render — same membership), extracted `applyLiveTicketUpdate(qc, ticketKey, message)` (the WS `onMessage` cache-sync: ticket refetch + history/commits/comments invalidation, same order + same `notification:new` CustomEvent dispatch + same `ticket_key`/`LIVE_EVENTS` guards) and `toTransitionOption(t, board)` (the "Move to →" descriptor mapper: target-state hex-color-when-valid else `--state-<name>` token). **Browser-verified**: ticket page renders, "Live" WS pill + notifications dropdown receive live events, "Move to →" popover shows `in_progress`(amber)/`done`(green) dots correctly, diff panel parses hunks/line-numbers/glyphs/expand-hunk, 0 console errors. **Verification**: `npm run typecheck` + `npm run build` both exit 0; eslint clean on all 5 touched files (the pre-existing `BranchGraph.tsx:220` issue the epic flagged is NOT in our touched set). The `.codemap` sync gate fired (`hooks/useWebSocket.ts`, `lib/diff/parseDiff.ts`, `components/git/branchGraphLayout.ts` → this page), so this page + `log.md` are updated in the same branch. Live SonarQube TS S3776 count still shows 5 until the post-merge full scan re-evaluates (issues API is scan-snapshot, not live-AST).

- TS structural smell cluster cleared via behavior-preserving extractions + component hoisting; a few naming-convention/intentional-pattern smells left as documented wontfix [PH-213] — epic PH-210 block B3, six SonarQube TS rules + a TS long-tail sweep across ~22 files, ALL changes structural/byte-equivalent at runtime. **S6478 (define-React-component-during-render, ×15, ALL in `MarkdownRenderer.tsx`)**: the 15 inline element renderers (`pre/code/a/h1-h3/ul/ol/table/thead/th/td/blockquote/p/hr`) were previously built inside a `useMemo(() => ({...}), [compact])` map — new function identities each time `compact` changed, the exact remount/perf bug the rule targets. Hoisted ALL 15 to **module-scope named components** (`MdPre`, `MdCode`, …) and built a single module-level `markdownComponents: Components` map (stable identity, passed to `<ReactMarkdown components={…}>` unconditionally). The closed-over `compact` prop is now delivered via a module-scope `CompactContext` (`createContext(false)`) that `MarkdownRenderer` provides (`<CompactContext.Provider value={compact}>`) and each styled component reads with `useContext` — preserving the compact-size behavior without per-render component definitions. This also incidentally cleared **S6767 (×2, unused `href`/`children` PropType)** since the consolidated components only destructure props they use. The `MermaidBlock` `key={code}` remount-on-source-change behavior is preserved in `MdCode`. **Browser-verified (MANDATORY for S6478)**: TicketDetail markdown (table/headings/inline-code/bold/list/pre all render identically via the hoisted components, no remount/lost state), 0 console errors. **S3358 (nested-ternary, ×23, ALL fixed)**: each nested ternary extracted to an `if/else` chain assigned to a `let` (status/label/tooltip/message derivations) or to a pre-`return` `React.ReactNode` variable for JSX trees (`TicketDetail` connection-pill `connColor/connBackground/connTitle/connLabel` + `commentsBody`; `FieldEditor` `fieldBody`; `MarkdownFieldEditor` `readView`; `FileDiffView` `diffBody` 4-way binary/truncated/no-hunks/hunk-list; `WorkflowList`/`WorkflowStateList`/`EdgePropertyPanel`/`WorkflowEditor`/`BoardSettings`/`BoardDetail`/`useWebSocket`/`SonarHealthPanel`/`MermaidBlock`). **S2004 (function-nesting>4, ×5, ALL in `BoardDetail.tsx`)**: the WS-message handler's deeply-nested `setQueryData` updater callbacks were hoisted to 3 module-scope pure factory helpers (`appendTicketToCache`/`removeTicketFromCache`/`replaceTicketInCache` returning `(old)=>old` transforms) and the `.then()` callbacks pulled into named locals (`onCreated`/`onUpdated`), flattening the nesting. **S3735 (void-operator, ×12, ALL in `api/__smoke__/git.types.ts`)**: the type-only smoke file's `void X;` symbol-reference markers (under an existing `eslint-disable no-unused-vars`) replaced with a single array-literal expression `[…];` referencing every symbol — same compile-time "read" with no `void`. **S4325 (redundant-assertion, ×8, ALL fixed)**: dropped `(err as Error)`/`(error as Error)` on TanStack `useMutation`/`useQuery` error values already typed `Error` (TicketDetail ×3, NewTicketDialog, Boards) and the two `liveEdge?.data?.allowed_roles as …`/`field_gates as …` casts in `WorkflowEditor.openEdgePanel` (edge `data` is already `WorkflowTransition`-typed via `initialEdges`) + the `as "new"|"active"|"done"` cast on the now-extracted `category` if/else. **S4624 (nested-template-literal, ×6, ALL fixed)**: the 5 `` `/path${q ? `?${q}` : ""}` `` query-suffix builders in `api/client.ts` extracted to a `const suffix = q ? `?${q}` : "";` local, and `TicketDetail`'s `requiredFields.map(f => `${f} ✓`)` mapped string pulled to a `satisfiedFields` local. **TS long-tail sweep (safe subset fixed)**: S3863 (duplicate `@/api/client` import in `useEnsureBoardWorkflow.ts` → merged), S4030 (write-only `spans` collection in `branchGraphLayout.ts` removed — only `spanAtRow` was ever read, lane/color rendering unaffected), S6571 (`SonarIssueSeverity | string`/`SonarIssuesStatus | string` collapsing to `string` → `… | (string & {})` to keep literal autocomplete without collapse), S6822 (redundant `role="complementary"` on `BranchGraph`'s `<aside>` removed — `<aside>` has the implicit role; `aria-label` retained). **LEFT AS-IS (wontfix, documented)**: **S6754 (×3)** — `ThemeProvider` `setThemeState`/`Login` `setLocalToken` are INTENTIONALLY renamed off the `set<State>` convention to avoid collision with the public `setTheme`/store `setToken`; `WorkflowStateList`'s `useState(() => setItems(states))` is a render-time prop-sync hack whose timing is risky to alter — all three are behavior-load-bearing, the convention can't be satisfied without a real change. **S6822/S6772/S6842/S6853/S6811/S6843 on TicketCommits `<ul role="list">` and BranchGraph aria internals (~lines 220-427)** — the `role="list"` is LOAD-BEARING under Tailwind Preflight (`list-style:none` makes Safari/VoiceOver drop the implicit list role, so the explicit role restores it; removing it regresses a11y), and the BranchGraph aria-role internals are entangled with the pre-existing `BranchGraph:220` eslint issue the epic flagged to leave alone. **S1135** — the `types/git.ts` TODO is a deliberate `[PH-163]`/G15+-referenced roadmap note, not stale debt. **S6551/S2486/S6481/S1874** — object-stringification, exception-handling, context-value-memo, and deprecated-API smells were not touched in B3 (behavior-change risk; out of the structural-cluster scope). **Verification**: `npm run typecheck` + `npm run build` both exit 0; eslint on touched files surfaces ONLY the pre-existing `BranchGraph.tsx:220` unused-`color` error (on `main`, not ours). The `.codemap` sync gate fired (`api/client.ts`, `hooks/useWebSocket.ts`, `components/diff/FileDiffView.tsx`, `components/git/BranchGraph.tsx`, `components/git/branchGraphLayout.ts` → this page), so this page + `log.md` are updated in the same branch.
- JSX/a11y smell cluster cleared mechanically + behavior-preserving; native `<dialog>` conversion deliberately deferred [PH-212] — epic PH-210 block B2, five SonarQube TS rules across ~24 files. **S6606 (nullish-coalescing, ×4, ALL fixed)**: `branchGraphLayout.ts ×2` (`free !== null ? free : nextLane++` → `free ?? nextLane++`; `free` is `number | null` where `0` is a VALID lane, so `??` is the correct operator — `0 ?? x` keeps `0`, identical to the `!== null` guard) + `diff/HunkView.tsx ×2` (`line.oldNo !== null ? line.oldNo : ""` → `line.oldNo ?? ""`; `oldNo`/`newNo` are `number | null`, no `undefined`, so byte-identical). **S6479 (array-index key, ×4, ALL fixed with REAL stable keys)**: `HunkView` diff rows → `${type}:${oldNo}:${newNo}:${content}` (each line unique by this composite); `FileDiffView` hunks → `${hunk.oldStart}:${hunk.newStart}:${hunk.header}` (hunks never overlap); `BranchGraph` lane segments → `${seg.kind}:${seg.d}` (SVG path geometry is unique per segment); `PermissionMatrix` LOADING-SKELETON rows → mapped an array of constant `skeleton-row-${i}` tokens instead of the bare index (no underlying data id exists for a fixed-count skeleton, so a stable token array is the idiomatic fix — the key is the string value, not the index). **S6819 (prefer-tag-over-role, ~20 of 31 fixed, 11 LEFT-AS-IS)**: `role="status"` → `<output>` (×11: LiveStatus, Boards, BoardDetail toast, SonarIssueDrawer loading+empty, SonarHealthPanel empty, DiffViewer ×2, RepositoryStatusPanel ×2, RepositoryOperationsPanel) — `<output>` carries an implicit `status`/`aria-live=polite` role; block-flow sites got an explicit `block`/`flex` class so the inline-default `<output>` keeps the original layout; `role="region"` → `<section aria-label>` (×2: SonarHealthPanel populated, RepositoryOperationsPanel code block); `role="list"`/`role="listitem"` → `<ul>`/`<li>` (BranchGraph commit rows now `<ul><li><button>`, WorkflowStateList dnd-sortable item root `<div>`→`<li>` + container `<ul>` — `useSortable setNodeRef` is element-agnostic; Tailwind preflight resets `ul/li` margins so layout is unchanged); `role="button"` div-as-button → native `<button>` (FileDiffView file header, TicketCommits file row [`disabled={isBinary}` replaces the `role`/`tabIndex`/`aria-disabled`/early-return branch], TicketCommits commit row) + WorkflowList row split (`<li role=button onClick>` with NESTED action `<button>`s → `<li>` wrapping a primary `<button>` + sibling action buttons, because a `<button>` cannot nest interactive children). **S6847 + S6848 (event-handler-on-non-interactive / non-native-interactive, ALL ~25 fixed)**: the modal-backdrop dismiss pattern (`<div role="dialog" onClick={onClose} onKeyDown={onActivateKeyDown}>` wrapping an inner `onClick=stopPropagation` surface — the PH-206 shape) was restructured to a **sibling dismiss `<button className="absolute inset-0" aria-label="Close" onClick={onClose}>`** + the content element carrying `role="dialog" aria-modal aria-labelledby` (now `relative z-10`, NO handlers). This removes BOTH the handler-on-non-interactive-div smell AND the inner stopPropagation surface (clicks inside the content no longer reach the backdrop button since it's a SIBLING, not a parent — so `stopPropagation`/`stopActivationKeyDown` are no longer needed). Applied to 10 modal/overlay files (AddMemberModal, SonarIssueDrawer, EdgePropertyPanel, NodePropertyPanel, WorkflowList ×2 dialogs, WorkflowStateList, DetachConfirmModal, RotateSecretModal, BoardSettings, TicketDetail ×2, NewTicketDialog). TicketDetail's two modals kept Escape-to-close via a NEW document-level `keydown` effect (parallel to the existing move-menu one) rather than an inline `onKeyDown` on the dialog content (which would re-trip S6847). **`lib/a11y.ts` is now unused** (all 12 importers dropped both helpers) — left in place as a documented utility (removing it is out of B2 scope; a future cleanup ticket can delete it). **The 11 `role="dialog"` S6819 sites are LEFT AS-IS (wontfix candidate)** — native `<dialog>` is the rule's target element but is NOT behavior-preserving here: it requires the imperative `.showModal()` API (these modals are conditionally MOUNTED, not toggled via `open`), brings UA `::backdrop` + `margin:auto`/`border`/`padding`/`background` styling that conflicts with the existing `fixed inset-0` glass-overlay system, and the e2e suite drives these via `getByRole("dialog")` + click-outside + Esc contracts (workflow-delete, members-tab, admin-ticket-delete specs). Converting all ~11 to native `<dialog>` is a styling+behavior change with real test risk, so per epic guidance ("don't force a behavior change — mark wontfix later") the `role="dialog"` attribute is retained on the dialog content; the genuine a11y BUGS (S6847/S6848 handler-on-non-interactive) ARE fixed. **Verification**: `npm run typecheck` + `npm run build` both exit 0; eslint on touched files surfaces ONLY the pre-existing `BranchGraph.tsx:220` unused-`color` error (on `main`, a different smell block, not ours); jsx-a11y is NOT in the local eslint config so the SonarQube re-scan is the verification of record. The `.codemap` sync gate fired (`components/diff/*.tsx`, `components/git/*.tsx`, `components/repository/*.tsx` → this page), so this page + `log.md` are updated in the same branch.
- All React component props marked read-only to clear SonarQube `typescript:S6759` (×57 sites, 33 component files) [PH-211] — **type-only, zero runtime/JSX change** (epic PH-210 block B1, the safest-first mechanical block). Each flagged component's props type was wrapped in `Readonly<...>` at the least-churn idiomatic point, matching the file's existing style: **named interface/type destructure** (`}: FooProps)` → `}: Readonly<FooProps>)`) for the majority (e.g. `TicketCard`, `FieldEditor`, `WorkflowEditor`, `PermissionMatrix`, the 5 `repository/*` panels, `BranchGraph`'s `LaneOverlay`/`CommitRow`/`BranchSidebar`/`CommitDiffPanel`/`BranchGraph`, `TicketCommits`'s `CommitFiles`/`TicketCommits`); **inline-object props** (`}: { foo: T })` → `}: Readonly<{ foo: T }>)`) for the inline-typed sites (`MarkdownCompact`, `LiveStatus`, `NotificationItem`, `SonarHealthPanel`'s `MetricTile`/`ClickableMetricTile`/`SonarHealthPanel`, `diff/DiffViewer`'s 4 inner components, `RepositoryOperationsPanel`'s `InlineToast`/`CopyCodeBlock`, `RotateSecretModal`'s `CopyButton`, and all 9 `TicketDetail.tsx` sub-components `StatePill`/`TypeChip`/`Avatar`/`RoleChip`/`ActorChip`/`TransitionErrorBanner`/`CommentCard`/`ActivitySection`/`GitEventBadge`); **`props: T` parameter form** (`Toast.tsx` `SuccessToast(props: Readonly<Omit<ToastProps,"variant">>)`). **No prop had to stay mutable** — `tsc --noEmit` stayed green with zero new errors, confirming no component actually mutates its props (readonly can surface real mutation; none did here). `npm run typecheck` + `npm run build` both exit 0. ESLint on touched files surfaces ONE pre-existing `no-unused-vars` error (`BranchGraph.tsx:220` `color`) that exists on `main` independent of this change — left untouched (out of scope; belongs to a different smell block), verified pre-existing via `git stash`. The `.codemap` sync gate fired for `components/diff/*.tsx`, `components/git/*.tsx`, and `components/repository/*.tsx` (all → this page), so this page + `log.md` are updated in the same branch.
- Two SonarQube TS bugs fixed without behavior change: explicit `localeCompare` sort comparator + removal of a dead latency-tier ternary branch [PH-207] — **S2871 (CRITICAL, `PermissionMatrix.tsx`)**: the orphan-role columns were sorted via `[...orphans].sort()` with NO comparator, so ordering relied on default UTF-16 code-unit collation (locale-naive, flagged unreliable). Fixed to `.sort((a, b) => a.localeCompare(b))` — board-defined roles still render first (board order, unsorted), orphans now follow in stable locale order. Browser-verified on the BENCH workflow editor: the matrix renders all 10 board-role columns + the `assignee(orphan)` column at the end, `reactComponent: PermissionMatrix`, 0 console errors. **S3923 (MAJOR, `useWebSocket.ts` pong handler)**: the connection-quality ladder `latency < 100 ? "excellent" : latency < 500 ? "good" : latency < 5000 ? "poor" : "poor"` had a redundant third threshold — both arms of `latency < 5000` produced the identical `"poor"`, so the condition was dead logic. **Intent analysis**: a pong literally just arrived inside the `data.type === "pong"` handler, so the socket is provably alive — the 4th status value `"disconnected"` is reserved for actual disconnection (set on close/error elsewhere) and must NOT be produced here; there is no distinct 5th quality state in the `ConnectionQuality["status"]` union (`excellent | good | poor | disconnected`), and the `excellent`/`good`/`poor` tiers are not consumed as distinct ladder logic anywhere outside this hook. The `latency < 5000` split therefore never mapped to a different value and was a copy-paste artifact. **Fix**: collapse to `latency < 100 ? "excellent" : latency < 500 ? "good" : "poor"` — any latency ≥ 500ms (while connected) is `"poor"`. Runtime output is byte-identical for every input; only the dead condition is removed. Browser-verified: BENCH board view shows the green "Live" indicator (WS connected, quality computed, no crash) with 0 console errors. tsc + eslint(touched) green. No `package.json`/type changes.
- Keyboard activation for non-interactive `onClick` elements goes through ONE shared `lib/a11y.ts` helper, not per-site inline handlers [PH-206] — SonarQube `typescript:S1082` (jsx-a11y/click-events-have-key-events) flagged 22 visible non-interactive elements (`div`/`li`/`form`) carrying an `onClick` but no keyboard listener: modal backdrops (dismiss-on-click), inner dialog surfaces (`onClick={(e)=>e.stopPropagation()}`), and one clickable list row (`WorkflowList` `<li onClick={onSelect}>`). The rule needs **at least one keyboard listener** on the element (it does NOT require role/tabIndex — that's the separate S6847 rule, not flagged here). Instead of hand-rolling `onKeyDown` at each of the 22 sites, a `frontend/src/lib/a11y.ts` exports two pure helpers: **`onActivateKeyDown(activate)`** returns an `onKeyDown` that fires `activate` on Enter/Space and calls `preventDefault()` for Space (so Space-activate doesn't also scroll) — used for dismissible backdrops + the clickable row; **`stopActivationKeyDown(e)`** is the `onKeyDown` counterpart to `onClick={(e)=>e.stopPropagation()}`, stopping Enter/Space from bubbling to a backdrop dismiss — used for the inner dialog surfaces. The clickable `WorkflowList` row ALSO gained `role="button"` + `tabIndex={0}` + `aria-pressed={selected}` (it is semantically a toggle button, so full interactive semantics are correct there); backdrops keep their existing `role="dialog"` and only gain the keyboard listener. **Visual + mouse behavior is byte-identical** — every existing `onClick` body is mirrored into the new `onKeyDown`, no markup/class changed. 10 files touched (AddMemberModal, EdgePropertyPanel, NodePropertyPanel, WorkflowList ×5, WorkflowStateList, repository/DetachConfirmModal, repository/RotateSecretModal, BoardSettings, TicketDetail, NewTicketDialog). **jsx-a11y is NOT in the local eslint config** (no `eslint-plugin-jsx-a11y` dep) — SonarQube's analyzer is the only enforcer, so a SonarQube re-scan is the verification of record. **Browser-verified** (Claude Preview, live PH board): WorkflowList rows render `<li role="button" tabIndex=0 aria-pressed>`, are focusable, and Enter flips selection (`aria-pressed false→true` + `bg-accent-soft` applied) — proving the keyboard handler is wired identically to onClick; NewTicketDialog backdrop click dismisses (unchanged), inner form click does NOT dismiss (stopPropagation intact), and a Space keydown on the backdrop dismisses with `defaultPrevented===true`; no console errors, no visual regression. tsc + eslint(touched) green.
- SonarHealthPanel tiles drill into a lazy issue drawer via `api.getSonarIssues` + `useSonarIssues` [PH-204] — the 3 issue-backed tiles (Bugs/Vulns/Smells) became `<button>`s opening `SonarIssueDrawer`; Coverage/Duplications stay non-clickable `<div>`s (no issue list backs an aggregate %). **Lazy fetch** is the load-bearing choice: `useSonarIssues` gates `enabled: type != null` and the drawer mounts only when `openType != null`, so a board view costs ZERO `/sonarqube/issues` requests until a tile is clicked (avoids 3 speculative queries per board render). **`retry: false` + read `data.status`** because the PH-203 endpoint is graceful-200 (status-coded errors are NOT thrown) — `total===0` (empty) is kept distinct from `status!=='ok'` (error); the query only rejects on real network failure, and both error paths funnel into one error state. The drawer **reuses the `DetachConfirmModal` overlay/ESC/aria shell verbatim** (no new modal lib — the app has no shadcn `ui/` dir) styled as a right drawer; disabled tiles when count is 0/null prevent opening an empty drawer; focus restores to the triggering tile on close. Severity→tone uses semantic tokens only (no baked hexes, theme-aware). Browser-verified on live PH data (35 bugs/269 smells/0 vulns): lazy network, correct `type`/`page` params, pagination on smells (>50), VULNS disabled, Coverage/Duplications inert, ESC+focus-restore, dashboard link host-facing; tsc+eslint green.
- Branch-graph color is keyed per-BRANCH (hashed tip sha), not per-lane-index; single-commit branches emit their divergence-descent too [PH-198] — two symptoms surfaced after the PH-190 geometry port. **Color:** `laneColor(lane)` is a pure function of the recycled LANE INDEX, so the many short feature branches that reuse a lane number all painted the same hue (PH-178..186 = all green). The fix adds `branchColor(key)` (FNV-1a hash of the span's tip sha → `LANE_COLORS.slice(1)`, the non-cyan palette) and stamps each `Span` with a `color`: lane-0 backbone stays `laneColor(0)` cyan, side lanes get a per-branch color used by every branch/merge/in-span-vertical segment + the side dot. Chose tip-sha as the identity (stable, present in the displayed list, distinct per disjoint span) over a global branch-name lookup (graph data has no per-commit branch label). **Fork edge:** a single-commit branch is a one-row span (`spanFirst===spanLast`); the old `isFirst` block drew only the upward branch-out and `return`ed before the `isLast` descent, so the divergence edge down to the base-lane parent was missing. Factored the descent into `emitDescentIfMerges()` and call it on BOTH the single-row and multi-row terminal paths — single-commit branches now draw the full fork→merge loop while the PH-190 invariants (single-row curves, main-anchored, dot-per-commit, no idle-gap bridge) stay intact. Geometry-and-color only; `assignLanes`/`laneColor`/`LANE_COLORS` untouched, `branchColor` additive. **Verified on REAL data** (PH-190 gotcha): live PH board graph → reused lane column went 1→7 distinct branch colors, all 8 palette hues present, PH-197/PH-194 single-commit branches show both curves; QA `branchGraphFork.test.ts` 5/5 + `branchGraphLayout.test.ts` 4/4 green, tsc green, 0 console errors.

- "Merged" is a TIP-is-parent-of-a-lane-0-commit test, NOT a tip's-first-parent-on-lane-0 (fork) test; open branches get a hollow dashed open-ring cap instead of a merge-return curve [PH-199] — the user reported that an open/unmerged branch (PH-178, never merged, ticket in_review) looked identical to genuinely merged branches (PH-177/PH-179). PH-198's `mergesOntoMain` keyed the merge-return descent off `laneOf(firstParent)===0`, but EVERY feature branch's tip forks off main so its first parent sits on lane 0 → that condition is true for merged AND open branches alike (a FORK test masquerading as a MERGE test). The real merge signal is orthogonal and already in the parent DAG: a branch merged iff its TIP sha appears as a parent of some lane-0 (main) commit (a merge commit referencing it). Implemented as a precomputed `mergedTips` set + `branchMerges(span)` predicate keyed on the span TIP (`commits[span.first].sha`). Chose the span TIP as the identity (not the descent row's first-parent) because the descent is emitted at the span's LAST row while merge status is a property of the branch TIP; for single-commit spans first===last so PH-198's single-commit merged loop is preserved. For the visible distinction (the user's actual ask) the layout emits an additive `openTips` array and `BranchGraph.tsx` renders open tips as a hollow dot + dashed ring cap (`<title>Open branch (unmerged)</title>`) in the branch hue — NO backend `is_merged` flag was needed (DAG sufficient). **Verified on REAL data** (PH-190 gotcha): live PH graph shows PH-178 `f3d25623` with the open-ring + no return curve vs PH-177/PH-179 closed loops, exactly 2 open rings graph-wide; QA `branchGraphOpenMerged.test.ts` 4/4 + siblings green, tsc green, 0 console errors.
- `SonarHealthPanel` reads `board.health` (no separate query); `sonarqube_synced` WS invalidates `["board",key]` [PH-196] — reuses the existing board query + the `git_synced` WS early-return pattern. A new presentational component `frontend/src/components/SonarHealthPanel.tsx` (prop `{ health: BoardHealth | null }`) renders a board-wide SonarQube strip mounted in `BoardDetail.tsx` directly under `<header>` and ABOVE the tab strip (so it shows in BOTH Kanban + Branch Graph without per-tab duplication). It is purely presentational: BoardDetail owns the `["board", boardKey]` query (PH-193 added `BoardResponse.health`) and passes `boardQuery.data?.health ?? null` down — NO separate health query/hook (the board refetch is the single source of truth). The panel guards `health == null` FIRST → renders a dashed-border empty state ("No SonarQube scan yet"); otherwise a quality-gate pill (OK→Passed/`text-success bg-success-soft`, ERROR→Failed/`text-danger bg-danger-soft`, WARN→Warning/`text-warning bg-warning-soft`, null→Unknown/`text-text-muted bg-raised`, mirroring the `LiveStatus` soft-tone `.badge` treatment) + 5 metric tiles (bugs, vulns, code_smells, coverage %, duplications %) in `bg-raised` chips with `.eyebrow` labels + `.mono` values (coverage/duplications `toFixed(1)%`, integers raw, null→`—`; bugs/vulns value tinted `text-danger` when >0) + a right-aligned relative `fetched_at` ("Synced 3m ago" via inline `Intl.RelativeTimeFormat`, no new dep). Design-system-pure: ONLY ProjectHub tokens (`.card .badge .eyebrow .mono`, semantic `text-success/warning/danger`, `bg-*-soft`, `text-text-*`, `bg-surface/raised/inset`, `border-hairline`, `rounded-pill/md/lg`, `text-2xs`) — no slate/indigo utilities, no baked hexes → theme-aware (dark + `html.light`) for free. **WS live-update**: a new `sonarqube_synced` branch in BoardDetail's `onMessage` (placed right after `isGitSyncedMessage`, BEFORE the generic ticket logic) calls `queryClient.invalidateQueries(["board", boardKey], refetchType:"active")` and **early-returns**. The early-return is load-bearing: the board-level `sonarqube_synced` envelope carries empty `ticket_id`/`ticket_key` sentinels (PH-193 `_publish_synced`), so falling through to the `REFETCH_EVENTS`/`message.ticket_key` path would call `api.getTicket('')` on an empty key — same defensive shape as `git_synced`. `BoardHealth` TS interface added to `types/api.ts` mirroring PH-193's `schemas.py:263-277` serializer verbatim, plus `health: BoardHealth | null` on `BoardResponse`. **Browser-verified** (Claude Preview, live app + real PH board, admin dev token): empty state (PH board, `health=null`) renders with no console errors and no kanban layout regression; populated state seeded via a temporary `SonarQubeMetric` row (gate ERROR → "Failed" red, tiles `3/1/42/78.4%/4.2%`); WS live-update by publishing a real `sonarqube_synced` EventEnvelope on `board:{id}` → panel flipped to "Passed" green + `0/0/12/91.7%/1.3%` WITHOUT a reload, network showed extra `GET /api/boards/PH` and NO empty-key `getTicket`, console clean. Seeded metric row + project_key reset afterwards (dev DB left clean). tsc green.
- Lane geometry = per-row, contiguous-span, lane-0-anchored (port of ui_kit `Gutter`) — supersedes PH-179's global-run + multi-row bezier [PH-190]. The PH-179 emitter's ONE vertical run per lane (`laneFirst→laneLast`) plus full-gap childY→parentY beziers looked correct on the seeded mock but produced wiggly/tangled lanes on the REAL PH history, because real history REUSES lane numbers (a lane is recycled by disjoint feature branches) and the global run bridged the idle rows between them while the multi-row beziers swept across many rows. The fix: compute each lane's **contiguous active spans** (maximal consecutive-active-row runs) and emit PER ROW — straight vertical only within a span (own-lane 1.0 / pass-through 0.55), a single-row branch-out curve at `spanFirst` from main lane (0) TOP → branch MID, a single-row merge-in curve at `spanLast` (when first parent is on lane 0) from branch MID → main BOTTOM, using the kit control-point ratios translated into the centered `laneCx`/top·mid·bottom space. Span = derived from the SAME `laneOf` map `assignLanes` produced (a commit's own row + first-parent pass-through fill), so it exactly matches where the lane is occupied and never bridges an idle gap. No `M..C..` curve spans more than one `rowH`; off-list parents end a span straight with NO fabricated merge (R3 preserved). `assignLanes`/`laneColor`/`LANE_COLORS`/lane-index are byte-identical — geometry-only. **Verified on REAL data, not mock** (the whole point of the 3rd iteration): production `assignLanes`+`computeLanePaths` over the live 319-commit/73-merge PH history → max segment y-extent = exactly one rowH, 51 idle gaps none bridged, 93 fork/merge curves all main-anchored, 319 dots==319 rows; rendered overlay (dark+light) is a crisp straight main backbone with single-row fork/merge to parallel branch columns, matching the kit STYLE. A `node:test` unit fixture (reused lane → two disjoint spans) is the regression guard.
- Branch Graph re-skinned to ui_kit `branchgraph.jsx`: CSS-grid 3-pane (200/1fr/340), floating card removed → inline diff panel, compact list on selection, ticketed-only filter [PH-188] — supersedes PH-179's floating-card approach. The user reported the live Branch Graph still didn't match the design; PH-179 had built against the WRONG reference (`branch-graph-row.html` specimen → flex row of gapped rounded cards + a floating glass quick-look card), while the AUTHORITATIVE design is `ui_kits/projecthub/branchgraph.jsx` + `kit.css .bg-*`/`.diff-*` (a full-bleed CSS-grid 3-pane, hairline dividers, no gaps/cards, right pane IS the diff panel). Key decisions: (1) **flex → CSS `grid`** (`gridTemplateColumns` `200px minmax(0,1fr) 340px` / `.no-diff` `200px minmax(0,1fr)`, `gap:0`, border-top + sidebar border-r + diff border-l) — `minmax(0,1fr)` (not `1fr`) so the list can shrink/ellipsize; the per-pane `rounded-lg border bg-surface` cards are removed and borders live on the dividers. (2) **`FloatingDetailCard` + the `diffOpen` two-step DELETED** — one `selectedCommitSha` state drives both compact list + diff panel; the card's quick-stats moved into the panel `.diff-stat` header (still from `getCommit`, same cache-shared queryKey). (3) **Compact mode** = `Boolean(selectedCommit)` hides Author+Time in rows AND list-head (replaces the ad-hoc `sm:block` author hide with the deterministic kit rule). (4) **`DiffViewer` gains `showSummary?: boolean` (default true)** — the branch-graph panel passes `showSummary={false}` so DiffViewer's internal "N files changed" header doesn't duplicate the panel's `.diff-stat`; additive + default-true so TicketCommits/DiffDemo callers are unaffected. (5) **`ROW_H 40→44`** + `LANE_PX 14→15`/`GUTTER_PAD 8→12` for kit pixel-parity — pure geometry, dots re-verified centered (`rowIdx*44+22`). (6) **"Ticketed only" is a REAL filter** (`commit.ticket_keys.length > 0`), not decorative — the lane gutter recomputes over the reduced displayed set (`computeLanePaths` takes the displayed list; R3 graceful-terminate handles off-list parents), browser-confirmed dot-count==row-count (no orphaned lanes). **Kept full-bleed OUT of scope**: the grid stays inside `Layout.tsx`'s `max-w-7xl` container (no negative-margin Layout surgery) — the user's complaint was grid-vs-flex/gaps/cards/floating-card, all fixed without viewport-edge bleed (a separate Layout ticket if ever wanted). Continuous-bezier lanes (PH-179), `assignLanes`, git fetching, WS live-highlight, branch filter, a11y all preserved. Both themes verified side-by-side vs the ui_kit `index.html` (Claude Preview, live PH board + kit served on :8899; harness-free — drove the real route).
- Notifications panel (`NotificationBell`) restyled to ui_kit `NotificationPanel`; `relativeTime` extracted to `lib/time.ts` (shared) [PH-187] — F2/PH-171 had only recolored the dropdown surface to `bg-raised` (see [PH-171] notes below); it did not match the kit *layout*. PH-187 makes the panel a **glass** surface (`width:340`, `background: color-mix(in srgb, var(--bg-raised) 96%, transparent)`, `border: 1px solid var(--hairline-cyan)`, `borderRadius: var(--radius-lg)` 14px, `boxShadow: var(--shadow-lg)`, `overflow:hidden`, `.animate-pop` pop-in) while KEEPING the anchored dropdown (`absolute right-0 top-9 z-50` + outside-click-to-close, NO scrim) and all data plumbing (`api.listNotifications`, mark-read/mark-all, unread badge, `notification:new` invalidation, ticket Link nav, empty state) byte-identical. **Per-item semantic icon** via a pure module-level helper `iconFor(n)` keyed on `event_type`: `state_changed`→Activity/`var(--success)`, `comment_added`→MessageSquare/`var(--text-secondary)`, `git_pr_merged`/`git_commit_linked`/`git_pr_linked`→GitMerge/`var(--accent)`, fallback message `/merged|→ main/i`→GitMerge/accent else Bell/`var(--text-muted)` (never throws — always returns a valid pair, so empty/legacy `event_type` is safe). Header keeps "Bildirimler" + mark-all and adds a `<span className="eyebrow">{unread} new</span>` (uppercase/letter-spaced/muted) when unread>0. Item body: 13px text-primary title (unread→font-medium), **11px mono relative time** via the shared `relativeTime(created_at)` replacing the old `toLocaleString('tr-TR')` full timestamp; unread (`!is_read`) rows keep `bg-accent-subtle` (ui_kit "fresh"). **DRY:** the `relativeTime` formatter (`45s/8m/1h/3d/2mo/1y ago`) was extracted verbatim from `components/git/BranchGraph.tsx` to **`frontend/src/lib/time.ts`** and is now imported by BOTH `NotificationBell` and `BranchGraph` (no duplicate definition); BranchGraph commit times render identically. `@keyframes ph-pop` (opacity 0→1 + scale .96→1, `transform-origin: top right`) added to `index.css @layer utilities` with `.animate-pop` — motion-safe for free via the global `prefers-reduced-motion` guard (index.css:250, animation-duration→0.01ms), no per-component media query. Both themes browser-verified (Claude Preview, live app + real data, seeded harness not committed): width 340px, glass bg `color(srgb …/0.96)`, shadow `0 16px 48px -12px`, `animation-name: ph-pop`; icon colors confirmed via DOM (comment_added→message-square text-secondary, state_changed→activity success); eyebrow uppercase letter-spacing 0.48px; unread rows `rgba(34,211,238,0.06)` accent-subtle; BranchGraph 150 commit rows still render relative times, no console errors. typecheck+build green. **This file is `.codemap`-gated for `BranchGraph.tsx` (`frontend/src/components/git/*.tsx`); the relativeTime extraction touched it, so this page is updated in the same branch (exit-protocol §11.2 sync gate).**
- Ticket Detail refit birebir to ui_kit `ticketdetail.jsx`/`primitives.jsx`/`kit.css` (visual/layout only) [PH-182] — F7/PH-176 had recolored TicketDetail to tokens but did not match the ui_kit *layout*. PH-182 reproduces the kit structure with F1 tokens (kit.css NOT imported): a new `@layer components` block in `index.css` adds the kit's component classes (`.crumb .key-chip .type-chip .label-chip .role-chip .status-pill .t-phase .avatar[.sm] .tabs/.tab/.tab-count/.underline .td-head/.td-grid/.td-col/.td-side .field-head/.field-title/.field-body .ac-list .activity-item .composer .side-row/.side-label/.side-val .branch-row .menu/.menu-label/.menu-item/.menu-hint .btn-sm .dot/.dot-sm`) so the JSX uses real classes instead of inline-only utilities. **Header**: rewritten from one flex-wrap row to a two-col `.td-head` — LEFT = inner chip row `[KeyChip + TypeChip + t-phase(when ticket.agent_phase)]` then the h1; RIGHT = StateControl + Live pill + admin Sil. **h1 trap fixed**: was `text-2xl` (28px) inheriting base `h1{28/700}`; now explicit `text-[24px] font-semibold tracking-[-0.015em] leading-[1.25]` (browser-verified 24px/600). **StateControl moved sidebar→header**: a `btn-secondary btn-sm` showing `<StatePill>` (state-colored `color-mix` pill carrying `data-testid="ticket-state-badge"` + inline state hex so workflow-state-color e2e still passes) + rotating chevron; the Move-to `.menu` popover now lists allowed transitions as `dot + RAW mono state id (in_review, NOT "in review") + amber "req fields" hint`; the OLD sidebar State ring + sidebar Move-to block + "Alanlar" quick-edit card + standalone AgentPhase card are DELETED. `transitionMutation`/`updateMutation`/`deleteMutation`, the workflow transition API, WebSocket live-sync, react-markdown+mermaid parsing, DiffViewer/TicketCommits internals are BYTE-IDENTICAL — only chrome/layout changed. **Field cards** (`FieldEditor`/`MarkdownFieldEditor`): read-mode chrome `card p-3` → `.card padding:0` + `.field-head`(title 13/600 + amber `*` + ghost Edit, bottom hairline) + `.field-body`(p-4); Edit/Preview tabs, save/cancel, dashed empty-required state, MarkdownRenderer all unchanged. **Activity**: `card p-3` → `.card padding:0` + inset `.tabs` row (counts + accent underline) + `.field-body` body; CommentCard + history rows re-laid as `.activity-item` (Avatar sm 22px + mono who + RoleChip + right mono time + 12.5px body); bottom textarea-form composer → inline `.composer` (single-line input + btn-primary sm Send icon, still a `<form>` for Enter-to-send). **Sidebar**: three+ cards collapsed to ONE sticky (`top-72`) `.td-side` card of `.side-row`s with bottom hairlines (Priority dot, Reporter mono, Assignee Avatar+mono, Labels `.label-chip`, Created mono-muted, Branch `.branch-row` pill — diff-modal onClick + disabled logic intact); grid col 18rem→**20rem (320px)**, gap-4→gap-5; mobile `@media(max-width:1023px)` collapses `.td-grid` to 1 col + static sidebar. `ticket-state-badge`/`delete-ticket-button` testids preserved (relocated, not dropped); the field_gate `#field-<key>` anchor-jump (TransitionErrorBanner) is the retained "jump to missing field" affordance after removing "Alanlar". Both themes browser-verified side-by-side vs the ui_kit `index.html` (Claude Preview, admin-token + theme-localStorage seeded harness, not committed); typecheck+build green; e2e: admin-ticket-delete 2/2 pass, workflow-state-color "TicketDetail state badge" pass (the unrelated kanban-column case is PH-183 surface, untouched here).
- Per-row gutter `<svg>` → ONE continuous bezier-lane overlay + `computeLanePaths` emitter [PH-179] — the PH-167 per-row inline `<svg>` (GutterCell, 0→ROW_H segments) produced visible seams and a "düz/kopuk" (flat/broken) lane look that did not match the design HTML. PH-179 DELETES GutterCell and renders ONE absolutely-positioned, full-height `<svg>` behind the rows (`pointer-events:none`, `aria-hidden`, height = `commits.length*ROW_H`, lives inside the scroll container so it scrolls with the rows). The new pure emitter `computeLanePaths(commits, laneOfSha, rowH, laneW, gutterPad, maxLanes) → { segments, dots, gutterW, height }` (added to `branchGraphLayout.ts`) produces, in ONE shared coordinate space (`laneCx = gutterPad + min(lane,maxLanes-1)*laneW + laneW/2`, `y = rowIdx*rowH + rowH/2`): one continuous vertical `M..L..` path per lane spanning its full first→last row extent (no stitching), plus cubic-bezier `M..C..` S-curves with vertical-tangent control points (`±rowH*0.5`) for branch-in (child-lane color) / merge (parent-lane color, `parents[i>0]`) connections, plus one dot per commit. Rows are plain `<button>`s with `padding-left: gutterW` so text clears the gutter; dots are drawn in the SAME svg so they sit exactly on the path. **`assignLanes`/`laneColor`/`LANE_COLORS` are BYTE-IDENTICAL** (only `computeLanePaths`+`LaneSegment`/`LaneDot`/`LaneGeometry` types added and `ROW_H` 36→40 to match the design's `height:40px`). Branch-filtered views (`listCommits`) where a parent sha is off-list terminate the bezier one row below toward the parent column (R3 guard, no crash). Browser-verified (seeded-cache Preview harness, deleted before commit) in dark+light: lanes are continuous smooth beziers, a merge shows a curve joining lanes, hollow selected dot, new-commit drop-shadow glow, lanes recolor on theme flip with no JS branch.
- Floating glass detail card (quick-look) COEXISTS with the DiffViewer pane (deep-dive) [PH-179] — the design HTML reveals a small glass `.detail` card on commit click; rather than replace the proven 3-pane `DiffViewer` (PH-167 fixed "diffler görünmüyor" with it), PH-179 adds a `FloatingDetailCard` anchored near the selected row (absolute, `right:14`, flips above the row near the list bottom — R5 clip guard) showing mono 12-char SHA + summary + ticket chip(s) (instant, from the `GitCommitSummary` in hand) + a stat row (`N files` / `+adds` success / `−dels` danger). A "View diff" button opens/keeps the right-hand DiffViewer pane (`diffOpen` state); card dismissible via X / Esc / click-away (mousedown on the list bg; rows + card `stopPropagation`) / re-clicking the row. Glass styling ported from `branch-graph-row.html .detail` → F1 tokens: `bg = color-mix(in srgb, var(--bg-raised) 96%, transparent)`, `border: var(--hairline-cyan)`, `rounded-lg p-[13px] w-[252px] z-50`. **Gotcha (resolved): `boxShadow` is set INLINE (`var(--shadow-lg), var(--glow-cyan-sm)`), NOT via a Tailwind `shadow-[...]` arbitrary class** — the comma inside `shadow-[var(--shadow-lg),var(--glow-cyan-sm)]` breaks Tailwind's JIT arbitrary-value parser so the class silently never generates and the shadow+glow is lost.
- Card stats sourced from `getCommit`/`GitCommitDetail` — NO backend change [PH-179] — `GitCommitSummary` (graph payload) carries no file/add/del counts, but `api.git.getCommit(boardKey, sha): GitCommitDetail` returns `files: GitCommitFile[]` with per-file `additions`/`deletions`. The card derives `files = detail.files.length`, `+adds = Σ additions`, `−dels = Σ deletions` via `useQuery({ queryKey: ["git","commit",boardKey,sha] })` — the SAME queryKey/endpoint `TicketCommits`' `CommitFiles` already uses, so it's cache-shared and free when the diff pane later opens. SHA/summary/ticket render instantly; the stat row shows a skeleton while detail loads and is HIDDEN on error (graceful degrade, `retry:false`).
- F8 React Flow is themed via a scoped `.react-flow --xy-*` token block, not per-component dark: [PH-177] — `@xyflow/react` exposes its palette through `--xy-*-default` CSS vars under the `.react-flow` root; F8 adds ONE scoped block in `index.css` mapping each to an F1 token (`--xy-node-background-color-default: var(--bg-surface)`, `--xy-edge-stroke-default: var(--hairline-strong)`, `--xy-node-boxshadow-selected-default: var(--glow-cyan)`, etc.). Because CSS vars cascade and SVG/inline styles resolve them at paint, `html.light` redefining the F1 tokens repaints the whole graph with ZERO JS theme logic and zero per-component `dark:`. The custom node's inline styles + `state.color` data-driven fallback are the resilient backup for node bg/border. Caveat (gotcha): the custom node must NOT hard-set its own `box-shadow` or it overrides `--xy-node-boxshadow-selected` and the cyan selection glow never shows — F8 dropped the node's `shadow-sm` class for exactly this reason.
- F8 destructive buttons use inline `background: var(--danger)`, not a `.btn-danger` class [PH-177] — F1 ships no danger button class (cyan is the only accent fill class). Rather than invent one, the delete/detach/remove confirm buttons keep the `.btn` base for shape + `text-text-on-accent` and set `style={{ backgroundColor: "var(--danger)" }}` inline (same one-off escape-hatch pattern as the glass `color-mix`). Red stays reserved for destructive actions per the design system; `text-text-on-accent` (dark ink) reads on the red fill in both themes.
- F8 PermissionMatrix sticky cells need an OPAQUE token bg [PH-177] — the matrix header row and first column are `position: sticky`; if they used a translucent `-soft` token the scrolled content bleeds through. They keep `bg-raised` (header) / `bg-surface` (first col) — the documented opaque surfaces — preserving the `sticky left-0 z-10` semantics. Checkbox cells use the Tailwind `accent-[var(--accent)]` form-control utility (the single net-new helper) so a checked box renders cyan; orphan/disabled cells use `accent-[var(--text-muted)]` + `opacity-40`.
- F8 role chips use a static `text-role-*` map (Recipe C), never a templated class [PH-177] — `MembershipRow` maps the member role (and `_dev` agent_role_hint variants) to a literal `text-role-admin|pm|architect|backend|frontend|reviewer|qa|orchestrator` via a hardcoded `ROLE_TEXT` record (mirrors TicketDetail ROLE_META). A templated `text-role-${role}` would be purged by Tailwind's JIT → invisible chips. Same map could feed PermissionMatrix column headers later.
- F6 lane colors are CSS-var reference strings, not hex or a JS theme branch [PH-175] — `branchGraphLayout.ts LANE_COLORS` holds `"var(--lane-cyan)"…` strings; `laneColor(lane)` returns them verbatim and every call site feeds them straight into SVG `stroke`/`fill` or inline `style`. SVG and inline styles resolve CSS custom properties at paint, so a single string repaints under `html.light` (which redefines `--lane-*`) — theme-aware lanes for free, zero JS theme logic, `laneColor` signature frozen. Order is kit-aligned (cyan=lane 0=accent=default branch). The two-pass `assignLanes` algorithm + `LANE_W`/`ROW_H` are byte-identical (visual-only ticket).
- F6 selection vs new-commit are distinct, motion-safe treatments [PH-175] — selected commit row = static `bg-accent-soft` + left cyan hairline (`border-l-2 border-hairline-cyan`) and a hollow dot (`fill="var(--bg-base)"` + lane-colored ring); new-commit row (in `highlightedShas`) = one-shot `.animate-glowin` (`@keyframes ph-glowin` accent-soft→transparent, added to `index.css @layer utilities`) + dot `drop-shadow(var(--accent))`. The two never visually collide (static tint+hairline vs transient fade). The global `prefers-reduced-motion` block in `index.css` caps the animation duration for free — no per-component media query — and the static selection stays visible regardless of motion pref. Adding one keyframe utility to `index.css` is the only non-component edit and is purely additive (no token block touched).
- F7 ticket-detail restyled to Cyan-on-Black; MermaidBlock made theme-aware [PH-176] — `pages/TicketDetail.tsx` + `components/FieldEditor.tsx` + `MarkdownFieldEditor.tsx` + `MarkdownRenderer.tsx` + `MermaidBlock.tsx` swapped every slate/indigo/`*-NNN`/`bg-white`/`dark:` utility for F1 semantic tokens (visual-only — all `useQuery`/mutation/`api.transitionTicket` calls, `allowedTransitions` derivation, react-markdown+remark-gfm parsing, and the MermaidBlock preprocessor pipeline are byte-identical). Two real interaction/logic changes beyond class swaps: **(1) the flat State transition list became a "Move to →" popover** — a `btn-secondary` trigger (`aria-haspopup="menu"`/`aria-expanded`, ChevronDown) toggling a `bg-raised` + `border-hairline-cyan` menu (NOT `bg-overlay`, per PH-171) with `role="menu"`/`role="menuitem"` items: a per-state dot (color from `state.color` hex or the `var(--state-<to>)` token), the mono target name, and a `text-warning` "req fields" hint when the transition has `field_gates.required_fields`; closes on Escape + click-outside (document `mousedown`/`keydown` listeners in a `moveMenuOpen`-gated effect); `transitionMutation.mutate(to)` + the success/`field_gate_not_met`/`invalid_transition` banners (retoned to `danger`/`warning`) are unchanged. **(2) MermaidBlock is now theme-aware**: the one-time module-level `mermaid.initialize` was removed; the component reads `useTheme()` and re-initializes mermaid (`theme: light?'base':'dark'` substrate + token-derived `themeVariables`) INSIDE the render effect with `theme` in the dep array, so a live theme flip re-runs the effect and re-renders the SAME diagram into the other palette (fresh `renderId` per effect keeps the StrictMode race fix intact; parsing untouched). Other surface details: header mono cyan key + TypeChip slate-fallback FIX (`bg-raised text-text-secondary`); Activity tabs (All│Comments│History│Git) + the edit/preview sub-tabs got an animated accent underline (`<span bg-accent shadow-glow-cyan-sm>` under the active `role="tab"` `aria-selected` tab); sidebar reporter/assignee render as avatar-initial + mono name + `text-role-<role>` chip (static class map, JIT-safe); branch row `bg-accent-soft`+`border-hairline-cyan` mono; both modals (branch-diff, delete) became glass (inline `var(--bg-overlay)`+`blur(4px)` scrim, `.card`+`shadow-glass` panel, delete confirm `var(--danger)` fill). Both themes browser-verified (Claude Preview, query-cache-seeded harness — stale env token 403s, so the real components were mounted via `setQueryData` mock data, harness not committed): dark node fill `#161D29`/stroke `#22D3EE`, light node fill `#EEF2F7`/stroke `#0891B2` — the mermaid SVG re-renders with a new id and recolors on toggle; Move-to menu opens with 3 items incl. the "req fields" hint, Escape closes; typecheck+build green; 5-file grep `slate-/indigo-/*-NNN/dark:` = zero.
- CSS variables are the single source of truth; Tailwind utilities are thin `var()` aliases [PH-170] — F1 token foundation. The skill ships tokens twice (CSS vars in `colors_and_type.css` AND baked hexes in `tailwind.config.snippet.js`) and the two already disagreed; copying the snippet's hexes would freeze that drift and force every utility to grow a `dark:` twin. Resolution: copy ONLY the CSS file's `:root`+`html.light` blocks into `index.css`, and map every `theme.extend` color to its var (`surface: "var(--bg-surface)"`). One source of truth, zero hex drift, and `html.light` repaints `bg-surface` automatically so F2–F8 delete `dark:` twins instead of rewriting them.
- Theme convention flipped to dark-default + `.light` override [PH-170] — `ThemeProvider` previously toggled `.dark` with a light default; the design system is dark-`:root` + `html.light`. `getInitialTheme()` now defaults to `"dark"` and only opts into light on `prefers-color-scheme: light`; the `useEffect` adds/removes `.light`. localStorage key (`"theme"`), context API, and `ThemeToggle` UX are unchanged. `darkMode: ["selector", "html:not(.light)"]` keeps any residual `dark:` utility resolving as a last-resort escape hatch.
- `text-text-primary` double-prefix is intentional (token→utility contract for F2–F8) [PH-170] — the color key is `text` (with shades `primary`/`secondary`/`muted`/`on-accent`), so the text-COLOR utility is `text-` + `text` + `-primary` = `text-text-primary`. Matches the skill snippet; do NOT rename. Prefer the pre-alpha'd `*-soft`/`*-subtle` utilities (`bg-accent-soft`) over Tailwind `/NN` opacity — hex/rgba var tokens can't compose with `<alpha-value>`; one-offs use inline `color-mix(in srgb, var(--token) N%, transparent)`.
- Base `@layer components` classes (`.btn*/.input/.card/.badge/.eyebrow/.mono`) use raw `var(--token)` CSS, not `@apply` of token utilities [PH-170] — see Known gotchas: Tailwind's `@apply` cannot resolve var-backed COLOR/RADIUS utilities (e.g. `@apply bg-accent-strong` / `@apply rounded-pill`) under Vite's PostCSS pass. The token utilities themselves still work fine in JSX (verified via JIT compile); only the `@apply` path inside component classes was the problem.
- F2 app shell migrated to Cyan-on-Black token utilities; LiveStatus pill added (presentational) [PH-171] — `Layout.tsx`/`NotificationBell.tsx` swapped every slate/indigo/`bg-white`/`bg-red-500`/`bg-blue-*` + `dark:` twin for F1 semantic utilities: top bar `bg-surface` + `border-hairline` (sticky, `z-10→z-40`), wordmark split into `text-text-primary` "Project" + `text-accent` "Hub", active nav `text-accent bg-accent-soft`, notification panel `bg-raised` + `border-hairline`, unread row `bg-accent-subtle`, timestamps `.mono`. `ThemeToggle.tsx`/`RequireAuth.tsx` verified zero-diff (already F1-clean). New `components/LiveStatus.tsx` is prop-driven (`live`/`connecting`/`off` → `*-soft` semantic tone + `motion-safe:animate-pulse` dot, `role="status" aria-live="polite"`). Bell gains `aria-expanded`/`aria-haspopup="menu"`. Both themes browser-verified (badge `bg-danger`+`ring-surface`, panel opaque, theme persistence).
- Notification dropdown uses `bg-raised`, NOT `bg-overlay` [PH-171] — the cheat-sheet lists `bg-overlay` for popovers loosely, but `--bg-overlay` is the **translucent modal scrim** (`rgba(5,7,10,.72)` dark / `rgba(15,23,42,.32)` light); using it on the dropdown made content bleed through. `--bg-raised` is the documented opaque "popovers / hovered rows / menus" surface and matches the kit's `NotificationPanel` reference (`color-mix(bg-raised 96%)`). Menus/popovers → `bg-raised`; only the dimming layer behind a centered modal → `bg-overlay`.
- F4 boards-list surface restyled to Cyan-on-Black token cards [PH-173] — `pages/Boards.tsx` is self-contained (inline card markup, no sub-component); the `useQuery(['boards'], api.listBoards)` block + `Link to={/boards/${board.key}}` routing + data shape stay **byte-identical** (visual-only). Each card keeps the flat `.card` base (`bg-surface` + `border-hairline` + `radius-lg` 14px) and layers a **mono cyan KEY badge** (`mono text-2xs font-semibold text-accent bg-accent-soft border border-hairline-cyan rounded-sm` — the only cyan fill on a resting card; overrides the indigo Stitch screenshot per epic non-negotiables, kit `.board-key` is authoritative), name `text-text-primary`, `line-clamp-2` description `text-text-secondary`, and bottom meta chips (`project_type`, `N states`) as grayscale `bg-raised`+`border-hairline rounded-pill mono` pills. **Two-tier hover** (architect Decision 2): Tier A = pure F1 utilities `group hover:border-hairline-cyan hover:[transform:translateY(-2px)] hover:shadow-md transition-all duration-base ease-out` + arrow `group-hover:text-accent group-hover:[transform:translateX(2px)]` (the required floor); Tier B = optional border-beam `<span aria-hidden>` sibling with inline `mask-composite:exclude` cyan gradient (`group-hover:opacity-100`, `motion-reduce:hidden`) — no pure utility exists for mask-composite so the inline-style escape hatch is used (PH-172 precedent). Resting card carries NO cyan glow flood. Loading→`text-text-muted` (`role="status"`), error→`text-danger` + inline `var(--danger-soft)` (`role="alert"`, mirrors Login), empty→`.card text-text-muted` + `.mono bg-inset` code chip. a11y: card is a real `<a href>` (keyboard-focusable, global cyan `:focus-visible` box-shadow ring renders outside the border-box so `overflow-hidden`—needed to clip the beam—does NOT clip it); arrow `aria-hidden`; lift+beam respect `motion-reduce` + global `prefers-reduced-motion`. **Transform utilities use arbitrary-value `[transform:translateY/X(...)]` not `-translate-y-*`/`translate-x-*`** to avoid the AC grep `slate-` false-positive on `tran**slate-**` (camelCase `translateY` has no `slate-` substring). Both themes browser-verified (dark KEY `#22D3EE`/`accent-soft .12`, light KEY `#0891B2`/`.10`; card `#0C1119` dark / `#fff` light), chips grayscale `text-secondary`, 0 console errors. AC grep clean (exit 1).
- F5 kanban surface restyled to Cyan-on-Black [PH-174] — `pages/BoardDetail.tsx` column markup + `components/TicketCard.tsx` + `components/NewTicketDialog.tsx` swapped every slate/indigo/`*-100`/`dark:` utility for F1 semantic tokens (visual-only; WS live-update, `<Link>`-to-detail navigation, and the create-ticket mutation are byte-identical). Columns: UPPERCASE eyebrow (`text-2xs font-semibold uppercase tracking-wide`) + a `bg-current` state dot (inherits the per-state color from the **reused** `resolveStateColor(state)` → inline `tone.style` hex or `text-state-*` fallback class — no hardcoded `--state-*`) + a mono `bg-raised/border-hairline/rounded-pill` count chip; column body `bg-surface` + `border-hairline ring-hairline`, dashed `border-hairline` empty-state. TicketCard sits on the flat `.card` with a token hover affordance (`hover:border-hairline-cyan motion-safe:hover:shadow-glow-cyan-sm`), live-update highlight `ring-accent border-accent bg-accent-soft`, mono key `text-text-muted`, and an **agent-phase cyan pulse** (`bg-accent-soft text-accent ring-1 ring-hairline-cyan rounded-md font-mono` + `Activity animate-pulse`, auto-muted by the F1 reduced-motion global) — cyan is reserved (key/phase/highlight/focus/active-tab/primary button only). **Deferred F1 nit fixed**: TYPE_BADGE fallback `bg-slate-100 dark:bg-slate-700` → `bg-raised text-text-secondary`. NewTicketDialog is a glass modal mirroring the PH-172 Login precedent: inline `color-mix(in srgb, var(--bg-surface) 94%, transparent)` + `backdropFilter/WebkitBackdropFilter blur(12px)` + `borderColor var(--hairline-cyan)` + `boxShadow var(--shadow-glass)` on a `border rounded-lg` form (NOT `.card`), scrim `style={{background:'var(--bg-overlay)'}}`, `.input`/`.btn-*` controls; form state + mutation untouched. **Hover lift uses `shadow-glow-cyan-sm` not `-translate-y-px`** to keep the AC `slate-` grep clean (`tran**slate**-y-px` matches the banned substring; same trap PH-173 hit). No dnd added — the F5 board has none (`@dnd-kit` only in `WorkflowStateList.tsx`/F8). No `index.css`/`tailwind.config.js`/`lib/utils.ts`/`lib/stateColor.ts` edits (F1 contract consumed read-only). Both themes browser-verified (Claude Preview, mock-data fetch-interceptor harness): dark `--accent #22D3EE` / card `#0C1119` / in_progress column `warning-soft`; light `--accent #0891B2` / card `#FFFFFF` / body `#F6F8FB`; glass modal both themes (card bg color-mix .94, blur 12px, hairline-cyan border, shadow-glass, scrim bg-overlay); scrim-click close works; 0 console errors; typecheck+build green; 3-file grep `slate-/indigo-/blue-/green-/red-/yellow-/gray-/dark:` = zero.
- F3 login surface restyled to Cyan-on-Black glass card [PH-172] — `pages/Login.tsx` was the last slate/indigo/`red-*`+`dark:` surface; swapped every utility for F1 semantic tokens (page `bg-base`, wordmark split `text-text-primary` "Project" + `text-accent` "Hub", label/subtitle `text-text-*`, dev divider `border-hairline`, dev chips `bg-raised`→`hover:bg-accent-soft hover:text-accent`, code chip `bg-inset`). The centered card is a **glass panel via inline `style`**, NOT the flat `.card` class: `background: color-mix(in srgb, var(--bg-surface) 94%, transparent)` + `backdropFilter: blur(12px)` (+`Webkit`) + `borderColor: var(--hairline-cyan)` + `boxShadow: var(--shadow-glass)`, structural utils as classes (`w-full max-w-sm rounded-lg border p-6`). Inline `color-mix`/`backdrop-filter` is the kit.css-sanctioned one-off path — `@apply` of var-backed utilities is forbidden (PH-170 @apply-500 gotcha) and `bg-overlay` is the scrim not a card fill (PH-171). Error box uses `text-danger` + inline `var(--danger-soft)` tint, keeps `role="alert"` + adds `aria-live="polite"`; token input keeps `.input font-mono` and gains `aria-label="Bearer token"` + `autoComplete="off"`. Optional faint `radial-gradient(... var(--accent-subtle) ...)` spotlight behind the card (aria-hidden). `.input`/`.btn-primary` base classes unchanged (already token-backed, cyan focus ring + glow). **Visual-only**: the dev auto-login `useEffect`, `devTokens` reads, `verifyToken(token.trim())`, `setToken`+`navigate("/")`, and empty-token disabled state are byte-identical (no logic line touched). Both themes browser-verified live (dark canvas `#05070A`, light `#F6F8FB`; glass bg `.94` alpha, cyan hairline `0.18`/`0.24`, error `--danger`/`--danger-soft`), 0 console errors.
- ESLint 9 flat config added — `npm run lint` works again [PH-165] — the project ran ESLint 9 with NO config (neither flat nor `.eslintrc.*`) and a `--ext`-flag lint script that ESLint 9 rejects, so `npm run lint` was fully broken since G8. Added `frontend/eslint.config.js` (flat) using only packages already present as (transitive) devDeps — `@eslint/js`, `globals`, `@typescript-eslint/parser`, `@typescript-eslint/eslint-plugin` — and changed the script to `eslint .`. NO new dependency. Type-aware rules are intentionally OFF (no project service) to keep lint fast and decoupled from tsconfig; `tsc --noEmit` remains the type gate. `react-hooks/exhaustive-deps` (plugin not installed, but referenced by 2 inline disable directives) is handled by a local no-op stub plugin + `reportUnusedDisableDirectives: "off"` so the directives stay forward-compatible without erroring. `npm run lint` now exits 0 across all of `src/`.
- Dead code from the PH-167 rework deleted [PH-165] — `CommitNode.tsx`, `BranchLegend.tsx`, `BranchPanel.tsx`, and `DiffDemo.tsx` were orphaned by the PH-167 SourceTree rework (only re-exported from the `components/git` barrel / mentioned in an `App.tsx` comment, never rendered). Verified unreferenced via grep, then deleted. `CommitNode`/`BranchLegend` carried the last `@xyflow/react` import in the git module (workflow editor still uses xyflow legitimately). The `components/git/index.ts` barrel was trimmed to the two live exports (`BranchGraph`, `TicketCommits`); its unused `branchGraphLayout` re-exports were dropped (consumers import that module directly). Supersedes the PH-167 "kept on disk to avoid touching the barrel" decision. tsc + lint clean after removal.
- Incidental unused-binding cleanup to reach lint-0 [PH-165] — making `npm run lint` green surfaced a handful of pre-existing dead bindings unrelated to git: unused imports `useEffect`/`useQuery` (`BoardSettingsDialog.tsx`), `flushSync` (`WorkflowEditor.tsx`, only in a comment), dead `pongMessage` local + orphaned `PongMessage` interface (`useWebSocket.ts`), unused `states` local + `WorkflowState` import (`TicketDetail.tsx`). All removed — pure dead-code deletion, zero behavior change, tsc clean.
- ph-159 / ph-160 e2e specs rewritten to the PH-167 UI + selector-hardened [PH-165] — both specs targeted the retired xyflow / `BranchPanel` DOM (`.react-flow*`, `aside[aria-label="Branch legend"|"Branch detail panel"]`) and were failing; ph-160's `getByText("Live")` also hit a strict-mode multi-match because live PH ticket titles contain the word "Live". Rewrote both against the current SourceTree DOM (`role="list"[aria-label="Commit history"]`, `role="listitem"` rows, `aside[aria-label="Branch list"|"Commit diff panel"]`) and hardened every locator: role-scoped queries, `.first()`/clamped `.nth()` on any multi-match locator, `count > 0` instead of exact counts, and the WS badge scoped via `[title="Live updates active"]` instead of `getByText("Live")`. Detailed SourceTree ACs stay in `ph-167-branch-graph-rework.spec.ts`; ph-159/ph-160 are now the G10/G11 regression nets. 22/22 pass.
- SourceTree vertical list replaces `@xyflow/react` node graph [PH-167] — user rejected the G10 node-card canvas as untraceable. Decision: drop the graph-canvas paradigm entirely for the commit view and render a dense newest-first vertical list with a custom per-row SVG lane gutter (like SourceTree / `git log --graph`). Rationale: linear top-to-bottom reading + a single continuous gutter column is far easier to follow than a 2D node graph; also removes runtime dependence on xyflow for this view (workflow editor still uses xyflow). `CommitNode.tsx` + `BranchLegend.tsx` are no longer rendered by `BranchGraph` but kept on disk (xyflow node + legend) to avoid touching the barrel/other importers in the same PR.
- Lane algorithm REUSED, only the renderer swapped [PH-167] — `branchGraphLayout.ts assignLanes()` (two-pass O(N+E)) is unchanged; PH-167 moved rendering from xyflow `nodes/edges` to inline per-row `<svg>` gutter cells. Keeping the proven lane math avoids re-deriving branch/merge placement and keeps the diff small + low-risk.
- Per-row inline SVG gutter (not one big SVG overlay) [PH-167] — each commit row owns a small `<svg aria-hidden height=ROW_H>` that draws only its own dot + the lane lines passing through that row. Chosen over a single absolutely-positioned full-height SVG so the gutter scrolls/virtualizes naturally with the row list and stays aligned with `ROW_H` without manual y-offset bookkeeping. "Active lanes per row" derived from lane first/last extents so pass-through lines render only where a lane is continuous. **[SUPERSEDED by PH-179 — the per-row `<svg>` GutterCell was deleted; lanes are now ONE continuous full-height bezier overlay via `computeLanePaths`. The lane-extent (first/last) logic was carried over into the emitter; the overlay still lives inside the scroll container so it scrolls with the rows.]**
- Commit→diff via existing G9 DiffViewer in a 3rd pane [PH-167] — clicking a row opens `<DiffViewer fetch={{ kind:"commit", boardKey, sha }}>` in a conditional right `<aside>`. Reuses the G9 fetch-mode component verbatim (no new diff code) and resolves the "git diffler görünmüyor" complaint by surfacing file/line changes directly in the graph view (previously diffs were only reachable from TicketDetail / the dev demo).
- Branch selection + commit selection moved INTERNAL to BranchGraph [PH-167] — reverses the G10/G11 callback-lift pattern. Because the diff pane + branch filter now live inside `BranchGraph`, `BoardDetail` no longer needs `selectedBranch`/`selectedCommit` state or `onCommitSelect`/`onBranchSelect` handlers. The props stay optional in `BranchGraphProps` for external reuse but are unused at the `BoardDetail` call site. Net: less prop plumbing, single source of truth for view state.
- `/dev/diff-demo` link + route removed (page kept) [PH-167] — AC4 removes the demo nav link from `Layout.tsx` AND the route from `App.tsx`. The `DiffDemo.tsx` file and the `DiffViewer` component are preserved on disk; with no route registered the demo is simply unreachable in the UI. Supersedes the PH-158 decision that kept `/dev/diff-demo` as a public route.
- G14 tab button `id` attributes for `aria-labelledby` [PH-163] — `BoardDetail.tsx` tab buttons lacked `id="tab-kanban"` and `id="tab-graph"`. The tabpanels already had `aria-labelledby="tab-kanban"/"tab-graph"` but the target elements did not exist, breaking screen-reader tab association. Fixed: `id` attributes added to both tab buttons. Zero visual change.
- G14 `BranchPanel` `aria-controls` render-gated [PH-163] — when `diffOpen=false`, the diff toggle button had `aria-controls={diffRegionId}` pointing to an unmounted `<div id>`. Fix: `aria-controls` removed from the collapsed button (the target `<div id>` does not exist in DOM when closed). The expanded button (inside the mounted `<div>`) retains `aria-controls`. `aria-expanded` kept on both buttons for state signal.
- G14 `BranchLegend` lane color offset fix [PH-163] — previous formula `sorted.findIndex(!is_default && name==branch) + (hasDefault ? 1 : 0)` double-shifted non-default lane indices: `findIndex` on the full sorted array returns the absolute position (already includes default at 0), then adding 1 gave an off-by-one. Fix: use `idx` directly — `sorted` puts default first (idx=0), non-default follow (idx=1,2,...), matching lane indices exactly.
- G14 `GitGraphResponse.tags` widened to `unknown[]` [PH-163] — `never[]` prevented runtime iteration over the always-empty array without a type error. Widened to `unknown[]` + JSDoc TODO for G15+ `GitTag` interface. tsc clean.
- G13 `api.git.refresh` hybrid Bearer/shared-secret auth [PH-162] — G8 original required `refreshToken` string; G13 extends to `opts?: { refreshToken?: string; useBearer?: boolean }`. Bearer path (UI admin) vs shared-secret path (git hooks) coexist. Smoke test updated. `api.git.refresh` gotcha in Known gotchas updated accordingly.
- Rotate secret one-shot modal pattern [PH-162] — plaintext cleared from `useState` on close, not sessionStorage (no XSS risk). Subsequent "Rotate" opens a fresh modal/API call. Pattern: confirm warning → mutate → show → close clears.
- Config form 422 inline errors from Pydantic ValidationError list [PH-162] — Pydantic `detail` may be a list of `{ loc, msg }` objects; RepositoryConfigForm maps `loc[-1]` to field name. Unrecognized fields fall back to global error banner.
- DiffViewer fetch.path optional propagation [PH-161] — G9 contract'a backward-compatible ekleme; per-file commit diff için TicketCommits kullanımı. Mevcut callers (DiffDemo) path geçmediği için queryKey sadece `path ?? null` ekler — backward compat.
- Modal pattern branch range diff için reuse [PH-161] — delete modal'ın kardeşi; generic Modal G14'e ertelendi. Sidebar dar olduğu için full-width modal tercih edildi.
- Git tab count = commits.length (not commits + history badges) [PH-161] — bir commit hem rich TicketCommits row hem GitEventBadge üretir; iki farklı count double-count confusion yaratır. History badges "Git olayları" başlığı altında ayrı feed olarak gösterilir.
- BoardResponse.repository field added to frontend types [PH-161] — backend schemas.py:274 zaten `repository: RepositorySummary | None = None` içeriyor ama frontend api.ts eksikti. Branch chip disable logic ve modal için gerekli. Field optional değil null (backend contract match).
- BranchGraph callback-based sync (controlled prop refactor G14'e ertelendi) [PH-160] — minimal değişiklik için BranchGraph kendi `selectedBranch` state'ini korur, parent (BoardDetail) ayna state tutar; tek source of truth çatışması yok çünkü callback chain tek yönlü. Controlled prop refactor (BranchGraph'ın `selectedBranch`'ı parent'tan alması) G14 scope'unda.
- 3-column layout (Legend | Canvas | Panel) [PH-160] — drawer yerine sticky 3-kolon SourceTree paritesi sağlar; mobile için flex-col fallback (<1024px). Drawer overlay graph'ı kapat maz; side-by-side canli keşif için daha iyi UX.
- Hand-written git types in dedicated module (not openapi-typescript regen) [PH-157] — preserves `api.ts` convention (string ISO datetime, explicit `null` literals, no `undefined`); openapi-typescript devDep present but no `generate:types` script; adding generator pipeline (script + CI + dual source-of-truth) is scope creep for G8. Mitigation: every interface has `@see schemas.py:<ClassName>` JSDoc; reviewer does line-by-line cross-check. openapi-typescript regen path documented as follow-up after G14.
- `api.git.*` nested namespace (not flat `api.*`) [PH-157] — 10 git methods would bloat the flat `api` object and reduce discoverability; nested namespace keeps git surface self-contained while maintaining the same `request<T>` + `ApiRequestError` contract. `api.setRepository`/`api.detachRepository` remain flat to mirror `api.updateBoard` (admin board-level symmetry).
- `api.git.refresh` uses direct `fetch`, not `request<T>` [PH-157] — the `POST /git/refresh` endpoint uses shared-secret auth (`X-Git-Refresh-Token` header), NOT Bearer token. Using `request<T>` would attach the Bearer header (incorrect); direct fetch avoids this while still wrapping error responses in `ApiRequestError` for consistency.
- `WebSocketMessage` exported from `useWebSocket.ts` [PH-157] — previously unexported (internal to hook); exported now so `isGitSyncedMessage()` type predicate can be used by G9+ consumers without duplicating the interface. Zero behavior change.
- Removed env-specific token fingerprint heuristic from WebSocket reconnect handler + 2 sibling sites [PH-148] — close codes are the source of truth, not token-prefix string match. `useWebSocket.ts:319-324` 1c7f53fb branch deleted; `utils/auth-fix.ts` deleted (dead code, zero importers); `pages/Login.tsx` DEV_TOKENS literal map replaced with `VITE_DEV_TOKEN_<role>` env-var reads from `.env.development.local` (gitignored, `.env.development.example` committed). Production bundle verified clean of fingerprint.
- Custom parser, no diff library [PH-158] — `diff2html` (~80 KB min+gz) and `react-diff-viewer` are over-engineering for inline unified diff display; backend produces clean `git diff -p --no-ext-diff` output with binary→null and byte-cap already handled; custom ~150 LOC parser covers 100 % of AC edge cases; parser module is isolated (`src/lib/diff/`) and independently testable; if word-level diff is ever needed, swap is low-cost.
- Container/presentational split for DiffViewer [PH-158] — `DiffViewer` (fetch + state) and `FileDiffView` (purely presentational, no queries) are separate files; G11/G12 consumers may use `FileDiffView` directly for already-fetched `FileDiff[]` while `DiffViewer` handles TanStack Query loading/error/cache lifecycle; testability: `FileDiffView` renders deterministically from props with no async code path.
- 2-mode API (`files` vs `fetch` prop) [PH-158] — TypeScript discriminated union `DiffViewerDataProps | DiffViewerFetchProps` enforces mutual exclusion at type-level; G11 (range diff, whole-page data) uses data mode; G12 (per-commit lazy fetch) uses fetch mode; callers cannot accidentally mix both.
- `collapseThreshold` prop (default 50) [PH-158] — large hunks (>50 lines) collapse by default, enabling lazy DOM rendering and avoiding visual overload for wide diffs; G11/G12 may override this if they want a higher threshold; threshold is a prop (not a constant) so no fork needed.
- `/dev/diff-demo` outside RequireAuth [PH-158] — moved to public route so QA/Reviewer can verify rendering without a live auth token; route only shows hardcoded samples (no sensitive data); live fetch form in section 3 still requires a valid board+sha; may be gated behind `import.meta.env.DEV` or removed in G14.
- G10 BranchGraph reuses `@xyflow/react` instead of `@gitgraph/react` or `mermaid` gitGraph [PH-159] — bundle parity (zero new deps) + full click/select/live-update + Tailwind dark-mode composability; xyflow v12 viewport culling handles 200+ nodes without extra virtualization; custom lane assignment is ~150 LOC pure function.
- Initial documentation [bootstrap] — auto-generated by jarwis-init bootstrap flow

## Known gotchas

- Global search hits navigate by board KEY, and the SpaceGraph seam wants a `"tag:"`-PREFIXED id — two off-by-namespace traps [PH-278]. (1) `TicketSearchHit.board` is the board KEY (e.g. "PH"/"KIM"), NOT `board_id` — the cross-board detail route is `/boards/:boardKey/tickets/:ticketKey`, so a hit clicked from board A correctly opens board B's ticket. Using `board_id` for the URL silently 404s the route. (2) The PH-277 SpaceGraph controlled seam keys on a TYPE-PREFIXED node id (`selectedTagId = "tag:" + tag.id`), while the `searchFilter` store + search hits hold the BARE tag UUID — feed the seam WITH the prefix (`activeSelectedTagId`) and STRIP it when reading `onSelectTag` back (`stripTagPrefix`), or chips ⇄ graph desync. (3) `searchAll` (`api/client.ts`, codemap-mapped) shares the same un-propagated-`tag.read` 403 caveat as `getConceptGraph` — a live 403 is an ENV caveat, not a defect; verify with a `/api/search` stub fixture (do NOT run `update_board_roles`). The seam is SINGLE-id by design → v1 highlights only the most-recently-selected chip; multi-tag graph narrowing is PH-279.
- Concept-tag `tag.*` caps are NOT propagated to the live PH board until `update_board_roles` runs → `GET /concept-tags` + attach/detach 403 in dev [PH-276] — the three new caps (`tag.read`/`tag.assign`/`tag.manage`) live in `backend/app/services/defaults.py` but the live board's roles JSON is only refreshed by `python -m app.cli update_board_roles` (CLAUDE.md: "bootstrap idempotent değil"), which the implementer MUST NOT run on the live DB. So in the running app the logged-in role may LACK the cap and `ConceptTagsEditor`'s list query + attach/detach return **403**. The editor handles this GRACEFULLY (inline "Bu işlem için yetkiniz yok" via `ApiRequestError(status===403)`, no crash) and — because mutations are invalidate-on-success not optimistic — NO phantom chip appears. To verify CHIP RENDERING independently of the un-propagated caps, seed a ticket's `concept_tags` (backend service / direct ORM insert of a `ConceptTag` + `TicketConceptTag` row) OR stub the ticket query; do NOT treat a live 403 as a code defect (it is an environment caveat). The `api/client.ts` `.codemap` HARD gate covers the three new client methods — keep them in `client.ts`.
- BranchGraph lane assignment must be verified on a 0-ROOT window, not just the seeded mock [PH-265] — `assignLanes`' lane-recycling path only fires when a branch's slot is freed on rejoin; the prior bug (root-only free guard) was invisible on any fixture/mock that contained a root commit or had no lane reuse, and only manifested on real time-windowed history (~0 roots → every merged branch leaks a monotonic lane → MAX_LANES=10 clamp piles commits on the last column). Guard fixtures (`branchGraphAssignLanes.test.ts`) MUST use a windowed backbone whose oldest commit still has a parent OUTSIDE the window (zero roots) AND assert `computeMaxLane` small + zero dots on `laneCx(MAX_LANES-1)`; asserting only on a small/seeded graph re-hides the bug. These node:test suites run HOST-only (Node 26, `--experimental-strip-types`); the frontend Docker container is Node 20 and errors `ERR_UNKNOWN_FILE_EXTENSION`, and there is no `test` npm script.
- Any identity-scoped TanStack query MUST include the token in its key, and the auth store MUST `queryClient.clear()` on a token CHANGE [PH-232] — `useMe()` keys `["me", token]` (NOT bare `["me"]`); a token-agnostic key + a 5-min `staleTime` will serve the PRIOR identity after an in-app login and silently mis-gate `isAdmin` (the original bug). The store's `clear()` is **clear-ALL, not surgical** — a token switch is a trust boundary, so surgical `invalidateQueries(["me"])` is WRONG (it leaves board-scoped prior-identity data in cache = cross-identity bleed). **Guard with `shouldClearCacheOnIdentityChange(prev,next)` (`prev !== next`)**: clearing on EVERY `setToken` (even no-op re-sets from Login's auto-login path / component re-mounts) causes a refetch loop / flicker — clear ONLY on a real value change. `set({token})` must run BEFORE `clear()` so the new `["me", token]` key is live when observers re-subscribe. The `QueryClient` singleton lives in `lib/queryClient.ts` (a leaf module) — do NOT import it from `main.tsx` (the store→`main.tsx` cycle: `main.tsx`→`<App/>`→store). `client.ts`'s two 401 auto-logouts route through `logout()`, so they inherit the clear FOR FREE — do not add a `clear()` call there (and avoid tripping the `client.ts` `.codemap` HARD gate).
- Editing `repos_path` back to its saved value is a NO-OP PATCH but MUST still clear a stale error [PH-230] — the General-tab path input is uncontrolled (`defaultValue`); `commitReposPath` skips the PATCH when `next === current` (avoids a redundant request on every focus loss). After a 422 the input shows the bad value; if the user retypes the ORIGINAL valid path the PATCH is skipped, so the error must be cleared at the START of the blur (it is) rather than only in the mutation's `onSuccess`. Also: "saved" is tracked via an explicit `pathSaved` useState set in `onSuccess`, NOT `updatePathMutation.isSuccess` — the `onSuccess` `invalidateQueries(['board', boardKey])` triggers a refetch+re-render that can desync a render keyed off `isSuccess`. Do NOT add a `key={repos_path}` to the input to "re-seed" it — it remounts on every save (focus loss / churn) for marginal benefit; board switches already remount via the route.
- Empty string clears `repos_path` to NULL; the `if x is not None` service guard can't express that alone [PH-230] — backend `update_board(repos_path=...)` treats `None` as "field not provided" (leave untouched) and `""`/whitespace as "clear to NULL" via `repos_path.strip() or None`. The PATCH handler validates only a NON-empty path through `repo_paths.to_container_path` (422 on relative / `..` / outside HOST_HOME); an empty path skips validation and clears. A board with no path is a VALID state (detection disabled), not an error — the UI input allows empty and the Repository tab shows `repository-no-path-hint`.
- To verify the ADMIN path-edit branch in-browser you must override the actor's PH role to `admin` [PH-230] — the live frontend_dev token renders the path field READ-ONLY (`<output>`), so the editable `<input>` + Save + inline-422 branches only appear when `useBoardRole(PH)==='admin'`. Override via `queryClient.setQueryData(['me'], …role:'admin')` on the captured client; the board PATCH itself uses `current_actor` (NOT `require_board_admin`) so frontend_dev's PATCH is NOT 403'd — the 403 branch is UI-defensive only. React synthetic `onBlur` does NOT fire from a programmatic `.blur()`/`focusout` dispatch in the Vite dev build; either steal focus with a real in-DOM `<button>.focus()` or invoke the input's `__reactProps$*.onBlur({target})` directly. RESTORE the me-cache + PH `repos_path` afterwards.
- SonarQube status query MUST use `['board', boardKey, 'sonar-setup']`, never `['board', boardKey]` [PH-226] — `SonarSetupSection` keys its status query with a dedicated 3-segment key. If you key it `['board', boardKey]` it clobbers/duplicates BoardDetail's `BoardResponse` query (same key, different shape → cache thrash / wrong-shape reads). Conversely, Sync's `onSuccess` DOES invalidate `['board', boardKey]` (to refetch `BoardResponse.health` so the board-detail `SonarHealthPanel` tile refreshes) PLUS the `['board', boardKey, 'sonar-issues', ...]` predicate family — mirror BoardDetail.tsx:263-277 exactly; dropping either leaves the tile stale until the WS event lands. The status key stays distinct from BOTH.
- The SonarQube `scan-plan` query MUST stay `isAdmin`-gated (`enabled: … && isAdmin`) + `retry:false` [PH-237] — `GET .../sonarqube/scan-plan` is admin-only (unlike member-level `GET .../status`). `SonarSetupSection`'s scan-plan `useQuery` is `enabled: Boolean(boardKey) && enabled && isAdmin`; drop the `isAdmin` clause and every non-admin opening the SonarQube tab fires a guaranteed-403 (a retry storm without `retry:false`). The "Scan now" button is already hidden for non-admins (Actions card is `{isAdmin && …}`), but the QUERY is the easier thing to leave ungated — keep both gated. Verified by the spec asserting the scan-plan request NEVER fires under a mocked non-admin `/me`.
- `SonarScanResult`'s field is `scan_status` (snake), NOT `status` — do not copy `SonarSetupStatus.status` [PH-237] — `request<T>` does NO key remapping. The scan response wire key is `scan_status`; naming the TS interface field `status` (as the sibling `SonarSetupStatus` legitimately does for ITS own `status` wire field) makes every `switch (result.scan_status)` branch read `undefined` and silently fall through → no message renders. Match the JSON field name 1:1.
- The dev/frontend_dev token 403s every SonarQube WRITE (setup/sync) on the live PH board [PH-226] — `GET .../sonarqube/status` is member-level (the dev token CAN read → the status panel always renders), but `POST .../setup` and `POST .../sync` go through `require_board_admin` → `403 {required: 'board.admin'}`. The UI hides the buttons for `!isAdmin` (`useBoardRole==='admin'`), AND both mutations' `onError` catch `ApiRequestError.status===403` → inline `role=alert` "Admin role required to run this action." (never crash). To exercise the WRITE buttons in-browser you must temporarily set the actor's PH membership `role='admin'` (so `isAdmin` flips true and the buttons render) — but the backend STILL 403s (`require_board_admin` resolves the path `{board_id}='PH'` as a UUID and fails the membership-role lookup), which is precisely how you verify the graceful-403 branch. RESTORE the membership to `frontend_dev` afterwards (board-admin membership ≠ the UI `role`, the PH-224/225 trap). The happy-path admin write (configured=true after a real setup) is covered by the PH-223 backend tests.
- Manual "Add repository" MUST POST the collection, not PUT the singular [PH-225] — `RepositoryConfigForm`'s default `mode='edit-primary'` submits the singular `api.setRepository` PUT, which UPSERTS the board's PRIMARY repo. If the Add panel reuses the form WITHOUT passing `mode='add'`, a "manual add" silently OVERWRITES the primary instead of creating a 2nd repo. The Add panel's Manual tab passes `mode='add'` explicitly → `api.git.addRepository` (POST `/repositories`). Keep that prop wired; the default stays PUT for the legacy single-repo edit path.
- The dev/frontend_dev token 403s every repository WRITE under the live PH board [PH-225] — list + detect are member-auth (the dev token CAN read), but add/remove/set-primary are admin-auth → `403 {required: 'board.admin'}`. The UI hides write actions for `!isAdmin` (primary defence) AND every write `onError` catches `status===403` → inline `admin yetkisi gerekli` (defensive — never crash). To exercise the WRITE paths in-browser you must seed the 2nd repo's DB row server-side (`add_repository` service) and/or temporarily grant the actor `admin` on PH — and RESTORE both afterwards (board-admin membership ≠ the UI `role`, the PH-224 trap). Detect's `already_linked` is computed from the live DB rows, so seeding/removing a repo flips a candidate between greyed-disabled and addable.
- `role="list"` on `<ul>` is LOAD-BEARING under Tailwind Preflight — do NOT "fix" SonarQube S6822 by removing it [PH-213] — Tailwind's base reset sets `list-style: none` on every `ul`/`ol`, and Safari + VoiceOver DROP the implicit `list` role when `list-style:none` is applied. So the explicit `role="list"` on `TicketCommits`'s file/commit `<ul>`s (and similar) is an intentional a11y compensation, NOT redundancy — removing it regresses screen-reader list semantics. SonarQube `typescript:S6822` flags it as "redundant explicit role"; this is a known false-positive for Tailwind-Preflight codebases and is left-as-is (wontfix). By contrast `role="complementary"` on `<aside>` IS genuinely redundant (an `<aside>`'s implicit role is unaffected by CSS) and was safely removed in B3. Rule of thumb: a redundant role on a CSS-list-style-reset element is load-bearing; a redundant role on a structural landmark element (`aside`/`nav`/`main`/`header`/`footer`) is safe to drop.
- The `api/__smoke__/git.types.ts` symbol references use an array-literal `[...]`, NOT `void X` [PH-213] — this type-only smoke module (never executed) must "read" every imported symbol to prove it type-checks. The original `void _sym;` markers tripped SonarQube `typescript:S3735` (no-void). They're now a single array-literal expression statement `[_sym1, _sym2, …];` (under the file's existing `eslint-disable @typescript-eslint/no-unused-vars`). If you add a new git API/type to this smoke file, append its symbol to that array — do NOT reintroduce `void`.
- `X | (string & {})` is the idiom for "literal hints + accept any string" without collapsing to `string` [PH-213] — `types/api.ts` `SonarIssueSeverity | string` / `SonarIssuesStatus | string` collapse to plain `string` (TS absorbs the literal union into `string`), losing IDE autocomplete AND tripping SonarQube `typescript:S6571`. The fix `… | (string & {})` keeps the literal members visible for autocomplete while still structurally accepting any string. Keep the parentheses; `string & {}` is deliberate (not a typo) and does not trip the local eslint config.
- Modal click-outside dismiss is a SIBLING `<button>`, not a handler on the backdrop div [PH-212] — every overlay modal (AddMemberModal, NewTicketDialog, DetachConfirmModal, RotateSecretModal, the WorkflowList/WorkflowStateList/BoardSettings/TicketDetail confirm dialogs, SonarIssueDrawer, Edge/NodePropertyPanel) renders `<div className="fixed inset-0 ...">` → `<button type="button" className="absolute inset-0" aria-label="Close..." tabIndex={-1} onClick={onClose} />` (the dismiss surface) + the content element (`role="dialog"`/`<form role="dialog">`, `relative z-10`, NO onClick/onKeyDown). The content MUST keep `relative z-10` or the transparent backdrop button paints over it and steals clicks. Do NOT re-add `onClick={onClose}` to the backdrop div (re-introduces S6847) or `onClick={(e)=>e.stopPropagation()}` to the content (no longer needed — the button is a sibling, not a parent — and re-trips S6847/S6848). The dismiss button is `tabIndex={-1}` (not in the tab order; the real close affordance is the in-dialog X button), `cursor-default` (it's an invisible scrim, not a visible control). Confirm-dialogs that block dismiss while a mutation runs pass `disabled={mutation.isPending}` to this button.
- `role="dialog"` is intentionally NOT converted to native `<dialog>` [PH-212] — SonarQube `typescript:S6819` wants `<dialog>` over `role="dialog"`, but these modals are conditionally MOUNTED (`{open && <Modal/>}`), not toggled via the `open` attribute, so native `<dialog>` would need an imperative `ref.showModal()` + `useEffect`, and it ships UA `::backdrop`/`margin:auto`/`border`/`padding`/`background` styling that fights the existing `fixed inset-0` glass-overlay system; the e2e suite also drives them via `getByRole("dialog")` + click-outside + Esc. The `role="dialog"` attribute is RETAINED (left-as-is / wontfix for S6819); only the S6847/S6848 handler smells were fixed. If a future ticket migrates to native `<dialog>`, do it holistically (showModal lifecycle + UA-style reset `m-0 border-0 p-0 bg-transparent` + re-verify all modal e2e specs), not piecemeal.
- `<output>` replaced `role="status"` — keep an explicit display class on block-flow sites [PH-212] — `<output>` is `display:inline` by default (like `<span>`), but it carries the implicit `status`/`aria-live=polite` role for free. Where the old `<div role="status">` relied on block flow (`text-center`, `space-y-*`, standalone line), the `<output>` was given an explicit `block`/`flex` class to preserve layout (e.g. `Boards` "Yükleniyor…", `DiffViewer` "Loading diff…", `RepositoryStatusPanel` connected panel `block space-y-3`). If you add a new `<output>` and it collapses inline unexpectedly, add `block`. `aria-live="polite"` is kept explicit on the loading/connection variants even though `<output>` defaults to it (screen-reader consistency).
- `lib/a11y.ts` (`onActivateKeyDown`/`stopActivationKeyDown`) is now UNUSED [PH-212] — PH-206 introduced these for the modal-backdrop keyboard handlers; PH-212's backdrop restructure (sibling `<button>`) removed every call site, so both exports have zero importers. The file is LEFT in place (documented utility, removing it is out of B2 scope) — a future cleanup ticket may delete it. Do not assume it's wired anywhere; grep before reusing.
- Lane geometry MUST be verified on REAL git history, never only the seeded mock [PH-190] — the wiggly-lane bug is a REUSED-LANE failure mode: it only appears when a lane number is recycled by disjoint branches with idle rows between them. The design's seeded mock (and any hand-crafted fixture that assigns each branch a unique lane) has NO lane reuse, so a broken global-run / multi-row-bezier emitter passes on mock while the live app stays wrong. Two prior iterations (PH-179, PH-188) shipped exactly this false-pass. When touching `computeLanePaths`, dump the live backend `graph_payload` (real PH history, 300+ commits, 70+ merges) and assert: max segment y-extent <= one `rowH` (no multi-row sweep, no idle-gap bridge), every fork/merge curve anchored to lane 0, dot-count == row-count. The `branchGraphLayout.test.ts` reused-lane fixture encodes the regression but the REAL-DATA run is what proves it.
- Branch-curve color is per-BRANCH via `branchColor(tipSha)`, NOT `laneColor(lane)` [PH-198] — lane index is recycled by disjoint feature branches, so coloring by `laneColor(lane)` makes unrelated branches that happen to reuse a lane share a hue (the original "her branch ayrı renkte olmalı" bug). The per-branch color is hashed from the span's TIP sha (commit at `span.first`) into `LANE_COLORS.slice(1)` (cyan/lane-0 reserved for `main`). Two consequences when touching this: (1) the side-lane DOT and ALL of that span's segments must use `span.color`, not `laneColor(lane)`, or the dot/curve colors desync; (2) the QA contract test asserts ≥2 distinct `kind==="branch"` colors on a reused lane AND that two disjoint spans on one lane differ — if you ever change the identity key (e.g. to a branch name), keep it stable-per-branch and distinct-per-disjoint-span or `branchGraphFork.test.ts` S2/S2-root break. `laneColor` itself is still used for the lane-0 backbone + the sidebar branch dots (those are intentionally lane/index-colored) — do not rip it out.
- Single-commit branch fork edge: the descent runs in BOTH `isFirst===isLast` and `isLast` [PH-198] — a one-commit branch is a single-row span, so the divergence-descent (branch MID → main BOTTOM) lives at the SAME row as the branch-out. The descent (now `emitTerminus()`, renamed from `emitDescentIfMerges()` in PH-199) is a shared helper; if you refactor the span emission, make sure the single-row path still calls it (the old early `return` in the `isFirst` block is exactly the bug). `branchGraphFork.test.ts` S1 asserts a lane-1 segment with y strictly below the dot's mid; S1b (multi-commit contrast) + the regression invariants pin the boundaries.
- "Merged" must be a TIP-is-parent-of-a-lane-0-commit test, NOT a tip's-first-parent-on-lane-0 (fork) test [PH-199] — the merge-return descent curve is gated by `branchMerges(span)` which checks `commits[span.first].sha ∈ mergedTips`, where `mergedTips` = side-lane shas referenced as a parent by any lane-0 (main) commit. Do NOT revert this to `laneOf(firstParent)===0`: every feature branch forks off main so its tip's first parent is ALWAYS on lane 0, which makes open (never-merged) branches draw the same closed fork→merge loop as merged ones — the original PH-178-looks-merged bug. The predicate keys on the span TIP (not the descent row's first-parent) because the descent is emitted at the span's LAST row but merge status is a property of the branch TIP (they coincide only for single-commit spans). `branchGraphOpenMerged.test.ts` AC1 asserts an open branch emits NO `kind:"merge"` segment on its lane; AC2 asserts merged≠open; AC3 (merged keeps its curve) guards against over-correction.
- BranchGraph merge-ness comes from the backend `merged_into_default` flag when present; the lane-0 `mergedTips` scan is ONLY a no-flag legacy fallback [PH-268] — `computeLanePaths` gates on `hasMergedFlag = commits.some(c => c.merged_into_default !== undefined)`. If ANY displayed commit defines the flag, trust it for ALL (do NOT mix the two sources — that's why it's all-or-nothing per render). Do NOT "restore" the lane-0 parent scan as a parallel source of truth: it only sees merge commits that landed on LANE 0, so a merge-of-merge (merge commit on a non-zero lane, e.g. live `43005309` on lane 2 merging `54804385`) is MISSED → false open ring on a tip that is actually an ancestor of `main`. The FE cannot distinguish a real merge-into-default from a git-stash/octopus 2nd-parent edge over a windowed DAG (stash `8c8f8f31` was misclassified by naive parent-inference), so backend reachability over the FULL cache is the authority. The flag is keyed on the merged tip's OWN sha (the backend sets it on the reachable commit), which is exactly the span TIP `branchMerges` checks — so no re-keying is needed. Keep the `?:` optional on `GitCommitSummary.merged_into_default` so a stale/cached payload degrades to the legacy path instead of treating every commit as unmerged. `branchGraphOpenMerged.test.ts` flag-path tests pin the merge-of-merge close + the false-flag-stays-open; the legacy-path test pins back-compat. Runs HOST Node 26 only (container is Node 20).
- Open-branch affordance is geometry-driven via `openTips`, NOT hardcoded in the component [PH-199] — `computeLanePaths` returns `openTips: OpenTip[]` (one per unmerged span at its tip); `BranchGraph.tsx` renders those tips HOLLOW (`fill=var(--bg-base)`) + a dashed ring cap (`strokeDasharray="2 1.5"`, `r=tip.r+2`, `<title>Open branch (unmerged)</title>`). The ring is drawn AFTER the dots (z-order) so it sits on top of the hollow tip dot; if you reorder the overlay `<circle>` groups, keep open rings last or they'll be painted over. `openTipShas` (a `useMemo` Set from `geom.openTips`) drives the hollow-dot styling so a tip dot and its ring stay in sync. No backend `is_merged` flag exists or is needed — the parent DAG in the displayed commits is sufficient.
- `branchGraphLayout.test.ts` runs OUTSIDE the app `tsc`/`vite build` [PH-190] — the frontend has NO vitest/jest and adding one needs Coordinator approval (package.json dep rule). The test uses Node's built-in `node:test` + native TS type-stripping (Node 22.6+; project runs 26): `node --test --experimental-strip-types src/components/git/branchGraphLayout.test.ts`. It imports `node:test`/`node:assert` + a `.ts`-extension relative import + an `import type` from `@/types/git` — none of which the app `tsc` (no `@types/node`, `moduleResolution: bundler`, no `allowImportingTsExtensions`) accepts, so `tsconfig.json` EXCLUDES `src/**/*.test.ts(x)`. Do NOT remove that exclude or the production typecheck/build breaks; do NOT import test files from app code (Vite would then try to bundle them).
- `ROW_H` in `branchGraphLayout.ts` is consumed by BOTH the row height AND `computeLanePaths` y-math [PH-188] — changing it (40→44 here) shifts every dot/curve y-coordinate. Because the overlay derives `y(rowIdx)=rowIdx*rowH+rowH/2` from the same constant, dots stay centered automatically, but if you ever set the row `<button>` height to a literal that diverges from the exported `ROW_H`, the dots desync from the rows (lanes drift off the dots). Keep the row `style={{ height: ROW_H }}` bound to the import, never a hardcoded px. Verified centered at `cy = rowIdx*44 + 22` after the bump.
- `DiffViewer`/`FileDiffView`/`HunkView` are SHARED by BranchGraph's diff panel + `TicketCommits` + `DiffDemo` [PH-188] — the `showSummary` prop added for the branch-graph panel is additive (default `true`) so the other callers are untouched, but any heavier restyle of these diff-body components (e.g. the `rounded-md→rounded-[10px]` file-card radius bump) also affects TicketCommits/DiffDemo. Scope risky visual changes to a wrapper class rather than the shared component if a caller must differ. The branch-graph diff panel's CHROME (`.diff-head`/`.diff-stat`) lives in `BranchGraph`'s `CommitDiffPanel`, NOT in DiffViewer — restyle the panel wrapper there, not the shared body.
- xyflow selected-node cyan glow loses to a hard-set node `box-shadow` [PH-177] — the React Flow theming relies on `--xy-node-boxshadow-selected-default: var(--glow-cyan)` to ring the selected `WorkflowStateNode` in cyan. The custom node previously carried a `shadow-sm` Tailwind class; that inline/utility box-shadow takes precedence over xyflow's selected-shadow var, so selection produced NO visible glow. Fix: drop the node's own box-shadow class entirely (let xyflow own the shadow). If you re-add elevation to the node, use a token shadow that does NOT collide, or add an explicit `.selected` glow — don't hard-set `boxShadow` on the node root.
- F8 `.react-flow --xy-*` overrides depend on xyflow's `-default`-suffixed var names [PH-177] — the scoped block in `index.css` targets vars like `--xy-edge-stroke-default`, `--xy-node-background-color-default`. These are the resolved-order defaults xyflow ships under `.react-flow`. If a future `@xyflow/react` upgrade renames them, node/edge styling silently reverts to LIBRARY defaults (still legible, not broken — but off-palette). The custom node's inline token styles are the resilient fallback for node bg/border; verify the graph in BOTH themes after any xyflow version bump.
- MermaidBlock theme tokens MUST be read off `<html>`, not a detached probe [PH-176] — mermaid needs concrete hex `themeVariables`, not `var()`, so `buildThemeVariables(theme)` reads the F1 tokens via `getComputedStyle`. First attempt used a detached `<div class="light">` probe — it returned the DARK values regardless, because the light tokens are defined by the `html.light { … }` selector which only matches the `<html>` ELEMENT; a child `.light` div does not match it and just inherits whatever `<html>` currently carries. Fix used: briefly `root.classList.toggle("light", theme==="light")` on `<html>`, read computed tokens (synchronous `getComputedStyle` forces a style flush so nothing is painted), then restore the class (ThemeProvider owns the real flip). This makes the palette deterministic for the TARGET theme even when the effect runs before ThemeProvider's class-flip lands — eliminating the one-frame-stale palette the architect flagged as risk (b). Browser-verified: the SAME diagram's node fill/stroke flip `#161D29`/`#22D3EE` (dark) ↔ `#EEF2F7`/`#0891B2` (light) on toggle with no lag. Do NOT replace this with an `requestAnimationFrame(render)` deferral — under React 18 StrictMode the effect's cleanup races the rAF and the diagram silently never renders (initial paint empty).
- NEVER string-concat a hex alpha suffix onto a `var()` color [PH-175] — once `LANE_COLORS` became `var(--lane-*)` strings (F6), the old CommitRow ref-chip `backgroundColor: color + "22"` (lane hex + alpha) silently produces the invalid CSS value `"var(--lane-cyan)22"` → the chip background fails to paint (chip looks empty). **tsc does NOT catch it** (still a `string`); build stays green; only an eyeball/inspect reveals the missing tint. Fix used: `backgroundColor: color-mix(in srgb, ${color} 14%, transparent)` (the F1-sanctioned one-off alpha path, same as PH-170/PH-172). Any future per-lane tint (dots, chips, soft fills driven by `laneColor()`) MUST use `color-mix`, never `+ "NN"`. Grep touched files for `+ "` / template hex-alpha before committing a lane-color change.
- `@apply` of var-backed token utilities fails under Vite's PostCSS pass [PH-170] — `@apply bg-accent-strong` / `text-text-on-accent` / `bg-accent-soft` AND custom-radius `@apply rounded-pill` / `rounded-md` throw `The <class> class does not exist` in the DEV server (the standalone `npx tailwindcss` CLI and `npm run build` tolerate them, so build-green is NOT sufficient — the dev server 500s the CSS module and the app renders unstyled). Fix used in F1: base `@layer components` classes write raw CSS `var(--token)` (the `kit.css` pattern) for color/radius/shadow/transition; keep `@apply` only for standard layout/spacing/type utilities (`inline-flex`, `px-3`, `text-sm`, `font-medium`). The token utilities still work in component JSX.
- Tailwind config changes need a Vite dev-server restart + `node_modules/.vite` cache clear [PH-170] — HMR does NOT reliably reload `tailwind.config.js`; after editing the config the dev server kept serving Tailwind's DEFAULT `font-mono`/utility stacks. Symptom: a `font-mono` element computes `ui-monospace, SFMono-Regular, …` instead of `JetBrains Mono`. Fix: stop the server, `rm -rf node_modules/.vite`, restart. (The production `npm run build` was always correct.)
- `.mono` class is JIT tree-shaken until a source file uses it [PH-170] — F1 ships the `.mono` component class + `font-mono` utility but no F1 source references `.mono` yet, so it isn't emitted in the running app's CSS and a probe element falls back to the inherited body font (Inter). The `code` element and the `font-mono` utility both correctly compute `JetBrains Mono`; `.mono` resolves the moment F2–F8 apply it. Not a bug — expected JIT behavior. RESOLVED in F2 [PH-171] — `NotificationBell` timestamps now apply `.mono`, so the class is emitted and renders JetBrains Mono in the running app.
- LiveStatus is presentational; shell-level live data is deferred [PH-171] — `components/LiveStatus.tsx` is a pure prop-driven pill and `Layout.tsx` hard-codes `status="off"`. The existing `useWebSocket` hook is **per-board** (needs `boardId`; only BoardDetail/TicketDetail mount it), and the shell has no board context. A truly always-on Live/Connecting/Off badge needs a new app-level WS lifecycle (connect on auth, board-agnostic channel) = behavior change, out of F2 visual scope. Wire real status in a follow-up; the component + its three visual states are already complete and testable in isolation.
- (RESOLVED [PH-165]) `CommitNode.tsx` + `BranchLegend.tsx` + `BranchPanel.tsx` dead-but-shipped after PH-167 → DELETED in PH-165 along with the orphaned `DiffDemo.tsx`. The `components/git/index.ts` barrel was trimmed to `BranchGraph` + `TicketCommits`. The git module no longer carries any `@xyflow/react` import (only `WorkflowEditor.tsx` does, legitimately). If you re-add an xyflow-based git view, recreate the component fresh rather than resurrecting these.
- (RESOLVED [PH-165]) `/dev/diff-demo` page file deleted — PH-167 removed the route + nav link but kept `src/pages/DiffDemo.tsx` on disk; PH-165 deleted the orphan file. `/dev/diff-demo` still falls through to the `*` catch-all → redirect to `/`. Verify diffs via the Branch Graph commit→diff pane or the TicketDetail Git tab. The `DiffViewer` component itself is preserved (rendered by BranchGraph's commit→diff pane + TicketCommits).
- Branch Graph diff pane fetches commit-vs-first-parent only [PH-167] — clicking a commit row opens `DiffViewer fetch={{ kind:"commit", sha }}` which is the commit's diff against its FIRST parent (G9 `getCommitDiff` semantics). For merge commits this shows only first-parent changes (can look empty/small); this matches `git show` default behavior, not a bug. Empty/synthetic commits (e.g. `--allow-empty` test commits) correctly render "0 files changed / No file changes".
- (RESOLVED [PH-165]) ESLint flat config — `frontend/eslint.config.js` added + lint script changed to `eslint .`; `npm run lint` now works and exits 0. The config registers a no-op `react-hooks/*` stub (the real plugin is NOT a dependency) so the 2 existing `// eslint-disable-next-line react-hooks/exhaustive-deps` directives resolve; if you add `eslint-plugin-react-hooks` later, replace the stub with the plugin's flat config and turn the rules back on. Type-aware lint is intentionally off — `tsc --noEmit` is still the type gate. (was: G14 flat config broken, tracked in PH-164/[PH-163]).
- `api.git.refresh` G13 hybrid auth — smoke test (`src/api/__smoke__/git.types.ts`) must use `{ useBearer: true }` or `{ refreshToken: "..." }` object form; passing a plain string now fails tsc [PH-162]. Update any call sites that pass a literal string — migrate to `{ refreshToken: "..." }`.
- RepositoryConfigForm: local_path initial value defaults to `/repos/` not empty — if the user opens the form without changing this field, the client-side prefix check passes but the backend may reject if the path is too short. Good UX: always show a real example placeholder like `/repos/my-project` [PH-162].
- `parseUnifiedDiff` silently drops `\ No newline at end of file` from line counters [PH-158] — the `\ ` meta line is pushed as `type: 'meta'` but does NOT advance oldNo/newNo. If you compare rendered line numbers to a raw git diff, the meta row will cause the next line's number to appear one ahead. This is correct behaviour (the meta line is not a real source line), but it can confuse testers who count lines manually.
- Rename-only patches produce 0 hunks (empty array from parser) [PH-158] — a patch that is purely `diff --git a/old b/new\nrename from ...\nrename to ...` with no `@@` hunk header is valid git output and `parseUnifiedDiff` correctly returns `[]`. `FileDiffView` renders "No content changes" in this case. If you add hunk-count assertions in tests, distinguish rename-only from empty string.
- `DiffViewer fetch mode` does not invalidate cache on WS `git_synced` event [PH-158] — stale cache for the same sha will be served from TanStack Query until default staleTime (0, immediate refetch on next mount). G14 will add `queryClient.invalidateQueries(['git','diff'])` in the `isGitSyncedMessage` handler; do NOT add it in G9-scope PRs as it is intentionally scoped out.
- `api.git.refresh` must NOT use Bearer token [PH-157] — `POST /api/boards/{key}/git/refresh` authenticates via `X-Git-Refresh-Token` shared secret (see `board.roles["refresh_secret"]`). The `request<T>` helper unconditionally sets `Authorization: Bearer <token>`. `api.git.refresh` uses direct `fetch` to avoid this. If you refactor auth headers in `request<T>`, ensure `api.git.refresh` remains unaffected.
- `ahead`/`behind` null sentinel means deep divergence — not an error [PH-157] — `GitBranchEntry.ahead` and `GitBranchEntry.behind` are `number | null`. `null` means the BFS walk exceeded `git_backfill_limit` (2000 commits), not that the values are unknown. G9 UI must render "..." or equivalent for null (not "0" or empty). Logging null as an error is a false positive.
- Three-dot range diff semantics (base...head) — not two-dot [PH-157] — `api.git.getRangeDiff` wraps `GET /git/diff?base=&head=`, which uses merge-base (three-dot) semantics: `main...feature` shows only feature-branch-specific changes. If you need two-dot `base..head` (all commits between two refs regardless of branch point), use `api.git.listCommits` with cursor pagination instead.
- `api.git.refresh` 403 vs 401 distinction [PH-157] — `refresh_secret` not configured on the board → 403 (`refresh_disabled`); secret IS configured but token mismatches → 401 (`unauthorized`). The client wraps both in `ApiRequestError` with the respective status code. Callers should distinguish these to surface useful error messages to admins.
- (HISTORICAL — fixed in PH-165) `npm run lint` was broken on main pre-G8 [PH-157] — ESLint 9.39.4 requires a flat `eslint.config.js`; the project had neither that nor a `.eslintrc.*`, so lint failed since G8. PH-165 added the flat config; lint now works. Retained for context on why G8–G14 PRs used `tsc --noEmit` as the only gate.
- `src/components/BoardSettingsDialog.tsx:62,80,110` — three `TODO: Implement actual API call` markers for board member add / remove / role-change inside the legacy `BoardSettingsDialog`; live membership management has since moved to `MembersTab.tsx` + `MembershipRow.tsx` against the real `/api/boards/:id/members` REST endpoints, so this dialog appears to be an obsolete code path that still ships. Reconcile or delete before next refactor. [bootstrap]
- `src/api/client.ts:158-172` — four `@deprecated` workflow REST helpers (`addWorkflowState`, `deleteWorkflowState`, `updateWorkflowStates`, `updateWorkflowTransitions`) hit endpoints the backend never implemented (PH-21 era, 404). They are still exported; new callers must use the `mcpCall`-backed workflow helpers below them (`listWorkflows`, `addTransition`, `setFieldGates`, etc.). [bootstrap]
- `src/ws/` directory is empty — the layout documented in `frontend/README.md` ("`src/ws/` — WebSocket client + subscription manager") no longer matches reality. The actual implementation lives in `src/hooks/useWebSocket.ts`; the empty `ws/` folder + README line will mislead newcomers until cleaned up. [bootstrap]
- FileDiffView truncated body koşulu file.truncated tek başına yeter [PH-161] — sliced non-null patch için de marker doğru basılır; sliced veri render edilmez (yarım veri yanıltıcı). Eski koşul `file.truncated && file.patch === null` sadece `file.truncated` oldu.
- Default branch seçilince "main'e diff" disabled — base==head merge-base diff anlamsız ve backend 422/empty döner [PH-160]. Diff button `disabled={isDefaultBranch || branchesQuery.isLoading}` + `title="Default branch — kendine diff yok"`. QA bu case'i test ederken main branch seçip diff butonunun disabled olduğunu doğrulamalı.
- Lane assignment is fragile when `commits[].parents` contains shas not in the result set (limit truncation) — render faded dashed edge stub toward the truncated parent, but no stub node is created; deep-history merges may show dangling edges [PH-159].
- `src/stores/auth.ts:20-43` — `setToken` performs a localStorage write then immediately re-reads to assert sync, logging a warning on mismatch. This is dev-only diagnostic noise (gated on `import.meta.env.DEV`) but indicates a previously-observed token desync; if you touch the auth store, preserve the write-then-verify pattern. [bootstrap]

## Related

- [[overview]]
- [[index]]
- [[components/backend]]
