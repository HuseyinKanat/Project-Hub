---
type: component
files:
  - backend/app/main.py
  - backend/app/cli.py
  - backend/app/schemas.py
  - backend/app/api/tickets.py
  - backend/app/api/boards.py
  - backend/app/api/actors.py
  - backend/app/api/auth.py
  - backend/app/api/websocket.py
  - backend/app/api/deps.py
  - backend/app/mcp/server.py
  - backend/app/core/permissions.py
  - backend/app/core/security.py
  - backend/app/core/exceptions.py
  - backend/app/core/config.py
  - backend/app/core/websocket_manager.py
  - backend/app/db/models/core.py
  - backend/app/db/session.py
  - backend/app/db/migrations/versions/
  - backend/app/services/tickets.py
  - backend/app/services/workflows.py
  - backend/app/services/stale_claims.py
  - backend/app/services/boards.py
  - backend/app/services/history.py
  - backend/app/services/notifications.py
  - backend/app/services/serializers.py
  - backend/app/events/bus.py
  - backend/app/events/dispatcher.py
  - backend/app/git/parser.py
  - backend/app/git/webhook.py
  - backend/app/git/reader.py
  - backend/app/git/sync.py
  - backend/app/git/_linkage.py
  - backend/app/api/repositories.py
  - backend/app/services/repositories.py
  - backend/app/db/migrations/versions/20260604_0006_ph_150_repositories.py
  - backend/app/db/migrations/versions/20260604_0007_ph_152_git_cache.py
  - backend/app/db/migrations/versions/20260606_0008_ph_193_sonarqube_metric.py
  - backend/app/services/sonarqube.py
  - backend/app/api/graph.py
  - backend/app/services/graph.py
  - backend/app/api/search.py
  - backend/app/services/search.py
  - backend/app/services/labels.py
  - backend/app/services/relationships.py
  - backend/app/services/git_overlap.py
  - backend/pyproject.toml
last_touched_ticket: PH-288
status: active
---

# Backend

> FastAPI 0.115 + SQLAlchemy 2 async + Alembic + PostgreSQL 16 + Redis 7 service that powers ProjectHub's ticket state machine, MCP tool catalog, and WebSocket fan-out.

## Current behavior

The backend is a single ASGI app (`backend/app/main.py`) that exposes ProjectHub as **two parallel surfaces over the same domain logic**: a REST API under `/api/*` for the React frontend and an MCP catalog under `/mcp/*` for agent clients (Claude Code, Jarwis sub-agents). Both surfaces route through identical service functions in `backend/app/services/`, so permission checks, history writes, and event publication are uniform regardless of caller. The app boots with a `lifespan` context manager that starts the Redis-backed `EventBus` and a `stale_claim_cron` background task, then mounts CORS plus the routers `auth`, `actors`, `boards`, `tickets`, `notifications`, `preferences`, `git`, `repositories` (PH-150 G1), `mcp_server`, and `websocket`. Centralized exception handlers (`app/core/exceptions.py`, registered via `register_exception_handlers`) translate domain errors — `NotFound`, `PermissionDenied`, `InvalidTransition`, `FieldGateNotMet`, `AlreadyClaimed` — into structured HTTP responses with consistent payload shapes.

Persistence is async SQLAlchemy 2 against PostgreSQL (with SQLite/`aiosqlite` fallback for tests). The ORM (`app/db/models/core.py`) models `Actor`, `Board`, `BoardMembership`, `Workflow`, `BoardWorkflow` (junction for per-board workflow ownership — PH-97), `Ticket`, `Comment`, `Notification`, `TicketHistory`, `UserPreference`, and `Repository` (PH-150 G1 — git config, 1 board : 0..1 repo, FK `boards.id` ondelete=CASCADE). **PH-281 PIVOT (epic PH-271)**: the cross-board concept graph (`/api/graph`) + search (`/api/search`) are driven by the **inline `Ticket.labels` free-text ARRAY** (`STRING_ARRAY_TYPE` = native `text[]` on Postgres / JSON-array-as-text on the sqlite test dialect), NOT a separate ConceptTag entity. The PH-272 `ConceptTag` / `TicketConceptTag` / `ConceptTagLink` **models + tables + migration `2722c9066c32` remain DORMANT** (no table drop) but their user-facing surface — the `/api/concept-tags` CRUD router, `services/concept_tags.py`, the `tag.read`/`tag.manage`/`tag.assign` caps, the `TicketResponse.concept_tags` serializer field, and the ticket attach/detach endpoints — was REMOVED. graph/search read-auth moved to a global `ticket.read` gate (`require_global_permission`, "holds `ticket.read` under ANY board membership"). JSON columns are dialect-aware (`JSONB` on Postgres, plain `JSON` elsewhere) and arrays degrade similarly. Tickets carry workflow-driven `state` plus rich domain fields (`technical_depth`, `impact_analysis`, `test_plan`, `acceptance_criteria`, `steps_to_reproduce`, `branch_name`, `labels[]`, `claimed_by`/`claimed_at`, `agent_phase`). Schema evolution is managed by Alembic — versions live in `backend/app/db/migrations/versions/` (currently 11 revisions, most carrying a `PH-XX` slug). **PH-228** adds `Board.repos_path` (String(500), nullable) — the board's HOST filesystem root (e.g. `/Users/huseyinkanat/Documents/kims`), backfilled for the 6 known boards by migration `20260608_0010` and translated to the in-container `/repos` path by `services/repo_paths.to_container_path` (the `host_home`/`HOST_HOME` setting + the broadened `${HOME}:/repos:ro` mount; full design on [[components/git-integration]]). Authentication is bearer-token based: `Actor.token_hash` stores a bcrypt hash (`app/core/security.py:hash_token` / `verify_token`); `api/deps.py:current_actor` resolves the request token by iterating active actors and `verify_token`-matching the bearer credential. Permission checks are board-scoped (`app/core/permissions.py:require_permission`) and understand wildcards (`*`, `state.transition:*`, `ticket.release:*`, `ticket.update_field:<field-list>`), plus an `:if_assignee` suffix that treats `claimed_by` as equivalent to `assignee_id` so a sub-agent that claimed without `assign_ticket` still passes the gate.

The MCP catalog (`app/mcp/server.py`) exposes the same tools over two transports — a legacy REST shape (`GET /mcp/tools`, `POST /mcp/call/{tool}`) and full MCP JSON-RPC 2.0 (`POST /mcp` with `initialize` / `tools/list` / `tools/call`) — both delegating to one `_dispatch_tool` so behavior, permissions, and history are identical. Headline tools: `list_boards`, `get_board`, `query_tickets`, `get_ticket`, `get_state` (~200-char Coordinator self-verify probe), `get_ticket_slice` (caller-projected field subset), `create_ticket`, `update_ticket`, `assign_ticket`, `transition_state`, `add_comment`, `claim_ticket`, `release_ticket`, `update_agent_phase`, `create_branch_for_ticket`, plus workflow CRUD. Write tools default to a minimal response (`{ok, id, updated_fields, state}`) and opt into the full ticket payload via `verbose=true` — a deliberate budget choice that lets Coordinator self-verify cheaply. The ticket state machine lives in `services/tickets.py:transition_ticket_state` and consults the active workflow's `transitions` list + `allowed_roles` for role gating, with `assignee` treated as equivalent to `claimed_by` so an agent that claimed without `assign_ticket` still passes the gate. Field gates (`_missing_gate_fields`) are workflow-configurable (`services/workflows.py:get_field_gates_for_ticket_transition`) — e.g. `in_review → in_test` may require `test_plan`. Every state mutation writes a `TicketHistory` row and publishes an `EventEnvelope` via the Redis pub/sub `EventBus` (`app/events/bus.py`); the `websocket` router subscribes per-board / per-ticket channels and streams envelopes to connected clients, with exponential-backoff reconnect and a `system_degradation` fallback envelope when Redis is unreachable. A separate `stale_claim_cron` (`services/stale_claims.py`, 60s interval, 5-minute timeout) sweeps tickets whose `claimed_at` exceeds the heartbeat threshold and auto-releases them — the safety net that lets sub-agents crash without leaving a dangling lock. A third lifespan task, `sonarqube_poll_cron` (`services/sonarqube.py`, PH-193), polls a self-hosted SonarQube Community Build for each board's main-branch quality metrics, upserts the latest snapshot into the `sonarqube_metrics` table (1 board : 0..1 row, unique `board_id`), and publishes a `sonarqube_synced` envelope on `board:{id}`; it is gated on `settings.sonarqube_enabled` (default False → task never created, no-op) and surfaces as `BoardResponse.health` (eager-loaded via `selectinload(Board.sonarqube_metric)`, mirroring `repository`). The `app/cli.py` module provides operational commands (board bootstrap, `create_jarwis_actors`, `update_board_roles`) used by `jarwis-init.sh` and manual maintenance. Git commit parsing (`app/git/parser.py`) extracts `[A-Z]{2,5}-\d+` ticket keys from messages and validates conventional-commit format for the webhook ingestion in `app/git/webhook.py`.

## Design decisions (recent)

- Weighted top-K SPARSE `/api/graph` — the `/space` hairball fix [PH-288] — epic PH-283; consumes the PH-287 weighted model (next bullet). The graph is now **TICKET↔TICKET only**: `label` NODES + `has_label` edges are GONE (they were the hairball — every ticket fanned out to dozens of shared-hub neighbours). Instead `build_graph` emits the **weighted top-K signal edges** (`dependency`/`reference`/`epic`/`shared_label`/`code_overlap`) from the PH-287 model, one per unordered pair carrying the HIGHEST-precedence signal as `type`+`reason`, plus additive `GraphEdge.strength: float` + `reason: str` (backward-compatible — the `type` Literal WIDENS additively, `has_label` kept for shape stability; `SpaceGraph.tsx` falls back via `EDGE_META[e.type] ?? has_label`). Specific labels survive as `shared_label` edge metadata (`context="shared specific labels: <lbl> (idf <x>)"`); **hub labels (n≥15, IDF 0) emit NO edge** (the load-bearing cross-product bound — they're never iterated). **Top-K UNION** (default k=6, `?k=` clamped 1..50 via `clamp_k`): an edge survives if EITHER endpoint ranks it top-K (union, not intersection — intersection would orphan an asymmetric hub's only-strong-link neighbour). **The perf crux — BATCHED all-pairs, NOT a per-node `related_tickets()` loop**: the SAME PH-287 signals are computed ONCE over the whole in-scope set via the NEW DRY seam `relationships.graph_edges(session, tickets, *, k)` — in-memory label GROUP BY (labels inline, 0 queries) + ONE NEW `git_overlap.all_overlapping_pairs()` set-based self-join (the all-pairs counterpart of the src-anchored `tickets_touching_paths`) + ONE batched `_resolve_keys` (empty-guarded) + inline-`epic_id` pairs → merge/score/top-K IN MEMORY. **DRY lift**: the per-signal weighting (`_signal_weights`) + precedence order (`_SIGNAL_ORDER`) are now module-level in `relationships.py`, called by BOTH the per-ticket `_Accumulator` AND the all-pairs `_PairSignals` — one formula, no drift. **N+1 invariant HELD**: statement count CONSTANT across 1-vs-50 tickets (the test's pinned literal updated 4→**5**, the +1 being the single `all_overlapping_pairs` self-join; the 1==50 EQUALITY is the real guard). **Live sparsity proof** (global, real 440-ticket board): label nodes **429→0**, has_label edges **gone**, total edges **~2600→1871** at k=6 (1051 at k=3, 7916 at k=50 — `?k` monotonic + clamped); EVERY edge carries strength+reason+context; board-scope collapse (`board:<key>` node + aggregated edge) UNCHANGED. Each function ≤15 cognitive (Sonar S3776) via extracted helpers (`_load_scoped_tickets`/`_apply_board_collapse`/`_keep_in_scope`/`_pair_to_edge` in graph.py; `_build_label_idf`/`_add_shared_label_pairs`/`_add_code_overlap_pairs`/`_add_epic_pairs`/`_add_ref_dep_pairs`/`_apply_ref_or_dep`/`_topk_union`/`_PairSignals` in relationships.py). Read-only — NO migration (`alembic heads` stays `2722c9066c32`). Frontend EDGE_META/legend styling for the new edge types is a trivial follow-up (backend ships shape-compatible).
- Weighted, noise-suppressed, explainable multi-signal connection model in `services/relationships.py` [PH-287] — epic PH-283 FOUNDATION; EVOLVES the PH-284 flat scorer (see next bullet, now SUPERSEDED) into 5 weighted signals merged per candidate. **Score** = `8*dependency + 5*reference + 3*epic + min(0.6*Σ label_idf, 8) + min(0.5*Σ file_idf, 6)`; reasons reconstruct it exactly (`_Accumulator._signal_weights()` is the single source of truth read by BOTH `score()` and `reasons()` → no drift). **(1) IDF labels** replace the flat `min(count,3)*1.0`: `idf=max(0,log((N+1)/(n_label+1)))`, with a HARD-DROP at `_HUB_LABEL_THRESHOLD=15` (a label on ≥15 tickets — `frontend`=128/`backend`=98/`bug`=53 — contributes 0 and emits NO `shared_label` reason; rare 2-3-ticket labels score high). Frequencies come from ONE batched GROUP BY over the dialect-aware unnest (`labels._labels_table_function`) scoped to the SRC's own labels + ONE `N_total` count — constant, never the 437-label space. **(2) code_overlap** (NEW signal) surfaces tickets that touched the SAME files via the PH-152 git cache (`git_commit_tickets ⋈ git_commit_files`), isolated in NEW `services/git_overlap.py` (mirrors how `labels.py` isolates the unnest): 2 SET-BASED queries (`src_file_paths` Q1 → guard-empty → `tickets_touching_paths` Q2) + 1 candidate load; file-IDF (a file touched by many tickets is less specific) derived from the SAME Q2 rows (no 3rd query); no commits → no reason (graceful); `_SRC_PATH_CAP=200` bounds the unindexed `path IN (...)` scan. **(3) dependency split**: `_parse_dependency_keys` extracts EXPLICIT `Depends on:`/`blocked by` (→`depends_on`) + `blocks` (→`blocks`) lines into a DISTINCT, HIGHEST-weight directed `dependency` reason; those keys are REMOVED from the plain-reference set so a dep never double-counts as a `reference` (plain `PH-XXX` mentions stay `reference`). **Precedence invariant** (the contract — assert ORDERING, not magic numbers): dependency > reference > epic > rare-label > hub-label(=0). **Read-gate fix**: `pm` + `orchestrator` GRANTED global `ticket.read` in `DEFAULT_WEB_ROLES` so the Coordinator's pm channel can call `related_tickets` (+graph/search); stranger-with-no-membership still denied (least-privilege held); **INERT on existing boards until `update_board_roles` re-applies the template** (the Coordinator runs it post-merge). `RelatedReason.type` Literal WIDENED additively (`+dependency,+code_overlap`) — PH-286 backward-compatible. Read-only — NO migration (`alembic heads` stays `2722c9066c32`). Each function ≤15 cognitive (Sonar S3776) via extracted helpers (`_idf`/`_label_freq_map`/`_label_idf_map`/`_parse_dependency_keys`/`_code_overlap_candidates`/per-signal `_merge_*`). **Live proof**: `related_tickets(PH-284)` → hub label `backend` (n≈98) DROPPED, only `agent-mcp (idf 4.1)` surfaces; `code_overlap` ranks PH-283/PH-275 via shared `relationships.py`/`server.py`/`schemas.py`; `related_tickets(PH-288)` → PH-287 ranks #1 via `dependency: PH-288 depends on PH-287 (explicit)`.
- `related_tickets` MCP tool + `services/relationships.py` relationship-scoring service [PH-284] — **SCORING SUPERSEDED by PH-287 (above)**; the seam/registration/N+1 discipline below still hold. — FOUNDATION child of epic PH-283 (consumed by PH-286 `recall_context`). NEW read-level MCP tool `related_tickets(ticket, cross_board=true, limit=20)` returns cross-board related tickets, each `{key, title, board, state, score, reasons:[{type, detail}]}` sorted by score desc. It is a **scoring/aggregation layer ON TOP of** the edges already derived by `services/graph.py` — it does NOT re-implement edge derivation: it imports graph.py's reference parsers (`_extract_keys`/`_resolve_keys`) and labels.py's `label_match_predicate` (dialect-aware unnest). Score = `5.0*reference + 3.0*epic + min(shared_label_count,3)*1.0` (reference strongest = explicit human link; epic next = structural; shared-label weakest + count-scaled, CAPPED at 3 so a hub label like `backend` can't outrank a real reference). `reasons[]` is built from the SAME signals that feed the score, so it is explainable. Deterministic tiebreak `score desc → updated_at desc → key asc`; self excluded; no relations → `[]`. **Auth**: read-level — the `TOOLS` catalog entry carries NO `permission=` (advertisement only, never enforced in `_dispatch_tool`); the SERVICE enforces global `ticket.read` via `_require_ticket_read` copied verbatim from `services/search.py` (the tool exposes cross-board ticket identity = a cross-board read). **MCP registration = the 4 coordinated edits** in `mcp/server.py`: `RelatedTicketsInput` model, `TOOLS` entry, `_dispatch_tool` arm (thin adapter → `relationships.related_tickets`, `.model_dump(mode="json")`), AND `_TOOL_INPUT_MODELS` map entry — BOTH the TOOLS entry and the map entry are mandatory or `_build_mcp_tool_list` silently drops the tool from `tools/list` (the sync gate, server.py:840-841). `NotFound`/`PermissionDenied` from the service auto-surface as MCP `isError` via `_jsonrpc_tools_call`. **N+1-free**: constant batched query count independent of relation count — ONE dialect-aware shared-label query, ONE batched outbound `_resolve_keys` + ONE inbound substring query, ONE epic column-predicate query, plus the membership gate + src fetch; candidate `board.key` eager-loaded, labels read inline. `cross_board=False` board-filters every candidate query to src's board. Read-only — NO migration (`alembic heads` stays `2722c9066c32`). The service stays a pure `(session, actor, ticket, cross_board, limit) -> list[RelatedTicket]` seam (no MCP coupling) so PH-286 can call it directly. Schemas: `RelatedReason{type: shared_label|reference|epic, detail}` + `RelatedTicket{key, title, board, state, score, reasons}` (near the search/graph schemas).
- `/api/graph` + `/api/search` re-pointed to the inline `Ticket.labels` ARRAY; ConceptTag user-facing surface removed, models dormant (no table drop) [PH-281] — child 6/7 of epic PH-271, a PIVOT: the user clarified "concept tags / kavram labelları" meant the EXISTING kanban `labels` free-text ARRAY, not a new entity. **graph** (`services/graph.py`): `label` nodes (`id="label:<rawValue>"`, `type:"label"`, raw value VERBATIM after the prefix — injective, NOT slugified; consumers exact-match, never split on `:`) replace `tag` nodes; `has_label` edges (`context="has-label"`) replace `has_tag`; the `tag_link` label↔label edge type is GONE (labels have no relation registry) and `GraphEdge.relation` is dropped. board-scope collapse + `board:<key>` node + `reference` + `epic` edges + per-edge `context` UNCHANGED; cross-board reach now detected via SHARED LABEL STRINGS (`labels_reach_query`, a LATERAL `Ticket ⋈ unnest/json_each`). `?tag=<slug>` → `?label=<rawValue>` (unknown label → EMPTY ticket set, NOT 404 — no label registry). **search** (`services/search.py`): grouped result `{tickets, labels}` (`labels: list[str]` distinct matching strings) replaces `{tickets, concept_tags}`; `?labels=<a,b>` AND-filter (EXACT per-element membership) replaces `?tags=`; a ticket whose ONLY hit is a label is still returned (label predicate OR-ed into the `or_(title/desc/key)` clause). **Removed**: `api/concept_tags.py` + `services/concept_tags.py` (deleted), the 3 `tag.*` caps (`core/permissions.py` + `services/defaults.py` + `docs/permissions.md`), `TicketResponse.concept_tags` + the `concept_tag_links` eager-load in `_ticket_load_options`, ticket attach/detach concept-tag endpoints, `InvalidConceptLink` (422; only raiser gone), the ConceptTag* schema classes. **Dormant**: ConceptTag models + migration + tables + `db/models/__init__.py` re-exports stay (no migration; `alembic heads` stays `2722c9066c32`). **N+1**: labels read inline off the Q1 ticket SELECT — NO junction query, NO `selectinload` for labels → strictly FEWER statements than PH-279 (the unscoped global graph is 4 stmts: ticket.read gate's membership SELECT + board selectinload, then Q1 tickets + board selectinload; the constant-statement-count test pins the new lower number). Backward-compat with the PH-273/274/275 ConceptTag surface explicitly NOT required (this supersedes it; PH-280 frontend updates in lockstep).
- Graph v2: board-scope collapse (`?scope=board`) + ticket-`reference` edges + per-edge `context` [PH-279] — ADDITIVE extension of the PH-274 `/api/graph` endpoint (child 6/7 of epic PH-271), backward-compatible for the PH-277 base. NEW `?scope=global|board` query flag (default `global` = PH-274 behavior verbatim, byte-compatible — the new literals NEVER appear unscoped); `scope=board` REQUIRES `?board` else `ValueError`→**422** (wrapped at `api/graph.py`). **Board-scope collapse mode** (`?board=A&scope=board`): board A's tickets+tags stay fully detailed, but every connection LEAVING the board collapses into a synthetic `board:<foreignKey>` node (`GraphNode.type` += `"board"`, id `board:<key>`, emitted ONLY in board-scope) + ONE aggregated `board` edge per (in_scope_node, foreign_board) pair (`GraphEdge.type` += `"board"`, `context="cross-board"`). Foreign tickets/tags are NEVER expanded — the detailed cross-board topology stays in the global view. Cross-board reach detected from TWO sources, both aggregated: shared-tag (Q5: ONE join `TicketConceptTag→Ticket` for in-scope tags on a different non-deleted board, materialising only `(tag_id, board_id)` pairs) + foreign ticket-reference (`key_map` from Q4). Q6 batch-loads foreign board metadata. **NEW `reference` edges** (both scopes): module-level `_TICKET_KEY_RE = \b[A-Z]{1,6}-\d+\b` scans each `Ticket.description` for candidate keys; the AUTHORITATIVE gate is "does the match equal a real, non-deleted `Ticket.key`?" — resolved via ONE batched `_resolve_keys` Q4 (`WHERE key IN (...)`, **empty-candidate guard** keeps the N+1 statement count constant when no descriptions mention keys), so regex false-positives (COVID-19, UTF-8, SHA-256) drop for free. Emit `reference` edge per resolved DISTINCT key (`_extract_keys` returns a `set` → repeated-mention dedupe); rules: self-skip; epic-wins ORDERED-pair dedupe (build `_epic_pairs` FIRST, skip a `reference` for the same `(parent→child)` pair, but KEEP a child→parent reference = different ordered pair); unresolved/soft-deleted keys → no edge. **Per-edge `context`** (`GraphEdge.context: str | None`, additive): `has_tag`→`"has-tag"`, `tag_link`→`relation` (mirror; `relation` field UNCHANGED/frozen), `epic`→`"epic"`, `reference`→`"ticket-reference"`, `board`→`"cross-board"`. **Complexity discipline** (PH-274 bounced on Sonar S3776): `build_graph` stays a thin orchestrator; all v2 logic extracted to flat single-purpose helpers (`_extract_keys`/`_resolve_keys`/`_epic_pairs`/`_reference_edges`/`_shared_tag_reach`/`_collapse_pairs_from_tags`/`_collapse_pairs_from_refs`/`_board_collapse`), each ≤~7 cognitive. Schema: GraphNode/GraphEdge field names + the `ticket:`/`tag:` id scheme FROZEN — only additive literals + the optional `context` field. No migration (`alembic heads` stays `2722c9066c32`). N+1 proof: `global` adds +1 query (Q4), `scope=board` adds +3 (Q4/Q5/Q6), all row-count-independent → PH-274's 1-vs-51 constant-statement-count test still passes; a NEW reference-bearing N+1 test also holds. All 12 PH-274 tests pass unchanged (backward-compat regression proof).
- Cross-board bipartite concept-graph endpoint `GET /api/graph` [PH-274] — child 3/7 of epic PH-271; the read-only topology endpoint that feeds the Obsidian-style `/space` view (PH-277). New GLOBAL router `app/api/graph.py` (`/api/graph`, registered after `concept_tags.router`) + pure-assembly service `app/services/graph.py`; new `GraphNode`/`GraphEdge`/`GraphResponse` schemas. **Bipartite** nodes = tickets + concept tags, disambiguated by a TYPE-PREFIXED `id` (`ticket:<uuid>` / `tag:<uuid>`) — the SOLE disambiguator (a ticket UUID and a tag UUID could otherwise collide in the id namespace); every node ALSO carries a redundant `type` discriminator so the frontend styles/filters without parsing the id. Three edge types: `has_tag` (ticket↔tag from the `TicketConceptTag` junction — a tag on tickets in 2 boards yields 2 edges sharing one tag node = the cross-board join), `tag_link` (tag↔tag from `ConceptTagLink`, directed source→target, `relation` carried, reverse pair NOT deduped — render-layer concern), `epic` (ticket↔ticket parent→child from `Ticket.epic_id`, emitted only when the parent node is in the set). **N+1-free (AC6)**: THREE bounded one-shot queries — Q1 tickets `selectinload(concept_tag_links→concept_tag, board)` (graph-local minimal options, NOT `_ticket_load_options` which over-fetches reporter/assignee/workflow), Q2 all `ConceptTagLink` with both endpoints eager, Q3 full `ConceptTag` map (node identity + orphan policy). `selectinload` issues a FIXED number of follow-up SELECTs regardless of row count → statement count is CONSTANT across 1 vs 50 tickets (test asserts this via a `before_cursor_execute` counter). Filters `?board=<key>` (case-insensitive, 404 unknown) + `?tag=<slug>` (1-hop subgraph) compose as INTERSECTION; a uniform final invariant — emit an edge IFF both endpoints survived into the node set — makes every filter mode correct by construction (no per-filter edge bookkeeping). Orphan tags (no junction + no link): INCLUDED as standalone nodes in the unfiltered graph, EXCLUDED under filters. Permission reuses the PH-273 global `tag.read` gate (`concept_tags._require_tag_read`) — the graph exposes cross-board ticket keys/titles/states, so it is a GLOBAL read. Soft-deleted tickets (`deleted_at`) filtered out. No migration (`alembic heads` stays `2722c9066c32`). Node/edge field shape is the STABLE contract PH-277 consumes — do not rename post-merge.
- ConceptTag CRUD API + ticket attach/detach + `tag.read`/`tag.manage`/`tag.assign` caps [PH-273] — child 2/7 of epic PH-271; the API/permission/serializer layer ON TOP of the frozen PH-272 data model. New GLOBAL router `app/api/concept_tags.py` (`/api/concept-tags`, registered after `tickets.router`) + service `app/services/concept_tags.py` (CRUD, tag↔tag link create/delete, ticket attach/detach). Permission checks live in the SERVICE layer (mirrors `services/tickets.py`), NOT the router. Three caps: `tag.read` is GLOBAL (every default role) — board-independent reads; `tag.manage` is GLOBAL **admin-gated** (granted ONLY via the admin `*` wildcard, NEVER on a non-admin role list); `tag.assign` is **board-scoped via the ticket's board** (`require_permission(actor, ticket.board, "tag.assign", resource=ticket)`). Because a global ConceptTag has no board and there is no global-admin concept (admin is always a per-board `BoardMembership.role`), the new `core/permissions.py:require_global_permission(actor, required, memberships_with_boards)` helper gates the board-less caps with "admin on ANY board" semantics — the caller loads the actor's memberships WITH boards' roles eager-loaded (`current_actor` only selectinloads `Actor.memberships`, not the board) and passes `(role, board)` pairs in, keeping the gate self-contained + async-safe without broadening `current_actor`. **Attach is idempotent-success (200, not 409)**: pre-check the junction → return the current ticket on a re-attach (no duplicate row, the `uq_ticket_concept_tag` invariant holds); detach of a non-attached tag is a symmetric 200 no-op (PH-276 chip toggles never special-case a non-error); an `IntegrityError` on a concurrent race is swallowed as the same idempotent path. `TicketResponse.concept_tags` is a SEPARATE field (`list[{id,slug,name,color}]`) — NEVER overloads `labels: list[str]`. Tag↔tag links: ConceptTag has no link back-ref collection yet (PH-272 deferred to PH-275), so a tag's edges are assembled with a SEPARATE `select(ConceptTagLink).where(source==id OR target==id)`, each row `selectinload`-ing both endpoints (N+1-free). App-layer guards added here (deferred from PH-272): self-loop → 422 `InvalidConceptLink`, duplicate `(source,target)` → 409 `Conflict` (reverse pair stays distinct); transitive cycle detection is PH-275's seam, NOT built here. No migration (`alembic heads` stays `2722c9066c32`). **Operator follow-up**: the new caps land in `DEFAULT_WEB_ROLES` but bootstrap is NOT idempotent — `docker compose exec backend python -m app.cli update_board_roles` must be run to propagate them to existing live boards (deferred to post-merge per the integration-branch no-live-DB-mutation rule).
- ConceptTag global model + TicketConceptTag junction + directed ConceptTagLink (self-M2M, explicit foreign_keys) + additive migration down_revision=b1468dc15870 [PH-272] — FOUNDATION child of epic PH-271 (concept-graph); data layer ONLY (API/permissions/serializers are PH-273). `ConceptTag` is GLOBAL (NO `board_id`, cross-board sharing is the point of the epic) with a named `UniqueConstraint(slug)` for stable/portable downgrade. `ConceptTagLink` is **DIRECTED** (`source_tag_id`→`target_tag_id` + nullable `relation` ∈ relates|parent|depends): directed is the only shape carrying both asymmetric (parent/depends) and symmetric (relates) semantics; the graph layer (PH-275) collapses to undirected when `relation` is symmetric. `uq_concept_tag_link_source_target` dedupes ONE direction — the reverse `(target, source)` is a DISTINCT row by design (lets `A parent B` and `B depends A` coexist). Both self-FK relationships MUST set explicit `foreign_keys=[source_tag_id]`/`[target_tag_id]` or mapper config raises `AmbiguousForeignKeysError`. Self-loop/cycle guards are app-layer (PH-273), NOT enforced at the DB. Migration `2722c9066c32` (down_revision `b1468dc15870`) is STRICTLY ADDITIVE — three plain `op.create_table` (no batch, no ALTER on existing tables, SQLite-safe), reverse-order downgrade; spurious autogen `alter_column`/`drop_index` noise on git_commits/sonarqube_metrics/repositories deliberately stripped. **ASYNC-SAFE note for PH-273 consumers**: `Ticket.concept_tag_links` is a lazy collection — any async read MUST `selectinload(Ticket.concept_tag_links).selectinload(TicketConceptTag.concept_tag)` or async lazy-load raises `MissingGreenlet`.
- per-repo SonarQube model: `Repository.sonarqube_project_key`, `SonarQubeMetric (board_id, repo_id)`, `SonarScanJob.repo_id` [PH-246] — the data-model foundation for per-repo Sonar (full scan/health behavior on [[components/sonarqube]]). `Repository` gains `sonarqube_project_key` (String(400), nullable) — the per-repo scan source of truth (primary repo backfilled from the board's legacy key, siblings derive lazily). `SonarQubeMetric` gains `repo_id` (FK `repositories.id`, nullable, CASCADE) and its unique constraint relaxes `uq_sonarqube_metric_board (board_id)` → `uq_sonarqube_metric_board_repo (board_id, repo_id)` — was 1 board:0..1, now 1 board:N (one upsert-latest row per repo); the `Board.sonarqube_metric` 1:1 relationship is renamed to `sonarqube_metrics` 1:N with a new `Board.primary_sonarqube_metric` back-compat accessor (so `BoardResponse.health` stays the primary repo's metric). `SonarScanJob` gains `repo_id` (FK, nullable, CASCADE) + `repo_slug` snapshot (one job per scannable repo, idempotent per `(board_id, repo_id)`). `Board.sonarqube_project_key` is KEPT (legacy fallback + PH self-scan identity), NOT dropped. Migration `b1468dc15870` is ADDITIVE-ONLY + backfilled + SQLite-safe (`batch_alter_table` for the FKs + constraint relax — SQLite has no ALTER-ADD/DROP-CONSTRAINT); backfills the primary repo's key from the board column (no rename) + metric/job repo_id from the board's primary repo, so KIM/PH/GXA keep their exact project_key + health. `get_board`/`list_boards` now `selectinload(Board.sonarqube_metrics).selectinload(SonarQubeMetric.repository)`.
- `BoardResponse.sonarqube_project_key` exposed (additive nullable) [PH-235] — `serializers.board_response` now passes `board.sonarqube_project_key` onto a new additive-nullable `BoardResponse.sonarqube_project_key` (null when unconfigured) so the frontend board-header `SonarHealthPanel` can distinguish "no key" from "key set, no analysis yet" without a separate status call. No migration (column exists since the original sonar work); read-only surface. Companion to the PH-235 `SonarSetupStatus` honesty change (`status` enum + `has_analysis`; see [[components/sonarqube]]).
- `Board.repos_path` HOST path field + `HOST_HOME` setting + broadened mount [PH-228] — C1 of epic PH-227. `Board` gains `repos_path` (String(500), nullable, placed after `sonarqube_project_key`) storing the **HOST** path (mount-independent, what the user sees in Finder/CLI) — NOT the container path, which is derived on demand by the NEW `services/repo_paths.py` helper (see [[components/git-integration]] for the translation contract + the mount/back-compat trap). `Settings` gains `host_home` (default `/Users/huseyinkanat`, env-overridable via `HOST_HOME`) — the host `$HOME` the bind mount maps onto `repos_root`; it must equal whatever `${HOME}` resolved to at mount time and is deliberately NOT derived from the container's own `$HOME` (which is `/root`, not the host home). Migration `20260608_0010` (down_revision `20260608_0009`) is additive + idempotent: nullable column, backfill the 6 known boards keyed by `key` (`WHERE repos_path IS NULL`), and relocate the PH repo's `local_path` to match the broadened mount — plain parametrized `UPDATE`s, dialect-portable across Postgres + the SQLite test DB; reversible downgrade. `BoardResponse` exposes `repos_path` (read-only — `BoardUpdate`/PATCH editability deferred to PH-230). No new poller, no new event; the detect/sonar consumers that USE the path land in PH-229.
- SonarQube board-health poller + `SonarQubeMetric` upsert-latest cache [PH-193] — new `services/sonarqube.py` httpx client (`BasicAuth(token, "")` — portable Community Build token auth) calls `GET /api/qualitygates/project_status` + `GET /api/measures/component`; the poller mirrors `git_poll_cron` (fresh session per tick, `except asyncio.CancelledError` clean shutdown, per-tick `except Exception`). **Layered error isolation**: the client returns `None` on any error (SonarQube down / 401 / malformed JSON / project not yet scanned) so the loop never crashes; a board with no resolvable projectKey (`Board.sonarqube_project_key` null + not in `sonarqube_project_key_map`) is skipped silently. Model stores **latest only** (`UniqueConstraint(board_id)`, upsert in place) — not append-history; trends are out of scope. `raw_measures` JSON keeps the verbatim measure map for forward-compat (new metrics need no migration). Migration `20260606_0008` additive (nullable `boards.sonarqube_project_key` + new `sonarqube_metrics` table).
- board-level `sonarqube_synced` event reuses the ticket-shaped `EventEnvelope` with empty `ticket_id`/`ticket_key` sentinels [PH-193] — the WS board channel ignores ticket fields; payload mirrors `BoardHealth` so the frontend can patch cached `board.health` without a refetch. Token is never logged, never in the API response, never in the event payload.
- `repair_workflow --board <KEY>` CLI added to restore a corrupted `backlog->to_do` transition [PH-168] — an E2E test reset PH's active workflow and stripped `allowed_roles` from the `backlog->to_do` transition (injecting a stray `technical_depth` field_gate), which locked the whole board because the transition engine rejects every actor when `allowed_roles` is absent. The command resolves the board's *active* workflow via `services/boards.get_active_workflow` (active `BoardWorkflow` junction row, falling back to `board.workflow_id`) — the same resolution the engine uses — then rewrites **only** the `backlog->to_do` entry back to the known-good `{"allowed_roles": ["pm", "architect"]}` shape (mirrors `DEFAULT_TRANSITIONS[0]`, no field gate). Logic split into a pure helper `repair_backlog_to_do_transitions(transitions) -> (new_list, changed)` (deep-copies every other transition verbatim, raises `ValueError` if no `backlog->to_do` exists) and the async DB wrapper `repair_workflow`. Idempotent: a healthy workflow is a no-op that prints "already healthy". Run against PH live: `docker compose exec backend python -m app.cli repair_workflow --board PH`.
- G3 sync service (`app/git/sync.py`) adds 4 cache tables (`git_commits`, `git_branches`, `git_commit_files`, `git_commit_tickets`) populated by `sync_repo(session, board)`; G4-G5 read endpoints will serve from cache without spawning git subprocesses per request [PH-152] — migration `20260604_0007` additive; downgrade drops all 4 tables
- `git_commit_tickets` unique constraint `(commit_id, ticket_id)` is the dedupe gate for both webhook and sync paths; first-observation wins for history timestamps [PH-152]
- board-scoped `git_synced` WS envelope uses `ticket_id="system"` sentinel (same pattern as `system_degradation`) to signal cache refresh to the frontend [PH-152]
- `_linkage.py` extracted as shared internal module for `find_ticket_by_key` + `get_system_actor_id` used by both `webhook.py` and `sync.py` [PH-152]
- Initial documentation [bootstrap] — auto-generated by jarwis-init bootstrap flow
- GitPython chosen over dulwich for G2+ reader (SourceTree-grade diff sadakati — rename detection, unified diff, line-level) [PH-150] — dulwich diff fidelity insufficient for epic goal; image cost (~30MB git binary) justified by load-bearing diff quality for G5
- Repository model uses string `provider` column + CHECK constraint instead of DB Enum (migration flexibility — avoids Alembic Enum drop/add complexity on provider set expansion) [PH-150]
- `GET /api/boards/{key}/git/status` readable by any board member (read-only, no admin); PUT/DELETE restricted to board admin (write contract) [PH-150]
- `selectinload(Board.repository)` added to `get_board` + `list_boards` — eager-loads repo for `board_response` serialization; avoids async MissingGreenlet error from lazy-load [PH-150]

## Known gotchas

- `Ticket.labels` ARRAY search/match is DIALECT-BRANCHED — PG `unnest` vs sqlite `json_each` [PH-281] — `Ticket.labels` is `STRING_ARRAY_TYPE` = native `text[]` on Postgres prod but a JSON-array-as-TEXT on the sqlite test dialect; a single SQL predicate CANNOT match both encodings. `services/labels.py` centralizes this: `_labels_table_function(dialect)` returns a TABLE-VALUED alias — `unnest(labels)` (Postgres) / `json_each(labels)` (sqlite) — with a `.c.value` element column, ALWAYS joined to `Ticket` via `.join(elements, true())` (an implicit LATERAL) so SQLAlchemy correlates it (a bare top-level `select(value_col)` triggers a cartesian-product SAWarning AND risks a true cross join). EXACT membership (`?labels` AND-filter) compares `elements.c.value == value` (no substring leakage — `bug` ≠ `debugger`); ci-substring (search) reuses the PH-275 `func.lower(col).contains(term.lower(), autoescape=True)` (wildcard-safe — `%`/`_` literal). A naive `_ci_contains` on the WHOLE array → false positives + type errors; never do it. (sqlite `lower()` is ASCII-only — Turkish-İ caveat, same as `ilike`; the AC is ASCII ci.) Helpers: `label_match_predicate` / `label_substring_predicate` (correlated EXISTS), `distinct_ticket_labels` (search labels group), `labels_reach_query` (board-scope cross-board reach).
- Graph `label:` node id keeps the RAW label value verbatim — do NOT slugify [PH-281] — `/api/graph` label nodes use `id="label:<rawValue>"` (e.g. `label:unity_platform`, `label:foo:bar`, `label:has space`); the value is the disambiguator and is injective by construction. Slugifying could collapse `foo:bar` and `foo-bar` into one node. A `:` inside a value is harmless because the id is an opaque string key (consumers do EXACT-string match, never split on `:` — same contract as `ticket:<uuid>`). The frontend (PH-280) maps a node→string by reading `node.label` OR stripping the FIRST `label:` prefix only; both round-trip losslessly. `has_label` edge ids (`has_label:<ticketId>:<rawValue>`) reference these verbatim.
- Graph node ids are TYPE-PREFIXED strings, never bare UUIDs [PH-274/PH-281] — `/api/graph` nodes use `ticket:<uuid>` / `label:<rawValue>` / `board:<key>`; edge `source`/`target` reference those prefixed ids verbatim. A raw-id scheme would let a ticket UUID and a label value collide in the id namespace. The frontend (`/space`) resolves an endpoint to its node by exact string match and relies on the `type` discriminator + the bipartite invariant (`has_label` connects ticket↔label, `epic` ticket↔ticket — NO `has_tag`/`tag_link` anymore). To stay N+1-free, NEVER lazy-access `.board` outside the eager-loaded set; labels are read inline off the Q1 ticket SELECT, so the assembly is pure in-memory after the ticket + board-selectinload queries.
- (RETIRED by PH-281) the PH-273 concept_tag_links stale-collection + MissingGreenlet gotchas no longer apply — attach/detach + the `concept_tag_links` eager-load in `_ticket_load_options` were removed; labels are an inline column (no identity-map collection caching, no per-ticket lazy hop).
- Mutating a JSON column in place may not persist [PH-168] — `Workflow.transitions` is a JSON column. Editing a nested list/dict element in place (e.g. `transition["allowed_roles"] = [...]`) is not seen by SQLAlchemy's default change tracking, so the UPDATE never fires. `repair_workflow` sidesteps this by **reassigning a brand-new list** (`workflow.transitions = new_transitions`) *and* calling `flag_modified(workflow, "transitions")`. Any future workflow-JSON edit must do the same or the write is silently dropped.

## Related

- [[overview]]
- [[index]]
- [[components/frontend]]
